{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/claude-hpc/schemas/resubmit.input.json",
  "title": "resubmit input spec",
  "type": "object",
  "required": [
    "failed_task_ids",
    "category"
  ],
  "additionalProperties": false,
  "properties": {
    "failed_task_ids": {
      "items": {
        "type": "integer"
      },
      "minItems": 1,
      "title": "Failed Task Ids",
      "type": "array"
    },
    "category": {
      "description": "Failure category that drives retry policy. Superset of ``classify_failure``'s ``CATEGORIES`` (which never emits ``preempted`` directly — that's a scheduler-level state).",
      "enum": [
        "gpu_oom",
        "system_oom",
        "segv",
        "walltime",
        "node_failure",
        "queue_stall",
        "code_bug",
        "unknown",
        "preempted"
      ],
      "title": "Category",
      "type": "string"
    },
    "overrides": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Resource overrides for retry (mem, walltime, gpus, ...).",
      "title": "Overrides"
    },
    "new_job_ids": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "New Job Ids"
    },
    "request_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional dedupe key. When omitted, claude-hpc derives a deterministic id from (sorted failed_task_ids, category, sorted overrides). A second resubmit with the same request_id returns the existing record without incrementing retry counters.",
      "title": "Request Id"
    },
    "consult_forecast": {
      "default": true,
      "description": "When true (the default), claude-hpc consults the queue-wait forecaster before resubmitting and attaches a ResubmitRecommendation envelope to the response. Advisory only — does not block the resubmit. Set to false to skip the forecast call (e.g. tight CI loops).",
      "title": "Consult Forecast",
      "type": "boolean"
    },
    "forecast_within_hours": {
      "default": 24,
      "description": "Horizon (hours) for the resubmit-window advisor. Ignored unless consult_forecast is true.",
      "maximum": 168,
      "minimum": 1,
      "title": "Forecast Within Hours",
      "type": "integer"
    },
    "submit_to_cluster": {
      "default": false,
      "description": "When true, claude-hpc composes resubmit_plan + backend.submit_array to actually re-issue the failed tasks. When false (legacy default), the operation is journal-only. Requires script, backend, and job_name to be set when true.",
      "title": "Submit To Cluster",
      "type": "boolean"
    },
    "script": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Path on the cluster to the submission script. Required when submit_to_cluster is true; ignored otherwise.",
      "title": "Script"
    },
    "backend": {
      "anyOf": [
        {
          "enum": [
            "sge",
            "slurm"
          ],
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Scheduler backend kind. Required when submit_to_cluster is true; ignored otherwise.",
      "title": "Backend"
    },
    "job_name": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Job name for the resubmitted batches. Required when submit_to_cluster is true; ignored otherwise.",
      "title": "Job Name"
    },
    "job_env": {
      "anyOf": [
        {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Environment variables to forward to each resubmitted batch. Same shape submit-flow accepts. Ignored unless submit_to_cluster is true.",
      "title": "Job Env"
    }
  }
}
