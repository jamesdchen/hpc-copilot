{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/claude-hpc/schemas/submit_flow_batch.output.json",
  "title": "submit-flow-batch output data",
  "description": "Shape of the `data` field on a successful `submit-flow-batch` envelope. Wraps a list of per-spec result records (each matching submit_flow.output.json) plus a count for cheap iteration. Order of `results` matches the order of input `specs`.",
  "type": "object",
  "required": [
    "results",
    "n_results"
  ],
  "additionalProperties": false,
  "properties": {
    "results": {
      "type": "array",
      "minItems": 1,
      "description": "Per-spec submit-flow result, in input order. Each entry has the shape of submit_flow.output.json (run_id, job_ids, total_tasks, deduped, canary_done, canary_run_id, canary_job_ids).",
      "items": {
        "type": "object",
        "required": [
          "run_id",
          "job_ids",
          "total_tasks",
          "deduped",
          "canary_done"
        ],
        "properties": {
          "run_id": {
            "$ref": "https://github.com/jamesdchen/claude-hpc/schemas/envelope.json#/$defs/run_id"
          },
          "job_ids": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "total_tasks": {
            "type": "integer",
            "minimum": 1
          },
          "deduped": {
            "type": "boolean"
          },
          "canary_done": {
            "type": "boolean"
          },
          "canary_run_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "canary_job_ids": {
            "type": [
              "array",
              "null"
            ],
            "items": {
              "type": "string"
            }
          }
        }
      }
    },
    "n_results": {
      "type": "integer",
      "minimum": 1,
      "description": "Length of `results`. Equals the number of input specs (deduped specs still contribute one result entry with deduped=true)."
    }
  }
}
