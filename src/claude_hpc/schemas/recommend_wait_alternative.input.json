{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/claude-hpc/schemas/recommend_wait_alternative.input.json",
  "title": "RecommendWaitAlternativeSpec",
  "type": "object",
  "required": [
    "current_priority"
  ],
  "additionalProperties": false,
  "properties": {
    "current_priority": {
      "description": "The user's current pending-job priority on the target partition.",
      "minimum": 0,
      "title": "Current Priority",
      "type": "integer"
    },
    "samples": {
      "description": "Past (observed_at_iso, priority) observations from pending jobs on the same partition. Two or more required for a non-trivial estimate; fewer surfaces ``method=insufficient_data`` and a zero-rate forecast.",
      "items": {
        "$ref": "#/$defs/_PrioritySampleSpec"
      },
      "title": "Samples",
      "type": "array"
    },
    "wait_horizons_hours": {
      "description": "Wait durations (hours) to forecast the climbed priority for. The agent surfaces 'wait N hours, your priority climbs to P' for each horizon.",
      "items": {
        "type": "number"
      },
      "title": "Wait Horizons Hours",
      "type": "array"
    }
  },
  "$defs": {
    "_PrioritySampleSpec": {
      "additionalProperties": false,
      "description": "One ``(observed_at_iso, priority)`` observation drawn from a\npast pending job on the same partition / cluster.",
      "properties": {
        "observed_at_iso": {
          "minLength": 1,
          "title": "Observed At Iso",
          "type": "string"
        },
        "priority": {
          "minimum": 0,
          "title": "Priority",
          "type": "integer"
        }
      },
      "required": [
        "observed_at_iso",
        "priority"
      ],
      "title": "_PrioritySampleSpec",
      "type": "object"
    }
  }
}
