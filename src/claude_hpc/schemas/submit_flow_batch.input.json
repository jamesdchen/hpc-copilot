{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/claude-hpc/schemas/submit_flow_batch.input.json",
  "title": "submit-flow-batch input spec",
  "description": "Wraps a JSON list of submit-flow specs that share ``(ssh_target, remote_path)``.\n\nThe batch atom does ONE rsync_push + ONE deploy_runtime + N\nqsubs (multiplexed via the ssh ControlMaster) instead of N times\nthe per-spec submit-flow pipeline. Use whenever a campaign\niteration or multi-executor /submit-hpc emits >1 specs to the\nsame cluster; heterogeneous (different ssh_target or remote_path)\nbatches raise spec_invalid. Each list element under ``specs``\nMUST individually validate against submit_flow.input.json (the\nCLI loads that schema and validates per-entry); this wrapper\nschema only constrains the outer shape so an agent or external\norchestrator can sanity-check the bundle without doing the\nper-entry validation itself.",
  "type": "object",
  "required": [
    "specs"
  ],
  "properties": {
    "specs": {
      "items": {
        "$ref": "#/$defs/_SubmitFlowSpecOuter"
      },
      "minItems": 1,
      "title": "Specs",
      "type": "array"
    },
    "rsync_excludes": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional rsync exclude patterns applied once across the bundle.",
      "title": "Rsync Excludes"
    },
    "skip_preflight": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Skip the single ssh probe; default false.",
      "title": "Skip Preflight"
    }
  },
  "$defs": {
    "_SubmitFlowSpecOuter": {
      "additionalProperties": true,
      "description": "Outer-shape only validation for one submit-flow spec inside a batch.",
      "properties": {
        "profile": {
          "title": "Profile",
          "type": "string"
        },
        "cluster": {
          "title": "Cluster",
          "type": "string"
        },
        "ssh_target": {
          "title": "Ssh Target",
          "type": "string"
        },
        "remote_path": {
          "title": "Remote Path",
          "type": "string"
        },
        "run_id": {
          "title": "Run Id",
          "type": "string"
        },
        "total_tasks": {
          "title": "Total Tasks",
          "type": "integer"
        },
        "backend": {
          "title": "Backend",
          "type": "string"
        },
        "job_name": {
          "title": "Job Name",
          "type": "string"
        }
      },
      "required": [
        "profile",
        "cluster",
        "ssh_target",
        "remote_path",
        "run_id",
        "total_tasks",
        "backend",
        "job_name"
      ],
      "title": "_SubmitFlowSpecOuter",
      "type": "object"
    }
  }
}
