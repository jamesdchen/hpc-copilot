{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/claude-hpc/schemas/predict_queue_wait.output.json",
  "title": "predict-queue-wait output data",
  "description": "Mirrors ``PredictionResult.to_dict()``.",
  "type": "object",
  "required": [
    "predicted_wait_sec",
    "confidence",
    "method",
    "n_bucket_samples",
    "n_total_samples",
    "bucket_hour_of_week",
    "fallback_reason",
    "features_adjustment_factor"
  ],
  "additionalProperties": false,
  "properties": {
    "predicted_wait_sec": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "description": "Median (p50) predicted wait in seconds. null when the prediction is cold.",
      "title": "Predicted Wait Sec"
    },
    "confidence": {
      "enum": [
        "high",
        "medium",
        "low",
        "cold"
      ],
      "title": "Confidence",
      "type": "string"
    },
    "method": {
      "description": "Which backend produced the prediction. 'des*' tags indicate the DES backend ran or attempted to run; 'diurnal_ma'/'blended_ma'/'global_ma' tag the v1 baseline tiers; 'no_data' is cold start.",
      "enum": [
        "diurnal_ma",
        "blended_ma",
        "global_ma",
        "no_data",
        "des",
        "des_no_snapshot",
        "des_no_profiles"
      ],
      "title": "Method",
      "type": "string"
    },
    "n_bucket_samples": {
      "minimum": 0,
      "title": "N Bucket Samples",
      "type": "integer"
    },
    "n_total_samples": {
      "minimum": 0,
      "title": "N Total Samples",
      "type": "integer"
    },
    "bucket_hour_of_week": {
      "maximum": 167,
      "minimum": -1,
      "title": "Bucket Hour Of Week",
      "type": "integer"
    },
    "fallback_reason": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "title": "Fallback Reason"
    },
    "features_adjustment_factor": {
      "description": "Order-book adjustment factor (Phase 1c). 1.0 on the wire-driven path and on the DES path; only non-1.0 for internal callers that pass live QueueFeatures directly (not exposed on the spec).",
      "title": "Features Adjustment Factor",
      "type": "number"
    },
    "p10_wait_sec": {
      "anyOf": [
        {
          "minimum": 0,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "DES p10 quantile in seconds. null on the diurnal_ma path.",
      "title": "P10 Wait Sec"
    },
    "p90_wait_sec": {
      "anyOf": [
        {
          "minimum": 0,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "DES p90 quantile in seconds. null on the diurnal_ma path.",
      "title": "P90 Wait Sec"
    },
    "n_replications": {
      "anyOf": [
        {
          "minimum": 1,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Number of DES passes executed. null on the diurnal_ma path.",
      "title": "N Replications"
    }
  }
}
