{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/claude-hpc/schemas/validate.input.json",
  "title": "validate input spec",
  "description": "Kwargs contract for ``claude_hpc.planning.validate.validate_submission``.\n\nProbes the scheduler's --test-only mode for the resource ask and\nreturns the predicted submission timing. No submit side effect.\nNot yet exposed as a CLI subcommand; consumers call the function\ndirectly. When the CLI subcommand lands, this schema will become\nthe wire contract for ``--spec``.",
  "type": "object",
  "required": [
    "profile",
    "cluster",
    "walltime_sec",
    "mem_mb",
    "cpus"
  ],
  "additionalProperties": false,
  "properties": {
    "profile": {
      "minLength": 1,
      "title": "Profile",
      "type": "string"
    },
    "cluster": {
      "minLength": 1,
      "title": "Cluster",
      "type": "string"
    },
    "walltime_sec": {
      "minimum": 1,
      "title": "Walltime Sec",
      "type": "integer"
    },
    "mem_mb": {
      "minimum": 1,
      "title": "Mem Mb",
      "type": "integer"
    },
    "cpus": {
      "minimum": 1,
      "title": "Cpus",
      "type": "integer"
    },
    "constraint": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "GPU type or feature constraint passed to --constraint. Null or '<cpu-only>' selects the CPU-only branch.",
      "title": "Constraint"
    },
    "gpus": {
      "default": 0,
      "minimum": 0,
      "title": "Gpus",
      "type": "integer"
    },
    "backfill_window_sec": {
      "default": 600,
      "description": "ETA threshold for the fits_backfill flag.",
      "minimum": 1,
      "title": "Backfill Window Sec",
      "type": "integer"
    }
  }
}
