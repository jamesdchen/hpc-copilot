{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/claude-hpc/schemas/combine_wave.output.json",
  "title": "aggregate (combine-wave) output data",
  "description": "Shape of the `data` field on a successful `aggregate --run-id <id> --wave <N>` envelope. Note: `combined: false` is a SUCCESSFUL envelope (the call recorded the failure to the journal); inspect data.combined to branch.",
  "type": "object",
  "required": [
    "run_id",
    "wave",
    "combined",
    "output_dir"
  ],
  "additionalProperties": false,
  "properties": {
    "run_id": {
      "$ref": "https://github.com/jamesdchen/claude-hpc/schemas/envelope.json#/$defs/run_id"
    },
    "wave": {
      "type": "integer",
      "minimum": 0
    },
    "combined": {
      "type": "boolean",
      "description": "True when the cluster-side combiner exited 0 and the wave landed in combined_waves. False when the combiner exited non-zero and the wave landed in failed_waves; inspect stderr_tail."
    },
    "output_dir": {
      "type": "string",
      "description": "Absolute path on the cluster where the wave's combined partial was written."
    },
    "stdout_tail": {
      "type": "string"
    },
    "stderr_tail": {
      "type": "string"
    }
  }
}
