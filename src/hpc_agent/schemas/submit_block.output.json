{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/submit_block.output.json",
  "title": "submit-block output data",
  "description": "Shared ``data`` block for every submit block (S1–S4).\n\nThe ``brief`` is the code-digested evidence the LLM drafts a proposal over\n(§2): the digested envelope for S1, \"canary green + est core-hours\" for S2,\nthe terminal status + arm decision for S3, the results table + interpretation\nslot for S4. ``needs_decision`` marks a ``y``/nudge terminator (True for the\nS1/S2/S4 briefs and for an S3 anomaly; False when S3 reached a clean terminal\nand simply suggests S4).",
  "type": "object",
  "required": [
    "block",
    "stage_reached",
    "needs_decision"
  ],
  "additionalProperties": false,
  "properties": {
    "block": {
      "description": "Which submit block produced this result.",
      "enum": [
        "s1",
        "s2",
        "s3",
        "s4"
      ],
      "title": "Block",
      "type": "string"
    },
    "stage_reached": {
      "description": "The terminator the block stopped at (decision-as-data, #231).",
      "enum": [
        "needs_resolution",
        "prior_run_found",
        "needs_scaffold_interview",
        "resolved",
        "canary_verified",
        "canary_failed",
        "deduped",
        "watching_terminal",
        "watching_anomaly",
        "watching_timeout",
        "harvested",
        "harvest_partial",
        "detached"
      ],
      "title": "Stage Reached",
      "type": "string"
    },
    "needs_decision": {
      "description": "True when a human must answer here (the y/nudge terminator). False only when the block ran clean to an unattended terminal (S3 complete).",
      "title": "Needs Decision",
      "type": "boolean"
    },
    "reason": {
      "default": "",
      "description": "Human-readable one-line summary of the terminator.",
      "title": "Reason",
      "type": "string"
    },
    "run_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The run this block operated on, when one exists yet.",
      "title": "Run Id"
    },
    "brief": {
      "additionalProperties": true,
      "description": "Code-digested evidence the LLM drafts a proposal over (§2). Shape varies per block; never interpreted raw by the LLM.",
      "title": "Brief",
      "type": "object"
    },
    "next_block": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The DETERMINISTICALLY-computed next block — ``{verb, why, spec_hint}`` — or null at a terminal / human-branch terminator (design §2, the ``_next_step_hint`` pattern generalized). ``verb`` names the next block's CLI verb; ``spec_hint`` is the minimal next-spec skeleton (run_id etc.). The LLM surfaces it the way /sync is proposed; the human's `y` greenlights THAT named verb; append-decision journals it under ``resolved.next_block``; the successor block's gate verifies the journaled greenlight names it. NEVER free-prose — a mis-sequenced call fails loudly.",
      "title": "Next Block"
    },
    "started": {
      "default": false,
      "description": "Detach-by-contract handle (design §3): True when the block spawned a durable detached worker and returned immediately instead of blocking the chat on the scheduler. The gate + drift guard already fired synchronously BEFORE the detach; the brief is not held in a process — read it from the journal via status-snapshot / the completion notification. False on the synchronous (detach=False) path.",
      "title": "Started",
      "type": "boolean"
    },
    "watch": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "How to learn the detached block's outcome — ``\"journal\"`` when ``started`` is True (the detached worker stamps the per-run journal record as it polls; poll it cluster-free). None on the synchronous path.",
      "title": "Watch"
    },
    "detached_pid": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The detached worker's OS process id (informational — do NOT wait on it; read the journal). None on the synchronous path. A dead worker is detected by the §5 watchdog via a lapsed next_tick_due, not by this pid.",
      "title": "Detached Pid"
    }
  }
}
