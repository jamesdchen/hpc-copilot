{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/net_triage.input.json",
  "title": "net-triage input spec",
  "description": "Input spec for the ``net-triage`` verb.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "host": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional extra host to triage (bare hostname or user@host — the user part is stripped), IN ADDITION to every host in the cluster config. Use it for a host not (yet) in clusters.yaml.",
      "title": "Host"
    },
    "control_timeout_sec": {
      "default": 5.0,
      "description": "Budget for the one control-plane HTTPS reachability check.",
      "maximum": 30.0,
      "minimum": 1.0,
      "title": "Control Timeout Sec",
      "type": "number"
    },
    "dns_timeout_sec": {
      "default": 5.0,
      "description": "Per-host budget for DNS resolution of the cluster hostname.",
      "maximum": 30.0,
      "minimum": 1.0,
      "title": "Dns Timeout Sec",
      "type": "number"
    },
    "tcp_timeout_sec": {
      "default": 8.0,
      "description": "Per-host budget for the single direct TCP connect to host:22.",
      "maximum": 60.0,
      "minimum": 1.0,
      "title": "Tcp Timeout Sec",
      "type": "number"
    },
    "probe_preamble": {
      "default": false,
      "description": "Opt in to the PREAMBLE-CLASS rung: after the connect legs, run the cluster's own activation (`module load … && source …/conda.sh && conda activate …`, taken from clusters.yaml unless `activation` overrides it) over SSH and report {connect, preamble} SEPARATELY. Opt-in because it costs real connections and a login-shell round-trip. It is the rung that discriminates the 2026-07-30 ambiguity: a cheap connect succeeding while the preamble hangs fits BOTH node-local degradation and a tunnel dropping mid-command, and only running the SAME command class over the DIRECT (jump-bypassed) route tells them apart.",
      "title": "Probe Preamble",
      "type": "boolean"
    },
    "probe_invariants": {
      "default": false,
      "description": "Opt in to the INVARIANT rungs: after the transport legs, sense the cluster's scratch (`test -d` + `df -P`), its scheduler CLI (the backend family's cheap version/usage banner, family resolved from clusters.yaml), and its env fingerprint (`hpc-agent --version` under the cluster activation) — reporting `scratch` / `scheduler` / `env` atoms alongside the transport ones. Opt-in for the same reason `probe_preamble` is: each rung costs a real connection. Reaching a login node is not the same as being able to USE the cluster, and these are the three invariants that difference is made of; they are reported as evidence and NEVER change the path verdict (a full scratch disk is not a dead path). net-triage is the sanctioned prober — the readiness ledger's own feed sites only ever harvest.",
      "title": "Probe Invariants",
      "type": "boolean"
    },
    "activation": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Override the activation prefix the preamble rung runs (default: the matched cluster's own, built from clusters.yaml). Ignored unless `probe_preamble` is true. Also prefixes the scheduler + env rungs when `probe_invariants` is set.",
      "title": "Activation"
    },
    "preamble_timeout_sec": {
      "default": 30.0,
      "description": "Per-attempt budget for one preamble-class SSH reading. Short by design: this rung exists to catch a preamble that HANGS.",
      "maximum": 300.0,
      "minimum": 1.0,
      "title": "Preamble Timeout Sec",
      "type": "number"
    }
  }
}
