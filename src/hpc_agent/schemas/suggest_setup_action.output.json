{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/suggest_setup_action.output.json",
  "title": "suggest-setup-action output",
  "description": "Recommended /submit-hpc Setup branch.\n\nAgent branches on ``action``; ``candidates`` carries the\nrelevant runs/sidecars to surface to the user. The\n``recommended_run_id`` field is named with a 'recommended_'\nprefix so the schema-defs consistency check (which forbids\nnullable run_id) doesn't trip.",
  "type": "object",
  "required": [
    "priority",
    "action",
    "recommended_run_id",
    "candidates",
    "reason"
  ],
  "additionalProperties": false,
  "properties": {
    "priority": {
      "enum": [
        0,
        1,
        2,
        3
      ],
      "title": "Priority",
      "type": "integer"
    },
    "action": {
      "enum": [
        "monitor",
        "reuse",
        "interview",
        "fresh"
      ],
      "title": "Action",
      "type": "string"
    },
    "recommended_run_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "description": "Recommended single best candidate (newest by submitted_at / mtime); null at priority 2/3.",
      "title": "Recommended Run Id"
    },
    "candidates": {
      "description": "Full candidate list at this priority. Shape varies: priority 0 carries journal RunRecord summaries; priority 1 carries sidecar dicts.",
      "items": {
        "additionalProperties": true,
        "type": "object"
      },
      "title": "Candidates",
      "type": "array"
    },
    "reason": {
      "title": "Reason",
      "type": "string"
    }
  }
}
