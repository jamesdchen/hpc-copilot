{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/submit_and_verify.output.json",
  "title": "submit-and-verify output data",
  "description": "Shape of the ``data`` field on a successful envelope.\n\nAlways carries the submit half; the verify half is None when the\ncanary was skipped (``submit.canary=False``) or when the submit\nwas a deduped replay (no fresh canary to wait on).",
  "type": "object",
  "required": [
    "run_id",
    "total_tasks",
    "deduped",
    "verified"
  ],
  "additionalProperties": false,
  "properties": {
    "run_id": {
      "description": "Main run id (mirrors submit-flow's run_id).",
      "title": "Run Id",
      "type": "string"
    },
    "job_ids": {
      "description": "Main array job ids. EMPTY when the canary failed verification — the main array never launched (the #160 two-phase gate).",
      "items": {
        "type": "string"
      },
      "title": "Job Ids",
      "type": "array"
    },
    "total_tasks": {
      "minimum": 1,
      "title": "Total Tasks",
      "type": "integer"
    },
    "deduped": {
      "description": "True when the submit half was a deduped replay.",
      "title": "Deduped",
      "type": "boolean"
    },
    "canary_run_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Run id of the canary sibling sidecar. None when canary was skipped (submit.canary=False) or on a deduped replay.",
      "title": "Canary Run Id"
    },
    "canary_job_ids": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Scheduler ids for the canary. None when canary skipped.",
      "title": "Canary Job Ids"
    },
    "verified": {
      "description": "True iff verify-canary returned ok=True — and ONLY then is the main array launched (#160). False on any canary-side failure (main never launches; job_ids empty) AND when verification was skipped (no canary fired / deduped replay).",
      "title": "Verified",
      "type": "boolean"
    },
    "failure_kind": {
      "anyOf": [
        {
          "enum": [
            "dispatcher_failed",
            "import_error",
            "module_not_found",
            "traceback",
            "oom_killed",
            "segfault",
            "nonzero_exit",
            "missing_output",
            "reporter_unreachable",
            "canary_failed",
            "completed_unknown",
            "timeout",
            "abandoned",
            "checkpoint_missing",
            "checkpoint_unloadable"
          ],
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Pass-through from verify-canary. None on success, None when canary was skipped.",
      "title": "Failure Kind"
    },
    "verify_result": {
      "anyOf": [
        {
          "$ref": "#/$defs/VerifyCanaryResult"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Full verify-canary envelope when verification ran; None when canary was skipped or submit was deduped."
    },
    "canary_skipped_reason": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Code-rendered disclosure naming WHY the two-phase GATE skipped the canary (latency-audit #10 / fallback-inventory S1): the #249 TTL cache hit — this exact (cmd_sha, version, cluster) was canary-validated within the 4h window, so the gate honours it instead of re-running the probe. verified=True with canary_run_id=None then means 'cached validation stood in', DISTINCT from a canary=false opt-out (verified=False) and from a failed canary. Null when a canary actually ran or was opted out. A gated skip is never silent.",
      "title": "Canary Skipped Reason"
    },
    "validated_age_sec": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Age in seconds of the cached canary validation the gate honoured (the structured form of canary_skipped_reason's '<age> ago'). Null unless canary_skipped_reason is set.",
      "title": "Validated Age Sec"
    },
    "reducer_check": {
      "anyOf": [
        {
          "$ref": "#/$defs/ReducerCheckResult"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Rung-2 reducibility check (docs/plans/amortized-reduction-check-2026-07-17.md): the outcome of EXECUTING the run's declared custom reducer against the verified canary's ONE real task-0 row, before the main array launches. None when no canary ran / the run declares no custom reducer / the check was opted out. A `disclosed` or `unverified` status is a LOUD brief disclosure, never a block — the bare `y` stands."
    }
  },
  "$defs": {
    "CanaryFailureFeatures": {
      "additionalProperties": false,
      "description": "Structured diagnostic evidence attached to a failed canary envelope.\n\nToday (pre-fix) a canary that died cluster-side returned\n``{failure_kind: \"dispatcher_failed\"}`` with no cluster context — the\norchestrator's prose-level \"go fetch the log\" step was the only path\nto the actual error, and agents kept skipping it. This object moves\nthat step into framework code: the same ``stderr_tail`` is restated\nunder ``cluster_log_tail`` for structured consumers, and\n``classified_error`` carries the\n:func:`hpc_agent.infra.failure_signatures.classify` result so a\ndecision-maker reads an ``error_class`` + ``suggested_fix`` instead\nof paraphrasing the log.",
      "properties": {
        "cluster_log_tail": {
          "description": "Raw last ~50 lines of the canary's cluster log, verbatim. The same content as the top-level ``stderr_tail`` field, restated under a structured key so downstream consumers don't have to know which top-level field carries it.",
          "title": "Cluster Log Tail",
          "type": "string"
        },
        "log_path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Remote path of the cluster log file the tail was read from, when known. Lets the operator ssh over and tail more if 50 lines isn't enough. ``None`` when no log was fetched.",
          "title": "Log Path"
        },
        "classified_error": {
          "anyOf": [
            {
              "additionalProperties": true,
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The ``{error_class, suggested_fix, matched_pattern}`` triple returned by :func:`infra.failure_signatures.classify` on the cluster log tail. ``None`` when the stderr was empty (nothing to classify).",
          "title": "Classified Error"
        }
      },
      "required": [
        "cluster_log_tail"
      ],
      "title": "CanaryFailureFeatures",
      "type": "object"
    },
    "ReducerCheckResult": {
      "additionalProperties": false,
      "description": "Rung-2 reducibility-ladder outcome: EXECUTING the run's declared custom\nreducer against the verified canary's ONE real task-0 row, before the main\narray launches (``docs/plans/amortized-reduction-check-2026-07-17.md``).\n\nRung 1 (S1) is a STATIC predicate — \"is a reducer *declared*?\"; this rung is\na DYNAMIC proof — \"does the declared reducer *execute*?\" — running the SAME\n``cluster_reduce`` the final harvest runs (one-definition), so a broken\nreducer (py3.8-vs-3.13, a missing import, a wrong output path, non-JSON\noutput) is caught NOW at zero critical-path wall-clock instead of hours later\nmid-harvest after the whole array computed. It asserts only the reducer's\ncontract SHAPE (exit 0, parseable JSON, top-level keys) against one row —\nnever a VALUE (a single canary row's aggregate number is meaningless;\ncorrectness is the aggregate stage's job).\n\n``status``:\n\n* ``passed`` — the reducer exited 0 and emitted parseable JSON.\n* ``disclosed`` — the reducer RAN and produced positive evidence of a problem\n  (non-zero exit / missing output / non-JSON). A LOUD, never-auto-masked\n  disclosure on the S2/S3 brief — NEVER a hard block: the human's bare ``y``\n  still crosses it (the failure MIGHT be a benign \"needs ≥2 rows\" false alarm\n  a single canary row cannot satisfy — the machinery surfaces the verbatim\n  stderr and stops, it never interprets \"broken code\" vs \"needs more rows\").\n* ``unverified`` — the check could not COMPLETE (ssh severed / breaker open /\n  timeout). UNKNOWN, never reported as passed (positive-evidence-only, the\n  same posture as ``reporter_unreachable`` and the combiner truncation rule).\n* ``skipped`` — the run declares no custom ``aggregate_cmd`` (the built-in\n  mean is framework code, nothing to check) or the check was opted out\n  (``HPC_NO_CANARY_REDUCER_CHECK=1``). Byte-identical to a pre-feature run.",
      "properties": {
        "status": {
          "description": "passed | disclosed | unverified | skipped — see the class docstring.",
          "enum": [
            "passed",
            "disclosed",
            "unverified",
            "skipped"
          ],
          "title": "Status",
          "type": "string"
        },
        "reducer_cmd": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The declared custom aggregate_cmd that was executed against the canary row. None when skipped (no custom reducer / opted out).",
          "title": "Reducer Cmd"
        },
        "exit_code": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The reducer's exit code when the reduce ran to an exit (0 on passed). None when the reduce could not complete (unverified / skipped) or the code is not recoverable from the failure surface (disclosed).",
          "title": "Exit Code"
        },
        "stderr_tail": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Verbatim reducer error text carried for the disclose / unverified paths so the human reads the real error. None on a clean pass / skip.",
          "title": "Stderr Tail"
        },
        "output_keys": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Sorted top-level keys of the reducer's JSON output on a pass — positive evidence of the contract SHAPE (never the values). None off the pass path.",
          "title": "Output Keys"
        },
        "disclosure": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "One-line code-rendered disclosure the block loop relays VERBATIM onto the S2/S3 brief (set on disclosed / unverified). None when there is nothing to surface (passed / skipped).",
          "title": "Disclosure"
        }
      },
      "required": [
        "status"
      ],
      "title": "canary reducer-check result",
      "type": "object"
    },
    "VerifyCanaryResult": {
      "additionalProperties": false,
      "description": "Result of the wait + grep + output-check protocol for a 1-task canary.\n\nCaller branches on ``ok``: True → main array submit; False →\nsurface stderr_tail to the user verbatim.",
      "properties": {
        "ok": {
          "title": "Ok",
          "type": "boolean"
        },
        "failure_kind": {
          "anyOf": [
            {
              "enum": [
                "dispatcher_failed",
                "import_error",
                "module_not_found",
                "traceback",
                "oom_killed",
                "segfault",
                "nonzero_exit",
                "missing_output",
                "reporter_unreachable",
                "canary_failed",
                "completed_unknown",
                "timeout",
                "abandoned",
                "checkpoint_missing",
                "checkpoint_unloadable"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Failure Kind"
        },
        "details": {
          "description": "One-line human-readable summary the slash command surfaces above the raw stderr_tail.",
          "title": "Details",
          "type": "string"
        },
        "stderr_tail": {
          "description": "Last ~50 lines of the canary's stderr log (or empty string when not retrievable).",
          "title": "Stderr Tail",
          "type": "string"
        },
        "metrics_fingerprint": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional sha256 of the canary's expected output file when the caller asked for a fingerprint. None on every failure path and when fingerprinting is skipped or fails.",
          "title": "Metrics Fingerprint"
        },
        "failure_features": {
          "anyOf": [
            {
              "$ref": "#/$defs/CanaryFailureFeatures"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Structured diagnostic evidence attached to failed-canary envelopes (``ok=False``). Carries the raw cluster log tail under a structured key plus a ``classify()`` result against the same failure-signature CATALOG ``ops/recover`` uses, so the orchestrator gets an actionable remediation instead of a bare ``failure_kind``. ``None`` on success."
        }
      },
      "required": [
        "ok",
        "details",
        "stderr_tail"
      ],
      "title": "verify-canary output",
      "type": "object"
    }
  }
}
