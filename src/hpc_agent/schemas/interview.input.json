{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/interview.input.json",
  "title": "interview intent",
  "description": "Structured campaign intent produced by an interview between the hpc agent and either an external orchestrator or a human.\n\nDeliberately bare-bones: captures *why* (goal, transcript,\nprovenance), *how big* (task_count), *what tags* (task_kind),\nand *what limits* (budget, abort_if). Does NOT typed-encode the\nsearch space — that would narrow the existing\nexperiment-agnostic tasks.py contract.",
  "type": "object",
  "required": [
    "goal",
    "task_count",
    "produced_by"
  ],
  "additionalProperties": false,
  "properties": {
    "goal": {
      "description": "Free-text campaign goal, ~one sentence.",
      "minLength": 1,
      "title": "Goal",
      "type": "string"
    },
    "task_count": {
      "description": "Expected number of tasks. The materializer asserts tasks.total() == task_count and refuses to write interview.json on mismatch — catches off-by-one bugs in the agent-written tasks.py at the interview stage.",
      "minimum": 1,
      "title": "Task Count",
      "type": "integer"
    },
    "task_kind": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Free-text tag the caller picks to group related campaigns for recall queries. hpc-agent does not own this vocabulary — there is no enum, no canonical set, and the framework treats it as an opaque string. Common shapes callers use today (purely as examples; you are not required to match them): 'ml-hparam-sweep', 'rl-rollout', 'llm-prompt-eval', 'benchmark-perf', 'data-shard'. recall groups by exact-match on whatever string the caller wrote, so sticking with a stable vocabulary within one project makes the rollup more useful — but that's a caller-side convention, not a framework requirement.",
      "title": "Task Kind"
    },
    "budget": {
      "anyOf": [
        {
          "$ref": "#/$defs/_BudgetSpec"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Soft caps. Opaque dict — units chosen by interview agent."
    },
    "abort_if": {
      "anyOf": [
        {
          "$ref": "#/$defs/_AbortIfSpec"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional early-stop criterion. Loose shape."
    },
    "cluster_target": {
      "anyOf": [
        {
          "$ref": "#/$defs/_ClusterTargetSpec"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional. When present, submit_flow uses these directly. When omitted, the planner is invoked."
    },
    "produced_by": {
      "$ref": "#/$defs/_Provenance",
      "description": "Who/what produced this intent. Indexed by cmd_recall."
    },
    "transcript": {
      "anyOf": [
        {
          "items": {
            "$ref": "#/$defs/_TranscriptTurn"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Interview Q/A turns. Optional but strongly recommended for human interviews — the value of being able to explain 'why did this campaign target cluster X' three months later vastly outweighs the few KB of storage. For agent-driven interviews this is typically the agent's tool-call trace.",
      "title": "Transcript"
    },
    "notes": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Free-form supplementary notes from the interview.",
      "title": "Notes"
    },
    "task_generator": {
      "anyOf": [
        {
          "discriminator": {
            "mapping": {
              "cartesian_product": "#/$defs/_CartesianProduct",
              "enumerated": "#/$defs/_Enumerated",
              "items_x_seeds": "#/$defs/_ItemsXSeeds",
              "numeric_linspace": "#/$defs/_NumericLinspace",
              "numeric_logspace": "#/$defs/_NumericLogspace"
            },
            "propertyName": "kind"
          },
          "oneOf": [
            {
              "$ref": "#/$defs/_Enumerated"
            },
            {
              "$ref": "#/$defs/_CartesianProduct"
            },
            {
              "$ref": "#/$defs/_ItemsXSeeds"
            },
            {
              "$ref": "#/$defs/_NumericLogspace"
            },
            {
              "$ref": "#/$defs/_NumericLinspace"
            }
          ]
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional opt-in: when present, the materializer regenerates tasks.py from a typed recipe instead of consuming the agent-written one. The five shapes cover common ground without locking out exotic campaigns. Materializer cross-checks the produced task count against intent.task_count before any disk writes.",
      "title": "Task Generator"
    },
    "entry_point": {
      "anyOf": [
        {
          "discriminator": {
            "mapping": {
              "python_module": "#/$defs/_PythonModuleEntry",
              "register_run": "#/$defs/_RegisterRunEntry",
              "shell_command": "#/$defs/_ShellCommandEntry"
            },
            "propertyName": "kind"
          },
          "oneOf": [
            {
              "$ref": "#/$defs/_RegisterRunEntry"
            },
            {
              "$ref": "#/$defs/_PythonModuleEntry"
            },
            {
              "$ref": "#/$defs/_ShellCommandEntry"
            }
          ]
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional declaration of the experiment's entry point. Three shapes, with a strong default: ``register_run`` (the canonical Python path — a pointer to an ``@register_run``-decorated function the framework discovers directly; no materialization) and ``python_module`` (a second canonical Python path — an importable module + function) are preferred whenever a Python entry-point function is available. ``shell_command`` (argv + signature; the interview materializes a ``@register_run`` wrapper at ``.hpc/wrappers/<run_name>.py`` that subprocess-invokes the argv) is the **fallback**, used only when direct ``@register_run`` decoration isn't possible (non-Python entry point, decorator conflict, vendor code). Lets a mature repo with ``main.py`` + frozen YAML configs participate in the same intake as a greenfield notebook — direct decoration is a two-line code edit; the wrapper is a subprocess shim that gives the framework something to introspect when the entry point itself can't be decorated.",
      "title": "Entry Point"
    },
    "audited_source": {
      "anyOf": [
        {
          "$ref": "#/$defs/_AuditedSource"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Opt-in link to an audited ``.py`` source and its audit trail (D7). When present, the graduation gate refuses an entry point not hash-linked to a current audit; when ABSENT every notebook-audit gate passes silently and interview.json is byte-identical to the pre-audit output. The caller authors the ``audit_id`` slug — core never invents or defaults it (the fabrication class)."
    },
    "packs": {
      "anyOf": [
        {
          "items": {
            "$ref": "#/$defs/PackOptIn"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Opt-in domain-pack bindings (bind-as-data). Sibling to ``audited_source``: when present, each entry references a pack manifest (relpath core reads + hashes) and the caller-authored receipt-binding slots a gate requires; when ABSENT every pack gate returns silently and interview.json is byte-identical to a repo that never opted in (the D7 fail-safe). Core copies the opaque ``{pack, version, sha}`` echo verbatim and never reads a declared pack value for meaning.",
      "title": "Packs"
    },
    "actors": {
      "anyOf": [
        {
          "$ref": "#/$defs/ActorsBlock"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Opt-in multi-human actor declaration (MH1). Sibling to ``packs`` / ``audited_source``: when present, ``ids`` names the shared repo's actor slugs and optional ``policy`` delegates gated blocks to actor subsets; when ABSENT (or with fewer than two ids) every identity comparison and policy consultation returns silently and interview.json is byte-identical to today's single-actor system (the D7 fail-safe). Slugs are opaque to core — never a role vocabulary; the attribution tier is harness-asserted, never verified. A session's own actor arrives via the HPC_ACTOR harness config, never this spec (the model must not choose its identity)."
    }
  },
  "$defs": {
    "ActorsBlock": {
      "additionalProperties": false,
      "description": "The multi-human ``actors`` opt-in on the InterviewSpec (MH1).\n\nSibling to ``packs`` / ``audited_source``: a caller-declared block,\npersisted verbatim in interview.json, absent → byte-identical (the D7\nfail-safe, ``exclude_none``). Makes today's IMPLICIT single-actor identity\nEXPLICIT — an opaque slug the harness stamps and gates COMPARE, without core\never verifying who anyone is (the honest tier is *harness-asserted*, never\nverified).\n\n``ids`` — the declared actor slugs. Each uses the shared filesystem-safe\ntag class (``RunIdStrict`` — the same ``^[A-Za-z0-9._\\-]+$`` class\n``state/scopes.py::validate_tag`` pins), because a slug becomes a PATH\nSEGMENT in the per-actor utterance-log locator (MH2): the shape is\nload-bearing, not stylistic. Opaque to core — NEVER a role vocabulary: core\nhas no idea what a \"PI\", \"postdoc\", or \"reviewer\" is and no field may ever\ncarry those words (the caller-vocabulary rule).\n\n**Default-single-actor semantics (the D7 posture, exactly):** an absent\nblock, or ``ids`` with fewer than two entries, means every identity\ncomparison and policy consultation in every gate returns silently,\nbyte-identical to today. Zero declared actors is not an error, not a\nwarning — it is today's system; so ``ids`` may be empty.\n\n``policy`` — optional delegation mapping (MH8): ``{<gated block name>:\n[<actor slug>, ...]}``. Keys are existing gated block names core already\nowns (``\"notebook-sign-off\"``, ``\"campaign-greenlight\"``, ``\"scope-unlock\"``,\n``\"registration\"`` when the sibling kernel lands) — opaque strings core\nmembership-tests, never a closed vocabulary pinned here. Values are subsets\nof ``ids``. Absent → no policy gating. A policy entry naming an actor NOT in\n``ids`` is a LOUD refusal at validation time (the dangling-reference\nposture: an opted-in reference core cannot resolve must never silently\npass) — deliberately NOT D7 silence, which belongs to the un-opted-in world.\n\n**How a session knows its actor is harness configuration** (``HPC_ACTOR``),\nNOT a spec field: an agent-suppliable actor would let the model choose its\nidentity. The actor arrives from outside the model's tool surface, exactly\nlike the utterance text itself.",
      "properties": {
        "ids": {
          "description": "Declared actor slugs (the shared filesystem-safe tag class — each becomes a path segment in the per-actor utterance-log locator). Opaque to core; never a role vocabulary. Fewer than two entries → single-actor semantics (comparisons stay off), byte-identical to today. May be empty (zero declared actors is not an error).",
          "items": {
            "pattern": "^[A-Za-z0-9._\\-]+$",
            "type": "string"
          },
          "title": "Ids",
          "type": "array"
        },
        "policy": {
          "anyOf": [
            {
              "additionalProperties": {
                "items": {
                  "pattern": "^[A-Za-z0-9._\\-]+$",
                  "type": "string"
                },
                "type": "array"
              },
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional delegation mapping {<gated block name>: [<actor slug>, ...]} (MH8). Keys are existing gated block names core membership-tests (opaque strings, never a closed vocabulary); values are subsets of ``ids``. Absent → no policy gating. A value slug not in ``ids`` is refused at validation (the dangling-reference posture). Pure lists+mappings core COMPARES, never evaluates — never a predicate, a role vocabulary, or a quorum scheme.",
          "title": "Policy"
        }
      },
      "title": "ActorsBlock",
      "type": "object"
    },
    "PackOptIn": {
      "additionalProperties": false,
      "description": "One domain-pack opt-in entry on the InterviewSpec ``packs`` block (bind-as-data).\n\nSibling to ``audited_source``: a caller-referenced pack, persisted verbatim\nin interview.json, absent → byte-identical. ``pack`` is the pack slug (it\nkeys the pack decision journal, so it is filesystem-safe); ``manifest`` is a\ncampaign-dir-relative path to the pack manifest core reads and hashes (the\n``_AuditedSource`` relpath precedent — never a blessed dir, never a search\npath, DP1). ``receipt_bindings`` lists the caller-authored slot→pack\nobligations the gate requires; empty means the pack contributes seam data\n(vocabularies, patterns) but gates on no receipt.\n\nCore copies the ``{pack, version, sha}`` echo verbatim onto every record\nthat consumed pack content and never reads a declared value for meaning —\nidentity only.",
      "properties": {
        "pack": {
          "description": "Pack slug — keys the pack decision journal (``.hpc/packs/<pack>.decisions.jsonl``), so it must be filesystem-safe. Opaque to core; never a core vocabulary.",
          "minLength": 1,
          "pattern": "^[A-Za-z0-9._\\-]+$",
          "title": "Pack",
          "type": "string"
        },
        "manifest": {
          "description": "Campaign-dir-relative path to the pack manifest core reads and hashes (the ``_AuditedSource`` relpath precedent). Core never asks how the bytes got there (DP3: distribution invisible).",
          "minLength": 1,
          "title": "Manifest",
          "type": "string"
        },
        "receipt_bindings": {
          "description": "Caller-authored slot→pack receipt obligations the gate requires (DP4). Empty → the pack contributes seam data but gates on no receipt. Renamed from ``required_receipts`` (coherence review) to disambiguate from the S6 manifest slug list.",
          "items": {
            "$ref": "#/$defs/ReceiptBinding"
          },
          "title": "Receipt Bindings",
          "type": "array"
        }
      },
      "required": [
        "pack",
        "manifest"
      ],
      "title": "PackOptIn",
      "type": "object"
    },
    "ReceiptBinding": {
      "additionalProperties": false,
      "description": "One caller-authored receipt obligation inside a ``packs`` opt-in entry.\n\nBinds a caller-named *slot* (the obligation, e.g. ``\"data-audit\"``) to the\n*pack* whose current receipt fills it. Both are opaque slugs core never\ninterprets — the slot is the caller's name for the requirement (DP4: a\nrequirement always originates with the caller, never the pack), the pack is\nthe name a bound pack manifest declares. Cross-pack is legal: the slot's\n``pack`` need not equal the enclosing entry's ``pack``.\n\n``receipt_bindings`` is the object form, renamed from ``required_receipts``\nin the coherence review (2026-07-07) to disambiguate it from the S6 manifest\nlist ``required_receipts: [<slot slug>]`` — that list is a plain slug list on\nthe pack side; this is the caller-side slot→pack binding.",
      "properties": {
        "slot": {
          "description": "Caller-authored slot slug — the caller's name for one receipt obligation a gate requires (opaque to core; the fabrication class forbids core inventing or defaulting one).",
          "minLength": 1,
          "pattern": "^[A-Za-z0-9._\\-]+$",
          "title": "Slot",
          "type": "string"
        },
        "pack": {
          "description": "The pack whose current receipt fills this slot (a bound-pack slug). Need not equal the enclosing entry's pack — a slot may bind cross-pack.",
          "minLength": 1,
          "pattern": "^[A-Za-z0-9._\\-]+$",
          "title": "Pack",
          "type": "string"
        }
      },
      "required": [
        "slot",
        "pack"
      ],
      "title": "ReceiptBinding",
      "type": "object"
    },
    "_AbortIfSpec": {
      "additionalProperties": true,
      "properties": {
        "metric": {
          "minLength": 1,
          "title": "Metric",
          "type": "string"
        },
        "after_tasks": {
          "minimum": 1,
          "title": "After Tasks",
          "type": "integer"
        }
      },
      "required": [
        "metric",
        "after_tasks"
      ],
      "title": "_AbortIfSpec",
      "type": "object"
    },
    "_AuditedSource": {
      "additionalProperties": false,
      "description": "Opt-in link from the campaign to an audited ``.py`` source and its audit trail (D7).\n\nPresent only when the notebook-audit prelude produced the experiment\ncode (idea → LLM draft → human audit → GRADUATION). When present the\ndownstream graduation gate refuses an entry point not hash-linked to a\ncurrent audit; when the whole field is ABSENT every notebook-audit gate\npasses silently and interview.json is byte-identical to the pre-audit\noutput (the fail-safe posture).\n\n``source`` and ``template`` are campaign-dir-relative paths to the\npercent-format ``.py`` (jupytext ``# %%`` cells) — the source of truth,\nnot a rendered notebook. ``audit_id`` is the CALLER-authored slug that\nkeys the audit's decision-journal trail\n(``.hpc/notebooks/<audit_id>.decisions.jsonl``); core never invents or\ndefaults it (the fabrication class, D3). ``rendered_notebook`` is\nopaque render metadata (the caller-side jupytext/nbclient projection):\nrecorded verbatim, NEVER hashed or validated by core — the audit\nidentity is the ``.py`` source, not its notebook render.",
      "properties": {
        "source": {
          "description": "Campaign-dir-relative path to the audited percent-format ``.py`` source of truth (jupytext ``# %%`` cells).",
          "minLength": 1,
          "title": "Source",
          "type": "string"
        },
        "audit_id": {
          "description": "Caller-authored slug keying the audit's decision-journal trail (``.hpc/notebooks/<audit_id>.decisions.jsonl``). Never invented or defaulted by core — the caller owns this identity (D3).",
          "minLength": 1,
          "title": "Audit Id",
          "type": "string"
        },
        "template": {
          "description": "Campaign-dir-relative path to the percent-format ``.py`` template the source was drafted from (diff-from-template is the audit view).",
          "minLength": 1,
          "title": "Template",
          "type": "string"
        },
        "rendered_notebook": {
          "anyOf": [
            {
              "additionalProperties": true,
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional opaque render metadata (the caller-side jupytext/nbclient notebook projection). Recorded verbatim; NEVER hashed or validated by core — the audit identity is the ``.py`` source, not its render.",
          "title": "Rendered Notebook"
        },
        "input_roots": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Opaque data-path roots the executes-live lint tested path literals against during the audit. Persisted so the graduation/sign-off gate recomputes the SAME lint findings (a data path that later vanishes flips the section's tier and moves its view_sha). Absent → [].",
          "title": "Input Roots"
        },
        "source_roots": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Opaque import roots the linked-sources lint resolved imports under during the audit. Persisted alongside input_roots so the canonical view is recomputable. Absent → [].",
          "title": "Source Roots"
        },
        "attention_order": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The section-slug presentation order the audit view used (T12). Feeds the module roll-up view_sha (it changes what the human saw). Absent → source order.",
          "title": "Attention Order"
        },
        "output_roots": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Opaque WRITE-target roots: a path literal under one is a declared output, exempt from the executes-live not-exists flag (reported in declared_outputs, never flagged — the run-#10 output-literal noise fix). Absent → [].",
          "title": "Output Roots"
        }
      },
      "required": [
        "source",
        "audit_id",
        "template"
      ],
      "title": "_AuditedSource",
      "type": "object"
    },
    "_BudgetSpec": {
      "additionalProperties": true,
      "properties": {
        "wall_clock_max_h": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Wall Clock Max H"
        }
      },
      "title": "_BudgetSpec",
      "type": "object"
    },
    "_CartesianProduct": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "cartesian_product",
          "title": "Kind",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/_CartesianProductParams"
        }
      },
      "required": [
        "kind",
        "params"
      ],
      "title": "_CartesianProduct",
      "type": "object"
    },
    "_CartesianProductParams": {
      "additionalProperties": false,
      "properties": {
        "axes": {
          "additionalProperties": {
            "items": {},
            "type": "array"
          },
          "description": "Named axes; tasks = full Cartesian product. resolve(i) returns a dict with one key per axis.",
          "minProperties": 1,
          "title": "Axes",
          "type": "object"
        }
      },
      "required": [
        "axes"
      ],
      "title": "_CartesianProductParams",
      "type": "object"
    },
    "_ClusterTargetSpec": {
      "additionalProperties": false,
      "properties": {
        "cluster": {
          "minLength": 1,
          "title": "Cluster",
          "type": "string"
        },
        "profile": {
          "minLength": 1,
          "title": "Profile",
          "type": "string"
        },
        "constraint": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Constraint"
        }
      },
      "required": [
        "cluster",
        "profile"
      ],
      "title": "_ClusterTargetSpec",
      "type": "object"
    },
    "_DataAxisHint": {
      "additionalProperties": false,
      "description": "Pre-declared series-axis classification.\n\nFor shell-out wrappers ``classify-axis`` cannot introspect the body\n(it's a ``subprocess.check_call``). When the experimenter knows the\nclassification — usually they do, because they wrote ``main.py`` —\nthey can declare it here and the interview persists it so\nclassify-axis records it directly without an introspection step.",
      "properties": {
        "kind": {
          "enum": [
            "independent",
            "associative",
            "bounded_halo",
            "sequential"
          ],
          "title": "Kind",
          "type": "string"
        },
        "halo": {
          "anyOf": [
            {
              "$ref": "#/$defs/_HaloHint"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Required when ``kind='bounded_halo'``; otherwise must be omitted."
        },
        "monoid": {
          "anyOf": [
            {
              "enum": [
                "sum",
                "moments"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional for ``kind='associative'``; defaults to 'moments'. Ignored for other kinds.",
          "title": "Monoid"
        }
      },
      "required": [
        "kind"
      ],
      "title": "_DataAxisHint",
      "type": "object"
    },
    "_Enumerated": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "enumerated",
          "title": "Kind",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/_EnumeratedParams"
        }
      },
      "required": [
        "kind",
        "params"
      ],
      "title": "_Enumerated",
      "type": "object"
    },
    "_EnumeratedParams": {
      "additionalProperties": false,
      "properties": {
        "items": {
          "description": "List of task-kwargs dicts. Most agnostic shape — just stores the list verbatim and resolve(i) returns items[i].",
          "items": {
            "additionalProperties": true,
            "type": "object"
          },
          "minItems": 1,
          "title": "Items",
          "type": "array"
        }
      },
      "required": [
        "items"
      ],
      "title": "_EnumeratedParams",
      "type": "object"
    },
    "_HaloHint": {
      "additionalProperties": false,
      "description": "Halo expression for a ``bounded_halo`` ``data_axis_hint``.",
      "properties": {
        "expr": {
          "description": "Arithmetic-only expression over the wrapper's parameters; the same restricted-AST form classify-axis already accepts. Example: ``train_window * 48``.",
          "minLength": 1,
          "title": "Expr",
          "type": "string"
        }
      },
      "required": [
        "expr"
      ],
      "title": "_HaloHint",
      "type": "object"
    },
    "_ItemsXSeeds": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "items_x_seeds",
          "title": "Kind",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/_ItemsXSeedsParams"
        }
      },
      "required": [
        "kind",
        "params"
      ],
      "title": "_ItemsXSeeds",
      "type": "object"
    },
    "_ItemsXSeedsParams": {
      "additionalProperties": false,
      "properties": {
        "items": {
          "items": {
            "additionalProperties": true,
            "type": "object"
          },
          "minItems": 1,
          "title": "Items",
          "type": "array"
        },
        "seeds": {
          "items": {
            "type": "integer"
          },
          "minItems": 1,
          "title": "Seeds",
          "type": "array"
        }
      },
      "required": [
        "seeds"
      ],
      "title": "_ItemsXSeedsParams",
      "type": "object"
    },
    "_NumericLinspace": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "numeric_linspace",
          "title": "Kind",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/_NumericLinspaceParams"
        }
      },
      "required": [
        "kind",
        "params"
      ],
      "title": "_NumericLinspace",
      "type": "object"
    },
    "_NumericLinspaceParams": {
      "additionalProperties": false,
      "properties": {
        "param": {
          "minLength": 1,
          "title": "Param",
          "type": "string"
        },
        "low": {
          "title": "Low",
          "type": "number"
        },
        "high": {
          "title": "High",
          "type": "number"
        },
        "n": {
          "minimum": 2,
          "title": "N",
          "type": "integer"
        }
      },
      "required": [
        "param",
        "low",
        "high",
        "n"
      ],
      "title": "_NumericLinspaceParams",
      "type": "object"
    },
    "_NumericLogspace": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "numeric_logspace",
          "title": "Kind",
          "type": "string"
        },
        "params": {
          "$ref": "#/$defs/_NumericLogspaceParams"
        }
      },
      "required": [
        "kind",
        "params"
      ],
      "title": "_NumericLogspace",
      "type": "object"
    },
    "_NumericLogspaceParams": {
      "additionalProperties": false,
      "properties": {
        "param": {
          "minLength": 1,
          "title": "Param",
          "type": "string"
        },
        "low": {
          "exclusiveMinimum": 0,
          "title": "Low",
          "type": "number"
        },
        "high": {
          "exclusiveMinimum": 0,
          "title": "High",
          "type": "number"
        },
        "n": {
          "minimum": 2,
          "title": "N",
          "type": "integer"
        },
        "base": {
          "default": 10.0,
          "exclusiveMinimum": 1,
          "title": "Base",
          "type": "number"
        }
      },
      "required": [
        "param",
        "low",
        "high",
        "n"
      ],
      "title": "_NumericLogspaceParams",
      "type": "object"
    },
    "_PetscSolverHint": {
      "additionalProperties": false,
      "description": "Checkpoint-instrumentation hint for a PETSc-based shell_command entry point.\n\nA PETSc app owns its solve loop (``TSSolve``/``SNESSolve`` are library\ncode), so the executor-side checkpoint helpers can't be called from a\nloop body the way ``run_iterations`` assumes. PETSc's options database is\nthe injection channel instead: any app that calls ``setFromOptions()``\nhonors ``PETSC_OPTIONS`` from the environment. Declaring this hint makes\nthe interview materialize a checkpoint-instrumented wrapper that exports\nthe per-step solution-dump option (into the stable per-task checkpoint\ndir), caps the solve at 2 steps under the checkpoint-canary probe, and —\nwhen ``resume_flag`` is declared — hands a previous attempt's dump back\nto the app on resume. The entry point itself stays opaque and untouched.\n\n``resume_flag`` is deliberately explicit: writing checkpoints is generic,\nbut *loading* one is app-specific (there is no universal PETSc restart\noption). Omit it and the wrapper still writes checkpoints; resume just\nnever feeds them back.",
      "properties": {
        "kind": {
          "const": "petsc",
          "title": "Kind",
          "type": "string"
        },
        "solver_object": {
          "default": "ts",
          "description": "Which PETSc object owns the solve loop: 'ts' (time stepper — the usual shape for a transient PDE solve) or 'snes' (nonlinear solver). Selects the option family the wrapper exports (-ts_monitor_solution / -snes_monitor_solution and the canary's -ts_max_steps 2 / -snes_max_it 2).",
          "enum": [
            "ts",
            "snes"
          ],
          "title": "Solver Object",
          "type": "string"
        },
        "resume_flag": {
          "anyOf": [
            {
              "pattern": "^-{1,2}[A-Za-z0-9_][A-Za-z0-9_\\-]*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The app's own restart flag (e.g. '-restart_file' or '--resume-from'). When declared, the wrapper appends ``<resume_flag> <path-to-previous-attempt-dump>`` to argv on a resumed attempt. The app must interpret the file (a PETSc binary viewer dump). Omit when the app has no restart surface.",
          "title": "Resume Flag"
        }
      },
      "required": [
        "kind"
      ],
      "title": "_PetscSolverHint",
      "type": "object"
    },
    "_Provenance": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "enum": [
            "agent",
            "human"
          ],
          "title": "Kind",
          "type": "string"
        },
        "session_sha": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Agent/orchestrator session identifier. Required when kind=agent; null otherwise.",
          "title": "Session Sha"
        },
        "at": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "ISO-8601 timestamp.",
          "title": "At"
        },
        "operator": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Human operator name/handle when kind=human; null when kind=agent.",
          "title": "Operator"
        }
      },
      "required": [
        "kind"
      ],
      "title": "_Provenance",
      "type": "object"
    },
    "_PythonModuleEntry": {
      "additionalProperties": false,
      "description": "Canonical Python entry point: an importable module + function.\n\nA second canonical Python shape, alongside ``register_run``: the\nframework can already introspect importable Python, so this kind is\na wire hint so the interview can validate the module/function exists\nbefore submit. No wrapper is materialized. Prefer ``register_run``\nwhen the user can decorate the function directly; use this kind for\nimportable Python the framework should target by dotted path\ninstead of by ``@register_run`` discovery.",
      "properties": {
        "kind": {
          "const": "python_module",
          "title": "Kind",
          "type": "string"
        },
        "module": {
          "description": "Dotted module path, e.g. ``my_pkg.train``.",
          "minLength": 1,
          "title": "Module",
          "type": "string"
        },
        "function": {
          "default": "main",
          "description": "The function inside ``module`` to treat as the entry point.",
          "minLength": 1,
          "title": "Function",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "module"
      ],
      "title": "_PythonModuleEntry",
      "type": "object"
    },
    "_RegisterRunEntry": {
      "additionalProperties": false,
      "description": "Canonical Python entry point: an ``@register_run``-decorated function.\n\nThe default and recommended shape for Python repos. The user puts\n``@register_run`` directly on the function the framework should treat\nas the entry point — whether it lives in a notebook\n(``notebooks/<name>.ipynb``) or a ``.py`` file (``train.py``,\n``main.py``). Pure pointer — no materialization. The framework's\nexisting ``discover_runs`` walks the experiment to find the function\nby ``run_name``. For a mature repo with an existing entry-point\nfunction, this is a two-line code edit (an import and a decorator)\nand is strongly preferred over ``shell_command`` whenever direct\ndecoration is possible.",
      "properties": {
        "kind": {
          "const": "register_run",
          "title": "Kind",
          "type": "string"
        },
        "run_name": {
          "description": "The ``@register_run`` function name to discover (in notebooks/ or .py files under the experiment).",
          "minLength": 1,
          "title": "Run Name",
          "type": "string"
        },
        "fixed_params": {
          "additionalProperties": true,
          "description": "Constant (non-axis) kwargs the executor requires but that do not vary per task — e.g. {'samples': 10000} when only 'seed' is a sweep axis. Each is baked into every materialized task's kwargs (resolve(i)), so the cluster exports HPC_KW_<param> and the executor command is complete. Keys must be valid Python identifiers; values are JSON scalars. A swept axis of the same name wins (the axis value is per-task; this is the constant fallback). Requires task_generator — a hand-written tasks.py must include the constants itself. Resolves #195: a required signature param left uncovered.",
          "title": "Fixed Params",
          "type": "object"
        }
      },
      "required": [
        "kind",
        "run_name"
      ],
      "title": "_RegisterRunEntry",
      "type": "object"
    },
    "_ShellCommandEntry": {
      "additionalProperties": false,
      "description": "Fallback entry point: a shell command (compiled binary, decorator-conflicting CLI, ...).\n\n**The fallback path, used only when direct ``@register_run``\ndecoration isn't possible** — non-Python entry points (shell\nscripts, compiled binaries), CLI libraries whose decorators conflict\nwith ``@register_run`` (e.g. ``@hydra.main`` rewrites the signature),\nor vendor code the user can't edit. For a Python repo with an\nimportable entry-point function, prefer ``register_run`` instead:\na two-line code edit beats a subprocess shim, and the framework\nintrospects the real function rather than the wrapper.\n\nThe interview materializes a thin ``@register_run`` wrapper at\n``.hpc/wrappers/<run_name>.py`` whose body subprocess-invokes the\nargv with kwargs substituted in. The wrapper's *signature* (built\nfrom ``signature``) is what downstream introspection reads;\nthe underlying entry point stays opaque to the framework.\n\n``frozen_configs`` lets the experimenter declare config files\nwhose content is part of the experiment's identity. The\nmaterializer hashes each path's bytes; the interview threads\n``<basename>_sha`` into every materialized task's kwargs so the\nframework's ``cmd_sha`` correctly distinguishes ``exp_42.yaml``\nfrom ``exp_43.yaml`` (and catches accidental in-place edits).\n\n*Constraint*: ``frozen_configs`` requires ``task_generator`` (so the\nframework has somewhere to thread the shas). A hand-written tasks.py\nplus ``frozen_configs`` is rejected at interview time — the framework\ncan't safely edit the user's hand-rolled file, and silently dropping\nthe shas would defeat the identity guarantee. Use ``task_generator``\nor include the shas in your own tasks.py kwargs.\n\n*Timing*: ``frozen_configs`` are hashed at interview time. If the\nYAML is edited between interview and submit, the stored sha is the\ninterview-time content; the cluster runs whatever rsync ships. The\nwindow is small (interview is typically immediately before submit)\nbut real — re-run the interview to refresh.",
      "properties": {
        "kind": {
          "const": "shell_command",
          "title": "Kind",
          "type": "string"
        },
        "run_name": {
          "description": "Name for the generated wrapper file (``.hpc/wrappers/<run_name>.py``) and the ``@register_run``-decorated function inside it. Must be a valid Python identifier.",
          "minLength": 1,
          "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
          "title": "Run Name",
          "type": "string"
        },
        "argv": {
          "description": "Argv template. ``{param}`` placeholders are substituted from the wrapper's kwargs at call time. Example: [\"python3\", \"main.py\", \"--config\", \"{config}\", \"--seed\", \"{seed}\"].",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Argv",
          "type": "array"
        },
        "signature": {
          "additionalProperties": {
            "enum": [
              "str",
              "int",
              "float",
              "bool"
            ],
            "type": "string"
          },
          "description": "Wrapper signature: ``{param_name: type_str}``. Each name must be a valid Python identifier; types are 'str' / 'int' / 'float' / 'bool'. The wrapper also takes ``**kwargs`` so framework-injected identity fields (e.g. ``config_sha``) flow through without polluting main.py.",
          "title": "Signature",
          "type": "object"
        },
        "frozen_configs": {
          "description": "Paths (relative to campaign_dir) to config files whose content should be part of the experiment's identity. For each entry the interview hashes the bytes and threads ``<basename>_sha`` into every task's kwargs. Requires ``task_generator`` (see class docstring).",
          "items": {
            "type": "string"
          },
          "title": "Frozen Configs",
          "type": "array"
        },
        "data_axis_hint": {
          "anyOf": [
            {
              "$ref": "#/$defs/_DataAxisHint"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Pre-declared series-axis classification. classify-axis cannot introspect a shell-out body, so when the experimenter knows the classification they declare it here. The interview persists it to ``interview.json._materialized.entry_point.data_axis`` so classify-axis records it directly. Omit when you want the interactive classification interview."
        },
        "solver": {
          "anyOf": [
            {
              "$ref": "#/$defs/_PetscSolverHint"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional checkpoint-instrumentation hint for a known solver library. When present, the materialized wrapper injects the library's checkpoint hooks around the argv (see ``_PetscSolverHint``) so a long solve becomes preemption-safe without touching the entry point. One adapter today: 'petsc'."
        },
        "fixed_params": {
          "additionalProperties": true,
          "description": "Constant (non-axis) kwargs the executor requires but that do not vary per task — e.g. {'samples': 10000} when only 'seed' is a sweep axis. Each is baked into every materialized task's kwargs (resolve(i)), so the cluster exports HPC_KW_<param> and the executor command is complete. Keys must be valid Python identifiers; values are JSON scalars. A swept axis of the same name wins (the axis value is per-task; this is the constant fallback). Requires task_generator — a hand-written tasks.py must include the constants itself. Resolves #195: a required signature param left uncovered.",
          "title": "Fixed Params",
          "type": "object"
        }
      },
      "required": [
        "kind",
        "run_name",
        "argv"
      ],
      "title": "_ShellCommandEntry",
      "type": "object"
    },
    "_TranscriptTurn": {
      "additionalProperties": false,
      "properties": {
        "role": {
          "enum": [
            "agent",
            "operator",
            "system"
          ],
          "title": "Role",
          "type": "string"
        },
        "text": {
          "title": "Text",
          "type": "string"
        },
        "at": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "At"
        }
      },
      "required": [
        "role",
        "text"
      ],
      "title": "_TranscriptTurn",
      "type": "object"
    }
  }
}
