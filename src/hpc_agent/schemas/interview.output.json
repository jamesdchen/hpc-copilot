{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/interview.output.json",
  "title": "interview output",
  "description": "Data block returned by ``hpc-agent interview``.\n\nReports the artifacts persisted (interview.json, optionally\nmeta.json) and a dry-resolve preview so the calling agent can\necho 'sweep starts here, midpoint here, ends here' back to the\noperator before submit.\n\nThe outer ``{ok, data}`` envelope is supplied by ``_ok`` in\n``hpc_agent.cli._helpers``; the shipped ``interview.output.json``\nmatches THIS data-block shape, not the envelope.",
  "type": "object",
  "required": [
    "campaign_dir",
    "artifacts",
    "total_tasks",
    "cmd_sha",
    "preview"
  ],
  "additionalProperties": false,
  "properties": {
    "campaign_dir": {
      "description": "Absolute path to the campaign workdir. interview.json is written here at the root; generator-mode tasks.py is materialized into campaign_dir/.hpc/tasks.py (the canonical framework location deploy + the dispatcher read).",
      "title": "Campaign Dir",
      "type": "string"
    },
    "artifacts": {
      "description": "Relative paths (under campaign_dir) of every file the primitive wrote or updated. Always includes interview.json; includes meta.json when intent.cluster_target or intent.budget triggered an update.",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "title": "Artifacts",
      "type": "array"
    },
    "total_tasks": {
      "description": "Result of calling tasks.total() against the campaign's tasks.py. Cross-checked against intent.task_count.",
      "minimum": 1,
      "title": "Total Tasks",
      "type": "integer"
    },
    "cmd_sha": {
      "description": "compute_cmd_sha() over the campaign's tasks.py. Embedded in interview.json so future cmd_recall queries can detect drift.",
      "pattern": "^[0-9a-f]{8,64}$",
      "title": "Cmd Sha",
      "type": "string"
    },
    "preview": {
      "$ref": "#/$defs/_InterviewPreview",
      "description": "Three sample resolve() calls. Each is a dict per hpc-agent's pre-existing tasks.py contract."
    },
    "stage_reached": {
      "const": "interviewed",
      "default": "interviewed",
      "description": "The one terminator this block has: the intent is journaled and tasks.py resolves. Every other outcome RAISES (a count mismatch, an unrunnable executor, an ambiguous audit template), so there is no second stage to declare.",
      "title": "Stage Reached",
      "type": "string"
    },
    "needs_decision": {
      "const": false,
      "default": false,
      "description": "Always false: reaching this result means nothing is being asked. The judgment points of the onboard chain sit UPSTREAM (the goal at audit-handoff, the pick / intent / argv escalations at wrap-entry-point-auto) and the next one DOWNSTREAM (submit-s1's greenlight). This block is pure transcription.",
      "title": "Needs Decision",
      "type": "boolean"
    },
    "next_block": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "`{verb, why, spec_hint}` naming `submit-s1` — the onboard chain's exit into the submit chain. The spec_hint IS submit-s1's complete input spec: a `walk` block composed from the intent just journaled, so the ambiguity walk never re-asks what the interview answered. `resolve` is deliberately absent — its remote_path / build-submit-spec fields are caller-owned deployment facts no interview record holds, so S1 lands on its PRE-RESOLVE boundary and parks (with the standing-consent offer) rather than being handed a fabricated spec.",
      "title": "Next Block"
    }
  },
  "$defs": {
    "_InterviewPreview": {
      "additionalProperties": false,
      "properties": {
        "first": {
          "description": "tasks.resolve(0) — opaque (whatever resolve returns).",
          "title": "First"
        },
        "mid": {
          "description": "tasks.resolve(total_tasks // 2)",
          "title": "Mid"
        },
        "last": {
          "description": "tasks.resolve(total_tasks - 1)",
          "title": "Last"
        }
      },
      "required": [
        "first",
        "mid",
        "last"
      ],
      "title": "_InterviewPreview",
      "type": "object"
    }
  }
}
