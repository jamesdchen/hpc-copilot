{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/status_block.output.json",
  "title": "status-block output data",
  "description": "Shared ``data`` block for every status block (snapshot / watch).\n\nThe ``brief`` is the code-digested evidence the LLM drafts a proposal over\n(§2): the running-where digest + changed-since-seen delta (+ stalled/anomaly\nevidence) for the snapshot, and the terminal status + harvest hand-off or the\ndrafted anomaly brief for the watch. ``needs_decision`` marks a ``y``/nudge\nterminator (True for a snapshot anomaly, a watch anomaly, or a watch timeout;\nFalse for a clean snapshot and for a watch that reached a clean terminal —\nwhich simply hands off to the harvest block).",
  "type": "object",
  "required": [
    "block",
    "stage_reached",
    "needs_decision"
  ],
  "additionalProperties": false,
  "properties": {
    "block": {
      "description": "Which status block produced this result.",
      "enum": [
        "snapshot",
        "watch"
      ],
      "title": "Block",
      "type": "string"
    },
    "stage_reached": {
      "description": "The terminator the block stopped at (decision-as-data, #231).",
      "enum": [
        "snapshot_clean",
        "snapshot_anomaly",
        "watch_terminal",
        "watch_anomaly",
        "watch_timeout"
      ],
      "title": "Stage Reached",
      "type": "string"
    },
    "needs_decision": {
      "description": "True when a human must answer here (the y/nudge terminator). False for a clean snapshot and for a watch that reached a clean terminal.",
      "title": "Needs Decision",
      "type": "boolean"
    },
    "reason": {
      "default": "",
      "description": "Human-readable one-line summary of the terminator.",
      "title": "Reason",
      "type": "string"
    },
    "run_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The run this block operated on (null for a fleet-wide snapshot).",
      "title": "Run Id"
    },
    "brief": {
      "additionalProperties": true,
      "description": "Code-digested evidence the LLM drafts a proposal over (§2). Shape varies per block; never interpreted raw by the LLM.",
      "title": "Brief",
      "type": "object"
    },
    "next_block": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The DETERMINISTICALLY-computed next block — ``{verb, why, spec_hint}`` — or null at a terminal / human-branch terminator (design §2, the ``_next_step_hint`` pattern generalized). ``verb`` names the next block's CLI verb; ``spec_hint`` carries the minimal next-spec skeleton (run_id etc.). Surfaced, greenlit, journaled under ``resolved.next_block``, and enforced by the successor gate — never free-prose.",
      "title": "Next Block"
    }
  }
}
