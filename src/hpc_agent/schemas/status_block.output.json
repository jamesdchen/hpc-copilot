{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/status_block.output.json",
  "title": "status-block output data",
  "description": "Shared ``data`` block for every status block (snapshot / watch).\n\nThe ``brief`` is the code-digested evidence the LLM drafts a proposal over\n(§2): the running-where digest + changed-since-seen delta (+ stalled/anomaly\nevidence) for the snapshot, and the terminal status + harvest hand-off or the\ndrafted anomaly brief for the watch. ``needs_decision`` marks a ``y``/nudge\nterminator (True for a snapshot anomaly, a watch anomaly, or a watch timeout;\nFalse for a clean snapshot and for a watch that reached a clean terminal —\nwhich simply hands off to the harvest block).",
  "type": "object",
  "required": [
    "block",
    "stage_reached",
    "needs_decision"
  ],
  "additionalProperties": false,
  "properties": {
    "block": {
      "description": "Which status block produced this result.",
      "enum": [
        "snapshot",
        "watch"
      ],
      "title": "Block",
      "type": "string"
    },
    "stage_reached": {
      "description": "The terminator the block stopped at (decision-as-data, #231).",
      "enum": [
        "snapshot_clean",
        "snapshot_anomaly",
        "watch_terminal",
        "watch_anomaly",
        "watch_timeout",
        "detached"
      ],
      "title": "Stage Reached",
      "type": "string"
    },
    "needs_decision": {
      "description": "True when a human must answer here (the y/nudge terminator). False for a clean snapshot and for a watch that reached a clean terminal.",
      "title": "Needs Decision",
      "type": "boolean"
    },
    "reason": {
      "default": "",
      "description": "Human-readable one-line summary of the terminator.",
      "title": "Reason",
      "type": "string"
    },
    "relay": {
      "default": "",
      "description": "The human-facing one-liner CODE renders FRESH from the journal digest — the agent relays it VERBATIM (design §5.3, finding 15). Rendered from the CURRENT record state on every snapshot, so the agent relays what the snapshot returns NOW rather than a brief it cached across a journal transition (the staleness fix). Each run's counts are rendered from its own digest row, so a canary's 1-task summary never bleeds the main array's total. Shares the one renderer with the submit blocks so the two surfaces agree.",
      "title": "Relay",
      "type": "string"
    },
    "run_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The run this block operated on (null for a fleet-wide snapshot).",
      "title": "Run Id"
    },
    "brief": {
      "additionalProperties": true,
      "description": "Code-digested evidence the LLM drafts a proposal over (§2). Shape varies per block; never interpreted raw by the LLM.",
      "title": "Brief",
      "type": "object"
    },
    "next_block": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The DETERMINISTICALLY-computed next block — ``{verb, why, spec_hint}`` — or null at a terminal / human-branch terminator (design §2, the ``_next_step_hint`` pattern generalized). ``verb`` names the next block's CLI verb; ``spec_hint`` carries the minimal next-spec skeleton (run_id etc.). Surfaced, greenlit, journaled under ``resolved.next_block``, and enforced by the successor gate — never free-prose.",
      "title": "Next Block"
    },
    "started": {
      "default": false,
      "description": "Detach-by-contract handle (design §3): True when status-watch spawned a durable detached worker (which owns the ONE cold dial per lifetime — warm engine, lease-single, watchdog-covered, exits at terminal) and returned immediately instead of dialing the cluster in-process. The brief is not held in a process — read it from the journal via status-snapshot / the completion notification. False on the synchronous (detach=False) path.",
      "title": "Started",
      "type": "boolean"
    },
    "watch": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "How to learn the detached watch's outcome — ``\"journal\"`` when ``started`` is True (the detached worker stamps the per-run journal record as it polls; poll it cluster-free). None on the synchronous path.",
      "title": "Watch"
    },
    "detached_pid": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The detached worker's OS process id (informational — do NOT wait on it; read the journal). None on the synchronous path. A dead worker is detected by the §5 watchdog / doctor dead-worker scan, not by this pid.",
      "title": "Detached Pid"
    }
  }
}
