{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/envelope.json",
  "$defs": {
    "CandidateAction": {
      "additionalProperties": false,
      "description": "One action a decision-maker may choose for an escalated cluster.\n\nThe deterministic resolver (#234) offers candidates it could not\nrank with confidence; the agentic layer (or a human) picks one. A\n``decided_by=\"code\"`` escalation may carry a single recommended\ncandidate (a deterministic recommendation surfaced for confirmation,\ne.g. ``campaign-advance``'s computed decision); a\n``decided_by=\"judgement\"`` escalation carries the option set the LLM\nreasons over.",
      "properties": {
        "action": {
          "description": "The action verb, e.g. 'increase-mem-per-gpu', 'reshard', 'retry-on-different-node', 'continue', 'stop_converged'. Mirrors the 'suggested_fix.action' vocabulary the failure-signature CATALOG emits and the campaign-advance decision values.",
          "title": "Action",
          "type": "string"
        },
        "params": {
          "additionalProperties": true,
          "description": "Parameters for the action, e.g. {'factor': 1.5}.",
          "title": "Params",
          "type": "object"
        },
        "rationale": {
          "default": "",
          "description": "Why this candidate is offered (the discriminating evidence behind it).",
          "title": "Rationale",
          "type": "string"
        },
        "source": {
          "anyOf": [
            {
              "enum": [
                "catalog",
                "policy",
                "history",
                "judgement"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Where the candidate came from: 'catalog' (signature CATALOG fix), 'policy' (context-keyed retry policy), 'history' (a memoized prior verdict from recall), or 'judgement' (LLM-proposed).",
          "title": "Source"
        }
      },
      "required": [
        "action"
      ],
      "title": "CandidateAction",
      "type": "object"
    },
    "ErrorEnvelope": {
      "additionalProperties": false,
      "description": "The ``ok=false`` shape of every hpc-agent stdout response.",
      "properties": {
        "ok": {
          "const": false,
          "title": "Ok",
          "type": "boolean"
        },
        "error_code": {
          "enum": [
            "ssh_unreachable",
            "model_endpoint_error",
            "scheduler_throttled",
            "spec_invalid",
            "executor_not_found",
            "cluster_unknown",
            "journal_corrupt",
            "remote_command_failed",
            "config_invalid",
            "combiner_failed",
            "cluster_timeout",
            "cluster_partially_degraded",
            "outputs_missing",
            "schema_incompat",
            "preempted",
            "precondition_failed",
            "internal"
          ],
          "title": "Error Code",
          "type": "string"
        },
        "message": {
          "title": "Message",
          "type": "string"
        },
        "category": {
          "enum": [
            "user",
            "cluster",
            "network",
            "internal"
          ],
          "title": "Category",
          "type": "string"
        },
        "retry_safe": {
          "title": "Retry Safe",
          "type": "boolean"
        },
        "remediation": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Remediation"
        },
        "failure_features": {
          "anyOf": [
            {
              "$ref": "#/$defs/FailureFeatures"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional structured diagnostic evidence for this failure (#230): the feature set a diagnosis would need (error_class, resource_spec, temporal_context, liveness_vs_correctness, log_tail, probes), independent of what failed. Evidence only — no recovery behavior; populated where the operation can supply it. Consumed by deterministic retry policy and the agentic escalation layer (#234)."
        },
        "escalation": {
          "anyOf": [
            {
              "$ref": "#/$defs/Escalation"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional 'needs a decision' block (#231). Present on a FAILURE when the deterministic resolver (#234) could not resolve it and the agentic layer must decide; carries the failure_features evidence, the candidate actions, and the affected-task cluster so a verdict fans back out per-task. The same block shape appears on SuccessEnvelope — one socket, both outcomes."
        }
      },
      "required": [
        "ok",
        "error_code",
        "message",
        "category",
        "retry_safe"
      ],
      "title": "ErrorEnvelope",
      "type": "object"
    },
    "Escalation": {
      "additionalProperties": false,
      "description": "One *\"needs a decision\"* block attached to an envelope (#231).\n\nAttached optionally to ``SuccessEnvelope`` (e.g. campaign-advance's\nsucceeded-but-decide case) or ``ErrorEnvelope`` (a failure the\ndeterministic resolver could not resolve). Evidence + options +\nprovenance; it does not itself carry the verdict or the holding state\n(the journal's ``pending_verdict`` state owns that — Phase 2 / #234).",
      "properties": {
        "decided_by": {
          "description": "The decision seam (reuses spawn_contract's DecidedBy). 'code' = a deterministic resolver produced this (the block is a recommendation surfaced for confirmation / audit); 'judgement' = the deterministic layer could not resolve it and the agentic layer must decide. The rate of 'judgement' for a given cluster fingerprint is the #234 health signal that a context-keyed rule should be promoted.",
          "enum": [
            "code",
            "judgement"
          ],
          "title": "Decided By",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Human-readable summary of why this escalated / what must be decided.",
          "title": "Reason",
          "type": "string"
        },
        "failure_features": {
          "anyOf": [
            {
              "$ref": "#/$defs/FailureFeatures"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The #230 evidence vector — the single inter-layer interface. The deterministic resolver pattern-matches on it; the agentic layer reasons over it as its prompt payload. Same evidence, two consumers."
        },
        "candidate_actions": {
          "description": "The actions the decision-maker may choose among.",
          "items": {
            "$ref": "#/$defs/CandidateAction"
          },
          "title": "Candidate Actions",
          "type": "array"
        },
        "cluster": {
          "anyOf": [
            {
              "$ref": "#/$defs/EscalationCluster"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Provenance back to the affected tasks, so a verdict fans back out per-task."
        }
      },
      "required": [
        "decided_by"
      ],
      "title": "Escalation",
      "type": "object"
    },
    "EscalationCluster": {
      "additionalProperties": false,
      "description": "Provenance back to the affected tasks so a verdict fans back out.\n\n*Cluster to decide once, do not dedup to discard* (#234): the\nescalation carries ONE decision per signature, but keeps the per-task\nrefs so the chosen fix applies to each task. ``fingerprint`` is the\ncluster key produced by\n``ops.recover.runner_failures.cluster_failures_by_fingerprint``.",
      "properties": {
        "fingerprint": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The cluster signature (the fingerprint failures were grouped by).",
          "title": "Fingerprint"
        },
        "run_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The run whose tasks this escalation concerns, when single-run.",
          "title": "Run Id"
        },
        "task_ids": {
          "description": "Per-task refs the verdict fans back out to. Kept inside the cluster so a single decision re-applies per task rather than being deduped away.",
          "items": {
            "type": "string"
          },
          "title": "Task Ids",
          "type": "array"
        },
        "wave": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The wave the affected tasks belong to, when applicable.",
          "title": "Wave"
        }
      },
      "title": "EscalationCluster",
      "type": "object"
    },
    "FailureFeatures": {
      "additionalProperties": false,
      "description": "Structured diagnostic evidence attached to an ``ok=false`` envelope on\noperation failure. Evidence only; no recovery logic. See issue #230.",
      "properties": {
        "error_class_raw": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The failure signature exactly as the producer (a failing op or a caller-supplied probe hook) wrote it. Open, ungoverned, preserved verbatim — the framework cannot enforce a vocabulary across producers it does not own, and never rejects or rewrites this value. Used for audit and as the input to normalization.",
          "title": "Error Class Raw"
        },
        "error_class": {
          "anyOf": [
            {
              "enum": [
                "gpu_oom",
                "system_oom",
                "segv",
                "walltime",
                "node_failure",
                "queue_stall",
                "code_bug",
                "unknown"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The canonical failure class. Reuses the framework-owned 'FailureCategory' vocabulary (the values 'classify_failure' returns and that DEFAULT_AUTO_RETRY_POLICY keys on) — NOT a parallel taxonomy. Populated by the existing classifier ('infra/failure_signatures.py:classify' + 'execution/mapreduce/reduce/classify.py:classify_failure') from the raw stderr signature in 'error_class_raw', not by a new normalizer. 'unknown' is the escape hatch: the classifier could not categorize the failure — the well-defined trigger to escalate to a decision-maker (e.g. the agentic layer). Service/staging classes (#231/#232) extend FailureCategory in '_shared.py' — the single governed place the vocabulary grows — rather than introducing a new enum here. Distinct from the coarser envelope 'error_code'.",
          "title": "Error Class"
        },
        "resource_spec": {
          "anyOf": [
            {
              "additionalProperties": true,
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The resource/sweep spec in effect at the moment of failure (e.g. GPU type/count, scheduler-level params, sampling width). The discriminator that turns a shared signature into opposite fixes — e.g. OOM at tp_size=2 vs OOM at n=128.",
          "title": "Resource Spec"
        },
        "temporal_context": {
          "anyOf": [
            {
              "$ref": "#/$defs/_TemporalContext"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "attempts_this_episode": {
          "anyOf": [
            {
              "$ref": "#/$defs/_AttemptsThisEpisode"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "liveness_vs_correctness": {
          "anyOf": [
            {
              "$ref": "#/$defs/_LivenessVsCorrectness"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "log_tail": {
          "anyOf": [
            {
              "$ref": "#/$defs/_LogTail"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "probes": {
          "anyOf": [
            {
              "items": {
                "$ref": "#/$defs/_Probe"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional targeted probe outputs from caller-supplied probe hooks.",
          "title": "Probes"
        }
      },
      "title": "FailureFeatures",
      "type": "object"
    },
    "SuccessEnvelope": {
      "additionalProperties": false,
      "description": "The ``ok=true`` shape of every hpc-agent stdout response.",
      "properties": {
        "ok": {
          "const": true,
          "title": "Ok",
          "type": "boolean"
        },
        "idempotent": {
          "title": "Idempotent",
          "type": "boolean"
        },
        "data": {
          "additionalProperties": true,
          "title": "Data",
          "type": "object"
        },
        "partial_errors": {
          "anyOf": [
            {
              "items": {
                "$ref": "#/$defs/_PartialError"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional top-level array surfacing soft per-source failures while the operation as a whole succeeded.",
          "title": "Partial Errors"
        },
        "escalation": {
          "anyOf": [
            {
              "$ref": "#/$defs/Escalation"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional 'needs a decision' block (#231). Present on a SUCCESS when the operation succeeded but a decision is still required — e.g. campaign-advance reaching 'stop_converged' / 'stop_over_budget', or a stage-out hitting a quota gate. 'Needs a decision' is orthogonal to ok, so it rides as data on either envelope rather than as a third wire state. A consumer that ignores it sees an ordinary success."
        }
      },
      "required": [
        "ok",
        "idempotent",
        "data"
      ],
      "title": "SuccessEnvelope",
      "type": "object"
    },
    "_AttemptsThisEpisode": {
      "additionalProperties": false,
      "description": "What recovery has already been tried this failure episode — lets a\ndecision-maker avoid re-trying an exhausted strategy.\n\nReuses the journal's existing per-task retry record\n(``state/run_record.py:RunRecord.retries`` = ``{task_id: {attempts,\ncategory, overrides}}``, written by ``ops/recover/runner.py``): ``count``\nsnapshots that ``attempts`` counter rather than tracking attempts\nseparately. ``strategies`` is the only new piece — it generalizes the\nrecord's single ``category``/``overrides`` into the ordered list of what\nwas tried.",
      "properties": {
        "count": {
          "description": "Number of recovery attempts already made this episode.",
          "minimum": 0,
          "title": "Count",
          "type": "integer"
        },
        "strategies": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Ordered list of recovery strategies already applied (e.g. 'retry', 'resubmit', 'restart_service', 'increase_walltime').",
          "title": "Strategies"
        }
      },
      "required": [
        "count"
      ],
      "title": "_AttemptsThisEpisode",
      "type": "object"
    },
    "_LivenessVsCorrectness": {
      "additionalProperties": false,
      "description": "Distinguishes 'down' from 'up-but-broken' by separating a liveness\nsignal (does it respond?) from a correctness signal (does a real request\nreturn a valid result?). The classic silent-rot case is liveness=pass,\ncorrectness=fail.",
      "properties": {
        "liveness": {
          "description": "Result of a liveness check (port open / process alive / health endpoint).",
          "enum": [
            "pass",
            "fail",
            "unknown"
          ],
          "title": "Liveness",
          "type": "string"
        },
        "correctness": {
          "description": "Result of a real-path request (an actual request that exercises the backing work, not a port ping).",
          "enum": [
            "pass",
            "fail",
            "unknown"
          ],
          "title": "Correctness",
          "type": "string"
        },
        "detail": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Detail"
        }
      },
      "required": [
        "liveness",
        "correctness"
      ],
      "title": "_LivenessVsCorrectness",
      "type": "object"
    },
    "_LogTail": {
      "additionalProperties": false,
      "description": "Bounded, normalized tail of the relevant log. Callers MUST bound this\n(the schema does not transport unbounded logs) and SHOULD normalize\nvolatile tokens (timestamps, paths, addresses) so signatures cluster.\n\nReuses the existing capture/normalize path rather than re-fetching:\npopulate ``text`` from ``infra/cluster_logs.py:fetch_task_logs(...)``\noutput run through\n``ops/recover/runner_failures.py:fingerprint_stderr_tail`` (which already\nstrips volatile tokens). ``truncated`` and ``source`` are the only new\nmetadata.",
      "properties": {
        "text": {
          "description": "The normalized log tail.",
          "title": "Text",
          "type": "string"
        },
        "truncated": {
          "default": false,
          "description": "True when content was dropped to fit the bound.",
          "title": "Truncated",
          "type": "boolean"
        },
        "source": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Where the tail came from, e.g. 'stderr', 'task.log', 'service.stdout'.",
          "title": "Source"
        }
      },
      "required": [
        "text"
      ],
      "title": "_LogTail",
      "type": "object"
    },
    "_PartialError": {
      "additionalProperties": false,
      "description": "One soft per-source failure surfaced on a successful envelope.\n\nDistinct from ``data.errors``: this key documents *cluster-side*\ndegradations (qhost timed out, one of N nodes unreachable, sacct\nunavailable) that the planner considered and chose to tolerate.\nOperations that genuinely failed should set ``ok: false`` instead.",
      "properties": {
        "code": {
          "description": "Machine-readable category, e.g. 'qhost_failed', 'scontrol_failed', 'qstat_unavailable', 'qacct_unavailable', 'malformed_row'.",
          "title": "Code",
          "type": "string"
        },
        "detail": {
          "title": "Detail",
          "type": "string"
        }
      },
      "required": [
        "code",
        "detail"
      ],
      "title": "_PartialError",
      "type": "object"
    },
    "_Probe": {
      "additionalProperties": false,
      "description": "One targeted probe result from a caller-supplied probe hook.",
      "properties": {
        "name": {
          "description": "Probe identifier, e.g. 'gpu_visible', 'service_real_request', 'scratch_writable'.",
          "title": "Name",
          "type": "string"
        },
        "ok": {
          "description": "Whether the probe passed.",
          "title": "Ok",
          "type": "boolean"
        },
        "detail": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Detail"
        }
      },
      "required": [
        "name",
        "ok"
      ],
      "title": "_Probe",
      "type": "object"
    },
    "_TemporalContext": {
      "additionalProperties": false,
      "description": "When in the operation's life it failed — the single most\ndiscriminating feature (structural vs. runtime).",
      "properties": {
        "phase": {
          "description": "'first_attempt' = failed before any unit of work succeeded (points at a structural/config problem). 'after_progress' = failed after N units succeeded (points at a runtime/data-dependent problem). 'unknown' when the operation cannot tell.",
          "enum": [
            "first_attempt",
            "after_progress",
            "unknown"
          ],
          "title": "Phase",
          "type": "string"
        },
        "successful_units": {
          "anyOf": [
            {
              "minimum": 0,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Count of units (tasks, requests, rounds) that succeeded before the failure, when known.",
          "title": "Successful Units"
        }
      },
      "required": [
        "phase"
      ],
      "title": "_TemporalContext",
      "type": "object"
    }
  },
  "discriminator": {
    "mapping": {
      "false": "#/$defs/ErrorEnvelope",
      "true": "#/$defs/SuccessEnvelope"
    },
    "propertyName": "ok"
  },
  "oneOf": [
    {
      "$ref": "#/$defs/SuccessEnvelope"
    },
    {
      "$ref": "#/$defs/ErrorEnvelope"
    }
  ]
}
