{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/extract_recipe.input.json",
  "title": "extract-recipe input spec",
  "description": "Inputs to ``extract-recipe`` — exactly one seed reference.\n\nExactly one of ``run_id`` / ``campaign_id`` / ``aggregate_path`` names the\ncitable artifact to walk back from. ``run_id`` / ``campaign_id`` are\nidentities core owns; ``aggregate_path`` is a path to a reduced-metrics\nartifact — a ``metrics_aggregate.json`` is read for its Task-1\n``contributing_run_ids`` provenance, while a pack ``*.csv`` is accepted only\nas an OPAQUE citation whose provenance is its containing run's (its content\nis NEVER parsed — the dossier no-parse boundary, R2).",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "run_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Walk back from this run's reduced table (its _aggregated/<run_id>/metrics_aggregate.json contributing set, or its lineage when no table was persisted). Excludes the other two seeds.",
      "title": "Run Id"
    },
    "campaign_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Walk back from this campaign — the campaign's runs minus canary / superseded / dead-end members. Excludes the other two seeds.",
      "title": "Campaign Id"
    },
    "aggregate_path": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Path to a reduced-metrics artifact. A metrics_aggregate.json is read for its contributing_run_ids provenance; a pack *.csv is an OPAQUE citation (never parsed) whose provenance is its containing run's. Excludes the other two seeds.",
      "title": "Aggregate Path"
    }
  }
}
