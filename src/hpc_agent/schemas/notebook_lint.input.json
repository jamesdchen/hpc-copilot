{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/notebook_lint.input.json",
  "title": "notebook-lint input spec",
  "description": "Spec for ``notebook-lint`` — all paths are caller-declared.\n\n``source`` / ``template`` are ``.py`` relpaths under the experiment dir\n(or absolute). ``input_roots`` / ``source_roots`` / ``output_roots`` are\nOPAQUE caller-declared root lists — core never attaches a meaning to a root,\nit only joins + tests existence (data roots), resolves import origins\n(import roots), or exempts write-target literals (output roots).",
  "type": "object",
  "required": [
    "source",
    "template"
  ],
  "additionalProperties": false,
  "properties": {
    "source": {
      "minLength": 1,
      "title": "Source",
      "type": "string"
    },
    "template": {
      "minLength": 1,
      "title": "Template",
      "type": "string"
    },
    "input_roots": {
      "items": {
        "type": "string"
      },
      "title": "Input Roots",
      "type": "array"
    },
    "source_roots": {
      "items": {
        "type": "string"
      },
      "title": "Source Roots",
      "type": "array"
    },
    "output_roots": {
      "items": {
        "type": "string"
      },
      "title": "Output Roots",
      "type": "array"
    }
  }
}
