{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/suggest_prelude_action.output.json",
  "title": "suggest-prelude-action output data",
  "description": "The ONE next prelude step, its ``why``, and the call to make.\n\nA TOTAL ladder: every state of the five substrates maps to exactly one\n``action``. ``rung`` is the priority the match came from (0 = a corrupt /\nunreadable substrate, which is always disclosed rather than crashed on; the\nlast rung is the catch-all \"the prelude is settled\"). ``substrates`` is the\nevidence vector the decision was made over.",
  "type": "object",
  "required": [
    "rung",
    "action",
    "why",
    "scaffold",
    "substrates"
  ],
  "additionalProperties": false,
  "properties": {
    "rung": {
      "description": "The priority tier that matched — lower fires first. The ladder is total, so a rung always resolves (no escalation).",
      "minimum": 0,
      "title": "Rung",
      "type": "integer"
    },
    "action": {
      "description": "The single next prelude step to take.",
      "enum": [
        "doctor",
        "pack-optin-repair",
        "notebook-record-config",
        "notebook-status",
        "notebook-audit-view",
        "audit-handoff",
        "interview",
        "classify-axis",
        "notebook-scaffold-template",
        "submit-s1"
      ],
      "title": "Action",
      "type": "string"
    },
    "why": {
      "description": "One line naming the substrate fact this action follows from.",
      "title": "Why",
      "type": "string"
    },
    "scaffold": {
      "$ref": "#/$defs/PreludeScaffold",
      "description": "The exact call the action is made with."
    },
    "findings": {
      "description": "Every named substrate observation, including the ones a lower rung pre-empted — so nothing read is silently dropped.",
      "items": {
        "$ref": "#/$defs/PreludeFinding"
      },
      "title": "Findings",
      "type": "array"
    },
    "disclosures": {
      "description": "Honest notes about what could not be determined (a source the parser refused, a run the AST scan did not find, additional audits not reported). Advisory; never blocks.",
      "items": {
        "type": "string"
      },
      "title": "Disclosures",
      "type": "array"
    },
    "substrates": {
      "$ref": "#/$defs/PreludeSubstrates",
      "description": "The disclosed evidence vector the ladder decided over."
    },
    "readiness": {
      "description": "The standing readiness ledger's one-liner for every cluster the suggested action would involve — pillar 1's render mandate (docs/design/s2-readiness.md): a verdict, its AGE, and the sensor that is not green. Non-empty ONLY for an action that hands off to a cluster (the terminal 'submit-s1' rung); the notebook / pack / axes rungs touch no cluster and carry no line, because a readiness line beside a rung that cannot use it is noise that trains readers to skip the field. CONSULT-ONLY: this verb opens no connection and runs no probe — every line is a reading of the durable ledger, ends in '(ledger read, not probed)', and is absent-and-honest ('unknown - age unknown - no readiness ledger on this machine') when nothing has ever been observed. Advisory: the ladder's rung is NEVER changed by it.",
      "items": {
        "$ref": "#/$defs/ReadinessDigest"
      },
      "title": "Readiness",
      "type": "array"
    }
  },
  "$defs": {
    "PreludeFinding": {
      "additionalProperties": false,
      "description": "One named substrate observation — the evidence a rung fired on.\n\nA finding is never a judgement: ``substrate`` names WHICH of the five was\nread, ``detail`` states the mechanical fact, and ``remedy`` (when there is a\nnamed one) states the repair in the repo's own vocabulary — e.g. the\n2026-07-30 live fumble, \"bound but not opted in — add the packs entry\".",
      "properties": {
        "substrate": {
          "description": "Which of the five prelude substrates this observation came from.",
          "enum": [
            "notebook-journal",
            "audit-config",
            "pack",
            "axes",
            "interview"
          ],
          "title": "Substrate",
          "type": "string"
        },
        "detail": {
          "description": "The mechanical fact observed (never an interpretation).",
          "title": "Detail",
          "type": "string"
        },
        "remedy": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The named repair, when the finding has one (e.g. 'bound but not opted in — add the packs entry'). Null for a finding that is only disclosure.",
          "title": "Remedy"
        }
      },
      "required": [
        "substrate",
        "detail"
      ],
      "title": "prelude substrate finding",
      "type": "object"
    },
    "PreludeScaffold": {
      "additionalProperties": false,
      "description": "The exact call the suggested action is made with.\n\nEither a VERB invocation (``cli`` carries the exact command line, ``spec``\nthe ``--spec`` skeleton) or a FILE EDIT (``verb`` names the file, ``cli`` is\nnull and ``spec`` carries the JSON fragment to add) — the pack opt-in remedy\nis the latter. ``unresolved_fields`` flags the placeholder paths the caller\nmust fill, the ``scaffold-spec`` convention.",
      "properties": {
        "verb": {
          "description": "The target verb to invoke, or the file to edit (e.g. `interview.json`) when the remedy is not a verb call.",
          "title": "Verb",
          "type": "string"
        },
        "cli": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The exact CLI invocation, with every derivable value filled. Null when the remedy is a file edit rather than a verb call.",
          "title": "Cli"
        },
        "spec": {
          "anyOf": [
            {
              "additionalProperties": true,
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The `--spec` skeleton for the target verb, or the JSON fragment to add to the named file. Null when the verb takes no spec.",
          "title": "Spec"
        },
        "unresolved_fields": {
          "description": "Dotted paths inside `spec` whose values are PLACEHOLDERS the ladder could not derive from a durable record — the caller fills them before invoking (never guessed).",
          "items": {
            "type": "string"
          },
          "title": "Unresolved Fields",
          "type": "array"
        }
      },
      "required": [
        "verb"
      ],
      "title": "prelude action scaffold",
      "type": "object"
    },
    "PreludeSubstrates": {
      "additionalProperties": false,
      "description": "The disclosed evidence vector the ladder decided over.\n\nEvery field is presence, a count, or an identity list over opaque content —\npublished so a caller can see WHY a rung fired without re-reading the five\nsubstrates itself.",
      "properties": {
        "audit_ids": {
          "description": "Every audit_id with a notebook decision journal on disk, sorted.",
          "items": {
            "type": "string"
          },
          "title": "Audit Ids",
          "type": "array"
        },
        "audit_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The audit the ladder is reporting on (the first, sorted, that a notebook rung fired for), or null when no notebook rung fired.",
          "title": "Audit Id"
        },
        "audit_config_recorded": {
          "default": false,
          "description": "True when the reported audit has a config seat — either interview.json's `audited_source` block or a journaled `notebook-audit-config` record.",
          "title": "Audit Config Recorded",
          "type": "boolean"
        },
        "audited_source_seat": {
          "default": false,
          "description": "True when interview.json's `audited_source` block names the reported audit's source + template (the only durable seat that does).",
          "title": "Audited Source Seat",
          "type": "boolean"
        },
        "notebook_passed": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "notebook-status's `passed` predicate for the reported audit, recomputed through the SAME reduction (never a second definition). Null when the source/template are not resolvable from a durable seat.",
          "title": "Notebook Passed"
        },
        "sections_awaiting": {
          "anyOf": [
            {
              "minimum": 0,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "How many REQUIRED sections are not current (awaiting sign-off) for the reported audit. Null when `notebook_passed` is null.",
          "title": "Sections Awaiting"
        },
        "packs_opted_in": {
          "description": "Pack names carried by interview.json's `packs` opt-in block, sorted.",
          "items": {
            "type": "string"
          },
          "title": "Packs Opted In",
          "type": "array"
        },
        "packs_bound": {
          "description": "Pack names with a CURRENT bind on their pack journal, sorted.",
          "items": {
            "type": "string"
          },
          "title": "Packs Bound",
          "type": "array"
        },
        "axes_yaml": {
          "default": false,
          "description": "Whether `.hpc/axes.yaml` exists.",
          "title": "Axes Yaml",
          "type": "boolean"
        },
        "interview_json": {
          "default": false,
          "description": "Whether an `interview.json` exists at either probed location.",
          "title": "Interview Json",
          "type": "boolean"
        },
        "materialized": {
          "default": false,
          "description": "Whether an interview.json carries a `_materialized` block.",
          "title": "Materialized",
          "type": "boolean"
        },
        "entry_point_run_name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The materialized entry point's `run_name`, when one was recorded — the key the axes.yaml `executors` staleness check is made against.",
          "title": "Entry Point Run Name"
        }
      },
      "title": "prelude substrate evidence",
      "type": "object"
    },
    "ReadinessDigest": {
      "additionalProperties": false,
      "description": "One cluster's readiness one-liner — pillar 1's render mandate, on the wire.\n\n``docs/design/s2-readiness.md`` pillar 1 does not stop at a substrate; it\nends in a render mandate: the standing ledger is \"rendered with age in the S1\nbrief and ``suggest-*`` surfaces\". This is the shape those surfaces carry. It\nis composed ONCE, by ``ops/readiness_digest.digest_for_host``, and declared\nONCE here so the two schema'd consumers (``doctor``,\n``suggest-prelude-action``) and the two free-form brief consumers\n(``status-snapshot``, the overnight morning brief) describe the same fields\nwith the same words.\n\n**A reading, never a probe.** Every field comes from\n``<journal home>/_readiness/<host>.json`` — the durable tier of the ledger,\nfed opportunistically by traffic the system was already making. Composing a\ndigest opens no connection, resolves no name and spawns no process: a surface\nthat sensed on read would put the fire-time discovery pillar 1 removes onto\nthe busiest surfaces in the product. ``line`` therefore ends in \"(ledger\nread, not probed)\", so a quoted line cannot be mistaken for a live check.\n\nA helper shape (no ``*Spec``/``*Result`` suffix), so it is inlined into each\nconsumer's schema rather than emitted as a verb schema of its own.",
      "properties": {
        "cluster": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The clusters.yaml key naming this host, when one does. Null for a host that has a ledger but no config entry — reached outside the config, and hidden by nothing.",
          "title": "Cluster"
        },
        "host": {
          "description": "The ssh host the ledger is keyed under (the bare host — the SAME normalization the circuit breaker, the throttle and the sensor layer use, so every substrate agrees on what 'a host' is).",
          "title": "Host",
          "type": "string"
        },
        "verdict": {
          "description": "The overall verdict from {ready, stale, degraded, unknown}, computed by state/readiness.overall_verdict — the ONE definition every surface routes through. 'stale' is not a milder 'degraded': it means no fresh failure but the evidence does not support ready (the host may have healed and nothing has looked since). 'unknown' means nothing has ever been observed here, or the ledger could not be read.",
          "title": "Verdict",
          "type": "string"
        },
        "age_seconds": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Age of the freshest atom in the ledger, in whole seconds. Null when there is no dateable atom — honest, never zero. A verdict without an age is the failure mode the ledger exists to remove.",
          "title": "Age Seconds"
        },
        "note": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Why the verdict is not 'ready', named as a sensor rather than as advice: 'connect/effective -> host: down', 'connect: no observation recorded', 'ledger file could not be read: not valid JSON', or 'no readiness ledger on this machine'. Null when the verdict is 'ready'.",
          "title": "Note"
        },
        "ledger_corrupt": {
          "default": false,
          "description": "True when the ledger FILE exists but this read could not use it (unreadable, malformed, or a schema_version this build does not understand). An ABSENT file is not corrupt: 'never looked' and 'cannot read what looked' are different facts.",
          "title": "Ledger Corrupt",
          "type": "boolean"
        },
        "ledger_corruption_reason": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Which corruption it was; null when the read was clean.",
          "title": "Ledger Corruption Reason"
        },
        "last_corruption": {
          "anyOf": [
            {
              "additionalProperties": {
                "type": "string"
              },
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "A persisted {at, reason} note left when an earlier write rebuilt over a corrupt file. Carried so the operator learns ONCE that something was lost, rather than having it vanish with the file. Null when no rebuild ever discarded one.",
          "title": "Last Corruption"
        },
        "line": {
          "description": "The composed one-liner: '<cluster> (<host>): <verdict> - <age> - <note> (ledger read, not probed)'. Rendered from this entry's own fields by ops/readiness_digest.digest_line, so the same digest always prints the same bytes on every surface.",
          "title": "Line",
          "type": "string"
        }
      },
      "required": [
        "host",
        "verdict",
        "line"
      ],
      "title": "cluster readiness digest",
      "type": "object"
    }
  }
}
