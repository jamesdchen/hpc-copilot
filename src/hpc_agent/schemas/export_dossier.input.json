{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/export_dossier.input.json",
  "title": "export-dossier input spec",
  "description": "Inputs to ``export-dossier``.\n\n``run_id`` names the run whose stores are bundled (``RunIdStrict`` — the\nsame filesystem-safe slug every run_id field carries). ``include_lineage``\nwidens the bundle from the single run to its whole supersession chain (the\nrun plus every run it superseded, back to the lineage root); the resolved\nset and its order are reported in :class:`ExportDossierResult.run_ids`.",
  "type": "object",
  "required": [
    "run_id"
  ],
  "additionalProperties": false,
  "properties": {
    "run_id": {
      "pattern": "^[A-Za-z0-9._\\-]+$",
      "title": "Run Id",
      "type": "string"
    },
    "output_path": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Destination path for the bundle archive. Omit to let the verb derive a conventional path; the resolved location is echoed back as archive_path.",
      "title": "Output Path"
    },
    "include_lineage": {
      "default": false,
      "description": "When true, bundle the run's whole supersession lineage (the run plus every run it superseded, to the lineage root) instead of the single run. The bundled set and its lineage order are reported in run_ids.",
      "title": "Include Lineage",
      "type": "boolean"
    }
  }
}
