{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/axes.json",
  "title": "experiment axes config",
  "description": "Schema for ``<experiment>/.hpc/axes.yaml``.",
  "type": "object",
  "required": [
    "axes_schema_version"
  ],
  "additionalProperties": false,
  "properties": {
    "axes_schema_version": {
      "enum": [
        1,
        2
      ],
      "title": "Axes Schema Version",
      "type": "integer"
    },
    "axes": {
      "anyOf": [
        {
          "items": {
            "$ref": "#/$defs/_AxisEntry"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Ordered list of every parallel axis in the experiment. Order is significant: it defines the cartesian-product convention by which task_id maps to axis values (last axis varies fastest, numpy/row-major). Required for submit-flow's wave_map building; the homogeneous_axes hint can stand alone for the cold-start picker without it.",
      "title": "Axes"
    },
    "homogeneous_axes": {
      "anyOf": [
        {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Names of axes the deployer believes have low runtime-cost variance. The cold-start axis_picker promotes the first one onto the task array. Once runtime priors exist, the warm-path picker uses observed CV instead. Must be a subset of axes when axes is present.",
      "title": "Homogeneous Axes",
      "uniqueItems": true
    },
    "executors": {
      "anyOf": [
        {
          "additionalProperties": {
            "$ref": "#/$defs/_ExecutorEntry"
          },
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Per-@register_run-function classified DataAxis (schema v2, additive). Keyed by the run function's name. Records how the experiment's totally-ordered series may be split correctly — orthogonal to the homogeneous_axes scheduling hint above. Written by the `classify-axis` primitive at interview time.",
      "title": "Executors"
    }
  },
  "$defs": {
    "_AxisEntry": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "minLength": 1,
          "title": "Name",
          "type": "string"
        },
        "size": {
          "minimum": 1,
          "title": "Size",
          "type": "integer"
        }
      },
      "required": [
        "name",
        "size"
      ],
      "title": "_AxisEntry",
      "type": "object"
    },
    "_DataAxisConfig": {
      "additionalProperties": false,
      "description": "A classified :data:`~hpc_agent.experiment_kit.axis.DataAxis`, serialized.\n\nThe (de)serializer is :mod:`hpc_agent.experiment_kit.axis_config`.",
      "properties": {
        "kind": {
          "description": "How the series axis is safe to split. 'independent': the loop body is a pure function of its row. 'associative': it accumulates an associative summary — also set `monoid`. 'bounded_halo': it depends on a bounded look-back window — also set `halo`. 'sequential': unbounded / order-dependent state, not splittable; the fail-safe default. 'cartesian': there is no ordered series to split — a plain cartesian sweep (distinct from 'independent', which has a parallelizable series). Recorded only on a confident no-loop signal; anything ambiguous stays 'sequential'.",
          "enum": [
            "independent",
            "associative",
            "bounded_halo",
            "sequential",
            "cartesian"
          ],
          "title": "Kind",
          "type": "string"
        },
        "halo": {
          "anyOf": [
            {
              "$ref": "#/$defs/_HaloConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Required for kind='bounded_halo'; forbidden otherwise."
        },
        "monoid": {
          "anyOf": [
            {
              "enum": [
                "sum",
                "moments"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "For kind='associative': the built-in monoid chunks reduce with.",
          "title": "Monoid"
        }
      },
      "required": [
        "kind"
      ],
      "title": "_DataAxisConfig",
      "type": "object"
    },
    "_ExecutorEntry": {
      "additionalProperties": false,
      "description": "One ``@register_run`` function's classified series axis + provenance.",
      "properties": {
        "run_signature_sha": {
          "description": "Stable hash of the run()'s AST-extracted signature (the Flag list discover_runs synthesizes). A mismatch against the live run means the signature changed → re-classify.",
          "minLength": 1,
          "title": "Run Signature Sha",
          "type": "string"
        },
        "data_axis": {
          "$ref": "#/$defs/_DataAxisConfig"
        },
        "classified_by": {
          "description": "How the classification was reached.",
          "enum": [
            "interview",
            "recall",
            "manual",
            "agent"
          ],
          "title": "Classified By",
          "type": "string"
        },
        "classified_at": {
          "description": "ISO-8601 UTC timestamp the classification was recorded.",
          "minLength": 1,
          "title": "Classified At",
          "type": "string"
        }
      },
      "required": [
        "run_signature_sha",
        "data_axis",
        "classified_by",
        "classified_at"
      ],
      "title": "_ExecutorEntry",
      "type": "object"
    },
    "_HaloConfig": {
      "additionalProperties": false,
      "description": "The bounded look-back distance of a :class:`BoundedHalo` series axis.",
      "properties": {
        "expr": {
          "description": "Arithmetic expression giving the warm-up row count, over the run()'s own parameter names (bare names, resolved from the sweep point), e.g. 'train_window * 48'. Evaluated by hpc_agent.experiment_kit.axis_config with a restricted AST walk — only names, numeric literals, + - * //, and min()/max() are permitted; never eval().",
          "minLength": 1,
          "title": "Expr",
          "type": "string"
        }
      },
      "required": [
        "expr"
      ],
      "title": "_HaloConfig",
      "type": "object"
    }
  }
}
