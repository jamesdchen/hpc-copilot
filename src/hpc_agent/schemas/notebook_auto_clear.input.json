{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/notebook_auto_clear.input.json",
  "title": "notebook-auto-clear input spec",
  "description": "Inputs to ``notebook-auto-clear`` — all paths / ids are caller-declared.\n\n``source`` / ``template`` are ``.py`` relpaths under the experiment dir (or\nabsolute), parsed by the same percent-format parser. ``input_roots`` /\n``source_roots`` are the OPAQUE roots the server-side lint recompute needs (a\nmissing path literal under ``input_roots`` flags its section, keeping it out\nof the auto-clear) — but for the MUTATE verb they must NOT be supplied by the\ncaller (adversarial review F2): the roots are read UNCONDITIONALLY from the\naudit's recorded config (interview.json) so a caller cannot point the lint at\na directory of planted dummy files and launder a flagged section into\n``auto_cleared``. Supplying either field is a loud ``SpecInvalid`` refusal.\n(The fields remain on the wire so the refusal is explicit and self-documenting\nrather than a silently-ignored override.) There is deliberately NO ``receipt``\nfield (T10): render receipts are read from the journal (sha-fresh only), never\ntrusted from the caller — see the module docstring.",
  "type": "object",
  "required": [
    "audit_id",
    "source",
    "template"
  ],
  "additionalProperties": false,
  "properties": {
    "audit_id": {
      "description": "The notebook decision-journal scope id (filesystem-safe slug) the auto-clear records are appended to (journal lives at .hpc/notebooks/<audit_id>.decisions.jsonl). Caller-authored.",
      "minLength": 1,
      "title": "Audit Id",
      "type": "string"
    },
    "source": {
      "description": "Experiment-relative path to the audited source .py (jupytext percent format). Section shas are recomputed fresh; an auto-clear binds the recomputed sha, never a caller-asserted one.",
      "minLength": 1,
      "title": "Source",
      "type": "string"
    },
    "template": {
      "description": "Experiment-relative path to the template .py. A section auto-clears only when it is byte-identical (inherited) to its template section.",
      "minLength": 1,
      "title": "Template",
      "type": "string"
    },
    "input_roots": {
      "description": "Must be EMPTY (adversarial review F2): the mutate verb reads the data-path roots from the audit's recorded config (interview.json), never from the caller — supplying non-empty roots is a SpecInvalid refusal so a section cannot be laundered into auto_cleared with planted roots.",
      "items": {
        "type": "string"
      },
      "title": "Input Roots",
      "type": "array"
    },
    "source_roots": {
      "description": "Must be EMPTY (adversarial review F2): import roots are read from the audit's recorded config, never from the caller — supplying non-empty roots is a SpecInvalid refusal.",
      "items": {
        "type": "string"
      },
      "title": "Source Roots",
      "type": "array"
    }
  }
}
