{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/notebook_status.input.json",
  "title": "notebook-status input spec",
  "description": "Inputs to ``notebook-status``.\n\nAll three relpaths / ids are resolved against the ``--experiment-dir``: the\n``source`` and ``template`` are ``.py`` files in jupytext percent format\n(parsed by the same section parser), and ``audit_id`` names the notebook\ndecision-journal scope whose sign-off / auto-clear records are replayed.",
  "type": "object",
  "required": [
    "audit_id",
    "source",
    "template"
  ],
  "additionalProperties": false,
  "properties": {
    "audit_id": {
      "description": "The notebook decision-journal scope id (filesystem-safe slug) whose sign-off / auto-clear records are reduced. Caller-authored.",
      "minLength": 1,
      "title": "Audit Id",
      "type": "string"
    },
    "source": {
      "description": "Experiment-relative path to the audited source .py (jupytext percent format). Its per-section shas are recomputed fresh on every call.",
      "minLength": 1,
      "title": "Source",
      "type": "string"
    },
    "template": {
      "description": "Experiment-relative path to the template .py. Its section slugs are the REQUIRED inventory the rollup verdict is computed over.",
      "minLength": 1,
      "title": "Template",
      "type": "string"
    },
    "review": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "OPAQUE render pointers chained from the `notebook-audit-view` span that ran immediately before this one in the `audit` block chain (`{view_sha, sections: [{slug, view_sha, render_path}]}`), composed in code by `block_chain`, never authored by a caller worth trusting. PURE DISCLOSURE: it is copied into the sign-off park's brief so the human is pointed at the exact renders they would be signing, and it feeds NOTHING else — not `passed`, not a status, not a sha this verb computes. The T8 sign-off gate independently recomputes `view_sha` and re-checks the render file, so a stale or forged pointer here can only mis-address a human's reading, never satisfy a gate. Absent (the default, and every standalone call) → byte-identical.",
      "title": "Review"
    }
  }
}
