{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/pack_status.output.json",
  "title": "pack-status output data",
  "description": "Pack state keyed by pack slug — one entry per reported pack.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "packs": {
      "additionalProperties": {
        "$ref": "#/$defs/PackStatusEntry"
      },
      "title": "Packs",
      "type": "object"
    }
  },
  "$defs": {
    "PackBind": {
      "additionalProperties": false,
      "description": "The current-bind projection for a pack (newest valid ``pack-bind``).",
      "properties": {
        "pack": {
          "title": "Pack",
          "type": "string"
        },
        "version": {
          "title": "Version",
          "type": "string"
        },
        "manifest_sha": {
          "title": "Manifest Sha",
          "type": "string"
        },
        "bound_at": {
          "description": "Timestamp of the current bind record.",
          "title": "Bound At",
          "type": "string"
        }
      },
      "required": [
        "pack",
        "version",
        "manifest_sha",
        "bound_at"
      ],
      "title": "pack current-bind",
      "type": "object"
    },
    "PackDanglingReference": {
      "additionalProperties": false,
      "description": "One dangling-reference finding for an opted-in pack (the LOUD path).\n\nAn opted-in repo whose manifest is missing/sha-drifted, whose bind names a\nfile that no longer resolves, or whose slot binding names a pack with no\ncurrent bind. ``reason`` is the honest mechanical note; ``path`` / ``slot``\nname the offending reference when applicable.",
      "properties": {
        "reason": {
          "title": "Reason",
          "type": "string"
        },
        "path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Path"
        },
        "slot": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Slot"
        }
      },
      "required": [
        "reason"
      ],
      "title": "pack dangling reference",
      "type": "object"
    },
    "PackLineage": {
      "additionalProperties": false,
      "description": "The lineage stamp of a PROGRAM pack + its freshness against the bound source.\n\nAdditive/optional (P3 observability, DC10): present only when the pack's\nmanifest carries a ``derived_from`` stamp. ``pack``/``seam``/``version``/``sha``\necho the stamp; ``freshness`` compares the recorded ``sha`` against the\ncurrently-bound source pack's seam-file sha:\n\n* ``current`` — the source pack is bound and its seam sha matches the stamp.\n* ``behind`` — the source pack is bound but its seam sha differs (a re-init from\n  upstream or a skeleton upgrade moved it; the edge is NOT severed — freshness\n  evidence only, DC2).\n* ``source-not-bound`` — the named source pack is not an opted-in current bind,\n  so freshness cannot be established.",
      "properties": {
        "pack": {
          "title": "Pack",
          "type": "string"
        },
        "seam": {
          "title": "Seam",
          "type": "string"
        },
        "version": {
          "title": "Version",
          "type": "string"
        },
        "sha": {
          "title": "Sha",
          "type": "string"
        },
        "freshness": {
          "enum": [
            "current",
            "behind",
            "source-not-bound"
          ],
          "title": "Freshness",
          "type": "string"
        }
      },
      "required": [
        "pack",
        "seam",
        "version",
        "sha",
        "freshness"
      ],
      "title": "pack lineage",
      "type": "object"
    },
    "PackSlotStatus": {
      "additionalProperties": false,
      "description": "One caller-authored slot's receipt currency — mechanical, never interpreted.\n\n``status`` is the reduction outcome: ``current`` (a fresh, passed receipt),\n``failed`` (fresh receipt but ``passed=false``), ``stale`` (a receipt exists\nbut content it covered drifted — stale = missing by construction), or\n``missing`` (no receipt for the slot). ``passed`` is the recorded boolean\nwhen a receipt exists; ``reason`` is an optional honest note.",
      "properties": {
        "slot": {
          "title": "Slot",
          "type": "string"
        },
        "status": {
          "enum": [
            "current",
            "stale",
            "missing",
            "failed"
          ],
          "title": "Status",
          "type": "string"
        },
        "passed": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Passed"
        },
        "reason": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Reason"
        }
      },
      "required": [
        "slot",
        "status"
      ],
      "title": "pack slot status",
      "type": "object"
    },
    "PackStatusEntry": {
      "additionalProperties": false,
      "description": "The full status digest for one pack.\n\n``bind`` is the current bind or null; ``slots`` the per-slot receipt\nstatuses; ``unfillable`` the advisory report; ``dangling`` the loud\ndangling-reference findings.",
      "properties": {
        "bind": {
          "anyOf": [
            {
              "$ref": "#/$defs/PackBind"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "slots": {
          "items": {
            "$ref": "#/$defs/PackSlotStatus"
          },
          "title": "Slots",
          "type": "array"
        },
        "unfillable": {
          "items": {
            "$ref": "#/$defs/PackUnfillableRequirement"
          },
          "title": "Unfillable",
          "type": "array"
        },
        "dangling": {
          "items": {
            "$ref": "#/$defs/PackDanglingReference"
          },
          "title": "Dangling",
          "type": "array"
        },
        "derived_from": {
          "anyOf": [
            {
              "$ref": "#/$defs/PackLineage"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The PROGRAM-pack lineage stamp + freshness, when the manifest carries a ``derived_from`` (P3 observability). ``None`` for a lineage-root (domain) pack or a manifest with no stamp — serialized as ``null`` for legacy packs (an additive optional field; the dispatch serializer does not exclude_none)."
        },
        "audit_template": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Experiment-dir-relative path to this current-bound pack's ``audit_template`` seam file, when it declares one — the AUDIT-FACING template prepared for building an experiment off the pack (run-#12 finding 1). Identity/pointer only: the on-ramp COMPOSES the default template path from this seam and presents a confirm-default rather than an open path question. ``None`` when the pack declares no ``audit_template`` seam or is not current-bound.",
          "title": "Audit Template"
        }
      },
      "title": "pack status entry",
      "type": "object"
    },
    "PackUnfillableRequirement": {
      "additionalProperties": false,
      "description": "A slot the caller bound to a pack whose manifest ``fills_slots`` omits it.\n\nADVISORY only (``fills_slots`` never becomes load-bearing — DP4): a\nrequirement always originates with the caller, so this is an early warning\nthat the pack does not claim it can fill the slot, never a gate.",
      "properties": {
        "slot": {
          "title": "Slot",
          "type": "string"
        },
        "pack": {
          "title": "Pack",
          "type": "string"
        },
        "reason": {
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "slot",
        "pack",
        "reason"
      ],
      "title": "pack unfillable requirement",
      "type": "object"
    }
  }
}
