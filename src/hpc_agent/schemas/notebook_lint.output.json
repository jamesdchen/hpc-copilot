{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jamesdchen/hpc-agent/schemas/notebook_lint.output.json",
  "title": "notebook-lint output data",
  "description": "The lint report — a shape T5/T9/the skill consume opaquely.\n\n* ``findings`` — every reported violation (empty = clean).\n* ``unverifiable_paths`` — computed path expressions that could not be\n  checked (the honest executes-live gap).\n* ``linked_sources`` — imports resolved under ``source_roots``, with hashes.\n* ``declared_outputs`` — path literals under a declared ``output_root``\n  (write targets, exempt from the executes-live flag — reported, never\n  flagged).\n* ``reader_call_echo`` — the opaque ``{pack, version, sha}`` echo carried\n  verbatim from the input WHEN a caller-declared reader match surfaced a\n  record (a finding, an unverifiable gap, or a declared output attributable\n  to a matched reader call) AND the caller supplied one; else ``None``. Core\n  copies it, never interprets it — it is the provenance of the pack whose\n  vocabulary drove the record.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "findings": {
      "items": {
        "$ref": "#/$defs/NotebookLintFinding"
      },
      "title": "Findings",
      "type": "array"
    },
    "unverifiable_paths": {
      "items": {
        "type": "string"
      },
      "title": "Unverifiable Paths",
      "type": "array"
    },
    "linked_sources": {
      "items": {
        "$ref": "#/$defs/LinkedSource"
      },
      "title": "Linked Sources",
      "type": "array"
    },
    "declared_outputs": {
      "items": {
        "$ref": "#/$defs/DeclaredOutput"
      },
      "title": "Declared Outputs",
      "type": "array"
    },
    "reader_call_echo": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Reader Call Echo"
    },
    "stage_reached": {
      "const": "linted",
      "default": "linted",
      "description": "The terminator this block stopped at (decision-as-data, #231). ONE stage: findings are REPORTED, never fatal (the graduation gate refuses, the lint reports), so the lint has exactly one deterministic successor regardless of what it found.",
      "title": "Stage Reached",
      "type": "string"
    },
    "needs_decision": {
      "default": false,
      "description": "Always false — the lint reports; it asks the human nothing.",
      "title": "Needs Decision",
      "type": "boolean"
    },
    "next_block": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The DETERMINISTICALLY-computed next block — `{verb, why, spec_hint}` — or null when no `audit_id` seat was supplied (the standalone lint, byte-identical to the pre-chain verb).",
      "title": "Next Block"
    }
  },
  "$defs": {
    "DeclaredOutput": {
      "additionalProperties": false,
      "description": "One path literal recognised as a WRITE target under a declared ``output_root``.\n\nReported, never flagged: an output does not exist before the run, so the\nexecutes-live not-exists check is exempt for it. ``section`` is the source\nsection slug the literal sits in (``None`` for the module preamble).",
      "properties": {
        "path": {
          "title": "Path",
          "type": "string"
        },
        "section": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Section"
        }
      },
      "required": [
        "path"
      ],
      "title": "notebook-lint declared output",
      "type": "object"
    },
    "LinkedSource": {
      "additionalProperties": false,
      "description": "One import that resolved to a file under a caller ``source_root``.\n\nJudges import ORIGIN IDENTITY only: ``module`` is the imported dotted name,\n``file`` is the resolved file's path (relative to the experiment dir when it\nis under it, else absolute), and ``module_sha`` is\n:func:`hpc_agent.state.audit_source.sha256_normalized` over the file text —\nthe SAME hashing primitive T9 recomputes to drift-check the link at sign-off.\n\nThe audit net (6a) ADDITIONALLY annotates each link with the tier the module\nclassifies into and the import chain that reached it — additive optional\nfields (default empty), so a link produced without the net stays wire-identical\nto the pre-6a shape. ``tier`` is the ``AuditNetTier`` value (``inherited`` /\n``external`` / ``unresolved`` / ``new_drifted``); ``via`` is the import chain\nfrom the seed (``via[-1] == module``).",
      "properties": {
        "module": {
          "title": "Module",
          "type": "string"
        },
        "file": {
          "title": "File",
          "type": "string"
        },
        "module_sha": {
          "title": "Module Sha",
          "type": "string"
        },
        "tier": {
          "default": "",
          "title": "Tier",
          "type": "string"
        },
        "via": {
          "items": {
            "type": "string"
          },
          "title": "Via",
          "type": "array"
        }
      },
      "required": [
        "module",
        "file",
        "module_sha"
      ],
      "title": "notebook-lint linked source",
      "type": "object"
    },
    "NotebookLintFinding": {
      "additionalProperties": false,
      "description": "One reported lint finding.\n\n* ``rule`` — which check produced it.\n* ``section`` — the source (or template) section slug the finding is about,\n  or ``None`` for a module-level finding. T5 counts findings per ``section``:\n  a section with zero findings is one auto-clear precondition.\n* ``detail`` — human-readable description of the specific violation.\n* ``evidence`` — opaque structured payload (the offending slug, path literal,\n  line number, …) for a renderer; never interpreted by core.",
      "properties": {
        "rule": {
          "enum": [
            "structural_completeness",
            "executes_live",
            "linked_sources",
            "template_import_shadowed",
            "executor_module_drift",
            "audit_net_unresolved"
          ],
          "title": "Rule",
          "type": "string"
        },
        "section": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Section"
        },
        "detail": {
          "title": "Detail",
          "type": "string"
        },
        "evidence": {
          "additionalProperties": true,
          "title": "Evidence",
          "type": "object"
        }
      },
      "required": [
        "rule",
        "detail"
      ],
      "title": "notebook-lint finding",
      "type": "object"
    }
  }
}
