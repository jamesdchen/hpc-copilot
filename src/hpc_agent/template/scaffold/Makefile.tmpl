# Project Makefile.
#
# The hpc-agent experiment-template targets (export / repro /
# new-experiment / lint / test) live in .hpc/template.mk, which is
# framework-owned and re-injected by `hpc-agent build-template`. Add
# project-specific targets below the include line.

include .hpc/template.mk
