[
  {
    "agent_facing": true,
    "cli": "hpc-agent append-decision --spec <path> [--experiment-dir <dir>]",
    "idempotent": false,
    "input_schema": "append_decision.input.json",
    "name": "append-decision",
    "output_schema": "append_decision.output.json",
    "python": "hpc_agent.ops.decision.journal.append_decision",
    "side_effects": [
      "file_write"
    ],
    "summary": "Append one y/nudge exchange to a run's or campaign's decision journal (append-only JSONL). The decision record — not the chat scroll — is the source of truth for why a run took the shape it did (design §2).",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent archive-dossier --spec <path>",
    "idempotent": true,
    "input_schema": "archive_dossier.input.json",
    "name": "archive-dossier",
    "output_schema": "archive_dossier.output.json",
    "python": "hpc_agent.ops.archive_dossier.archive_dossier",
    "side_effects": [
      "network-upload"
    ],
    "summary": "Upload an exported dossier archive to an S3-compatible bucket (AWS S3 / R2 / B2 / MinIO) with a SHA-256 integrity fingerprint. Refuses to overwrite an existing key unless overwrite=true (client-side immutability posture; real immutability is bucket versioning + Object Lock — see docs/internals/dossier-archival.md). Needs the [s3] extra (boto3); credentials come from the standard AWS chain, never the spec.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent cluster-reduce [--experiment-dir <dir>] --run-id <run_id> [--aggregate-cmd <aggregate_cmd>] [--output-path <output_path>] [--local-dir <local_dir>] [--extra-env <extra_env>] [--timeout-sec <timeout_sec>]",
    "idempotent": true,
    "input_schema": null,
    "name": "cluster-reduce",
    "output_schema": "cluster_reduce.output.json",
    "python": "hpc_agent.ops.aggregate.cluster_reduce.cluster_reduce",
    "side_effects": [
      "ssh",
      "sync-pull"
    ],
    "summary": "Run the user's reducer on the cluster, pull only its single output JSON. Eliminates the bulk per-task rsync_pull failure mode at /aggregate-hpc + campaign-loop time.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent aggregate [--experiment-dir <dir>] --run-id <run_id> --wave <wave> [--force] [--require-outputs <require_outputs>] [--expect-output <expect_output>]",
    "idempotent": true,
    "input_schema": null,
    "name": "combine-wave",
    "output_schema": "combine_wave.output.json",
    "python": "hpc_agent.ops.aggregate.combine.combine_wave",
    "side_effects": [
      "runs",
      "ssh",
      "writes-cluster",
      "writes-journal"
    ],
    "summary": "Run the on-cluster combiner for one wave; records outcome to journal.",
    "verb": "mutate"
  },
  {
    "agent_facing": false,
    "cli": "hpc-agent conformance-record --spec <path> [--experiment-dir <dir>]",
    "idempotent": false,
    "input_schema": "conformance_record.input.json",
    "name": "conformance-record",
    "output_schema": "conformance_record.output.json",
    "python": "hpc_agent.ops.conformance.record_op.conformance_record",
    "side_effects": [
      "file_write"
    ],
    "summary": "Journal ONE live conformance observation against the registration it tests — the emitter's evidence FOR/AGAINST a sealed hypothesis at production cadence. Recomputes the payload content_sha SERVER-SIDE over {payload, labels, observed_at} and binds it (a caller cannot assert a sha; there is no sha field on the spec), stamps the registration's reduced status_at_record (the live dossier-sha leg — current/stale/revoked), and appends exactly one line to <experiment>/_aggregated/_conformance/<registration_id>.jsonl. An absent registration is refused loudly (no hypothesis to test); a stale/revoked/superseded registration is RECORDED and its status disclosed (fail-open for evidence — production is the experiment that never stops). payload/observed_at values stay caller-attested (the emitter's truthfulness, not core's). Local read + one journal append, no SSH.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent data-manifest [--spec <path>] [--experiment-dir <dir>]",
    "idempotent": false,
    "input_schema": "data_manifest.input.json",
    "name": "data-manifest",
    "output_schema": "data_manifest.output.json",
    "python": "hpc_agent.ops.data_manifest.data_manifest",
    "side_effects": [
      "file_write"
    ],
    "summary": "Mint an identity record (sha256 + size + opaque built_by per file) for the experiment's DECLARED input data into .hpc/data_manifest.json, journal the mint, and refresh the (size, mtime) fast-path cache. Converts silent data changes from invisible to attributed — the quiet-corruption class (same filename, rebuilt bytes) that no robustness layer catches because nothing throws. Roots default to interview.json's audited_source.input_roots (the ONE input declaration); no roots + no declaration REFUSES (core never guesses a data/ dir). Re-minting is the journaled 'new known-good' act. Pure local read + hash + write, no SSH.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent decorate-entry-point --path <path> --function-name <function_name>",
    "idempotent": true,
    "input_schema": null,
    "name": "decorate-entry-point",
    "output_schema": null,
    "python": "hpc_agent.incorporation.decorate_entry_point.decorate_entry_point",
    "side_effects": [
      "filesystem"
    ],
    "summary": "Insert `from hpc_agent import register_run` + `@register_run` onto a named module-level function via a structure-preserving AST line-splice (body untouched). Refuses signature-rewriting decorators (hydra/click/typer) — route those to the wrapper fallback.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent doctor-install --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "doctor_install.input.json",
    "name": "doctor-install",
    "output_schema": "doctor_install.output.json",
    "python": "hpc_agent.ops.recover.doctor_install.doctor_install",
    "side_effects": [
      "file_write",
      "scheduler"
    ],
    "summary": "Install (or uninstall) an OS-scheduled `hpc-agent doctor` scan for this experiment dir — the out-of-session half of the driver dead-man's switch (design §5). Opt-in, never auto-installed. Windows → Task Scheduler (schtasks); POSIX → crontab. Writes a durable doctor spec under the journal home and points the scheduler at it with notify=true, so the scan alerts (OS notification) when it finds a stalled/orphaned run. Idempotent: re-running never duplicates the task. It only schedules the DETECTOR — it never re-arms a run.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent emit-skill-return [--experiment-dir <dir>] --skill <skill>",
    "idempotent": true,
    "input_schema": null,
    "name": "emit-skill-return",
    "output_schema": null,
    "python": "hpc_agent.cli.skill_returns.emit_skill_return",
    "side_effects": [
      "filesystem"
    ],
    "summary": "Validate the staged sub-skill return envelope at <experiment_dir>/.hpc/_returns/<skill>.staged.json against the per-skill schema, then atomically rename to <skill>.json. Use as the sub-skill's FINAL tool call to hand off to the parent skill without firing an end-of-turn chat message.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent export-attestations --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "export_attestations.input.json",
    "name": "export-attestations",
    "output_schema": "export_attestations.output.json",
    "python": "hpc_agent.ops.export_attestations.export_attestations",
    "side_effects": [
      "file_write"
    ],
    "summary": "Project a run's sealed dossier evidence into portable in-toto Statements (one per sealed store entry), each wrapped in an unsigned DSSE envelope, as a JSONL stream ecosystem tooling can verify without hpc-agent. Delegates the gather to export-dossier (the stores are never walked twice); subject digests are copied verbatim and record bytes ride verbatim in the predicate — the export never parses what it attests. --include-lineage widens it to the whole supersession chain. Pure local reads + one local write, no SSH.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent export-dossier --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "export_dossier.input.json",
    "name": "export-dossier",
    "output_schema": "export_dossier.output.json",
    "python": "hpc_agent.ops.export_dossier.export_dossier",
    "side_effects": [
      "file_write"
    ],
    "summary": "Bundle a run's core-owned record trail — sidecar, decision journal, briefs, block terminals, journal record, scope journals + look ledgers, and harvested aggregates — into one integrity-sealed .zip with a provenance manifest. --include-lineage widens it to the run's whole supersession chain. Pure local reads + one local write, no SSH.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent host-retarget --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "host_retarget.input.json",
    "name": "host-retarget",
    "output_schema": "host_retarget.output.json",
    "python": "hpc_agent.ops.host_retarget.host_retarget",
    "side_effects": [
      "writes-journal"
    ],
    "summary": "Move an IN-FLIGHT run to a different login node of the SAME cluster (run-12 finding 23): patch the run's journaled `cluster` key so resolve_ssh_target derives the new user@host at use time — no journal surgery. The new cluster MUST serve the same scheduler & scratch (a login-node failover, not a cluster move); a different scheduler/scratch is REFUSED — use retarget-run for that. Journals the failover as a decision (the provenance trail the hand-edit lacked).",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent kill --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "kill.input.json",
    "name": "kill",
    "output_schema": "kill.output.json",
    "python": "hpc_agent.ops.monitor.kill.kill",
    "side_effects": [
      "ssh",
      "writes-journal"
    ],
    "summary": "Kill a run's scheduler jobs: journal the intent, attempt cancellation through the backend seam (if one exists), verify against the scheduler, journal the verified-gone subset, and report 'N requested, N confirmed gone'. Request -> journaled -> verified.",
    "verb": "mutate"
  },
  {
    "agent_facing": false,
    "cli": null,
    "idempotent": true,
    "input_schema": null,
    "name": "mark-run-terminal",
    "output_schema": null,
    "python": "hpc_agent.ops.monitor.reconcile.mark_terminal",
    "side_effects": [
      "writes-journal"
    ],
    "summary": "",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent notebook-auto-clear --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "notebook_auto_clear.input.json",
    "name": "notebook-auto-clear",
    "output_schema": "notebook_auto_clear.output.json",
    "python": "hpc_agent.ops.notebook.auto_clear_op.notebook_auto_clear",
    "side_effects": [
      "file_write"
    ],
    "summary": "Journal CODE auto-clear attestations for the template-inherited, clean sections of an audited source .py — the machine mirror of a notebook sign-off. RECOMPUTES the lint + D-attention tier server-side (caller findings / tier claims are never trusted), then appends a notebook-auto-clear record (attestor=code, bound to the recomputed section hash) for every auto_cleared section not already cleared at its current hash. human_required sections are never cleared; a re-run at unchanged hashes appends nothing. Pure local reads + journal appends, no SSH.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent notebook-draft --spec <path> [--experiment-dir <dir>]",
    "idempotent": false,
    "input_schema": "notebook_draft.input.json",
    "name": "notebook-draft",
    "output_schema": "notebook_draft.output.json",
    "python": "hpc_agent.ops.notebook.draft_op.notebook_draft",
    "side_effects": [
      "file_write"
    ],
    "summary": "Journal a CODE draft attestation for one section of an audited source .py — the drafter-attribution seam (multi-human). Records the actor whose SESSION drove the drafting as the section's author, at DRAFT time (the reviewer!=author gate resolves it). Parses the source ON DISK and binds the draft to the freshly-parsed section sha (a draft can only be recorded against current source, and the old draft reads stale the moment the section is redrafted). The actor is resolved SERVER-SIDE from the session env (HPC_ACTOR) and validated against the interview's declared actors.ids — NEVER a wire field (an agent-suppliable actor would let the model choose its identity). Refuses when >1 actor is declared and no session actor resolves (an anonymous act is the laundering channel); records attestor_id=None when zero/one actor is declared. Pure local read + journal append, no SSH.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent notebook-record-config --spec <path> [--experiment-dir <dir>]",
    "idempotent": false,
    "input_schema": "notebook_record_config.input.json",
    "name": "notebook-record-config",
    "output_schema": "notebook_record_config.output.json",
    "python": "hpc_agent.ops.notebook.record_config_op.notebook_record_config",
    "side_effects": [
      "file_write"
    ],
    "summary": "Record the audit configuration (input_roots / source_roots / attention_order / output_roots - opaque relpath lists) for a STANDALONE notebook audit, as a journaled record in the audit's own decision journal. Without it a standalone audit (no interview.json audited_source opt-in) runs ROOTLESS-canonical: the lint recomputes with empty roots and the template-mandated source_roots engine-drift binding is silently inactive (run #10). The canonical view reads the interview block FIRST when present (the opt-in path owns the config), else this journaled record. Refuses when interview.json already carries audited_source for this audit_id (one source of truth) and when a config record already exists (immutable-per-audit - supersede with a NEW audit_id). Recording into an audit that already has journal entries succeeds with a LOUD warning: every view_sha moves, prior sign-offs read stale. Optionally carries the audit-OPEN intent (goal + task_axes - the free-text campaign goal and the names of what varies across tasks) on the same immutable seat, the durable record audit-handoff reads (absent = byte-identical). Pure local read + journal append, no SSH.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent notebook-record-receipt --spec <path> [--experiment-dir <dir>]",
    "idempotent": false,
    "input_schema": "notebook_record_receipt.input.json",
    "name": "notebook-record-receipt",
    "output_schema": "notebook_record_receipt.output.json",
    "python": "hpc_agent.ops.notebook.record_receipt_op.notebook_record_receipt",
    "side_effects": [
      "file_write"
    ],
    "summary": "Journal CODE render receipts for the sections of an audited source .py — the emitter's evidence that a section was rendered (executed) and whether its assertions errored. Parses the source ON DISK and binds each receipt to the freshly-parsed section sha (a receipt can only be recorded against current source, and drifts stale when the section moves), then appends a notebook-render-receipt record (attestor=code) per known slug. Unknown slugs are reported skipped. notebook-auto-clear reads these journaled receipts (sha-fresh only) — it never accepts an inline caller receipt. Freshness (the sha bind) is recomputed here; output_sha/error stay caller-attested (D9), weighed by the graduation consumers. Pure local read + journal append, no SSH.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent notebook-scaffold-template --spec <path> [--experiment-dir <dir>]",
    "idempotent": false,
    "input_schema": "notebook_scaffold_template.input.json",
    "name": "notebook-scaffold-template",
    "output_schema": "notebook_scaffold_template.output.json",
    "python": "hpc_agent.ops.notebook.scaffold_template_op.notebook_scaffold_template",
    "side_effects": [
      "file_write"
    ],
    "summary": "Scaffold a content-free notebook-audit template: write a jupytext percent-format .py with one # %% cell per requested section slug, each opening with its hpc-audit-section marker (the ONE marker grammar from state/audit_source.py) plus a one-line placeholder comment. Cell bodies are caller-owned; the verb emits format machinery only, plus a marker-less path-bootstrap preamble cell (before the first marker, outside every audit section) that normalizes sys.path + cwd to the .hpc-marked experiment root under interactive kernels. Round-trip verified: the written file is re-parsed and must yield exactly the requested slugs, else it is deleted and the call refused. Refuses empty/duplicate/malformed slugs and an existing output file (no force flag - delete it first). Pure local write, no SSH.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent pack-bind --spec <path> [--experiment-dir <dir>]",
    "idempotent": false,
    "input_schema": "pack_bind.input.json",
    "name": "pack-bind",
    "output_schema": "pack_bind.output.json",
    "python": "hpc_agent.ops.pack.bind_op.pack_bind",
    "side_effects": [
      "file_write"
    ],
    "summary": "Bind a domain pack into an experiment AS DATA: read the caller-referenced manifest ON DISK, recompute every listed file's raw-bytes SHA-256 and the manifest sha server-side (no sha is caller-suppliable), refuse loudly on any drift or a missing/unreadable manifest, then append a CODE attestation (block pack-bind, response 'bound') under the pack's decision journal, bound through the one kernel against the fresh manifest hash. Editing pack content later moves the sha and revokes every clearance signed under the old standards — no drift state machine. Core never imports, executes, or interprets a pack file. Pure local read + journal append, no SSH.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent pack-record-receipt --spec <path> [--experiment-dir <dir>]",
    "idempotent": false,
    "input_schema": "pack_record_receipt.input.json",
    "name": "pack-record-receipt",
    "output_schema": "pack_record_receipt.output.json",
    "python": "hpc_agent.ops.pack.record_receipt_op.pack_record_receipt",
    "side_effects": [
      "file_write"
    ],
    "summary": "Journal a CODE receipt that a domain check reported passed for a caller-authored slot, against a set of checked files, under a pack's current bind. Recomputes every checked file's sha AND the bind's manifest sha ON DISK and builds the composite content_sha server-side (no caller-suppliable sha), then binds it through the one attestation kernel and appends a pack-receipt record. The receipt reads stale the instant any checked file or the bind moves; passed/evidence stay caller-attested (weighed by the gate, never re-derived). No current bind for the named pack is a loud dangling reference. Pure local read + journal append, no SSH.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent pack-refresh --spec <path> [--experiment-dir <dir>]",
    "idempotent": false,
    "input_schema": "pack_refresh.input.json",
    "name": "pack-refresh",
    "output_schema": "pack_refresh.output.json",
    "python": "hpc_agent.ops.pack.refresh_op.pack_refresh",
    "side_effects": [
      "file_write"
    ],
    "summary": "Re-seal every opted-in domain pack whose manifest is STALE against on-disk bytes (the minimal set — a stale pack never forces another's rebuild) from its declarative sweep.json recipe (pure hashing; core never runs a pack build/check script), rebind each via the pack-bind path (journaling old→new shas — the drift event is the archive record), and report which caller-authored receipt slots must be re-earned plus each one's caller-side check command (core never runs the check). Not opted in → empty and silent. Pure local read + manifest write + journal append, no SSH.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent program-init --spec <path> [--experiment-dir <dir>]",
    "idempotent": false,
    "input_schema": "program_init.input.json",
    "name": "program-init",
    "output_schema": "program_init.output.json",
    "python": "hpc_agent.ops.pack.init_op.program_init",
    "side_effects": [
      "file_write"
    ],
    "summary": "Materialize (create) or migrate (adopt) a PROGRAM pack layer from a domain pack's audit_template seam: instantiate the program template (verbatim skeleton copy + provenance header in create mode; untouched in adopt mode), stamp the derived_from lineage MECHANICALLY into the sweep.json recipe, seal the manifest via the generic pack re-seal, and bind the packs (journaling old→new). Core never interprets template content and never composes a check argv from pack content — a caller-authored check (spec.check) is run in the experiment dir, else the domain receipt slots are reported as to-earn. Pure local read + file write + journal append, no SSH.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent provenance-manifest --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "provenance_manifest.input.json",
    "name": "provenance-manifest",
    "output_schema": null,
    "python": "hpc_agent.ops.provenance_manifest.provenance_manifest",
    "side_effects": [
      "file_write"
    ],
    "summary": "Build and write the per-campaign provenance manifest at <experiment>/.hpc/provenance/<campaign_id>.json — one signable record pairing every run_id/trial_token of the campaign with its full {code, data, env, params, cluster} fingerprint, recomputed from the run sidecars on demand.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": null,
    "idempotent": true,
    "input_schema": null,
    "name": "prune-orphan-sidecars",
    "output_schema": null,
    "python": "hpc_agent.state.runs.prune_orphan_sidecars",
    "side_effects": [
      "removes-files"
    ],
    "summary": "",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent reconcile [--experiment-dir <dir>] --run-id <run_id> --scheduler <scheduler>",
    "idempotent": true,
    "input_schema": null,
    "name": "reconcile-journal",
    "output_schema": "reconcile.output.json",
    "python": "hpc_agent.ops.monitor.reconcile.reconcile",
    "side_effects": [
      "ssh",
      "writes-journal"
    ],
    "summary": "Re-derive ground truth from the cluster (status, waves, alive jobs).",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent reconcile-stale [--experiment-dir <dir>] [--now <now>] [--stale-after-hours <stale_after_hours>]",
    "idempotent": true,
    "input_schema": null,
    "name": "reconcile-stale",
    "output_schema": null,
    "python": "hpc_agent.ops.monitor.reconcile_stale.reconcile_stale",
    "side_effects": [
      "ssh",
      "writes-journal"
    ],
    "summary": "Bulk-close in-flight runs the scheduler no longer knows: ONE scheduler query per login node (via batch-status), then every run whose job_ids are all unknown is closed through the existing reconcile settle classification (abandoned/no-evidence) — never one-by-one SSH, never a cluster action. Ambiguous runs stay open and are listed.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent resubmit [--experiment-dir <dir>] --run-id <run_id> --spec <spec>",
    "idempotent": true,
    "input_schema": "resubmit.input.json",
    "name": "resubmit-failed",
    "output_schema": null,
    "python": "hpc_agent.ops.recover.runner.resubmit_failed",
    "side_effects": [
      "scheduler-submit",
      "writes-journal"
    ],
    "summary": "Record a resubmission attempt in the journal (caller does the actual qsub).",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent scope-lock --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "scope_lock.input.json",
    "name": "scope-lock",
    "output_schema": "scope_lock.output.json",
    "python": "hpc_agent.ops.decision.journal.scope_lock.scope_lock",
    "side_effects": [
      "file_write"
    ],
    "summary": "Lock a caller-tagged experiment scope. Locking is the SAFE direction (it only restricts), so there is no authorship bar. Idempotent-in-effect: re-locking an already-locked scope appends an audit record but leaves the lock state unchanged (already_locked=true). Unlocking is a HUMAN act journaled via append-decision (block=scope-unlock), not a verb here.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent update-run-constraints --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "update_run_constraints.input.json",
    "name": "update-run-constraints",
    "output_schema": "update_run_constraints.output.json",
    "python": "hpc_agent.ops.monitor.update_constraints.update_run_constraints",
    "side_effects": [
      "ssh",
      "writes-sidecar"
    ],
    "summary": "Mutate a run's cluster-side SLURM Features in place via `scontrol update jobid=<id> Features=<expr>` — retargets constraints WITHOUT cancel+resubmit, so the jobs keep their accumulated age priority; mirrors the new feature set onto the run sidecar.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent watcher-install [--experiment-dir <dir>] --run-id <run_id> [--action <action>] --scheduler <scheduler> [--stale-sec <stale_sec>] [--interval-min <interval_min>]",
    "idempotent": true,
    "input_schema": "watcher_install.input.json",
    "name": "watcher-install",
    "output_schema": "watcher_install.output.json",
    "python": "hpc_agent.ops.monitor.watcher_install.watcher_install",
    "side_effects": [
      "scheduler-submit",
      "ssh"
    ],
    "summary": "Install/uninstall/status a cluster-side heartbeat watcher for a run via an install-time probe ladder (crontab -> scrontab -> self-resubmitting job -> none, loud). The watcher survives the laptop and alarms when the client stops reading status.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent write-run-sidecar --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "write_run_sidecar.input.json",
    "name": "write-run-sidecar",
    "output_schema": null,
    "python": "hpc_agent.ops.write_run_sidecar.write_run_sidecar",
    "side_effects": [
      "file_write"
    ],
    "summary": "Write the per-run sidecar JSON at <experiment>/.hpc/runs/<run_id>.json. Use this to satisfy the submit-flow write-first guard via the CLI instead of introspecting the Python helper. Auto-stamps submitted_at and hpc_agent_version.",
    "verb": "mutate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent apply-safe-defaults --spec <path>",
    "idempotent": true,
    "input_schema": "apply_safe_defaults.input.json",
    "name": "apply-safe-defaults",
    "output_schema": "apply_safe_defaults.output.json",
    "python": "hpc_agent.ops.submit.apply_safe_defaults.apply_safe_defaults",
    "side_effects": [],
    "summary": "Consume a {resolved, ambiguities} envelope and fill each ambiguity from its safe_default. Structurally cannot fill task_generator (no slot, by the field partition); re-checks may_have_safe_default and raises spec_invalid if a required-caller field carries a safe_default. Reports what stayed unresolved (chiefly goal / task_generator).",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent attention-queue --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "attention_queue.input.json",
    "name": "attention-queue",
    "output_schema": "attention_queue.output.json",
    "python": "hpc_agent.ops.attention_op.attention_queue",
    "side_effects": [],
    "summary": "The attention queue: the fleet-wide digest ordered by needs-your-verdict-first. Collects every place a human action is the blocking edge (pending greenlights, committed-but-unadvanced decisions, anomaly briefs, campaign completion briefs, unsigned/stale audit sections, dead workers, alerts, open ssh circuits) across one experiment or the whole fleet (fleet=True), orders by leverage (unblock fan-out) then class then oldest, and renders a deterministic markdown digest relayed verbatim. Read-only, no SSH, watermark-neutral (moves no state); recomputed on every read (no cache).",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent audit-handoff --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "audit_handoff.input.json",
    "name": "audit-handoff",
    "output_schema": "audit_handoff.output.json",
    "python": "hpc_agent.ops.notebook.audit_handoff_op.audit_handoff",
    "side_effects": [],
    "summary": "Project the durable audit records into a DRAFT InterviewSpec (the audit->interview bridge). Reads the audit-open intent (goal + task_axes) and config roots off the notebook-audit-config seat, and AST-scans the source for @register_run entry points and $HPC_RESULT_DIR write candidates. Every field is DERIVED-and-disclosed or an explicit PLACEHOLDER the caller must fill - the verb never guesses (a guessed field becomes a journaled fact via the interview). summary_artifact and entry-point candidates are detected-and-disclosed (multiple = listed, caller confirms). Deterministic: same records -> byte-identical draft. No SSH.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent audit-preflight --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "audit_preflight.input.json",
    "name": "audit-preflight",
    "output_schema": "audit_preflight.output.json",
    "python": "hpc_agent.ops.audit_preflight.audit_preflight",
    "side_effects": [],
    "summary": "GO/NO-GO preflight for the notebook-audit loop. Composes existing substrate checks — template present + parses + git-committed-clean (uncommitted = unsigned), version skew (reused from doctor), declared roots exist and are non-empty, and prior audit state (resuming vs fresh) — into one decision-ready brief. Read-only, no SSH; detects nothing new and blocks nothing itself (the gates it predicts remain the enforcement). The kickoff collapses to 'run audit-preflight; if GO, begin'.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent batch-status [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": null,
    "name": "batch-status",
    "output_schema": null,
    "python": "hpc_agent.ops.monitor.batch_status.batch_status",
    "side_effects": [
      "ssh"
    ],
    "summary": "Poll ALL in-flight runs with one scheduler query per login node (qstat -u $USER / squeue) instead of one per run — the connection-storm fix. Returns per-run {job_id: TaskStatus} maps. Read-only: does not mutate the journal.",
    "verb": "query"
  },
  {
    "agent_facing": false,
    "cli": "hpc-agent campaign advance [--experiment-dir <dir>] --campaign-id <campaign_id> [--max-iters <max_iters>] [--metric <metric>] [--target <target>] [--direction <direction>] [--plateau-window <plateau_window>] [--plateau-tolerance <plateau_tolerance>] [--plateau-mode <plateau_mode>] [--max-jobs <max_jobs>] [--max-tasks <max_tasks>] [--max-walltime-sec <max_walltime_sec>] [--max-core-hours <max_core_hours>] [--circuit-breaker-failures <circuit_breaker_failures>] [--max-task-resubmits <max_task_resubmits>] [--async-refill] [--max-in-flight <max_in_flight>]",
    "idempotent": true,
    "input_schema": null,
    "name": "campaign-advance",
    "output_schema": "campaign.output.json",
    "python": "hpc_agent.meta.campaign.atoms.advance.campaign_advance",
    "side_effects": [],
    "summary": "Decide the next campaign action (continue / stop_converged / stop_over_budget / stop_circuit_breaker / stop_resubmit_cap / wait_in_flight / refill).",
    "verb": "query"
  },
  {
    "agent_facing": false,
    "cli": "hpc-agent campaign budget [--experiment-dir <dir>] --campaign-id <campaign_id> [--max-jobs <max_jobs>] [--max-tasks <max_tasks>] [--max-walltime-sec <max_walltime_sec>] [--max-core-hours <max_core_hours>]",
    "idempotent": true,
    "input_schema": null,
    "name": "campaign-budget",
    "output_schema": "campaign.output.json",
    "python": "hpc_agent.meta.campaign.atoms.budget.campaign_budget",
    "side_effects": [],
    "summary": "Roll up campaign-level spend and compare to optional caps.",
    "verb": "query"
  },
  {
    "agent_facing": false,
    "cli": "hpc-agent campaign converged [--experiment-dir <dir>] --campaign-id <campaign_id> [--max-iters <max_iters>] [--metric <metric>] [--target <target>] [--direction <direction>] [--plateau-window <plateau_window>] [--plateau-tolerance <plateau_tolerance>] [--plateau-mode <plateau_mode>]",
    "idempotent": true,
    "input_schema": null,
    "name": "campaign-converged",
    "output_schema": "campaign.output.json",
    "python": "hpc_agent.meta.campaign.atoms.converged.campaign_converged",
    "side_effects": [],
    "summary": "Apply user-supplied stop criteria to a campaign's history.",
    "verb": "query"
  },
  {
    "agent_facing": false,
    "cli": "hpc-agent campaign health [--experiment-dir <dir>] [--campaign-id <campaign_id>] [--since-iso <since_iso>] [--profile <profile>] [--cluster <cluster>]",
    "idempotent": true,
    "input_schema": "campaign_health.input.json",
    "name": "campaign-health",
    "output_schema": "campaign_health.output.json",
    "python": "hpc_agent.meta.campaign.atoms.health.campaign_health",
    "side_effects": [],
    "summary": "Structured run-history aggregation for an LLM agent. Returns walltime cliff rates, failure breakdown, GPU utilization, and a ready-to-feed-LLM suggested_prompt.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent campaign list [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": null,
    "name": "campaign-list",
    "output_schema": null,
    "python": "hpc_agent.meta.campaign.atoms.list_campaigns.campaign_list",
    "side_effects": [],
    "summary": "List every campaign with at least one sidecar in this experiment.",
    "verb": "query"
  },
  {
    "agent_facing": false,
    "cli": "hpc-agent campaign replay [--experiment-dir <dir>] --campaign-id <campaign_id> [--last-n <last_n>]",
    "idempotent": true,
    "input_schema": null,
    "name": "campaign-replay",
    "output_schema": "campaign.output.json",
    "python": "hpc_agent.meta.campaign.atoms.replay.campaign_replay",
    "side_effects": [],
    "summary": "Return the last N iterations of a campaign with reduced metrics.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent campaign status [--experiment-dir <dir>] --campaign-id <campaign_id>",
    "idempotent": true,
    "input_schema": null,
    "name": "campaign-status",
    "output_schema": null,
    "python": "hpc_agent.meta.campaign.atoms.status.campaign_status",
    "side_effects": [],
    "summary": "Report per-iteration reduced metrics for one campaign. Walks every sidecar tagged with --campaign-id, runs reduce_metrics on each, and emits the history dict-list.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent capabilities [--full]",
    "idempotent": true,
    "input_schema": null,
    "name": "capabilities",
    "output_schema": "capabilities.output.json",
    "python": "hpc_agent._kernel.extension.capabilities.capabilities",
    "side_effects": [],
    "summary": "Machine-readable feature flags: subcommands, schedulers, schema dirs.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent challenge-status --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "challenge_status.input.json",
    "name": "challenge-status",
    "output_schema": "challenge_status.output.json",
    "python": "hpc_agent.ops.challenge_status_op.challenge_status",
    "side_effects": [],
    "summary": "Read the standing dissent over a committed record. Two views: a challenge_id (the filing/verdict/withdraw thread) or a target address (a content_sha, or a subject_kind+subject_id pair) — 'what stands against this record?'. Reduces each challenge to open / upheld / dismissed / withdrawn / superseded, re-resolves the target (found-current / found-superseded) and each cited evidence sha (verified / unresolvable) — DISCLOSED, never refused. Contested is an orthogonal flag beside the target's status, never blocking. Fleet-capable. Read-only; renders a deterministic markdown brief relayed verbatim, whose canonical-JSON view_sha a verdict may bind.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent classify-axis-easy --source-path <source_path> --run-name <run_name>",
    "idempotent": true,
    "input_schema": null,
    "name": "classify-axis-easy",
    "output_schema": null,
    "python": "hpc_agent.incorporation.classify_axis_easy.classify_axis_easy",
    "side_effects": [],
    "summary": "Stdlib-only AST pattern-match for a @register_run function's DataAxis. Fast path used by the hpc-classify-axis skill: returns {kind, evidence, halo_expr?, tried}. `kind` is one of independent / bounded_halo / sequential / no_loop_detected / unclassifiable / function_not_found. `halo_expr` is populated when kind == bounded_halo. The skill records a confident hit directly — including no_loop_detected as the terminal `cartesian` (no-series) verdict; only unclassifiable / function_not_found fall back to its LLM decision tree.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent classify-campaign-path --source-path <source_path>",
    "idempotent": true,
    "input_schema": null,
    "name": "classify-campaign-path",
    "output_schema": null,
    "python": "hpc_agent.incorporation.classify_campaign_path.classify_campaign_path",
    "side_effects": [],
    "summary": "Stdlib-only AST pattern-match for a campaign's tasks.py: is it a manual fixed grid (Path A) or strategy-driven adaptive sampling (Path B)? Returns {path, decided_by, signals, supports_async_concurrency, candidates}. `path` is manual / strategy / unclassifiable. A confident hit is decided_by=code; an unparseable / unrecognized tasks.py escalates (decided_by=judgement) with both candidates for the LLM.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent clusters describe <name> [--strict]",
    "idempotent": true,
    "input_schema": null,
    "name": "clusters-describe",
    "output_schema": "clusters_describe.output.json",
    "python": "hpc_agent.ops.clusters.describe.describe_cluster",
    "side_effects": [],
    "summary": "Print one cluster's config.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent clusters list",
    "idempotent": true,
    "input_schema": null,
    "name": "clusters-list",
    "output_schema": "clusters_list.output.json",
    "python": "hpc_agent.ops.clusters.list.list_clusters",
    "side_effects": [],
    "summary": "List all clusters.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent compute-run-id [--experiment-dir <dir>] --run-name <run_name>",
    "idempotent": true,
    "input_schema": null,
    "name": "compute-run-id",
    "output_schema": null,
    "python": "hpc_agent.incorporation.build.compute_run_id.compute_run_id",
    "side_effects": [],
    "summary": "Compute the run_id from .hpc/tasks.py cmd_sha (run_id = <run_name>-<sha[:8]>).",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent conformance-status --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "conformance_status.input.json",
    "name": "conformance-status",
    "output_schema": "conformance_status.output.json",
    "python": "hpc_agent.ops.conformance.status_op.conformance_status",
    "side_effects": [],
    "summary": "Report a registration's LIVE conformance: reduce its journal, read its sealed conformance declaration + baseline, select a caller-named window over the ledger ({since,until?} or last_n), and judge it against the REGISTERED evidence via the one comparator. Returns per-key verdicts (within/outside the envelope, or a thin/novel/incomparable route to needs_verdict), the overall tier, both sides' range-phrased evidence, and a deterministic brief. Verdicts are DERIVED on every read — no verdict store, nothing marked seen. A reporter: a nonconforming window is a FINDING that changes no registration status. Read-only, no SSH.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent dag-frontier [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": null,
    "name": "dag-frontier",
    "output_schema": null,
    "python": "hpc_agent.ops.dag_frontier.dag_frontier",
    "side_effects": [],
    "summary": "Read-only view of the recorded run graph: per-node lifecycle state + parent edges (from sidecar parent_run_ids), the frontier of complete runs eligible to serve as parents, and per-node blocking ancestors. Pure local read; the walker's observation instrument — it computes, never submits.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent decide-concurrency [--supports-async] [--remaining-jobs <remaining_jobs>] [--in-flight <in_flight>] [--k-cap <k_cap>]",
    "idempotent": true,
    "input_schema": null,
    "name": "decide-concurrency",
    "output_schema": null,
    "python": "hpc_agent.meta.campaign.atoms.decide_concurrency.decide_concurrency",
    "side_effects": [],
    "summary": "Decide campaign iteration concurrency from observable evidence: the strategy's async support (classify-campaign-path's supports_async_concurrency) and budget headroom (campaign-budget's remaining, which already nets in-flight). Resolves sequential deterministically (decided_by=code) when async is unsupported or there's no headroom; otherwise escalates the how-aggressive choice (decided_by=judgement) carrying the computed safe max_in_flight bound.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent decide-monitor-arm --spec <path>",
    "idempotent": true,
    "input_schema": "decide_monitor_arm.input.json",
    "name": "decide-monitor-arm",
    "output_schema": "decide_monitor_arm.output.json",
    "python": "hpc_agent.ops.monitor.arm.decide_monitor_arm",
    "side_effects": [],
    "summary": "Pick cron/loop/none + cadence + cron schedule string from the run's current summary. Returns ready-to-pass CronCreate args for scheduling the next monitor tick. Replaces /monitor-hpc Step 5 agent judgment.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent decide-partial-handling --failed-count <failed_count> --combined-count <combined_count> [--retries-exhausted]",
    "idempotent": true,
    "input_schema": null,
    "name": "decide-partial-handling",
    "output_schema": null,
    "python": "hpc_agent.ops.aggregate.decide_partial_handling.decide_partial_handling",
    "side_effects": [],
    "summary": "Decide whether to proceed on incomplete aggregate waves from observable evidence: failed-wave count, combined-wave count, and whether combiner retries are exhausted. Resolves retry deterministically (decided_by=code) while retries remain; once exhausted, escalates the accept-partial-vs-force-retry choice (decided_by=judgement) carrying the computed missing fraction.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent decide-resubmit --failed-count <failed_count> --total-tasks <total_tasks> [--resubmit-failed-threshold <resubmit_failed_threshold>]",
    "idempotent": true,
    "input_schema": "decide_resubmit.input.json",
    "name": "decide-resubmit",
    "output_schema": "decide_resubmit.output.json",
    "python": "hpc_agent.ops.decide_resubmit.decide_resubmit",
    "side_effects": [],
    "summary": "Decide complete / resubmit / escalate for a terminal-with-failures wave from observable evidence: failed-task count, total-task count, and the resubmit_failed_threshold. failed_fraction == 0 is actually complete; <= threshold auto-resubmits; > threshold escalates the resubmit/investigate/abandon choice (safe_default investigate) rather than wasting cluster time re-running the same bug. The default threshold is 0.0 — every failure escalates unless the caller opts into auto-resubmit by passing a threshold > 0. Replaces hpc-status Step 6 prose.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent describe <name> [--schema]",
    "idempotent": true,
    "input_schema": null,
    "name": "describe",
    "output_schema": null,
    "python": "hpc_agent.cli.setup.describe",
    "side_effects": [],
    "summary": "Print a skill's procedure or a primitive's contract from the installed package data. A delegated worker uses this to fetch a cross-reference on the branch it is executing.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent detect-entry-point --experiment-dir <experiment_dir>",
    "idempotent": true,
    "input_schema": "detect_entry_point.input.json",
    "name": "detect-entry-point",
    "output_schema": "detect_entry_point.output.json",
    "python": "hpc_agent.ops.detect_entry_point.detect_entry_point",
    "side_effects": [],
    "summary": "Scan an experiment dir for Python entry-point candidates (main.py/train.py/console-scripts/python -m targets/shell scripts), classify each candidate's argv style (argparse / click / typer / hydra / fire / __main__), and locate files carrying @register_run. Collapses the duplicated six-probe shell block in hpc-wrap-entry-point Steps 0 + 1. Also surfaces interview.json's materialized entry-point block (kind shell_command / register_run / python_module) so one call answers submit.md Step 0b's wrapper-honor + mature-repo probe.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent dir-digest --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "dir_digest.input.json",
    "name": "dir-digest",
    "output_schema": "dir_digest.output.json",
    "python": "hpc_agent.ops.dir_digest.dir_digest",
    "side_effects": [
      "ssh"
    ],
    "summary": "Bounded, code-rendered digest of a directory tree — replaces raw `ls`/`find` in agent prose (a context-budget hazard on large trees). Reports file/dir counts, total size, the newest N entries, a top-~10 extension histogram, and (opt-in) per-marker line-hit counts across *.log/*.err files. LOCAL by default (path confined to the experiment dir); set --cluster to digest a scratch-confined REMOTE tree over ONE throttled ssh read (never ships a listing). Fails open on a missing/unreadable root. The `render` field is relayed verbatim.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent discover [--experiment-dir <dir>] [--search-dirs <search_dirs>]",
    "idempotent": true,
    "input_schema": null,
    "name": "discover-executors",
    "output_schema": "discover.output.json",
    "python": "hpc_agent.state.discover.discover_executors",
    "side_effects": [],
    "summary": "List executor scripts in --experiment-dir (CLIs with __main__).",
    "verb": "query"
  },
  {
    "agent_facing": false,
    "cli": "hpc-agent discover-reducers [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": null,
    "name": "discover-reducers",
    "output_schema": null,
    "python": "hpc_agent.state.discover.discover_reducers",
    "side_effects": [],
    "summary": "List candidate reducer / aggregator scripts in --experiment-dir (matches by filename stem and top-level function names like aggregate / reduce / score). Use at /aggregate-hpc time to find an existing reducer instead of writing a fresh one.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent discover-runs [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": null,
    "name": "discover-runs",
    "output_schema": null,
    "python": "hpc_agent.state.discover.discover_runs",
    "side_effects": [],
    "summary": "List @register_run functions under --experiment-dir (path, name, gpu, sha, flags).",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent doctor --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "doctor.input.json",
    "name": "doctor",
    "output_schema": "doctor.output.json",
    "python": "hpc_agent.ops.recover.doctor.doctor",
    "side_effects": [],
    "summary": "Driver watchdog (dead-man's switch). Scan live runs for a missed driver-tick deadline and surface each as a DRAFTED recovery proposal plus the evidence. Read-only, no SSH, no scheduler. It NEVER restarts or re-arms anything — detection is its whole job; safe recovery is guaranteed by tick idempotency. Run it out-of-session from an OS scheduler (Task Scheduler / cron).",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent evidence-brief --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "evidence_brief.input.json",
    "name": "evidence-brief",
    "output_schema": "evidence_brief.output.json",
    "python": "hpc_agent.ops.evidence_brief_op.evidence_brief",
    "side_effects": [],
    "summary": "The evidence brief: the POINT query over the research program's sealed records. Keyed by scope tags and/or a lineage run_id, it projects the cross-store digest for that scope — dated sha-cited conclusions (newest current first), per-tag prior-work counts, per-lineage determinism envelopes quoted verbatim from the fingerprint ledger, and each conclusion's citations re-resolved at read (verified / unresolvable — disclosed, never refused). Cheap (journal-first, no SSH), content-cached, fleet-capable. Read-only; renders a deterministic markdown digest relayed to the human verbatim.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent evidence-period --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "evidence_period.input.json",
    "name": "evidence-period",
    "output_schema": "evidence_period.output.json",
    "python": "hpc_agent.ops.evidence_period_op.evidence_period",
    "side_effects": [],
    "summary": "The evidence PERIOD digest: a time-window projection over the one evidence collector (conclusions, per-tag activity, determinism envelopes) that ENDS with the unconcluded-campaigns list — the standing invitation to close the conclusion loop. since is the inclusive window start; until the inclusive end (default open/now). Read-only, no SSH; recomputed on every read (a content-keyed cache only memoizes — deleting it changes no output). A pure projection: identity, counting, and verbatim record fields — it never interprets what a tag means or what a finding says.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent failures [--experiment-dir <dir>] --run-id <run_id> [--lines <lines>]",
    "idempotent": true,
    "input_schema": null,
    "name": "failures",
    "output_schema": "failures.output.json",
    "python": "hpc_agent.ops.recover.failures_atom.fetch_failures",
    "side_effects": [
      "ssh"
    ],
    "summary": "Cluster failed tasks by stderr fingerprint for triage.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent fetch-skill-return [--experiment-dir <dir>] --skill <skill> [--no-clear]",
    "idempotent": true,
    "input_schema": null,
    "name": "fetch-skill-return",
    "output_schema": null,
    "python": "hpc_agent.cli.skill_returns.fetch_skill_return",
    "side_effects": [
      "filesystem"
    ],
    "summary": "Read, re-validate, and emit the committed sub-skill return envelope at <experiment_dir>/.hpc/_returns/<skill>.json — the parent skill's seam after the Skill(<sub>) tool call returns. Deletes the file after reading unless --no-clear is set.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent find <query> [--limit <limit>]",
    "idempotent": true,
    "input_schema": null,
    "name": "find",
    "output_schema": null,
    "python": "hpc_agent.cli.setup.find",
    "side_effects": [],
    "summary": "Search the operations catalog by intent or half-remembered name and return a thin candidate list ({name, verb, cli, summary}) — the explore step between the full-catalog dump (`capabilities --full`) and a single contract (`describe <name>`). Pass a phrase like 'submit a batch'; cap with --limit.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent find-prior-run [--experiment-dir <dir>] --cmd-sha <cmd_sha>",
    "idempotent": true,
    "input_schema": null,
    "name": "find-prior-run",
    "output_schema": "find_prior_run.output.json",
    "python": "hpc_agent.cli.setup_actions.find_prior_run",
    "side_effects": [],
    "summary": "Look up a prior run by cmd_sha for /submit-hpc Step 6c resume detection. Returns {found, run_id, is_orphan, status, age_sec, ...}.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent harness-capabilities [--spec <path>] [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "harness_capabilities.input.json",
    "name": "harness-capabilities",
    "output_schema": "harness_capabilities.output.json",
    "python": "hpc_agent.ops.harness_capabilities.harness_capabilities",
    "side_effects": [],
    "summary": "Detect and report the harness capability set as CODE can observe it (LSP-style negotiation for the harness contract). Reports the contract capabilities — the out-of-band utterance log (installed capture hooks + this repo's log presence + MCP elicitation flag), relay/verbatim enforcement (the relay-audit Stop hook), backgrounding (core-side, always present; watchdog hook reported honestly), trusted display (unknown — no detection seam yet), and the Stop-hook append channel (unknown until a conformance probe activates it) — each with the named tier its absence degrades to. Read-only, no SSH, fail-open on an unreadable settings.json. The declaration IS what code can verify.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent inspect-deployment [--experiment-dir <dir>] --cluster <cluster> [--run-id <run_id>] [--path <path>] [--depth <depth>]",
    "idempotent": true,
    "input_schema": "inspect_deployment.input.json",
    "name": "inspect-deployment",
    "output_schema": "inspect_deployment.output.json",
    "python": "hpc_agent.ops.inspect_deployment.inspect_deployment",
    "side_effects": [
      "ssh"
    ],
    "summary": "Inspect a deployed experiment tree on the cluster (read-only, throttled): ls/find under REPO_DIR (--run-id) or an explicit scratch path (--path), through the connection-storm-safe ssh seam. Replaces raw `ssh <host> \"ls ...\"`, which bypasses the safe_interval / ConnectTimeout guards. Scratch-confined; no caller command string.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent inspect-parallel-axes [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "inspect_parallel_axes.input.json",
    "name": "inspect-parallel-axes",
    "output_schema": "inspect_parallel_axes.output.json",
    "python": "hpc_agent.ops.inspect_parallel_axes.inspect_parallel_axes",
    "side_effects": [],
    "summary": "Inspect an experiment's parallel axes in one call: parse .hpc/axes.yaml (axes / homogeneous_axes / classified executors) and surface .hpc/tasks.py's body so the build-executor / axes-init companion can classify dimensions without a multi-Read.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent list-in-flight [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": null,
    "name": "list-in-flight",
    "output_schema": "list_in_flight.output.json",
    "python": "hpc_agent.ops.monitor.list_in_flight.list_in_flight",
    "side_effects": [],
    "summary": "List runs with status=in_flight in the journal (recovery path).",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent load-context [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": null,
    "name": "load-context",
    "output_schema": "load_context.output.json",
    "python": "hpc_agent.meta.campaign.atoms.load_context.load_context",
    "side_effects": [],
    "summary": "Reconstruct workflow context (latest run + config snapshot, in-flight runs, campaigns) from on-disk state. Run this first in any fresh-context step instead of relying on memory.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent logs [--experiment-dir <dir>] --run-id <run_id> [--task-id <task_ids>] [--all-failed] [--lines <lines>]",
    "idempotent": true,
    "input_schema": null,
    "name": "logs",
    "output_schema": null,
    "python": "hpc_agent.ops.monitor.logs_atom.fetch_logs",
    "side_effects": [
      "ssh"
    ],
    "summary": "Fetch per-task stderr logs from the cluster (requires --task-id or --all-failed).",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent monitor-summary [--experiment-dir <dir>] --run-id <run_id>",
    "idempotent": true,
    "input_schema": null,
    "name": "monitor-summary",
    "output_schema": "monitor_summary.output.json",
    "python": "hpc_agent.ops.monitor.summary.monitor_summary",
    "side_effects": [],
    "summary": "Render the canonical user-facing tick summary for a run. Reads the journal runs dir's <run_id>.monitor.jsonl (the tick writers' path) + the run journal and returns {lifecycle_state, headline, body, armed_hint}. Slash command prints these verbatim.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent net-triage [--spec <path>]",
    "idempotent": true,
    "input_schema": "net_triage.input.json",
    "name": "net-triage",
    "output_schema": "net_triage.output.json",
    "python": "hpc_agent.ops.recover.net_triage.net_triage",
    "side_effects": [],
    "summary": "Connectivity differential: WHY can't I reach the cluster? For each configured cluster host (plus an optional --spec host): circuit-breaker state (read-only), one control-plane HTTPS probe, bounded DNS, and ONE bounded TCP connect to host:22 (skipped while the breaker is open and cooling; a lapsed cooldown reads as half_open_eligible and gets the probe as evidence). Verdict per host with remediation — run this before concluding a network cause; never diagnose with improvised ssh probes.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent notebook-audit-view --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "notebook_audit_view.input.json",
    "name": "notebook-audit-view",
    "output_schema": "notebook_audit_view.output.json",
    "python": "hpc_agent.ops.notebook.view_op.notebook_audit_view",
    "side_effects": [
      "file_write"
    ],
    "summary": "Render the deterministic per-section audit VIEW of a source .py against its template. Read-only, no SSH. Each SOURCE section carries its classification (inherited / added / modified, by hash), diff-from-template, static assertion table, lint flags, D-attention tier (auto_cleared iff inherited + no flags + assertions green), and a per-section view_sha (what a sign-off binds). By DEFAULT the CANONICAL view: lint recomputed server-side from the audit's recorded roots + journaled receipts, so its view_shas match what the T8 gate accepts (`canonical: true`); explicit findings/receipt/roots make it a preview. The result includes `markdown` — the code-rendered projection the skill relays VERBATIM. By DEFAULT `markdown` is the DIGEST (per-section metadata + render-file pointers, NO diff/assertion/flag body bytes — run-#12 finding 12); pass `full: true` for the whole-body render. Recomputed from the .py on every call. Per section it also WRITES the content-addressed TRUSTED-DISPLAY render file (.hpc/renders/<audit_id>/<slug>.<view_sha12>.md) and returns its render_path — the artifact the T8 sign-off gate requires (and where the full per-section body lives).",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent notebook-draft-context --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "notebook_draft_context.input.json",
    "name": "notebook-draft-context",
    "output_schema": "notebook_draft_context.output.json",
    "python": "hpc_agent.ops.notebook.draft_context_op.notebook_draft_context",
    "side_effects": [],
    "summary": "Render the deterministic DRAFTING context for a notebook-audit template: the template's slugs + cell prose verbatim; each engine the template imports resolved to its defining file under source_roots (path:lineno, signature, first docstring line, module_sha - the same resolution notebook-lint uses, never executing an import); name-match Call sites across source_roots (path:lineno, count, capped with the cap disclosed); and an inventory of files + sha12 + size under input_roots and each inventory_roots entry (a file recorded in .hpc/data_manifest.json is cited, not re-hashed). Roots default from the audit's recorded config when audit_id is given. The projection LISTS, never nominates - no baseline config, no section ranking. Read-only: the only write is the disposable content-keyed cache in the standard cache home. The result includes markdown - the code-rendered projection the drafting agent reads and the skill relays VERBATIM.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent notebook-status --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "notebook_status.input.json",
    "name": "notebook-status",
    "output_schema": "notebook_status.output.json",
    "python": "hpc_agent.ops.notebook.status_op.notebook_status",
    "side_effects": [
      "file_write"
    ],
    "summary": "Report the per-section audit state of an audited source .py against its template inventory and audit_id journal. No SSH. Each required (template) section reduces to signed_current / auto_cleared / signed_stale / unsigned (drift-revoked by construction); `passed` is the graduation gate's whole-module predicate (every section current). Section shas are recomputed from the .py on every call. A TERMINAL verdict (passed, or failed via a drift-revoked sign-off) journals a relay-due marker the Stop hook discharges only when the verdict is actually relayed (deduplicated; non-terminal runs write nothing).",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent pack-status --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "pack_status.input.json",
    "name": "pack-status",
    "output_schema": "pack_status.output.json",
    "python": "hpc_agent.ops.pack.status_op.pack_status",
    "side_effects": [],
    "summary": "Report an experiment's domain-pack state: per opted-in pack, the current bind (or null), each caller-authored receipt slot's currency (current / failed / stale / missing), an advisory unfillable-requirement report (a slot the bound pack's manifest fills_slots omits), and dangling-reference findings (a missing/sha-drifted manifest, or a slot bound to an unbound pack). Read-only, no SSH; a query REPORTS a dangling reference as data, never raising — the loud refusals live in the mutate verbs and the gate. Not opted in → empty and silent. Recomputed on every call (no cache).",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent plan-throughput --cluster <cluster> --total-tasks <total_tasks> [--est-task-duration-s <est_task_duration_s>] [--cores-per-task <cores_per_task>] [--gpus-per-task <gpus_per_task>] [--interactive]",
    "idempotent": true,
    "input_schema": null,
    "name": "plan-throughput",
    "output_schema": null,
    "python": "hpc_agent.ops.submit.plan_throughput.plan_throughput",
    "side_effects": [],
    "summary": "Pack a task grid into batched submission waves. Pure-local: reads the cluster's constraints from clusters.yaml and returns the wave plan + wave_map for the per-run sidecar.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent poll-detached --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "poll_detached.input.json",
    "name": "poll-detached",
    "output_schema": "poll_detached.output.json",
    "python": "hpc_agent.ops.monitor.poll_detached.poll_detached",
    "side_effects": [],
    "summary": "Snapshot a detached worker's state right now (lease pid-liveness + journal status + block-terminal presence; local, no SSH, non-blocking). The instant, MCP-safe complement to wait-detached: reports running / exited_recorded / exited_unrecorded / no_lease.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent status [--experiment-dir <dir>] --run-id <run_id> [--min-rows <min_rows>]",
    "idempotent": true,
    "input_schema": null,
    "name": "poll-run-status",
    "output_schema": "status.output.json",
    "python": "hpc_agent.ops.monitor.status.record_status",
    "side_effects": [
      "ssh",
      "writes-journal"
    ],
    "summary": "Poll cluster status for a run_id; one-shot, returns snapshot.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent read-decisions --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "read_decisions.input.json",
    "name": "read-decisions",
    "output_schema": "read_decisions.output.json",
    "python": "hpc_agent.ops.decision.journal.read_decisions",
    "side_effects": [],
    "summary": "Read a run's or campaign's decision journal — the append-ordered y/nudge audit trail (design §2).",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent recall [--limit <limit>] [--include-runtime] [--include-generator-stats] [--root <root>] [--task-kind <task_kind>] [--operator <operator>] [--since <since>]",
    "idempotent": true,
    "input_schema": "recall.input.json",
    "name": "recall",
    "output_schema": "recall.output.json",
    "python": "hpc_agent.ops.memory.recall.recall_campaigns",
    "side_effects": [],
    "summary": "Query past interview.json files under --root. Returns recency-sorted campaign summaries (goal, task_kind, task_count, operator, materialized_at, cmd_sha) for use as context in the next interview.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent recommend-partition --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "recommend_partition.input.json",
    "name": "recommend-partition",
    "output_schema": "recommend_partition.output.json",
    "python": "hpc_agent.ops.submit.recommend_partition.recommend_partition",
    "side_effects": [],
    "summary": "Recommend a partition for a requested walltime against the cluster's partition list. Returns the partition name + a human-readable rationale (debug priority leverage, debug-overrun refusal, user-preference honoured). Call this before constructing the submit spec when partition choice isn't obvious.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent recoveries list",
    "idempotent": true,
    "input_schema": null,
    "name": "recoveries-list",
    "output_schema": null,
    "python": "hpc_agent.recovery.cli.recoveries_list",
    "side_effects": [],
    "summary": "List every failure ``kind`` known to the recovery registry. Reports both ported kinds (with menus) and the un-ported punch list, so a SKILL.md author can see what recoveries are available before referencing one.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent recoveries show --kind <kind> [--placeholders <placeholders>]",
    "idempotent": true,
    "input_schema": null,
    "name": "recoveries-show",
    "output_schema": null,
    "python": "hpc_agent.recovery.cli.recoveries_show",
    "side_effects": [],
    "summary": "Print the canonical recovery menu for one failure kind. Use this from SKILL.md prose to reference the menu by kind name instead of re-embedding the literal options.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent resolve-resources --cluster <cluster> [--experiment-dir <experiment_dir>] [--profile <profile>] [--cmd-sha <cmd_sha>] [--walltime-sec <walltime_sec>] [--gpu-type <gpu_type>] [--safety-mult <safety_mult>] [--partition <partition>] [--user-preferred-partition <user_preferred_partition>] [--mpi-pe <mpi_pe>] [--mpi-ranks <mpi_ranks>]",
    "idempotent": true,
    "input_schema": "resolve_resources.input.json",
    "name": "resolve-resources",
    "output_schema": "resolve_resources.output.json",
    "python": "hpc_agent.ops.resolve_resources.resolve_resources",
    "side_effects": [],
    "summary": "Resolve hpc-submit Step 6 resources in one call: walltime_sec (caller, else read-runtime-prior p95 × safety_mult, else cold-start null), gpu_type (caller, else clusters.<cluster>.gpu_types[0]), partition (caller, else recommend-partition), and mpi_pe (caller, else recommend-pe from parallel_environments when mpi_ranks is set). A missing/erroring read-runtime-prior is cold-start, not an error.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent run-story --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "run_story.input.json",
    "name": "run-story",
    "output_schema": "run_story.output.json",
    "python": "hpc_agent.ops.run_story.run_story",
    "side_effects": [],
    "summary": "Render a run's complete journal trail as ONE deterministic, ordered, attributed timeline — decision journal, briefs, block terminals, journal-record stamps + verdict history, scope journals + look ledgers, and the notebook attestation journal — fingerprinted by story_sha. Read-only, no SSH. --include-lineage widens the read to the run's whole supersession chain. since_ts / limit window honestly: the result carries total_events / omitted_count and the markdown surfaces the omission, so a window never masquerades as the whole story. It is a PURE projection — identity, ordering, and counting over opaque records; it never interprets what any record means.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent scaffold-spec [--experiment-dir <dir>] --verb <verb> [--cluster <cluster>] [--run-name <run_name>] [--from-context]",
    "idempotent": true,
    "input_schema": null,
    "name": "scaffold-spec",
    "output_schema": "scaffold_spec.output.json",
    "python": "hpc_agent.ops.scaffold_spec.scaffold_spec",
    "side_effects": [],
    "summary": "Emit a populated, schema-valid --spec skeleton for another verb (build-submit-spec / resolve-submit-inputs / validate-campaign / campaign-run / interview), pulling cluster / run_id / context values from clusters.yaml, compute-run-id, and load-context so the agent stops divining the schema one spec_invalid at a time (#287). Read the returned spec, fill its unresolved_fields, then invoke the target verb.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent scope-status --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "scope_status.input.json",
    "name": "scope-status",
    "output_schema": "scope_status.output.json",
    "python": "hpc_agent.ops.decision.journal.scope_lock.scope_status",
    "side_effects": [],
    "summary": "Report the lock + look state of one caller-tagged scope, or every scope under .hpc/scopes/ when the tag is omitted. Pure read: {scope: {locked, looks: {prior_looks, distinct_lineages}, lock_history_len}}. Look counts are IDENTITY counts — no metric is ever consulted.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent stray-sweep --spec <path> --ssh-target <ssh_target> [--reap] [--max-age-sec <max_age_sec>] [--warn-threshold <warn_threshold>]",
    "idempotent": true,
    "input_schema": "stray_sweep.input.json",
    "name": "stray-sweep",
    "output_schema": "stray_sweep.output.json",
    "python": "hpc_agent.ops.recover.stray_sweep.stray_sweep",
    "side_effects": [
      "ssh"
    ],
    "summary": "Login-node process-hygiene probe (run-12 finding 20). One fork-minimal `ps -u $USER` over ssh: counts total processes, identifies MARKED framework processes (HPC_AGENT_OP) older than the max legitimate deadline as strays, and — only with --reap — kills exactly those PIDs (never an unmarked process). Surfaces the stray count the quota-wedge incident lacked.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent suggest-setup-action [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": null,
    "name": "suggest-setup-action",
    "output_schema": "suggest_setup_action.output.json",
    "python": "hpc_agent.cli.setup_actions.suggest_setup_action",
    "side_effects": [],
    "summary": "Run the /submit-hpc Setup priority cascade and recommend {action: monitor|reuse|interview|fresh, run_id, candidates}. Replaces the priority-list-walking prose at Step 0.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent summarize-submit-plan --spec <path>",
    "idempotent": true,
    "input_schema": null,
    "name": "summarize-submit-plan",
    "output_schema": "summarize_submit_plan.output.json",
    "python": "hpc_agent.ops.submit.plan_summary.summarize_submit_plan",
    "side_effects": [],
    "summary": "Render the canonical pre-submit confirmation summary for a submit_flow.input.json spec. Returns {headline, body, confirm_prompt} the slash command prints verbatim. Eliminates per-submit wording drift.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent trace [--experiment-dir <dir>] [--campaign-id <campaign_id>] [--run-id <run_id>] [--format <trace_format>]",
    "idempotent": true,
    "input_schema": null,
    "name": "trace",
    "output_schema": "trace.output.json",
    "python": "hpc_agent.ops.trace.trace",
    "side_effects": [],
    "summary": "Assemble a derived execution DAG for a campaign (--campaign-id) or a single run's lineage (--run-id) by joining the per-run journal records, the per-run sidecars, and the signable provenance manifest. Read-only, no SSH. --format dag (default) emits run + wave nodes and member/derived-from/contains edges; --format flat emits the run list only; --format dot adds a Graphviz `dot` string.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent trace-diff --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "trace_diff.input.json",
    "name": "trace-diff",
    "output_schema": "trace_diff.output.json",
    "python": "hpc_agent.ops.trace_diff_op.trace_diff",
    "side_effects": [],
    "summary": "Overlay TWO traces from the local store and report, per stage and per atom, where their measurements diverge — dispatched through the ONE semantics registry (exact / set-delta / tolerance / exact-per-key / equality-chain / exact-endpoints). Highlights the FIRST diverging (stage, atom). Read-only, no SSH. Differences are FACTS (`row_count rows 100 → 90`), never verdicts; absence is disclosed. Tolerance is caller-owned (absent → exact).",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent trace-render --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "trace_render.input.json",
    "name": "trace-render",
    "output_schema": "trace_render.output.json",
    "python": "hpc_agent.ops.trace_render_op.trace_render",
    "side_effects": [],
    "summary": "Render one task's data trace as FOUR deterministic markdown views — row waterfall (with conservation flags), label-chain line, feature lineage (col_set deltas + column births), and the sketch table (value_sketch / null_count per column per stage) — under a self-describing run/config header. Read-only, no SSH. Selectors (exactly one): DIRECT {scope_kind, scope_id, task?}, or the REFERENCE lookup {cmd_sha} / {profile} (latest-by via a sidecar join). Absence is honest: 'no trace recorded for this scope' rides present/skipped, never an error. The render carries NO verdict vocabulary — the trace SHOWS, the scientist concludes; relay it verbatim.",
    "verb": "query"
  },
  {
    "agent_facing": false,
    "cli": "hpc-agent verify-aggregation-complete [--experiment-dir <dir>] --run-id <run_id> [--combiner-dir <combiner_dir_local>] [--results-dir <results_dir_local>]",
    "idempotent": true,
    "input_schema": null,
    "name": "verify-aggregation-complete",
    "output_schema": "verify_aggregation_complete.output.json",
    "python": "hpc_agent.ops.aggregate.invariants.verify_aggregation_complete",
    "side_effects": [],
    "summary": "Walk the run sidecar's wave_map + the locally-pulled _combiner/ dir; report all_waves_combined / all_tasks_present / provenance_present invariants. Returns ok plus the missing / unexpected lists.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent verify-registration --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "verify_registration.input.json",
    "name": "verify-registration",
    "output_schema": "verify_registration.output.json",
    "python": "hpc_agent.ops.registration.verify_op.verify_registration",
    "side_effects": [],
    "summary": "Verify a registration and REPORT its status (current | stale | revoked | superseded | absent) with per-leg detail — the live dossier signature re-gathered vs the sha it bound, the template raw-bytes drift, every prerequisite re-checked through its kind's one checker, and field completeness by counting. Takes exactly one of registration_id / run_id. A REPORTER: it never blocks and never raises on a stale/revoked/missing subject — the deploy refusal is wired caller-side against status. The brief's canonical-JSON sha is the view_sha a sign-off must carry. Read-only, no SSH.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent verify-relay --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "verify_relay.input.json",
    "name": "verify-relay",
    "output_schema": "verify_relay.output.json",
    "python": "hpc_agent.ops.decision.journal.verify_relay.verify_relay",
    "side_effects": [],
    "summary": "Audit an agent's draft relay text against a run's durable records (decision journal, sidecar, RunRecord, briefs). Deterministic claim-extraction — numbers, run/job ids, state words — diffed against the record. Returns a verdict; never blocks (conduct rule 10).",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent verify-reproduction --spec <path> [--experiment-dir <dir>]",
    "idempotent": false,
    "input_schema": "verify_reproduction.input.json",
    "name": "verify-reproduction",
    "output_schema": "verify_reproduction.output.json",
    "python": "hpc_agent.ops.verify_reproduction.verify_reproduction",
    "side_effects": [
      "filesystem"
    ],
    "summary": "Compare a reproduction run's reduced metrics against the original it names (sidecar `reproduces` link), reduce the experiment's determinism envelope, tier the verdict (auto_cleared / needs_verdict / mismatch), append a durable receipt, and append the comparison back as a fingerprint sample. A mismatch/incomparable is a FINDING (needs_decision), never an error.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent verify-submitted [--experiment-dir <dir>] --run-id <run_id>",
    "idempotent": true,
    "input_schema": null,
    "name": "verify-submitted",
    "output_schema": "verify_submitted.output.json",
    "python": "hpc_agent.ops.verify_submitted.verify_submitted",
    "side_effects": [
      "ssh"
    ],
    "summary": "Post-submit health check: map each of a run's submitted job ids to its scheduler state and flag error (SGE Eqw) / held jobs. Returns {ok, states, healthy, error, held, missing, details}. Use it instead of raw `ssh qstat` to confirm an array landed cleanly.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent wait-detached --spec <path>",
    "idempotent": true,
    "input_schema": "wait_detached.input.json",
    "name": "wait-detached",
    "output_schema": "wait_detached.output.json",
    "python": "hpc_agent.ops.monitor.wait_detached.wait_detached",
    "side_effects": [],
    "summary": "Block until the detached worker for a run exits (lease-pid wait; local, no SSH). Launch via the harness's backgrounding so the harness notifies you exactly once, when the brief is ready — never timed /loop polling.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent walk-submit-ambiguities --spec <path>",
    "idempotent": true,
    "input_schema": "walk_submit_ambiguities.input.json",
    "name": "walk-submit-ambiguities",
    "output_schema": "walk_submit_ambiguities.output.json",
    "python": "hpc_agent.ops.walk_submit_ambiguities.walk_submit_ambiguities",
    "side_effects": [],
    "summary": "Walk hpc-submit Steps 2-6 as deterministic branches: resolve cluster / entry_point / data_axis / homogeneous_axes / uncovered_param, reuse resolve-resources for walltime/gpu/partition/mpi_pe, and accumulate Ambiguity entries (safe_default ONLY for auto-resolvable fields; goal/task_generator get none). Returns the needs_resolution envelope {resolved, ambiguities}.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent worker-log-digest --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "worker_log_digest.input.json",
    "name": "worker-log-digest",
    "output_schema": "worker_log_digest.output.json",
    "python": "hpc_agent.ops.worker_log_digest.worker_log_digest",
    "side_effects": [],
    "summary": "Code-rendered digest of a LOCAL worker log (no SSH). Counts lines carrying each known engine marker ([throttle], [dispatch] FATAL/FAILED/ERROR/WARN), reports the total line count, and echoes the last tail_lines lines verbatim in a fenced block. Fails open on a missing/unreadable file (a clear diagnostic, never a traceback). Mechanizes the premortem's manual raw-log scan (run-#10 G2). The `render` field is relayed verbatim.",
    "verb": "query"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent axes-init [--experiment-dir <dir>] [--axes <axes>] [--homogeneous-axes <homogeneous_axes>] [--force]",
    "idempotent": true,
    "input_schema": null,
    "name": "axes-init",
    "output_schema": null,
    "python": "hpc_agent.incorporation.axes_init.axes_init",
    "side_effects": [
      "writes-sidecar"
    ],
    "summary": "Write <experiment>/.hpc/axes.yaml with per-axis homogeneity hints used by the cold-start axis_picker. The agent typically calls this once per repo at deploy time after introspecting tasks.py.",
    "verb": "scaffold"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent build-executor --name <name> [--output-dir <output_dir>] [--type <type>] [--force]",
    "idempotent": false,
    "input_schema": null,
    "name": "build-executor",
    "output_schema": "build_executor.output.json",
    "python": "hpc_agent.incorporation.build.executor.build_executor",
    "side_effects": [
      "writes-file"
    ],
    "summary": "Scaffold a new executor from a starter template.",
    "verb": "scaffold"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent build-submit-spec --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "build_submit_spec.input.json",
    "name": "build-submit-spec",
    "output_schema": null,
    "python": "hpc_agent.incorporation.build.submit_spec.build_submit_spec",
    "side_effects": [],
    "summary": "Assemble + validate a submit_flow.input.json spec from resolved interview values (profile/cluster/ssh_target/.../cmd_sha/total_tasks). Emits the spec on stdout. Slash commands pipe the output straight into 'submit-flow --spec'.",
    "verb": "scaffold"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent build-tasks-py [--experiment-dir <dir>] --spec <spec> [--force]",
    "idempotent": true,
    "input_schema": "build_tasks_py.input.json",
    "name": "build-tasks-py",
    "output_schema": null,
    "python": "hpc_agent.incorporation.build.tasks_py.build_tasks_py",
    "side_effects": [
      "writes-sidecar"
    ],
    "summary": "Scaffold <experiment>/.hpc/tasks.py from the canonical cartesian-product template (Pattern 1 of tasks_example.py). Spec file is {axes: [{name, values}], flags_by_executor: {module_path: [{name, type, default?}]}}. Refuses to overwrite an existing tasks.py without --force so hand-edited Pattern 2/3 conversions survive.",
    "verb": "scaffold"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent build-template [--repo-dir <repo_dir>] [--force] [--shape <shape>]",
    "idempotent": true,
    "input_schema": null,
    "name": "build-template",
    "output_schema": null,
    "python": "hpc_agent.incorporation.build.template.build_template",
    "side_effects": [
      "writes-file"
    ],
    "summary": "Inject the experiment-template scaffold into a repo.",
    "verb": "scaffold"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent campaign acknowledge-budget [--experiment-dir <dir>] --campaign-id <campaign_id> [--note <note>] [--max-jobs <max_jobs>] [--max-tasks <max_tasks>] [--max-walltime-sec <max_walltime_sec>] [--max-core-hours <max_core_hours>]",
    "idempotent": true,
    "input_schema": null,
    "name": "campaign-acknowledge-budget",
    "output_schema": "campaign.output.json",
    "python": "hpc_agent.meta.campaign.atoms.acknowledge_budget.campaign_acknowledge_budget",
    "side_effects": [
      "writes-sidecar"
    ],
    "summary": "Acknowledge a campaign budget halt so the loop may continue. Snapshots current spend; optionally raises caps (written through to the manifest).",
    "verb": "scaffold"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent campaign init [--experiment-dir <dir>] --campaign-id <campaign_id> [--goal <goal>] [--max-iters <max_iters>] [--metric <metric>] [--target <target>] [--direction <direction>] [--plateau-window <plateau_window>] [--plateau-tolerance <plateau_tolerance>] [--plateau-mode <plateau_mode>] [--max-jobs <max_jobs>] [--max-tasks <max_tasks>] [--max-walltime-sec <max_walltime_sec>] [--max-core-hours <max_core_hours>] [--circuit-breaker-failures <circuit_breaker_failures>] [--max-task-resubmits <max_task_resubmits>] [--on-anomaly <on_anomaly>] [--strategy-name <strategy_name>] [--strategy-params-json <strategy_params_json>] [--async-refill] [--max-in-flight <max_in_flight>]",
    "idempotent": true,
    "input_schema": null,
    "name": "campaign-init",
    "output_schema": "campaign.output.json",
    "python": "hpc_agent.meta.campaign.atoms.init.campaign_init",
    "side_effects": [
      "writes-sidecar"
    ],
    "summary": "Write the campaign manifest from CLI args.",
    "verb": "scaffold"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent classify-axis --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "classify_axis.input.json",
    "name": "classify-axis",
    "output_schema": null,
    "python": "hpc_agent.incorporation.classify_axis.classify_axis",
    "side_effects": [
      "writes-sidecar"
    ],
    "summary": "Record a @register_run function's classified DataAxis (independent / associative / bounded_halo / sequential) into <experiment>/.hpc/axes.yaml's `executors` block. Spec file is {run_name, run_signature_sha, data_axis: {kind, halo?, monoid?}, classified_by?}. The agent classifies; this primitive only records.",
    "verb": "scaffold"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent classify-axis-auto [--spec <path>] [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "classify_axis_auto.input.json",
    "name": "classify-axis-auto",
    "output_schema": "classify_axis_auto.output.json",
    "python": "hpc_agent.incorporation.classify_axis_auto.classify_axis_auto",
    "side_effects": [
      "writes-sidecar"
    ],
    "summary": "Composite head of hpc-classify-axis: preflight (discover-runs + cache-check + recall) → classify-axis-easy → classify-axis recorder, in ONE call. Resolves the single @register_run, then branches: caller data_axis (interview) / cache hit (reuse) / recall structural match / a confident AST match (agent) all record into .hpc/axes.yaml; an unclassifiable/function_not_found matcher verdict records NOTHING and returns {needs_llm_tree: true, source_path, run_name, run_signature_sha, evidence, tried} so the caller walks the LLM decision tree. The LLM makes one call and only works the genuine long tail.",
    "verb": "scaffold"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent export-package [--experiment-dir <dir>] [--force]",
    "idempotent": true,
    "input_schema": "export_package.input.json",
    "name": "export-package",
    "output_schema": null,
    "python": "hpc_agent.incorporation.export_package.export_package",
    "side_effects": [
      "writes-sidecar"
    ],
    "summary": "Build <experiment>/src/ from the notebooks under notebooks/{pipeline,executors,scripts}/ — jupytext percent-format .py (the native format) plus .ipynb for back-compat. Convention-driven: the output module name and the exporter (strict-AST for @register_run executors, # export-marker for pipeline libraries) are both derived. Content-hash caches against .hpc/.build-cache.json — a second run with no notebook edits is all cache hits.",
    "verb": "scaffold"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent install-commands [--dry-run] [--claude-dir <claude_dir>]",
    "idempotent": true,
    "input_schema": null,
    "name": "install-commands",
    "output_schema": null,
    "python": "hpc_agent.cli.setup.install_commands",
    "side_effects": [
      "filesystem"
    ],
    "summary": "Copy the bundled slash commands and skills into ~/.claude/commands/ and ~/.claude/skills/. The pip-install entry point — run once after `pip install hpc-agent` to wire the agent assets into Claude Code. Idempotent (overwrites in place). Pass --claude-dir to target a non-default config dir.",
    "verb": "scaffold"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent interview --spec <path> --campaign-dir <campaign_dir>",
    "idempotent": true,
    "input_schema": "interview.input.json",
    "name": "interview",
    "output_schema": "interview.output.json",
    "python": "hpc_agent.ops.memory.interview.record_interview",
    "side_effects": [
      "file_write"
    ],
    "summary": "Validate an agent-written tasks.py against the structured intent from an interview, then persist intent + cmd_sha + dry-resolve preview to <campaign-dir>/interview.json.",
    "verb": "scaffold"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent prepare-followup-specs --experiment-dir <experiment_dir> --run-id <run_id> [--cmd-sha <cmd_sha>] [--profile <profile>]",
    "idempotent": true,
    "input_schema": "prepare_followup_specs.input.json",
    "name": "prepare-followup-specs",
    "output_schema": "prepare_followup_specs.output.json",
    "python": "hpc_agent.ops.prepare_followup_specs.prepare_followup_specs",
    "side_effects": [
      "writes-followup-specs"
    ],
    "summary": "Pre-stage monitor_spec.json + aggregate_spec.json into the experiment dir from submit-time state, so a later /monitor-hpc and /aggregate-hpc can skip the operator interview. Each spec carries run_id + a cmd_sha staleness gate + a null sentinel for the operator-choice field (monitor wait_terminal; aggregate allow_partial/stage). The consuming skill validates cmd_sha against the journal before honoring the pre-staged spec (#278).",
    "verb": "scaffold"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent scaffold-strategy [--shape <shape>] [--name <name>] [--arms <arms>] [--output-dir <output_dir>] [--force] [--async-refill]",
    "idempotent": true,
    "input_schema": null,
    "name": "scaffold-strategy",
    "output_schema": null,
    "python": "hpc_agent.incorporation.scaffold_strategy.scaffold_strategy",
    "side_effects": [
      "writes-file"
    ],
    "summary": "Scaffold a campaign strategy (optuna|pbt ask/tell) or a fixed grid into a repo.",
    "verb": "scaffold"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent setup [--dry-run] [--claude-dir <claude_dir>] [--cluster <cluster>] [--experiment-dir <experiment_dir>] [--install-cron]",
    "idempotent": true,
    "input_schema": null,
    "name": "setup",
    "output_schema": null,
    "python": "hpc_agent.cli.setup.setup",
    "side_effects": [
      "filesystem",
      "ssh"
    ],
    "summary": "One-shot setup: copy the bundled slash commands and skills into ~/.claude/. Run this once after `pip install hpc-agent`. Idempotent — safe to re-run.",
    "verb": "scaffold"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent submit --spec <path> [--experiment-dir <dir>] [--dry-run]",
    "idempotent": true,
    "input_schema": "submit.input.json",
    "name": "submit-spec",
    "output_schema": "submit.output.json",
    "python": "hpc_agent.ops.submit.runner.submit_and_record",
    "side_effects": [
      "scheduler-submit",
      "writes-journal"
    ],
    "summary": "Record a submission in the journal. Idempotent on run_id: the bundled atomic-ops layer dedups so a retry on transient network errors does not double-submit.",
    "verb": "submit"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent aggregate-preflight --experiment-dir <experiment_dir> [--reconcile-scheduler <reconcile_scheduler>]",
    "idempotent": true,
    "input_schema": "aggregate_preflight.input.json",
    "name": "aggregate-preflight",
    "output_schema": "aggregate_preflight.output.json",
    "python": "hpc_agent.ops.aggregate_preflight.aggregate_preflight",
    "side_effects": [],
    "summary": "Composite preflight at the top of aggregate: install-commands ∥ load-context fanned concurrently, then (when the journal says 'monitor' and --reconcile-scheduler is supplied) reconcile.",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent preflight [--spec <path>] [--cluster <cluster>]",
    "idempotent": true,
    "input_schema": null,
    "name": "check-preflight",
    "output_schema": "preflight.output.json",
    "python": "hpc_agent.ops.preflight.check.check_preflight",
    "side_effects": [],
    "summary": "Health check: SSH agent, ssh/rsync on PATH, clusters.yaml parses; with --cluster also runs a TCP :22 probe + an actual ssh round-trip; with --spec <built submit-flow spec> also runs the runtime (uv) probe submit-flow would, so a uv-on-a-uv-less-cluster spec is caught here, before qsub (#275).",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent check-task-generator-mismatch --caller-task-generator <caller_task_generator> [--cached-task-generator <cached_task_generator>]",
    "idempotent": true,
    "input_schema": "check_task_generator_mismatch.input.json",
    "name": "check-task-generator-mismatch",
    "output_schema": "check_task_generator_mismatch.output.json",
    "python": "hpc_agent.ops.check_task_generator_mismatch.check_task_generator_mismatch",
    "side_effects": [],
    "summary": "Canonical-JSON compare a caller-supplied task_generator against the cached/derived one (hpc-submit Step 3). Returns match=true when their canonical content is identical, else match=false with both shapes + their sha256 so the caller can branch on on_task_generator_mismatch (fail / refresh).",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent classify-axis-preflight --experiment-dir <experiment_dir> [--run-name <run_name>] [--run-signature-sha <run_signature_sha>] [--root <root>] [--task-kind <task_kind>] [--data-axis-supplied]",
    "idempotent": true,
    "input_schema": "classify_axis_preflight.input.json",
    "name": "classify-axis-preflight",
    "output_schema": "classify_axis_preflight.output.json",
    "python": "hpc_agent.ops.classify_axis_preflight.classify_axis_preflight",
    "side_effects": [],
    "summary": "Composite preflight at the top of classify-axis: discover-runs + cache-check (axes.yaml reuse) + (when no cache hit and no caller-supplied data_axis) recall, sequenced, returned as one envelope.",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": null,
    "idempotent": true,
    "input_schema": "dry_run_local.input.json",
    "name": "dry-run-local",
    "output_schema": "dry_run_local.output.json",
    "python": "hpc_agent.ops.validate.dry_run_local.dry_run_local",
    "side_effects": [],
    "summary": "",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent notebook-lint --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "notebook_lint.input.json",
    "name": "notebook-lint",
    "output_schema": "notebook_lint.output.json",
    "python": "hpc_agent.ops.notebook.lint.notebook_lint",
    "side_effects": [],
    "summary": "Lint a notebook-audit source .py against its template and caller-declared, opaque path/import roots. Four read-only checks: structural completeness (template marker slugs as an order-preserving subsequence of the source), executes-live (path-shaped string literals exist under input_roots; a literal under a declared output_root is a WRITE target — reported in declared_outputs, never flagged; computed paths recorded as unverifiable), linked_sources (imports resolving under source_roots reported with their module_sha), and template_import_shadowed (a source section defining or rebinding a name the template imports — the template's imports are the declared engines; a verbatim re-import is clean). Findings are REPORTED, never raised — the graduation gate refuses, the lint reports.",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent prepare-phase2-spec --spec <path>",
    "idempotent": true,
    "input_schema": null,
    "name": "prepare-phase2-spec",
    "output_schema": "prepare_phase2_spec.output.json",
    "python": "hpc_agent.ops.prepare_phase2_spec.prepare_phase2_spec",
    "side_effects": [],
    "summary": "Derive the Phase-2 main-array submit-flow spec from a Phase-1 two-phase-canary spec by flipping canary=false, canary_only=false, validated locally against SubmitFlowSpec. Eliminates the spec-rebuild round-trip between verify-canary and the Phase-2 submit.",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent smoke-test-executor --module-path <module_path> [--output-file <output_file>]",
    "idempotent": true,
    "input_schema": "smoke_test_executor.input.json",
    "name": "smoke-test-executor",
    "output_schema": "smoke_test_executor.output.json",
    "python": "hpc_agent.ops.smoke_test_executor.smoke_test_executor",
    "side_effects": [
      "filesystem",
      "runs"
    ],
    "summary": "Smoke-test a scaffolded executor: import its module from a file path and call compute(Namespace(output_file=...)) in a subprocess. Returns {exit_code, stdout_tail, stderr_tail}; non-zero exit_code means fix-then-retry.",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent status-preflight --experiment-dir <experiment_dir>",
    "idempotent": true,
    "input_schema": "status_preflight.input.json",
    "name": "status-preflight",
    "output_schema": "status_preflight.output.json",
    "python": "hpc_agent.ops.status_preflight.status_preflight",
    "side_effects": [],
    "summary": "Composite preflight before status: install-commands ∥ load-context, fanned concurrently, returned as one envelope.",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent submit-preflight --experiment-dir <experiment_dir> [--cluster <cluster>] [--profile <profile>] [--cmd-sha <cmd_sha>] [--walltime-sec <walltime_sec>] [--gpu-type <gpu_type>] [--safety-mult <safety_mult>] [--partition <partition>] [--user-preferred-partition <user_preferred_partition>]",
    "idempotent": true,
    "input_schema": "submit_preflight.input.json",
    "name": "submit-preflight",
    "output_schema": "submit_preflight.output.json",
    "python": "hpc_agent.ops.submit_preflight.submit_preflight",
    "side_effects": [],
    "summary": "Composite preflight at the top of submit: install-commands + load-context, then (when --cluster is supplied) check-preflight and resolve-resources fanned out concurrently.",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": null,
    "idempotent": true,
    "input_schema": "validate_executor_signatures.input.json",
    "name": "validate-executor-signatures",
    "output_schema": "validate_executor_signatures.output.json",
    "python": "hpc_agent.ops.validate.executor_signatures.validate_executor_signatures",
    "side_effects": [],
    "summary": "",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": null,
    "idempotent": true,
    "input_schema": "validate_input_dataset.input.json",
    "name": "validate-input-dataset",
    "output_schema": "validate_input_dataset.output.json",
    "python": "hpc_agent.ops.validate.input_dataset.validate_input_dataset",
    "side_effects": [],
    "summary": "",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": null,
    "idempotent": true,
    "input_schema": "validate_parents_ready.input.json",
    "name": "validate-parents-ready",
    "output_schema": "validate_parents_ready.output.json",
    "python": "hpc_agent.ops.validate.parents_ready.validate_parents_ready",
    "side_effects": [],
    "summary": "",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": null,
    "idempotent": true,
    "input_schema": null,
    "name": "validate-scaffold-staleness",
    "output_schema": "validate_scaffold_staleness.output.json",
    "python": "hpc_agent.ops.validate.scaffold_staleness.validate_scaffold_staleness",
    "side_effects": [],
    "summary": "",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": null,
    "idempotent": true,
    "input_schema": "validate_self_qos_limit.input.json",
    "name": "validate-self-qos-limit",
    "output_schema": "validate_self_qos_limit.output.json",
    "python": "hpc_agent.ops.validate.self_qos_limit.validate_self_qos_limit",
    "side_effects": [],
    "summary": "",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": null,
    "idempotent": true,
    "input_schema": "validate_stochastic_marker.input.json",
    "name": "validate-stochastic-marker",
    "output_schema": "validate_stochastic_marker.output.json",
    "python": "hpc_agent.ops.validate.stochastic_marker.validate_stochastic_marker",
    "side_effects": [],
    "summary": "",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": null,
    "idempotent": true,
    "input_schema": "validate_walltime_against_history.input.json",
    "name": "validate-walltime-against-history",
    "output_schema": "validate_walltime_against_history.output.json",
    "python": "hpc_agent.ops.validate.walltime_against_history.validate_walltime_against_history",
    "side_effects": [],
    "summary": "",
    "verb": "validate"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent aggregate-check --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "aggregate_check.input.json",
    "name": "aggregate-check",
    "output_schema": "aggregate_block.output.json",
    "python": "hpc_agent.ops.aggregate_blocks.aggregate_check",
    "side_effects": [
      "ssh"
    ],
    "summary": "Aggregate block CHECK (readiness + integrity): aggregate-preflight + the terminal-status readiness gate + the verify-aggregation-complete integrity gate. Brief = which waves combined, what's missing, integrity issues found — each surfaced as a decision point with a conservative recommendation, NEVER auto-masked. Terminates → y/nudge when a gate fails or an issue exists; else 'ready' → run.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent aggregate-flow [--spec <path>] [--experiment-dir <dir>] [--dry-run] [--run-id <run_id>]",
    "idempotent": true,
    "input_schema": "aggregate_flow.input.json",
    "name": "aggregate-flow",
    "output_schema": "aggregate_flow.output.json",
    "python": "hpc_agent.ops.aggregate_flow.aggregate_flow",
    "side_effects": [
      "ssh",
      "sync-pull",
      "writes-journal"
    ],
    "summary": "Workflow atom: ensure all waves combined on the cluster, rsync the _combiner/ partials locally, reduce_partials over them, optionally pull per-task summaries. Third atom in the submit-flow → monitor-flow → aggregate-flow campaign chain.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent aggregate-run --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "aggregate_run.input.json",
    "name": "aggregate-run",
    "output_schema": "aggregate_block.output.json",
    "python": "hpc_agent.ops.aggregate_blocks.aggregate_run",
    "side_effects": [
      "ssh",
      "sync-pull"
    ],
    "summary": "Aggregate block RUN (combine + reduce + extract): the deterministic aggregate-flow pipeline → a code-extracted results table + error-sweep summary + an EMPTY proposed_interpretations slot the LLM fills at the terminator. Owns the terminal-or-explicitly-partial invariant via aggregate-flow's gate (never assumes aggregate-check ran). Terminates → y/nudge (harvested / harvest_partial).",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent block-drive --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "block_drive.input.json",
    "name": "block-drive",
    "output_schema": "block_drive.output.json",
    "python": "hpc_agent.ops.block_drive_op.block_drive",
    "side_effects": [
      "spawns-subprocess",
      "writes-journal"
    ],
    "summary": "block-drive (wave 4): one stateless resumable tick that DRIVES a block chain. Chains the deterministic spans in code (S1-resolve → decision; S2-canary → decision; …) and, at a human decision point, writes {brief, pending marker, resume cursor} to durable state and EXITS. On a resume it consumes the approved 'resolved' spec (never a nudge string) and routes by identity + ownership (§4): advance / rerun / advance_carrying. The LLM no longer executes the transition.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent campaign-complete --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "campaign_complete.input.json",
    "name": "campaign-complete",
    "output_schema": "campaign_block.output.json",
    "python": "hpc_agent.meta.campaign.blocks.campaign_complete",
    "side_effects": [],
    "summary": "Campaign block (end): the completion brief — spend vs budget, iterations, stop reason, and a code-extracted per-iteration outcome table — plus an EMPTY proposed_interpretations slot the LLM fills at the y/nudge boundary (code extracts outcomes; the human concludes). Terminates → y/nudge.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent campaign-greenlight --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "campaign_greenlight.input.json",
    "name": "campaign-greenlight",
    "output_schema": "campaign_block.output.json",
    "python": "hpc_agent.meta.campaign.blocks.campaign_greenlight",
    "side_effects": [
      "writes-campaign-state"
    ],
    "summary": "Campaign block (start): digest the greenlit-once campaign spec (goal / budget / strategy / stop_criteria / anomaly_policy / async_refill) into a brief for y/nudge. An un-greenlit manifest digests to needs_greenlight (nothing stamped); --spec confirm=true (the post-`y` re-invocation) stamps mark_greenlit + journals the decision; an already-greenlit manifest is an idempotent re-read.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent campaign-refill --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "campaign_refill.input.json",
    "name": "campaign-refill",
    "output_schema": "campaign_refill.output.json",
    "python": "hpc_agent.ops.campaign_refill.campaign_refill",
    "side_effects": [
      "scheduler-submit",
      "writes-campaign-state"
    ],
    "summary": "RFC #362 refill actor: top a greenlit async campaign's pool back up this tick. Calls campaign-advance authoritatively; if it decides refill, resolves + submits refill_count iterations SEQUENTIALLY (each a detached campaign-run). Refuses an un-greenlit campaign (greenlight is the standing consent). Stages: refilled / no_refill_needed / refill_blocked. No new state file, no cursor — partial ticks self-correct via next tick's shrunk refill_count.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent campaign-run --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "campaign_run.input.json",
    "name": "campaign-run",
    "output_schema": "campaign_run.output.json",
    "python": "hpc_agent.ops.campaign_run.campaign_run",
    "side_effects": [
      "scheduler-submit",
      "ssh",
      "writes-aggregate-output"
    ],
    "summary": "One campaign iteration's spine in one call: submit-pipeline → status-pipeline → aggregate-flow. Reports one typed stage_reached outcome; sets needs_decision only on the failure / budget stages. Does NOT advance the campaign cursor — that stays a driver judgement.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent campaign-watch --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "campaign_watch.input.json",
    "name": "campaign-watch",
    "output_schema": "campaign_block.output.json",
    "python": "hpc_agent.meta.campaign.blocks.campaign_watch",
    "side_effects": [],
    "summary": "Campaign block (async execution surface): a READ-ONLY digest of the running campaign for a health / anomaly brief. Composes campaign-advance's evidence — it OBSERVES, never runs a tick (ticks self-chain via the driver). Terminators: watching_healthy (no boundary) / watching_refill (free pool slot → hand off to campaign-refill, no boundary) / watching_anomaly (loud-fail or budget halt → y/nudge, surfaces the anomaly_brief) / watching_complete (stop criterion fired → hand off to campaign-complete).",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent migrate-remainder --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "migrate_remainder.input.json",
    "name": "migrate-remainder",
    "output_schema": "migrate_remainder.output.json",
    "python": "hpc_agent.ops.migrate.migrate_remainder.migrate_remainder",
    "side_effects": [
      "ssh",
      "writes-derived-run"
    ],
    "summary": "Move a run's UNDONE tasks to another cluster as ONE gated verb (USER DIRECTIVE 2026-07-16). Censuses the source's per-task done-set, mints a DERIVED enumerated run over exactly the undone cells (per-run-scoped tasks.py, parents=[source], cell-ownership map), estimates the footprint over the undone count from the source-observed canary runtime, and returns a persisted migration brief with next_block=submit-s2. Returns in SECONDS: actuates NOTHING — the human's y greenlights submit-s2 to stage & canary the DERIVED run, and the source remainder is killed ONLY after that canary is verified GREEN (inverts retarget-run's supersede-first order — a remainder migration must not sacrifice partial progress). A same-cluster target / missing census / empty undone set is REFUSED.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent monitor-flow --spec <path> [--experiment-dir <dir>] [--dry-run]",
    "idempotent": true,
    "input_schema": "monitor_flow.input.json",
    "name": "monitor-flow",
    "output_schema": "monitor_flow.output.json",
    "python": "hpc_agent.ops.monitor_flow.monitor_flow",
    "side_effects": [
      "ssh",
      "writes-journal"
    ],
    "summary": "Workflow atom: poll a run to terminal lifecycle (or wall-clock budget); auto-combine waves as they finish; write the same .monitor.jsonl tick log /monitor-hpc writes. Pairs with submit-flow for the campaign loop composition. MVP does not auto-resubmit failed tasks.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent reproduce-run --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "reproduce_run.input.json",
    "name": "reproduce-run",
    "output_schema": "reproduce_run.output.json",
    "python": "hpc_agent.ops.reproduce_run.reproduce_run",
    "side_effects": [
      "writes-sidecar"
    ],
    "summary": "Mint a pinned-identity REPRODUCTION of a finished run (reproduction receipt, task T5): re-resolve it under a NEW run_name (<orig>-repro) against the SAME code + params + env, under a DISJOINT remote_path, recording a one-directional `reproduces` provenance link (the original is NEVER superseded). Refuses if the tree has DRIFTED since the original ran — param drift (cmd_sha) OR code drift (executor/tasks_py_sha), naming the evidence. Returns in seconds with next_block=submit-s2: the human's re-y greenlights S2, whose DETACHED worker owns the re-canary poll. Never runs the canary inline. A later verify-reproduction compares the two runs' metrics + writes the receipt.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent resolve-submit-inputs --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "resolve_submit_inputs.input.json",
    "name": "resolve-submit-inputs",
    "output_schema": "resolve_submit_inputs.output.json",
    "python": "hpc_agent.ops.resolve_submit_inputs.resolve_submit_inputs",
    "side_effects": [
      "writes-sidecar"
    ],
    "summary": "Deterministic submit input-resolution chain in one call: (build-tasks-py if tasks.py absent) → compute-run-id → find-prior-run → build-submit-spec. Returns a ready-to-submit context as one typed stage_reached outcome; sets needs_decision only on a live prior run or a needed scaffold interview.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent retarget-run --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "retarget_run.input.json",
    "name": "retarget-run",
    "output_schema": "retarget_run.output.json",
    "python": "hpc_agent.ops.retarget_run.retarget_run",
    "side_effects": [
      "ssh",
      "writes-sidecar"
    ],
    "summary": "Cluster-retarget recovery arm (proving-run-5 wave 5.2; detached hand-off since run #8): re-resolve a failed attempt under a NEW run_name + the NEW cluster (re-deriving job_env/ssh_target/backend/activation) and SUPERSEDE the old attempt (best-effort, NON-BLOCKING — an unreachable old cluster records pending_closure, never grinds on qdel). Returns in seconds with next_block=submit-s2: the human's re-y greenlights S2, whose DETACHED worker owns the re-canary poll. The patch must name a NEW cluster (a same-cluster delta → revise-resolved); a derived field is REFUSED. Does NOT bypass the append-decision / canary / S3 greenlight gates.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent revise-resolved --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "revise_resolved.input.json",
    "name": "revise-resolved",
    "output_schema": "revise_resolved.output.json",
    "python": "hpc_agent.ops.revise_resolved.revise_resolved",
    "side_effects": [
      "writes-sidecar"
    ],
    "summary": "Apply a resolver-owned FIELD DELTA {field: value} to a run's latest greenlit resolved and RE-RESOLVE, re-deriving job_env/executor/run_id/cmd_sha/sidecar from the delta (proving-run-5 wave 5.1). The patch may name only INPUT fields (cluster, goal, task_generator, walltime_sec, entry_point, …); a derived field (job_env, executor, ssh_target, …) is REFUSED. Returns the amended brief for the human's re-y — it does NOT bypass the append-decision gates.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent settle-run --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "settle_run.input.json",
    "name": "settle-run",
    "output_schema": "settle_run.output.json",
    "python": "hpc_agent.ops.settle_run.settle_run",
    "side_effects": [
      "ssh",
      "writes-journal"
    ],
    "summary": "Human-directed terminal settle (run-12 finding 25): given directed terminal evidence, journal it as a sign-off, set the terminal status, and run the SAME mark_run + harvest_on_terminal transition the probe path runs — no journal surgery. Refuses a non-terminal status or empty evidence. The harvest fires only on a status TRANSITION (an idempotent re-settle does not re-pull).",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent status-pipeline --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "status_pipeline.input.json",
    "name": "status-pipeline",
    "output_schema": "status_pipeline.output.json",
    "python": "hpc_agent.ops.status_pipeline.status_pipeline",
    "side_effects": [
      "ssh",
      "writes-tick-log"
    ],
    "summary": "Deterministic status spine in one call: monitor-flow (wait to terminal/budget) → lifecycle_state dispatch. Reports one typed stage_reached outcome; sets needs_decision only on failed / abandoned.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent status-snapshot --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "status_snapshot.input.json",
    "name": "status-snapshot",
    "output_schema": "status_block.output.json",
    "python": "hpc_agent.ops.status_blocks.status_snapshot",
    "side_effects": [
      "ssh"
    ],
    "summary": "Status block (snapshot): a one-shot, journal-first digest of what is running where and what changed since the human last looked. Computes the changed-since delta against last_seen_by_human_at, then re-stamps the watermark. Sets needs_decision only on evidence — a stalled driver or a failed/abandoned run. Optional reconcile re-derives ground truth from the cluster first (the only SSH path). Terminates → y/nudge.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent status-watch --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "status_watch.input.json",
    "name": "status-watch",
    "output_schema": "status_block.output.json",
    "python": "hpc_agent.ops.status_blocks.status_watch",
    "side_effects": [
      "ssh",
      "writes-tick-log"
    ],
    "summary": "Status block (watch): blocking poll to terminal or anomaly via monitor-flow (which owns the throttled SSH spine and the guaranteed terminal harvest). A clean terminal hands off to the harvest block (needs_decision=False); a failed/abandoned anomaly or a timeout raises the y/nudge boundary with a drafted-evidence brief. A timeout arms the next tick when an invocation_argv is supplied.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent submit-and-verify --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "submit_and_verify.input.json",
    "name": "submit-and-verify",
    "output_schema": "submit_and_verify.output.json",
    "python": "hpc_agent.ops.submit_and_verify.submit_and_verify",
    "side_effects": [
      "scheduler-submit",
      "ssh"
    ],
    "summary": "Submit a run plus its canary, then verify the canary lands before returning. One call instead of /submit-hpc then /verify-canary. Returns {run_id, job_ids, deduped, verified, failure_kind, verify_result}.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent submit-flow --spec <path> [--experiment-dir <dir>] [--dry-run] [--partial-ok] [--invalidate-on-code-change]",
    "idempotent": true,
    "input_schema": "submit_flow.input.json",
    "name": "submit-flow",
    "output_schema": "submit_flow.output.json",
    "python": "hpc_agent.ops.submit_flow.submit_flow",
    "side_effects": [
      "scheduler-submit",
      "sync-push",
      "writes-journal"
    ],
    "summary": "Workflow atom: pre-flight + rsync + deploy + qsub + record in one shot. Auto-dispatches to submit-flow-batch when the spec is a {specs: [...]} object — callers always invoke this one subcommand whether the iteration emits 1 spec or N. Idempotent on run_id (or per-spec run_id when batched).",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent submit-flow-batch --spec <path> [--experiment-dir <dir>] [--dry-run]",
    "idempotent": true,
    "input_schema": "submit_flow_batch.input.json",
    "name": "submit-flow-batch",
    "output_schema": "submit_flow_batch.output.json",
    "python": "hpc_agent.ops.submit_flow.submit_flow_batch",
    "side_effects": [
      "scheduler-submit",
      "sync-push",
      "writes-journal"
    ],
    "summary": "Workflow atom: rsync + deploy ONCE, then qsub N specs sharing the same (ssh_target, remote_path). Use whenever a campaign or sweep submits >1 specs to the same cluster — bundles 13×N ssh handshakes into ~3 (rsync + deploy + multiplexed qsubs). Spec file is a JSON list.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent submit-pipeline --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "submit_pipeline.input.json",
    "name": "submit-pipeline",
    "output_schema": "submit_pipeline.output.json",
    "python": "hpc_agent.ops.submit_pipeline.submit_pipeline",
    "side_effects": [
      "scheduler-submit",
      "ssh",
      "writes-followup-specs"
    ],
    "summary": "Deterministic submit spine in one call: submit-and-verify (the canary gate) → verify-submitted (post-qsub health) → prepare-followup-specs. Reports one typed stage_reached outcome; sets needs_decision only on the genuine gate failures.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent submit-s1 --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "submit_s1.input.json",
    "name": "submit-s1",
    "output_schema": "submit_block.output.json",
    "python": "hpc_agent.ops.submit_blocks.submit_s1",
    "side_effects": [
      "ssh"
    ],
    "summary": "Submit block S1 (resolve): preflight + walk-submit-ambiguities to the decision boundary. Brief = the {resolved, ambiguities} envelope with each ambiguity's safe_default surfaced as a RECOMMENDATION (never auto-applied). When the walk is clean and a resolve spec is supplied, chains resolve-submit-inputs. Terminates → y/nudge.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent submit-s2 --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "submit_s2.input.json",
    "name": "submit-s2",
    "output_schema": "submit_block.output.json",
    "python": "hpc_agent.ops.submit_blocks.submit_s2",
    "side_effects": [
      "scheduler-submit",
      "ssh"
    ],
    "summary": "Submit block S2 (stage & canary): submit-and-verify STOPPED after a verified canary (the main array does NOT launch), plus the estimate-core-hours footprint. Brief = 'canary green, est N core-hours'. Terminates → y/nudge; S3 launches the main array.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent submit-s3 --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "submit_s3.input.json",
    "name": "submit-s3",
    "output_schema": "submit_block.output.json",
    "python": "hpc_agent.ops.submit_blocks.submit_s3",
    "side_effects": [
      "scheduler-submit",
      "ssh"
    ],
    "summary": "Submit block S3 (submit & watch): launch the main array (Phase-2 of the two-phase gate — canary verified in S2), monitor to terminal/anomaly, and arm the next monitor tick. Runs UNATTENDED — no human boundary inside; an anomaly is a block terminator.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent submit-s4 --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "submit_s4.input.json",
    "name": "submit-s4",
    "output_schema": "submit_block.output.json",
    "python": "hpc_agent.ops.submit_blocks.submit_s4",
    "side_effects": [
      "ssh"
    ],
    "summary": "Submit block S4 (harvest): aggregate-flow → a code-extracted results table + a slot for proposed interpretations. Terminates → y/nudge. (Calls the existing aggregate entry; Unit B's harvest_on_terminal guarantee is a parallel add.)",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent submit-speculate --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "submit_speculate.input.json",
    "name": "submit-speculate",
    "output_schema": "submit_speculate.output.json",
    "python": "hpc_agent.ops.submit_speculate.submit_speculate",
    "side_effects": [
      "scheduler-submit",
      "ssh"
    ],
    "summary": "Speculative canary (design §3): run S2's canary EARLY, under the S1 recommended defaults, while the human reviews the brief. Composes submit-and-verify(stop_after_canary=True) — only the 1-task canary enters the queue; the main array never launches. Budget = 1 per brief and nudge-invalidation come free from the (cmd_sha, version) canary TTL cache: a validated-fresh canary is refused (S2 reuses it). Never cancels anything.",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent validate-campaign --spec <path> [--experiment-dir <dir>]",
    "idempotent": true,
    "input_schema": "validate_campaign.input.json",
    "name": "validate-campaign",
    "output_schema": "validate_campaign.output.json",
    "python": "hpc_agent.meta.validate_campaign.validate_campaign",
    "side_effects": [],
    "summary": "Validate a campaign spec end-to-end (executor signatures, dataset, QoS limits, walltime history, stochastic marker).",
    "verb": "workflow"
  },
  {
    "agent_facing": true,
    "cli": "hpc-agent verify-canary [--experiment-dir <dir>] --canary-run-id <canary_run_id> [--expect-output <expect_output>] [--fingerprint <fingerprint>] [--verify-checkpoint] [--checkpoint-result-dir <checkpoint_result_dir>] [--poll-interval-sec <poll_interval_sec>] [--wait-budget-sec <wait_budget_sec>]",
    "idempotent": true,
    "input_schema": null,
    "name": "verify-canary",
    "output_schema": "verify_canary.output.json",
    "python": "hpc_agent.ops.verify_canary.verify_canary",
    "side_effects": [
      "ssh"
    ],
    "summary": "Wait + grep + output-check for a 1-task canary submission. Polls until terminal, scans stderr for known failure markers, optionally checks expect_output exists. Returns {ok, failure_kind, details, stderr_tail}.",
    "verb": "workflow"
  }
]
