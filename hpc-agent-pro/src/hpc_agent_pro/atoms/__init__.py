"""Primitive atoms contributed by the hpc-agent-pro plugin.

Each module here registers a ``@primitive`` on import; the modules are
listed in :mod:`hpc_agent_pro.plugin`'s ``primitive_modules`` so the
host imports them after the core primitive modules.
"""
