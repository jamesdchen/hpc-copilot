---
slug: lifecycle-verdicts
order: 2
title: "Lifecycle verdicts and run identity: one definition, named tests"
scope: "One definition per terminal-verdict / run-identity / run-execution decision, every call site routing through it."
---

# Lifecycle verdicts and run identity: one definition, named tests

A run's terminal verdict (did it complete / fail / vanish?) and its dedup
identity (is this the same run; did its code drift?) are decisions that were
each historically re-derived at several call sites whose copies then disagreed —
the abandoned-vs-failed cluster (#351 #4: monitor, reconcile, and aggregate each
turned the reporter's counts into a verdict differently) and the executor-drift
replay that had to be fixed twice, once per dedup layer (#351 #5). The rule:
each such decision has exactly ONE definition that every call site routes
through, and the precedence it encodes is pinned by a property test, not a
comment.

Two corollaries the history earned:

- **The verdict is revisable; the evidence is durable.** Terminal states are NOT
  monotonic here — reconcile legitimately downgrades a premature `complete` to
  `failed` when new evidence arrives (#351 #4 *is* that correction). So do not
  add a "terminal is sticky" transition guard: it would re-break the bug it
  looks like it prevents. Record WHY each verdict was reached
  (`last_status.verdict_reason`, from `classify.settle`) so a wrong verdict is
  debuggable without re-running reconcile.
- **Centralize the decision, keep side-effects local.** `classify_polling` /
  `settle` decide; `_gather_failure_features` and `mark_run` stay at the call
  site. A pure decision over explicit evidence is testable without a cluster.

## Enforcement map

| Rule | Enforced by | Fires when |
|---|---|---|
| One count→verdict definition (poll + settle) | `tests/ops/monitor/test_classify.py` | a call site re-derives complete/failed/abandoned from raw counts instead of `classify_polling` / `settle` (`_is_terminal` is a thin adapter over the former) |
| Settle precedence: failure outranks absence; strict completion is never claimed while a failure is present (#351 #4) | `tests/ops/monitor/test_classify.py::test_settled_failure_outranks_absence`, `::test_settled_never_complete_while_failure_present` | a positive `failed` count reads as abandoned or complete |
| One executor/code-drift predicate for both dedup layers (#351 #5) | `tests/state/test_code_drift.py::test_layers_share_one_drift_predicate` | layer-1 (`runner._layer1_code_drift`) or layer-2 (`runs.find_run_by_cmd_sha`) re-inlines the drift comparison instead of routing through `state.code_drift.detect_code_drift` |
| Layer-1 dedup decision is pure + named (dedup/proceed/redo by status × drift × lever, #276 / #351 #5) | `tests/ops/submit/test_layer1_dedup.py` | the `submit_and_record` run_id-dedup tree changes behavior on any branch (terminal-failure proceeds, in_flight blocks, complete dedups / redoes-in-place / warns) without the unit test moving in lockstep |
| Verdict provenance is recorded | `tests/ops/monitor/test_classify.py::test_settle_carries_reason_and_evidence_for_each_arm` | `settle` stops carrying a reason + evidence snapshot for any arm |
| Remote reads feeding a verdict carry a positive-evidence ack (B3, one ack definition; philosophy audit 2026-07-12): aggregate's `verify_per_task_outputs` routes through `wrap_with_ack`/`split_ack` — an rc-0 empty/severed/truncated channel raises `RemoteCommandFailed` (UNKNOWN), never "all per-task outputs present" (the false-green at the combine gate; fixed `65e9b14` after the pre-fix fire was reproduced first-hand). The repo-wide lint over NEW ack-less `.stdout` consumers is the banked B3′ spec in the upstream-fixes plan, not yet a row | `tests/test_runner.py` ack fire tests (silent-ackless input raises; a failed remote `cd` stays loud via the `;`-sequenced ack rc) | a per-task-outputs read returns a verdict-bearing list from a channel that produced no ack, or the ack rc is swallowed |
| The aggregate non-empty (`min_rows`) gate is ONE reporter invocation, and a severed single report reads UNKNOWN for BOTH row sets — never `rows_observed=0` → every task insufficient (F3, latency train 2026-07-16): a reporter new enough to stamp the top-level `rows_observed_emitted` marker carries the LENIENT complete set plus each complete task's observed data-row count in a single `min_rows=0` report, so `_nonempty_failing_task_ids` derives the strict set locally (no second SSH round-trip); a severed channel RAISES inside `ssh_status_report` (rc-0-no-ack) so the exception propagates (UNKNOWN) instead of the missing field being parsed as zero rows; a reporter predating the field (no marker) falls back to the historical two-call diff. The parse of the reporter's row surface has ONE home — `cluster_status.rows_observed_from_report` | `tests/ops/aggregate/test_flow_gates.py::test_min_rows_single_report_one_invocation`, `::test_min_rows_old_reporter_two_call_fallback`, `::test_min_rows_severed_report_is_unknown_not_all_failing`, `::test_nonempty_gate_docstring_is_one_invocation_not_two_ssh`; `tests/infra/test_cluster_status_reporter.py::test_rows_observed_from_report_parses_marker_completes_and_counts`, `::test_severed_report_raises_before_rows_observed_parse`; `tests/execution/mapreduce/test_status.py::TestRowsObservedEmit` | `_nonempty_failing_task_ids` reads a `rows_observed`-less report as all-tasks-failing, spends a second SSH call when the marker is present, parses a severed/ackless report as `rows_observed=0` instead of raising, or the deployed reporter stops emitting the per-task `rows_observed` / the `rows_observed_emitted` marker |
| The canary reducer check EXECUTES a declared custom reducer against the verified canary's ONE real task-0 row before the main array launches (amortized-reduction-check-2026-07-17): it runs the SAME `cluster_reduce` the final harvest runs (one-definition — no rung re-derives the reduction, so rung-2's dynamic proof and rung-3's authoritative reduce can never disagree), asserts ONLY the contract SHAPE (exit 0, parseable JSON, top-level keys) never a VALUE (a single canary row's aggregate number is meaningless), DISCLOSES any reducer error VERBATIM onto the S2/S3 brief without ever refusing the submit (the bare `y` stands — the failure MIGHT be a benign "needs ≥2 rows" false alarm), and reports a severed/breaker-open/timed-out check as `unverified` — NEVER `passed` (positive-evidence-only, the `reporter_unreachable` posture). Gated on a declared `aggregate_defaults.aggregate_cmd`; opt-out `HPC_NO_CANARY_REDUCER_CHECK` mirrors `HPC_NO_DOUBLE_CANARY`; a run with no custom reducer is byte-identical to a pre-feature submit | `tests/ops/test_canary_reducer_check.py::test_passed_on_valid_json_shape`, `::test_discloses_not_refuses`, `::test_severed_check_is_unverified_not_pass`, `::test_uses_cluster_reduce_not_inlined`, `::test_no_custom_reducer_skips_byte_identical`, `::test_env_opt_out_skips` | the check inlines its own reducer invocation instead of `cluster_reduce`, hard-refuses/blocks a submit on a reducer error, asserts a value, reports a severed/absent read as `passed`, or fires when no custom `aggregate_cmd` is declared / the opt-out is set |
| The relay decision-state guard is supersession-aware (B12 journal-as-truth; the item-5 hook runs `verify_relay` in-process at `relay_audit_stop.py:841` — one enforcement seat, two verifiers; philosophy audit 2026-07-12): a truthful "superseded" relay about a run whose `run_record.superseded_by` is stamped is NOT a contradiction, and the guard still blocks a fabricated superseded/revoked claim when no supersession record exists (both directions demonstrated at `65e9b14`) | `tests/_kernel/hooks/test_relay_audit_stop.py` (incl. `test_unrelated_run_id_still_flagged_when_superseded_by_present` — the no-blanket-exemption leg) + `tests/ops/test_verify_relay.py` | the hook false-blocks a truthful supersession relay, or a fabricated superseded/revoked claim passes because the exemption widened past the stamped record |
| Polling-vs-settled completion divergence stays intentional | `tests/ops/monitor/test_classify.py::test_polling_and_settled_diverge_on_complete_with_stale_failure` | the lenient (mid-flight) and strict (settled) completion predicates are silently unified |
| Poll-failure-class precedence: a DETERMINISTIC broken-env fault (rc 126/127) escalates fast; a TRANSIENT fault rides the wait budget (#12) | `tests/ops/aggregate/test_canary_verify.py::test_deterministic_env_rc127_escalates_early_with_marker`, `::test_deterministic_env_rc127_escalates_early_without_marker`, `::test_transient_polls_ride_budget_not_early_failed` | `verify_canary._classify_poll_failure` stops splitting rc 126/127 from transient, or the canary treats a deterministic broken env as a transient budget-rider (or vice-versa). 2.2 liveness split: the canary loop's per-tick read is now LIGHT (no run python) so this fault can only surface at the ONE terminal reporter — the split + envelopes are preserved there by `verify_canary._terminal_reporter_verdict` |
| Row 6 (2.2) — Light-poll deterministic-env escalation parity: the canary poll loop issues only LIGHT liveness reads (`read_announcements` / `ssh_batch_scheduler_states`, no cluster-side python) and runs the HEAVY `ssh_status_report` EXACTLY once at terminal; a broken-env (rc 126/127) / torn-sidecar reporter fault can only surface at that single terminal reporter and escalates loud+fast (never a full-budget spin), and a transient breaker trip there is deadline-waited + retried ONCE (G1) so a finished job's verdict is never lost | `tests/ops/aggregate/test_canary_verify.py::test_heavy_reporter_invoked_exactly_once_at_terminal`, `::test_broken_env_leaves_queue_escalates_at_terminal_reporter_not_budget`, `::test_breaker_open_at_terminal_recovers_after_deadline_wait_and_retry` | the canary poll loop runs the full `ssh_status_report` per tick again (re-arming `polling.canary-heavy-poll`), a terminal-reporter deterministic fault rides the full budget instead of escalating, or a transient breaker trip at the terminal reporter loses a finished job's verdict instead of waiting out the deadline + retrying once |
| One driver-watchdog tick-stamp definition for both poll loops (monitor + canary route through `state.journal.stamp_watchdog_tick`, #12) | `tests/ops/monitor/test_watchdog_stamp_contract.py::test_both_poll_loops_share_one_watchdog_stamp_definition` | `monitor_flow._stamp_watchdog` re-inlines the stamp body, or the canary poll loop stamps liveness without routing through the shared helper |
| Row 8 (2.4) — Pull-manifest cache is content-keyed, success-only, evict-on-sha-move (F2, D2/run-13 finding-13 pull direction): `_pull._local_present_manifest`'s quick-check cache keys on `.hpc_cmd_sha` where present and stat (`mtime_ns`+`size`) only ever decides UNCHANGED — a moved `cmd_sha` MUST evict even when stat collides (the torn-overwrite class), entries younger than the skew window count dirty, a read that raises is cached by neither the manifest nor the cache (success-only, breaker-invalidated posture), the cache is per-run keyed + temp-then-rename + excluded from the include-match, and foreign rows drop out of the rewritten cache | `tests/infra/test_transport_pull.py::test_local_present_manifest_moved_cmd_sha_evicts_on_stat_match`, `::test_local_present_manifest_severed_read_not_cached`, `::test_local_present_manifest_young_file_rehashed`, `::test_local_present_manifest_foreign_rows_dropped`, `::test_local_present_manifest_reuses_cache` | a moved `.hpc_cmd_sha` with unchanged stat is served from cache, a failure/severed read is cached, a young file is trusted from stat alone, or a foreign-keyed row survives into the rewritten cache |
| Row 9 (2.4) — Engine-seam laws EXTEND to the transfer plane (F7 verify-during-build): every transfer-plane op (`_tar_ssh_push`, `_remote_preclean`/`_ssh_bounded`, the `_delta` hash manifest, the `_prune` read/write/`rm`, `rsync_push`/`_rsync_deploy`, and the `_pull` engine) reaches the cluster through the ONE-SHOT `run_capture_bounded` bounded runner, NEVER `remote.ssh_run` — so it never consults the asyncssh engine (the 2026-07-16 default-ON flip leaves its dial counts byte-identical) and its remote command line is preamble-free (no `module load`/`conda activate`/`source` control-plane preamble, no `HPC_AGENT_OP=`/`timeout -k` `ssh_run` wrapper — byte-equal to the raw shell). The cluster-side reduce (`run_final_reduce`) is a COMPUTE op, not a transfer, and keeps its activation | `tests/contracts/test_src_subprocess_timeout_discipline.py::test_transport_ssh_sites_route_through_bounded_runner`; `tests/infra/test_remote_rsync_fallback.py::test_transfer_plane_remote_commands_are_preamble_free`, `::test_transfer_plane_push_never_consults_ssh_engine`, `::test_warm_delta_push_stays_within_four_opens`; `tests/infra/test_transport_pull.py::test_pull_remote_commands_are_preamble_free`, `::test_pull_transfer_drives_bounded_runner_not_ssh_run` | a transfer routes through `ssh_run` (re-arming the engine gate + the `HPC_AGENT_OP`/`timeout` wrapper), a transfer command acquires a `module`/`conda`/`source` preamble, or the warm rsync-less delta re-push grows past 4 cold ssh opens |
| Row 11 (2.6) — A marker WAKES, never SETTLES: the P3 per-host census waiter (`ops/monitor/announce.wait_for_announce_change`) returns only a `{woke, acked, waited}` HINT (no lifecycle verdict); a forged / premature `.run_terminal` wake marker breaks the remote poll loop, but the control plane re-reads the per-task markers (the truth) via `read_announcements` and a non-terminal census keeps the run watching | `tests/ops/monitor/test_announce.py::test_waiter_wake_is_a_hint_never_a_settle`, `::test_waiter_severed_wait_is_not_a_wake`, `::test_waiter_wakes_in_one_dial_with_zero_intervening_census_reads` | `wait_for_announce_change` returns a terminal/lifecycle verdict instead of a wake hint, or a WOKE line without the positive `_WAIT_ACK` (a severed link) is counted as a real wake |
| Row 12 (2.6) — Run-terminal WAKE marker constant in LOCK-STEP: the standalone dispatcher writer (`execution/mapreduce/dispatch._ANNOUNCE_RUN_TERMINAL`, scp'd to the compute node without `hpc_agent` on the path) and the control-plane reader/waiter (`ops/monitor/announce.ANNOUNCE_RUN_TERMINAL`) name the ONE `.run_terminal` filename (the `exit_no_output_constant` lockstep pattern) | `tests/ops/monitor/test_announce.py::test_vocabulary_lockstep_with_standalone_dispatcher` | the dispatcher's `_ANNOUNCE_RUN_TERMINAL` drifts from the reader's `ANNOUNCE_RUN_TERMINAL`, so the waiter watches for a filename the dispatcher never writes (and never wakes) |
| Row 13 (2.6) — The blocked per-host census wait routes its watchdog liveness stamp through the ONE `state.journal.stamp_watchdog_tick` definition (via `monitor_flow._stamp_watchdog`), stamped BEFORE the single blocking remote dial so a poller that dies mid-wait lapses its `next_tick_due`; the remote bounded-wait command runs through `remote.ssh_run` (the engine channel / one-shot `run_capture_bounded` bounded runner), NEVER a raw subprocess | `tests/ops/monitor/test_announce.py::test_waiter_stamps_watchdog_before_the_blocked_wait`; `tests/ops/monitor/test_flow_announce.py::test_announce_wait_moves_the_poll_remote_side_when_enabled`, `::test_announce_wait_off_by_default_keeps_client_sleep` | the waiter dials before stamping (a mid-wait death goes unseen), or the remote wait bypasses `ssh_run`/`run_capture_bounded` for a bare unbounded subprocess |
| A kill-confirmed run settles terminal from the KILL evidence, reporter-independent (proving run #5, finding 14) | `tests/ops/monitor/test_reconcile_kill_confirmed.py::test_kill_confirmed_reporter_dead_settles_abandoned` | reconcile leaves a run whose scheduler jobs were confirmed gone (`journal.is_kill_confirmed`) stranded `in_flight`/`unable_to_verify` because the per-task reporter crashed — the reporter's counts are irrelevant to a deliberate kill |
| Control-plane activation is cluster-derived, ONE definition every reporter/reconcile/combine routes through (proving run #5, finding 13) | `tests/infra/test_remote_activation.py::test_for_sidecar_derives_from_cluster_when_env_dropped` | `remote_activation_for_sidecar` returns "" (a bare `python` the cluster's Lmod default hijacks, `exit 127`) for a hand-carried sidecar that dropped its `env` activation block but kept `cluster` — activation must derive from `clusters.yaml[cluster]`, never depend on a field a sidecar can drop |
| Exit 0 is not success — a task leaving an EMPTY result dir produced no result and is a FAILURE, so the canary catches "the array will produce nothing" on ONE task (proving run #5, finding 16) | `tests/execution/mapreduce/test_dispatch.py::TestDispatchEmptyOutputIsFailure`, `::test_exit_no_output_constant_in_lockstep_with_preamble`, `tests/execution/mapreduce/test_status.py::TestCheckResultsIgnoresFrameworkArtifacts` | dispatch promotes an empty WIP as complete, or the reporter counts the always-written `_runtime.json`/framework sidecars as a produced result — so an outputless exit-0 task reads complete and the canary greens |
| A hand-runnable executor CLI must not diverge from the framework's result contract — a `@register_run` `__main__` routes through the injected `compute()` result-writer, never print-and-exit (finding 16b) | `tests/incorporation/build/test_template.py::test_script_main_routes_through_compute_result_writer` | the scaffold template's `__main__` prints the result instead of writing `metrics.json` via `compute()` |
| The submit spine's agent-authored task counts are cross-checked against ground truth — `submit.total_tasks` and `sidecar.task_count` must both equal `compute-run-id`'s `total` (== `tasks.total()` == `len(trial_params)`) (finding 21) | `tests/ops/test_resolve_submit_inputs.py::test_undercount_task_count_refused_naming_both_counts`, `::test_overcount_task_count_refused_naming_both_counts`, `::test_one_count_disagreeing_refused` | `resolve_submit_inputs` builds the spec / writes the sidecar for a spec whose declared `total_tasks`/`task_count` disagrees with `tasks.total()` — an undercount sizes the array `1-total_tasks` and silently drops the higher task_ids |
| Categorical (non-numeric) `task_generator` claims face the human-authorship bar, not just numbers (finding 25) | `tests/ops/test_decision_journal_primitives.py::test_authorship_gate_refuses_fabricated_categorical_when_numbers_derive` | `_assert_human_authorship`'s structured path routes a required-caller value's non-numeric string leaves past the `human_words` overlap check — a fabricated categorical axis rides a passing number check into `resolved` |
| A sidecar per-task `executor` must be RUNNABLE, not merely non-empty/non-dispatcher — a bare script name (`train.py`: no interpreter, no path sep) is refused at submit-time, not exit-127 on the cluster (finding 17) | `tests/ops/submit/test_executor_env_guards.py::test_is_runnable_executor_refuses_bare_script_name`, `::test_ensure_run_sidecar_refuses_prewritten_bare_script_executor`; `tests/ops/test_write_run_sidecar.py::test_bare_script_name_executor_refused` | `submit_flow._is_runnable_executor` accepts a bare `*.py/*.sh/*.R/*.jl` token with no interpreter prefix and no path separator, or `check_per_task_executor` lets `write-run-sidecar` write one |
| `ssh_target` must equal `ClusterConfig(clusters.yaml[cluster]).ssh_target` — a spec whose cluster and ssh_target name different clusters is refused at build-time (finding 18; finding-9's split-brain root) | `tests/incorporation/build/test_submit_spec.py::test_finding18_ssh_target_mismatch_refused` | `build_submit_spec` accepts an `ssh_target` disagreeing with the cluster's derived `user@host` when the entry yields a derivable target |
| `backend` must equal `clusters.yaml[cluster].scheduler` unless a `scheduler_profile` pins the family (finding 19) | `tests/incorporation/build/test_submit_spec.py::test_finding19_backend_scheduler_mismatch_refused`, `::test_finding19_scheduler_profile_pin_is_the_sanctioned_override` | `build_submit_spec` accepts a `backend` disagreeing with the cluster's `scheduler` while no `scheduler_profile` is pinned |
| An unknown `cluster` against a POPULATED clusters.yaml is refused `ClusterUnknown` at build-time, never silently degraded to `{}` (finding 20; an empty `{}` config stays a pass-through for ad-hoc clusters) | `tests/incorporation/build/test_submit_spec.py::test_finding20_unknown_cluster_refused_against_populated_config`, `::test_finding20_empty_config_is_passthrough` | `build_submit_spec` lets a cluster absent from a non-empty clusters.yaml through instead of raising `ClusterUnknown` |
| `Activation` accepts `conda_env` only with POSITIVE evidence conda is on PATH — a `conda_source`, or a conda-naming module (anaconda/miniconda/miniforge/mamba); a non-conda module list (`gcc/11`) is not proof (finding 24) | `tests/incorporation/build/test_submit_spec.py::TestActivationCondaEvidence` | `Activation.__post_init__` accepts `conda_env` with empty `conda_source` and a non-conda `modules` list (the pre-tightening `or self.modules` hole) |
| The human-facing relay is CODE-rendered from a block's own structured evidence and relayed verbatim — never reconstructed; the S2 canary summary renders the canary's 1 task, NEVER the main array's total (finding 15) | `tests/ops/submit/test_blocks.py::test_render_relay_s2_canary_summary_uses_canary_one_task_not_main_total`, `tests/ops/monitor/test_blocks.py::test_snapshot_relay_renders_new_state_after_transition_not_stale` | `ops/relay_render.render_relay` interpolates `cost_estimate.total_tasks` into the 1-task canary line, or `status-snapshot`'s relay renders a cached state instead of the record's current `status` |
| CODE-DERIVED fields are un-authorable at every agent decision surface — one partition class (`field_partition.CODE_DERIVED_FIELDS`) BOUND by both `revise-resolved`'s patch refusal and `append-decision`'s `resolved` refusal, never copied (run #6 F1) | `tests/ops/submit/test_field_partition.py::test_revise_resolved_binds_the_partition`, `::test_journal_unauthorable_is_code_derived_minus_sanctioned_echoes`; `tests/ops/test_decision_journal_primitives.py::test_append_refuses_code_derived_resolved_field` | a `patch` or a journal `resolved` names `executor`/`job_env`/`ssh_target`/… and is accepted, or either guard's field set drifts from the partition (re-declared instead of bound) |
| The entry_point→executor derivation always emits a RUNNABLE per-task command — an unrunnable derived `executor_cmd` is a framework bug refused at derivation time, never exit-127 on the cluster (run #6 F1) | `tests/ops/memory/test_interview.py::TestDerivedExecutorRunnableAssert` | `record_interview` materializes an `executor_cmd` that `check_per_task_executor`/`_is_runnable_executor` would refuse (bare script, bare module:function, dispatcher-shaped, empty) |
| A task-interface-BLIND executor (single bare token: no args, no `$RESULT_DIR`/`$TASK_ID`/`$HPC_KW_*` ref) WARNS loudly, never refuses — refusal is unwinnable without the cluster's `$PATH`; the canary is the hard backstop (run #6 F1, finding 17 generalized from extension-proxy to property) | `tests/incorporation/build/test_submit_spec.py::TestTaskInterfaceBlindWarn` | `check_per_task_executor` stays silent on an extension-less bare token (`monte_carlo_pi`), or escalates the warn into a refusal (the `mybinary`-is-real false-positive) |
| The canary sidecar MIRRORS the main run's dispatch-essentials by CONTENT, not existence — a corrected/re-resolved main (`cmd_sha` is param identity; an executor fix keeps the run_id) re-mirrors instead of re-running the stale command (run #6 F1 follow-up) | `tests/ops/submit/test_canary_gate.py::test_mirror_canary_sidecar_remirrors_on_divergent_main`, `::test_mirror_canary_sidecar_noop_when_in_sync` | `_mirror_canary_sidecar` no-ops on an existing canary sidecar whose `executor`/`result_dir_template`/`cmd_sha`/`env`/`cluster`/`remote_path` diverge from the main's |
| An UNKNOWN cost footprint (walltime unresolved → the kernel's defensive 0.0) is never "free": under a configured `max_estimated_core_hours` it confirms/refuses (never budget-overridable), and every render says "unknown core-hours", never "0" (run #6) | `tests/ops/submit/test_plan_throughput.py::TestCostGateUnknownFootprint`; `tests/infra/test_cost.py::TestFootprintUnknown`; `tests/ops/test_relay_render_footprint.py` | `evaluate_cost_gate` passes a `footprint_unknown` estimate under a set threshold (or lets `HPC_AGENT_COST_BUDGET` override it), or an S2/retarget brief renders the defensive 0.0 as a literal number |
| The per-task reduce aggregates ONLY the run's own rows — the `<run_id>-canary` sibling (same `results/` subtree, main id as name prefix) is excluded via the one `-canary` suffix definition, and MORE rows than `total_tasks` after exclusion is provable foreign contamination, refused loudly (run #6 harvest: an 11-row mean for a 10-task run) | `tests/ops/aggregate/test_flow_ssh_default_reducer.py::test_ssh_fallback_excludes_canary_sibling_results`, `::test_ssh_fallback_refuses_foreign_row_overcount` | `_per_task_metrics_reduce` averages a dir under a `<run_id>-canary` path segment, or reduces more contributing dirs than the run's `total_tasks` instead of refusing |
| `write-run-sidecar`'s declared identity is cross-checked against the materialized task list — `cmd_sha`/`task_count`/conventional `run_id` must match `compute-run-id`'s truth when `.hpc/tasks.py` exists; `-canary` ids exempt (finding 21 at the CLI surface, run #6 F1 family) | `tests/ops/test_write_run_sidecar.py::TestIdentityCrossCheck` | the primitive writes a sidecar whose declared count/sha disagrees with `tasks.total()`/`compute_cmd_sha` while tasks.py is present, or the check fires on a canary mirror / a tasks.py-less hand-written setup |
| A composite-preflight shells `hpc-agent`→`ssh`; the bounded-capture runner all four funnel through (`infra.bounded_subprocess.run_capture_bounded`) kills the whole PROCESS TREE on timeout (POSIX `killpg` on the child's session / Windows `taskkill /T`), so a grandchild `ssh` can't outlive the deadline holding the stdout pipe — the proving-run-#3 wedge re-armed at the shell-`hpc-agent`-which-shells-`ssh` double layer (live Hoffman2 `submit-s1` left two `ssh` orphaned ~17 min, past the 60s timeout) (run #7) | `tests/infra/test_bounded_subprocess.py::test_grandchild_pid_is_reaped_not_orphaned`, `::test_grandchild_pipe_does_not_hang_the_drain`; the cited `_EXEMPT_BY_DESIGN` entry in `tests/contracts/test_src_subprocess_timeout_discipline.py::test_exempt_by_design_entries_still_exist` | `run_capture_bounded` leaves a descendant alive after its timeout (the tree wasn't killed), or a composite-preflight `_run_subprocess` reverts to bare `subprocess.run` so the post-timeout drain blocks on the orphaned grandchild's pipe |
| The bounded tree-kill runner covers the TRANSPORT layer, not only the 4 preflights: every ssh/rsync/tar/scp push+pull in `infra.transport` funnels through `run_capture_bounded` (`stdin`/`env` forwarded for the `tar | ssh` pipe and rsync's `RSYNC_RSH`) — a bare `subprocess.run(..., timeout=)` is NOT a hard deadline on Windows for an ssh-spawning call (its post-kill `communicate()` drains an unbounded grandchild pipe), so `rsync`/`tar` shelling `ssh` cannot wedge staging past the deadline — the run #7 S2-staging wedge (a detached `submit-s2` worker parked with a 0-byte log staging to Hoffman2) (run #7) | `tests/contracts/test_src_subprocess_timeout_discipline.py::test_transport_ssh_sites_route_through_bounded_runner`, `::test_bounded_runner_audit_fires`; `tests/infra/test_bounded_subprocess.py::test_forwards_stdin_pipe`, `::test_forwards_env` | any of `_remote_preclean`/`_tar_ssh_push`/`_scp_pull`/`rsync_push`/`_rsync_deploy`/`rsync_pull` reverts to a bare `subprocess.run`/`Popen` for the network call instead of `run_capture_bounded`, so a slow/hung Hoffman2 ssh drains unbounded on Windows |
| The preflight probe's deadline is DERIVED from `SSH_TIMEOUT_SEC` (never a tighter restated constant) — a probe stricter than the staging/submit ssh budget it gates is a false-positive machine: a healthy-but-loaded login node (Hoffman2 ~31s handshake, run #7 live) reads as down, false-tripping the breaker while the 60s-bounded real operations would succeed (run #7) | `tests/ops/submit/test_flow.py::TestPreflightProbe::test_probe_deadline_never_tighter_than_the_ops_it_gates` | `_PREFLIGHT_PROBE_TIMEOUT_SEC` reverts to a hardcoded value below `SSH_TIMEOUT_SEC`, so a slow-handshake cluster the real ops could use trips `ssh_unreachable` at the probe |
| The preflight-check `cluster_ssh_echo`/`runtime_uv` probe deadline (`_cluster_ssh_timeout`) is DERIVED from `SSH_TIMEOUT_SEC` too — the `_PREFLIGHT_PROBE_TIMEOUT_SEC` lesson's uncovered SIBLING one directory over: its hardcoded 15s default (itself a bump from a 5s that false-tripped 2026-06-06) failed a loaded-but-healthy hoffman2 twice in run #8, walking the per-host breaker to 2/3 while the 60s-bounded real ops would have passed; `HPC_CLUSTER_SSH_TIMEOUT` stays the explicit per-ops override (run #8) | `tests/ops/preflight/test_check_parallel_probes.py::test_probe_deadline_never_tighter_than_the_ops_it_gates`, `::test_cluster_ssh_timeout_defaults_to_ssh_timeout_sec` | `_cluster_ssh_timeout` reverts to a restated constant below `SSH_TIMEOUT_SEC`, so a slow login node fails preflight probes the real submit would survive AND each false trip counts toward the ssh circuit breaker |
| The preflight probe verdict cache (`ops/preflight/probe_cache`) is SUCCESS-only, TTL-bounded, and BREAKER-INVALIDATED: a fully-passing probe block replays within the TTL with ZERO network traffic (each elided connection is one fewer for the intrusion filter — ban-safety and latency point the same way), but a failure is never cached, any breaker-recorded connection failure newer than the verdict (or an open circuit) forces a live probe, and replayed checks carry an honest `(cached: ...)` detail. Every funnel stage's composite preflight otherwise re-pays a full cold handshake (ControlMaster multiplexing broken on Windows) — 30-60s each on a loaded hoffman2 (run #8) | `tests/ops/preflight/test_probe_cache.py::test_second_preflight_within_ttl_issues_zero_network_traffic`, `::test_failed_probe_is_never_cached`, `::test_breaker_failure_newer_than_verdict_invalidates` | a red verdict gets cached (masking a degraded host for a whole TTL), the cache replays over newer breaker evidence, or the replayed checks drop the `(cached)` marker and read as live probes |
| Any engine failure falls back to one-shot; the asyncssh engine (`infra/ssh_engine`, opt-in `HPC_SSH_ENGINE=asyncssh`) is never load-bearing — the first (and now ONLY) fast path in the `ssh_run` seam (engine → one-shot), it is CAPTURE-ONLY, its gate is skipped for streaming, and ANY `EngineUnavailable` falls straight through to the permanent one-shot hard fallback, so an opt-in engine can never regress the ban-sensitive default (worst case equals today) (ssh outsourcing, 2026-07-06; the deprecated phase-1 in-process broker rung that once sat between engine and one-shot — `infra/ssh_broker`, `HPC_SSH_BROKER` — was RETIRED + DELETED 2026-07-07 per the `docs/design/connection-broker.md` trigger, evidence = proving run #9's three zero-`EngineUnavailable`/zero-fallback worker logs) | `tests/infra/test_remote.py::TestSshRunEngineFastPath::test_engine_unavailable_falls_through_to_one_shot`, `::test_engine_short_circuits_before_one_shot`, `::test_engine_disabled_is_not_consulted`, `::test_capture_false_never_consults_the_engine`, `::test_engine_enabled_and_succeeds_skips_one_shot` | the seam propagates an engine error instead of degrading to the one-shot path (engine becomes load-bearing), consults the engine under `capture=False`, or short-circuits the one-shot seam even when the engine did NOT serve the command |
| When NO interview materialized the executor (`interview.json` absent → the CALLER supplied it, and "the framework, not the LLM, decides" did not apply), a task-interface-BLIND executor's warning rides the S1 resolved REASON — visible at the y/nudge, pre-SSH and pre-cost — instead of dying in a detached worker's log (run #8: a hand-onboarded `executor: "run"` sailed to a FAILED canary on two clusters; the WARN had fired where nobody looks). Warn-not-refuse stands; the canary stays the hard backstop | `tests/ops/test_resolve_submit_inputs.py::test_no_interview_interface_blind_executor_warns_in_reason`, `::test_resolved_builds_submit_spec` (runnable caller executor stays warning-free) | `resolve_submit_inputs` drops the reason-surfacing (the blind-executor warning is only visible in a worker log again), or starts warning on runnable caller-supplied executors (noise that trains agents to ignore it) |
| A `run_name` shared by >1 `@register_run` function across files is AMBIGUOUS and refused loudly (`ambiguous_run`, naming every colliding file) at the interview's `_validate_register_run_entry` — never silently resolved to the first match by path sort (run #8: a stale `executors/monte_carlo_pi.py` and the intended root `train.py` both decorated `def run`; `executors/...` sorted first and won, so the WRONG file's signature + executor_cmd were materialized and the canary failed a run the human never meant to submit). Matches `classify_axis_auto`'s existing `ambiguous_run` contract — one refusal semantics for the same condition | `tests/ops/memory/test_interview.py::TestRegisterRunAmbiguity` | `_validate_register_run_entry` reverts to returning the first name match (a stale copy of an experiment silently hijacks onboarding), or the refusal stops naming the colliding files (the human has to grep for what to rename) |
| `retarget-run` is NON-BLOCKING by contract and MCP-curated by derivation: it re-resolves + best-effort-supersedes (an unreachable old cluster records `pending_closure`, never grinds on qdel) and returns in seconds with `next_block=submit-s2` — S2's DETACHED worker owns the re-canary poll; the verb itself never calls submit-and-verify. Its Result's `next_block` field is what derives it into the curated MCP catalog, closing the run-#8 gap (the agent, unable to reach the verb over MCP, hand-ran kill→confirm→revise against a MaxStartups-throttled hoffman2 and wedged) | `tests/ops/submit/test_retarget_run.py::test_retarget_module_has_no_inline_canary_seam`, `::test_retarget_supersedes_reresolves_and_hands_off_to_s2`; `tests/test_mcp_curated.py::test_curated_catalog_is_derived_blocks_union_extras` (retarget-run anchor) | an inline canary (`submit_and_verify`/`stop_after_canary`) regrows in the retarget module (a curated MCP call that blocks for ~30 min), the hand-off drops `next_block` (falling silently out of the MCP catalog), or the supersede leg starts BLOCKING on the old cluster's qdel |
| The canary reporter's activation can never silently fall to bare login-node python: verify_canary seeds the journal record's `cluster` into the sidecar dict before deriving (the flow's own canary sidecar carries neither `env` nor `cluster`, so the #176/#281 backfill arm never fired), AND the reporter command probes `import hpc_agent` first, exiting 127 so module-absence lands in the deterministic-env class instead of rc=1 "transient" — run #7 live: 30 min of rc=1 polls against a canary that had been green since 17:31 (run #7) | `tests/ops/aggregate/test_canary_verify.py::test_activation_derived_from_record_cluster_when_sidecar_bare`; `tests/infra/test_cluster_status_reporter.py::test_reporter_command_guards_module_absence_as_127` | verify_canary derives activation from a bare sidecar without seeding record.cluster (→ `""` → bare python), or ssh_status_report drops the import-guard so a missing module exits 1 and rides the full wait budget as "transient" |
| A detached submit block (S2/S3) re-invoked after its worker reached the block's terminal for the CURRENT tree REPLAYS the recorded terminal (`state/block_terminal`) — no new worker, no SSH — never re-detaches: the single-lease only refuses a LIVE sibling and self-heals on a dead pid, so a FINISHED block would otherwise re-execute (redundant canary, agent scrapes the log). Keyed on the sidecar `cmd_sha` so a nudge (moved sha) re-executes; the record captures `needs_decision=False` CLEAN terminals too (the S3-clean-terminal sibling the provenance-brief journal never stored), and the brief is appended once per fresh terminal so a replay can't double-append (run #7) | `tests/ops/submit/test_blocks.py::test_s2_reinvoke_replays_recorded_terminal_without_respawn`, `::test_s2_reinvoke_after_nudge_respawns`, `::test_s3_clean_terminal_is_replayable`, `::test_replay_does_not_double_append_provenance_brief`; `tests/state/test_block_terminal.py` | a detached block's detach branch spawns without first checking `_replay_recorded_terminal`; `_persist_brief` stops recording the terminal (re-invoke has nothing to replay); or the replay ignores a moved `cmd_sha` and replays a stale canary |
| The MAIN-array monitor's status reporter activates the run's conda env, exactly like the canary reporter: `record_status` seeds the journal record's `cluster` into the (env/cluster-less) sidecar before `remote_activation_for_sidecar`, so it never falls to bare login-node python → `import hpc_agent` fail → rc=127 — run #7 live: the S3 watch rode 28+ ticks of rc=127 as "transient" while a finished 20-task array sat unread (the b1b05f7d verify_canary fix's uncovered sibling on the monitor caller) (run #7) | `tests/ops/monitor/test_flow_poll_tolerance.py::test_record_status_activation_seeded_from_record_cluster` | `record_status` derives activation from a bare sidecar without seeding `record.cluster` (→ `""` → bare login python → rc=127 the monitor rides as transient) |
| The MAIN-array monitor ESCALATES a deterministic broken-env poll fault (reporter rc 126/127) to a loud reporter-unreachable TIMEOUT after `_DETERMINISTIC_ENV_POLLS_TO_FAIL` consecutive, never riding the whole wall-clock budget as "transient" — such a fault repeats every poll and never heals by waiting (run #7: the S3 watch rode 28+ ticks of rc=127 while a finished 20-task array sat unread); the `escalation_reason` carries the diagnosis and becomes the S3 `watching_timeout` reason (not the misleading "budget hit") (run #7) | `tests/ops/monitor/test_flow_poll_tolerance.py::test_deterministic_env_failure_escalates_to_reporter_unreachable` | `monitor_flow` rides rc 126/127 as transient to budget instead of escalating; or the S3 `watching_timeout` reason drops `mon.escalation_reason` |
| `remote_activation_for_sidecar` takes a `fallback_cluster` so a cluster-less submit-flow sidecar still backfills conda (#281) — ALL control-plane consumers (verify_canary, record_status, aggregate_flow, aggregate/combine, monitor/reconcile, logs `--all-failed`) handle a bare sidecar, not just the 2 that were hand-seeded; the deriver is the ONE seam instead of a per-consumer copy (run #7: the harvest/combine/reconcile surfaces were unseeded → would run bare login python → rc=127) | `tests/infra/test_remote_activation.py::test_fallback_cluster_seeds_activation_for_a_cluster_less_sidecar` | a control-plane consumer calls `remote_activation_for_sidecar` on a bare sidecar without `fallback_cluster` (→ "" → bare python → rc=127 the reporter/combiner/reducer can't recover from) |
| G6 one-definition: EVERY status-reporter consumer (`ssh_status_report`/`_ssh_status_report`) seeds `remote_activation` — the enumeration is a TEST, not prose, so a new consumer cannot ship unseeded (the run-#7/#8 rc=127 class was re-armed three times by unseeded consumers: `logs --all-failed` #13, then `fetch_failures` and the aggregate Check-1 `_nonempty_failing_task_ids` reporter, both found + seeded by this lens). AND the control-plane activate rule (`remote_activation_prefix`) shares the finding-24 conda-reachability predicate (`_modules_provide_conda` OR `conda_source`) with the submit-time `Activation` invariant — accept-at-submit and activate-at-control-plane are one definition (#33 drift closed) | `tests/contracts/test_activation_one_definition.py::test_every_reporter_consumer_seeds_remote_activation`, `::test_reporter_consumer_set_is_the_enumerated_list`, `::test_control_plane_activate_matches_submit_acceptance` | a reporter call site omits `remote_activation` (→ bare login python → rc=127), a new reporter consumer appears without being enumerated, or `remote_activation_prefix` and `Activation` disagree on whether a `conda activate` is emittable for a given cluster shape |
| The provenance gate's prior-nudge path (b) is TOKEN-EXACT, not a substring match — a nudge must NAME the field as a whole token (`result_dir_template`), so an unrelated mention ("seeds 0-19" does NOT authorize key `seed`; "running" does not authorize `run`) can't launder a diverted `resolved` field past conduct rule 9 (#26) | `tests/ops/test_decision_journal_primitives.py::test_gate_prior_nudge_is_token_exact_not_substring` | `_prior_nudge_named` reverts to `needle in response` (substring) so a partial mention authorizes a field the brief never recommended |
| Activation fields (`conda_source`/`conda_env`/`modules`) are `CALLER_OVERRIDABLE_DERIVED` — derived-by-default from the cluster but a caller pin WINS (`remote_activation_for_sidecar` tier-1, `test_explicit_env_activation_wins`), so they are exempt from `JOURNAL_UNAUTHORABLE_FIELDS`: append-decision no longer refuses a caller-supplied activation as a hand-authored derived field, resolving the 13-residual contradiction (the refusal fought the caller-wins contract). A genuinely code-owned derived field (`executor`) stays unauthorable | `tests/ops/test_decision_journal_primitives.py::test_activation_is_caller_overridable_not_journal_unauthorable` | activation is moved back into `JOURNAL_UNAUTHORABLE_FIELDS` (refusing a legit override), or a truly code-owned field like `executor` is added to `CALLER_OVERRIDABLE_DERIVED_FIELDS` |
| A clean-walk S1 (no ambiguities, no resolve spec) is the PRE-RESOLVE boundary — run_id is UNMINTED and resolve needs caller inputs the walk can't supply (`remote_path`); `next_block` STAYS the table target submit-s2 (the block↔SUCCESSORS agreement contract forbids special-casing it to None), but the brief's REASON flags run_id-unminted and directs the caller to supply resolve FIRST, so the agent doesn't read the submit-s2 pointer as "advance now" and jump the resolve leg into a hand-called submit-s2 (run #7 — fixed by the reason + the hpc-submit skill's pre-resolve step, NOT a routing change) | `tests/ops/submit/test_blocks.py::test_s1_clean_walk_without_resolve_reason_flags_pre_resolve_boundary` | `_submit_s1_impl`'s clean-walk-without-resolve brief drops the run_id-unminted / supply-resolve directive from its reason, or special-cases `next_block` away from the table's submit-s2 |
| The guaranteed terminal harvest treats an OPEN ssh breaker as a DEADLINE, not a verdict: on `SshCircuitOpen` whose structured `deadline` (attached by `ssh_circuit._open_error`, never parsed from the message) is within one BASE cooldown (`CIRCUIT_WAIT_CAP_SEC`), the guard sleeps to the deadline and retries the aggregate ONCE — the retry IS the sanctioned half-open probe, not the hammering the breaker forbids. A doubled cooldown (probe already failed → host genuinely unhealthy) or a deadline-less error records `harvest_ok:false` without waiting, as before (run #7: a 3×60s hoffman2 latency spike opened the breaker mid-harvest; the backstop recorded failure and parked a finished 20/20 run 292s short of the deadline its own exception named) | `tests/ops/monitor/test_harvest_guard.py::TestCircuitOpenBoundedRetry`; `tests/infra/test_ssh_circuit.py::test_open_error_carries_structured_host_and_deadline` | `harvest_on_terminal` records `SshCircuitOpen` without consulting `exc.deadline` (finished runs park on transient latency blips), waits on a cooldown past the cap / retries more than once (riding an unhealthy host), or `_open_error` stops attaching `host`/`deadline` so consumers are back to message-parsing |
| The `REQUIRED_CALLER_FIELDS` partition (`goal`/`task_generator`) is bound at the ELICITATION surface too, not only the gate: the interview skill elicits them as FREE-TEXT the human types, never a pre-filled `AskUserQuestion` option (a click carries no authorship → refused at `append-decision`), so elicitation and the `_assert_human_authorship` gate cannot contradict at runtime (run #7 — the ask-then-reject awkwardness) | `tests/contracts/test_authorship_elicitation_guidance.py::test_wrap_skill_elicits_required_caller_fields_as_free_text` | the wrap-entry-point SKILL drops the free-text elicitation rule, or a new `REQUIRED_CALLER_FIELDS` member is added without naming it in that rule |
| Every `decide_monitor_arm` caller projects `last_status` through the shared both-shape `summary_from_last_status` — `MonitorFlowResult.last_status` carries the per-task counts FLAT (no `"summary"` nesting), so a consumer that reaches for `last_status["summary"]` shears the counts to `{}` and a terminal run falls through to a `running_fallback` cron instead of `arm="none"` (run #8: a 20/20-complete S3 brief armed a `*/1 * * * *` monitor on a finished run) | `tests/ops/monitor/test_arm.py::test_summary_from_last_status_accepts_flat_and_nested`, `::test_flat_terminal_summary_through_arm_is_none`; `tests/ops/submit/test_blocks.py::test_s3_terminal_flat_last_status_arms_none`, `::test_s3_running_flat_last_status_arms_cron` | the S3 arm path (`submit_blocks`) re-inlines a `last_status["summary"]` lookup instead of `summary_from_last_status`, so a flat terminal `last_status` projects to `{}` and arms a cron on a complete run |
| Every surface that relays a `monitor_arm` brief carries the FULL cron lifecycle — `arm=="cron"` → `cron_create_args` to `CronCreate` VERBATIM (one cron per run: delete any prior first), `arm=="none"` / an unresolvable `run_id` → `CronDelete` every cron naming the run — because `decide-monitor-arm`'s `arm=none`-at-terminal is dead prose unless the agent guidance instructs the delete (run #8: the primitive doc mandated CronDelete but NO skill carried it — create was improvised from the brief, delete never happened, and a `*/1` headless monitor kept firing against a finished, then wiped, run) | `tests/contracts/test_monitor_arm_cron_lifecycle_guidance.py::test_monitor_arm_surfaces_document_the_full_cron_lifecycle`, `::test_status_skill_deletes_the_cron_for_an_unresolvable_run`, `::test_monitor_command_mirrors_the_delete_mandate` | the hpc-status/hpc-submit SKILL (or monitor-hpc command) drops the lifecycle rule, loses the CronDelete/VERBATIM binding, or stops covering the wiped-journal tick — regressing to create-improvised/delete-never |
| No UNATTENDED ssh — the cron-fired `block-drive --workflow status` path is JOURNAL-ONLY: `status-snapshot` is journal-first and the ungated `snapshot→status-watch` hop DETACHES (`status-watch` is detach-by-contract, in `SUPPORTED_DETACHED_BLOCK_VERBS`, connection-broker.md 2026-07-07), so the ONE cold dial rides a durable child and the unattended tick itself never dials; a re-firing tick returns a LIVE worker's handle (no second spawn) and a DEAD lease is re-spawned via the same launcher (never re-dialed inline), and only a GENUINE watch terminal (`watch_terminal`/`watch_anomaly`, not `watch_timeout`) is recorded so a re-invoke replays instead of re-dialing a completed run | `tests/ops/status/test_block_detach.py::test_unattended_watch_tick_does_zero_inline_ssh`, `::test_snapshot_chain_hop_into_watch_spawns_and_returns`, `::test_watch_returns_live_worker_handle_no_second_spawn`, `::test_watch_dead_lease_respawns`, `::test_watch_timeout_is_not_recorded_as_terminal` | `status_watch`'s `detach` branch calls `monitor_flow` (the ssh spine) inline instead of spawning, a re-firing tick spawns a second worker instead of returning the live handle, `status-watch` leaves `SUPPORTED_DETACHED_BLOCK_VERBS` (the hop dials synchronously again), or a `watch_timeout` gets recorded as a replayable terminal (the keep-watching self-loop wedges) |
| A reduction over a run carrying a LOCKED evidence scope is refused loudly at the one aggregate seam AND synchronously before the S4 detach; scope-less runs reduce byte-identically, and the recorded looks are PRIOR-by-construction (snapshot before the reduction's own look) and `(scope, run_id)`-deduped so a replay never inflates the count (rigor-primitives T3: `scope_gate.assert_scopes_unlocked` is ONE definition, TWO call sites — `ops/aggregate_flow` and the `ops/submit_blocks` S4 pre-detach seat — so a lock caught in the parent surfaces at the human boundary, never buried in a detached harvest child's log; a lock is deliberate human state — reducing it would spend a reserved look) | `tests/ops/test_scope_gate.py::test_locked_scope_refuses_reduction`, `::test_unlocked_and_untagged_runs_pass`; `tests/ops/submit/test_block_detach.py::test_s4_locked_scope_refuses_before_detach`, `::test_s4_unlocked_scope_detaches`; `tests/ops/aggregate/test_scope_rigor_flow.py::test_gate_fires_before_any_ssh_on_a_locked_run`, `::test_scope_looks_counts_prior_not_self`, `::test_replay_does_not_double_count_look`, `::test_scope_less_run_reports_none_and_is_byte_identical` | the gate call is dropped from either seat (the S4 detach or `aggregate_flow`), fires on an untagged / sidecar-less run (a false trip on a scope-less run), or `_record_scope_looks` stops snapshotting `count_prior_looks` BEFORE `record_look` (counting the current look into its own report) or drops the `(scope, run_id)` dedup (a replay double-counts a look) |
| The notebook graduation gate is ONE definition, TWO synchronous seats (notebook-audit D8): `ops/notebook_gate.assert_source_audited` is called at `ops/resolve_submit_inputs` PRE-SIDECAR (the S1 human boundary) AND `ops/submit_flow` PRE-STAGING (before any rsync/SSH) — so an opted-in source not signed at its current hash refuses at the y/nudge, never buried in a detached submit-flow worker's log, and the pre-staging seat still catches a bare submit-flow path or a source edited in the window after S1. Both seats call the ONE definition (no local variant); each fires BEFORE its downstream work (sidecar-write / shared-prelude) | `tests/ops/test_notebook_gate.py::test_resolve_seat_gate_fires_before_sidecar`, `::test_resolve_seat_passes_when_signed`, `::test_submit_flow_seat_gate_fires_before_staging`, `::test_submit_flow_seat_passes_proceeds` | the gate call is dropped from either seat (resolve pre-sidecar or submit-flow pre-staging), a seat inlines its own audit check instead of calling `assert_source_audited`, or either seat's gate stops firing before its sidecar-write / staging work |
| The notebook graduation gate is OPT-IN + FAIL-SAFE and refuses by NAMING the offending sections (notebook-audit D7/D8, the `scope_gate` posture): NO `audited_source` block on interview.json → byte-identical no-op (no interview.json, or one without the key — the ONLY filesystem probe on the opt-out path); opted in → every required (template) section must be signed at its CURRENT hash (`signed_current`/`auto_cleared`), a section edited after signing reads unsigned by construction (drift = unsigned, no state machine), and a drifted/missing `linked_sources` dependency revokes the section too; any offender raises `errors.SourceUnaudited` (reuses `precondition_failed`, no new error_code) naming each section+status; an opted-in repo whose source/template `.py` is missing/unparseable is a LOUD `SpecInvalid` naming the path, never a silent pass | `tests/ops/test_notebook_gate.py::test_no_interview_json_is_silent_noop`, `::test_interview_without_audited_source_is_silent_noop`, `::test_all_sections_signed_current_passes`, `::test_one_unsigned_section_fires_naming_it`, `::test_edit_after_sign_fires`, `::test_linked_source_drift_fires`, `::test_missing_source_file_is_loud`, `::test_missing_template_file_is_loud` | the gate probes the filesystem (or raises) when no `audited_source` is present (a false trip on an undisciplined repo), stops naming the drifted/unsigned sections, treats a signed-then-edited section as still signed, ignores a drifted `linked_sources` entry, or silently passes an opted-in repo whose audit `.py` is missing/unparseable instead of refusing loudly |
| A scope UNLOCK faces the human-authorship bar — a bare ack or clicked option cannot unlock, the rationale must derive from a logged human utterance when the harness tier is present, the unlock is journaled permanently (`block="scope-unlock"`, `resolved.scope_action="unlock"`, append-only so it never erases the lock history), and it has NO agent affordance (no unlock verb; not in any chain/`next_block`/skill sequence — locking is the safe direction and needs no bar, unlocking RELAXES the reservation so it is a deliberate HUMAN act) | `tests/ops/test_decision_journal_primitives.py::test_unlock_bare_ack_refused`, `::test_unlock_human_typed_rationale_passes_journal_response_tier`, `::test_unlock_human_typed_rationale_passes_utterance_tier`, `::test_unlock_utterance_mismatch_refused`, `::test_unlock_block_convention_enforced_both_directions` | `_assert_unlock_authorship` is unwired from `append_decision`'s gate stack, accepts a bare ack (`_is_bare_ack`) as an unlock, drops the both-directions `scope-unlock` block convention, or a `scope-unlock` verb / chain-reachable unlock step appears in any `next_block`/chain/skill table (an affordance the bar exists to deny) |
| A notebook sign-off requires the content-addressed render current at append (the trusted-display lock, v1.5): the audit view relayed in chat is model-carried and unforceable, so the T8 gate additionally refuses unless the CODE-written render file addressed by the resolved `view_sha` (`.hpc/renders/<audit_id>/<slug>.<view_sha12>.md`) exists, parses, agrees on `view_sha`/`section`, and carries a header `section_sha` equal to the gate's FRESHLY RECOMPUTED sha — so an edit-after-render (record sha updated but the render never re-run) is caught even though the record's own sha is already covered by `attestation.bind`. `notebook-audit-view` writes that artifact per section with DETERMINISTIC bytes (content-addressed → idempotent); the requirement applies to redundant/auto-cleared sign-offs too (the human claims a review; the artifact must exist). The gate reaches `render_store` through the `notebook_view` facade (subject-import lint), never a re-implemented reader | `tests/ops/test_decision_journal_primitives.py::test_signoff_no_render_file_refused`, `::test_signoff_render_present_and_current_passes`, `::test_signoff_edit_after_render_refused`, `::test_signoff_render_view_sha_mismatch_refused`, `::test_signoff_redundant_requires_render`; `tests/ops/notebook/test_view_op.py::test_view_writes_byte_deterministic_renders` | `_assert_signoff_render_current` is unwired from `_assert_signoff_authorship`, lands a sign-off with no render file / a stale-sha render / a mismatched-view_sha render, waives the render for a redundant sign-off, or `notebook-audit-view` stops writing the deterministic render (the trusted display reverts to the unforceable chat relay) |
| A notebook sign-off RECOMPUTES `view_sha` in full (the full-view-recompute upgrade, v1.6 — the "statically-recomputable legs only" boundary is RETIRED): the T8 gate rebuilds the CANONICAL section view SERVER-SIDE and refuses unless the recomputed per-section `view_sha` equals the resolved one. The canonical view has ONE definition — `ops/notebook/canonical.py::build_canonical_view` (real lint recomputed in-process from the RECORDED roots, journaled FRESH receipts, recorded `attention_order`; never a caller finding) — and the gate, `notebook-audit-view`, `notebook-auto-clear`, and the render plugin all route through it, so their view shas agree by construction. The recorded config (`input_roots`/`source_roots`/`attention_order`) is persisted on interview.json's `audited_source` (all optional, None-default so `exclude_none` keeps a config-less block byte-identical). Since the bind lock covers section-body drift and the trusted-display lock covers a stale render, a `view_sha`-only mismatch names the moved VIEW ingredient (a vanished data path under `input_roots`, a changed receipt, or a changed order); the tier is now REAL so a section human-required SOLELY by a lint flag refuses a bare-slug sign-off. The gate reaches the helper through the `notebook_view` facade (subject-import lint), and a template that cannot be resolved is REFUSED (the view is a diff-from-template projection) | `tests/ops/test_decision_journal_primitives.py::test_signoff_stale_view_sha_refused_when_lint_input_vanishes`, `::test_signoff_tier_by_real_lint_flag_refuses_bare_slug`, `::test_signoff_canonical_flow_end_to_end`, `::test_signoff_preview_view_sha_refused`, `::test_signoff_no_template_refused`, `::test_signoff_gate_routes_through_canonical_view`; `tests/ops/notebook/test_canonical.py::test_build_canonical_view_recomputes_lint`; `tests/ops/memory/test_interview.py::test_audited_source_config_persisted_verbatim`, `::test_audited_source_config_absent_is_byte_identical` | the gate validates `view_sha` present without recomputing it, re-inlines its own view build instead of calling `build_canonical_view`, trusts a caller-supplied finding/receipt into the tier, the verbs/plugin build a divergent view the gate then refuses, a config-less `audited_source` block leaks empty-list config keys (breaking byte-identity), or an absent template is softened to conservative-human-required instead of refused |
| The reproduction receipt is COMPUTED by code under a caller-owned tolerance (`ReproTolerance`: default/per-key abs+rel), and a mismatch or an incomparable pair is a FINDING (`stage_reached` `mismatch`/`incomparable`, `needs_decision=True`, exit-0) — NEVER a raised internal error, so a divergent reproduction lands at the human boundary as evidence, not a crash. The comparator carries NO metric-name special cases (`n_samples` compares like any other number; a NaN is incomparable, not a raw `!=`) and prefers `metrics_aggregate.json` over a stale `_combiner` wave by ladder, not by key. The receipt only verdicts a GENUINE reproduction pair — the repro sidecar's `reproduces` MUST name the original, else the verb refuses `SpecInvalid` (T6 reproduction-receipt) | `tests/ops/aggregate/test_verify_reproduction.py::test_n_samples_no_special_casing`, `::test_nan_is_incomparable_not_a_raw_neq`, `::test_metrics_aggregate_preferred_over_combiner`; `::test_exact_mismatch_is_successful_finding`, `::test_incomparable_missing_repro_artifact`; `::test_refuses_when_reproduces_is_none`, `::test_refuses_when_reproduces_names_wrong_original` | `verify_reproduction`'s comparator special-cases a key name (an `n_samples`-style exemption), a mismatch/incomparable returns a non-zero exit or an internal-error envelope instead of a `needs_decision` finding, or the reproduces-pair refusal is dropped so the receipt verdicts a non-reproduction pair |
| A `claim-check` is NEVER laundered into a reproduction (onboard-by-reproduction ruling 6b): the external-baseline mode of `verify-reproduction` compares a fresh OBSERVED run against a human-authored claim, writes a `receipt_kind: "claim-check"` receipt to `claim_check_receipts.jsonl` (never the reproduction ledger), embeds the claim verbatim, and appends NO fingerprint sample (the observed-runs-only lock — an unobserved claim can never widen the envelope). Both receipt-write paths route through the ONE anti-laundering seam `verify_reproduction._assert_receipt_kind_matches_baseline`, which refuses a reproduction-kind receipt paired with an external baseline (and a claim-check with none) — the launder is impossible by construction. On a match the receipt/brief carry the CODE-emitted consistency sentence ("the claim is consistent with a fresh observed run (within caller tolerance)"), never an LLM characterization; a mismatch is a dated FINDING (`needs_decision`, exit-0) whose drift disclosure names the data dimension WITH a manifest ("the data changed since the claim") or discloses its absence WITHOUT ("cannot distinguish result decay from data drift — no manifest"). The two modes are mutually exclusive at the spec (`external_baseline` forbids `original_run_id` + top-level `tolerance`) (Phase 6.5 onboard-by-reproduction) | `tests/ops/aggregate/test_verify_reproduction.py::test_reproduction_receipt_never_written_with_external_baseline`, `::test_claim_check_receipt_kind_is_claim_check_never_reproduction`, `::test_claim_check_appends_no_fingerprint_sample`, `::test_claim_check_match_emits_consistency_sentence`, `::test_claim_check_mismatch_with_manifest_names_data_dimension`, `::test_claim_check_mismatch_is_a_finding_never_blocking`, `::test_claim_check_embeds_claim_verbatim`, `::test_spec_external_and_original_are_mutually_exclusive`, `::test_recorded_reproduction_receipt_carries_reproduction_kind` | a claim-check receipt is written to the reproduction ledger or with `receipt_kind: "reproduction"`, the claim-check mints a fingerprint sample (an unobserved claim widens the envelope), the consistency/drift line becomes LLM-composed, the anti-laundering seam is unwired from either write path, or `external_baseline` stops being mutually exclusive with the recorded-original knobs |
| The MCP elicitation SEND side is free-text-only BY CONSTRUCTION: the `requestedSchema` the server emits (`mcp_server._ELICITATION_REQUESTED_SCHEMA`) contains string fields only — never `enum`, never option lists — so there is nothing to click and the clicked-option hazard is closed before the receive-side filter (`_accepted_utterance`: `is_harness_injected` + typed-only + non-empty) ever runs; a response with no qualifying free text is a decline, and the original refusal envelope returns unchanged (mcp-elicitation D3/D5) | `tests/test_mcp_elicitation_firing.py::test_non_capture_outcomes_return_original_refusal`, `::test_timeout_returns_original_refusal`; the schema shape is pinned by the firing tests importing `_ELICITATION_REQUESTED_SCHEMA` | the requested schema grows an `enum`/options field (a clickable answer becomes launderable as typed text), or a non-capture outcome starts mutating the refusal envelope |
| The elicitation PROMPT is CODE-RENDERED, never model-authored: `mcp_server._render_elicitation_prompt` is a pure function over code-selected identifiers (block, scope kind/id, section slug) and deliberately never embeds the model's `proposal`/`response` free text NOR the refusal envelope's `message` (which can quote model text) — if the model could author the prompt it would bait the human's reply into the trust anchor, the exact laundering channel the authorship gate closes (mcp-elicitation D5) | `tests/test_mcp_elicitation_firing.py::test_prompt_has_code_selected_identifiers_and_no_model_free_text`, `::test_prompt_omits_section_for_non_notebook_scope` | the prompt renderer gains an input slot carrying model-authored text, or starts quoting the refusal message |
| The E-render sign-off popup is the PRIMARY read-and-sign channel with a HARD fallback, and its digest is HONEST-OR-REFUSES (mcp-elicitation E-render amendment, 2026-07-09): for a human-required notebook sign-off the server elicits FIRST (`mcp_server._elicit_then_retry`) — the model never sees the interim refusal — but ONLY when `_elicitation_applies` holds (append-decision + client declared elicitation + not `_client_elicitation_dark` + live transport + none in flight), so an undeclared/declared-but-dark/timed-out client degrades byte-for-byte to the pre-promotion refusal → hook path and a valid (`ok:true`) append never pops; the promotion is server-orchestration only (the authorship gate `_assert_signoff_authorship` is untouched, one-definition). The DIGEST v2 (`_render_digest_block`) is a signing surface — BIND (identity + freshness; a stale/missing content-addressed render says do-NOT-sign + pointer only, never a summarized digest), WHY (tier-trigger headline, declared-assertion table marked unverified since the trusted render is STATIC — no fabricated computed value, lint-flag NAMES+locations, per-hunk one-liners — never the diff body), ROUTE (the on-disk path) — and when the honest digest overruns `_DIGEST_BLOCK_MAX_BYTES` it emits `too large to digest honestly: N hunks, M flags — read the render` rather than compress until a judgment-critical item silently drops; every capped list discloses its elision | `tests/test_mcp_elicitation_firing.py::test_primary_popup_fires_before_any_refusal_for_notebook_signoff`, `::test_valid_utterance_append_never_pops`, `::test_client_without_capability_no_elicitation`, `::test_timeout_marks_session_dark_next_refusal_immediate`; `tests/test_mcp_elicitation_render.py::test_prompt_embeds_digest_v2_three_jobs`, `::test_oversize_digest_emits_honest_refusal_not_a_silent_trim`, `::test_capped_lists_disclose_elision_never_silent`, `::test_stale_render_says_do_not_sign`, `::test_prompt_surfaces_lint_flag_names_and_locations`; `tests/ops/notebook/test_render_store_digest.py::test_digest_carries_tier_and_diff_hunk_one_liners`, `::test_digest_lists_lint_flag_names_and_locations` | the popup starts surfacing an interim refusal to the model before eliciting, a dark/undeclared/timed-out session stops degrading byte-for-byte (a wedge on a flow that used to work), a valid append pops, the firing promotion reaches into `_assert_signoff_authorship`, the digest summarizes a stale render as current, fabricates a computed assertion value the static render lacks, embeds the diff body, or silently trims a capped list / a judgment-critical item instead of the honest-refusal-with-counts |
| The durable comparator artifact `_aggregated/<run_id>/metrics_aggregate.json` has ONE persistence definition — `ops/aggregate_flow._persist_local_aggregate` — and EVERY local-reducing path routes its reduced metrics through it: the SSH combiner-only default, the pure-API path, AND the cluster-reduce path, so verify-reproduction reads a byte-readable artifact regardless of which reduce ran (closes verifier finding L2: only the default used to persist, so a cluster-reduced / pure-API run was honestly-but-needlessly `incomparable`). A cluster-reduce whose reducer emits a non-dict JSON persists `aggregated_metrics: {}` — the honest empty, never a fabricated scalar comparison. The opt-in cluster-final (`HPC_CLUSTER_FINAL_REDUCE`) path keeps its RICHER cluster-produced aggregate but lands it at the SAME canonical FLAT location (not the old `_aggregated/<id>/_aggregated/<id>/` nest verify never read) (T6 reproduction-receipt, L2) | `tests/ops/aggregate/test_reduce_paths_persist_for_verify.py::test_pure_api_path_persists_artifact_with_honest_source`, `::test_pure_api_original_and_reproduction_are_comparable`, `::test_cluster_reduce_path_persists_artifact_with_honest_source`, `::test_cluster_reduce_original_and_reproduction_are_comparable`, `::test_cluster_reduce_non_dict_reducer_output_persists_empty_and_stays_incomparable`, `::test_cluster_final_pull_lands_at_flat_verify_read_location`; `tests/ops/aggregate/test_flow_persists_local_aggregate.py::test_default_local_reduce_persists_durable_artifact` (default byte-parity regression pin) | a reduce path (pure-API / cluster-reduce) returns without calling `_persist_local_aggregate` (verify-reproduction goes `incomparable` for that mode again), a path re-inlines its own `metrics_aggregate.json` writer instead of the ONE seam, a non-dict cluster-reduce output fabricates a scalar aggregate, or the cluster-final pull re-nests the artifact where no comparator looks |
| The reproduction dedup lever is SURGICAL: `reproduction_of` pierces `cmd_sha` (parameter-identity, #207) dedup ONLY for the NAMED original and its prior reproductions (a sidecar whose `reproduces` equals the original), so a deliberate re-run of identical params actually RUNS instead of silently replaying the run it means to reproduce — while an UNRELATED same-params prior (different run_id, no matching `reproduces`) still dedups, and the lever UNSET is byte-identical to the historical match-on-identity behaviour. It is ONE lever bound at BOTH dedup layers (`state.runs.find_run_by_cmd_sha` and the `submit_and_record` runner) and threaded through the reproduce resolve, which stamps `reproduces` onto the repro sidecar and refuses when an unrelated live prior blocks the fresh resolve (T1 reproduction lever) | `tests/state/test_runs_reproduction_lever.py::test_reproduction_of_skips_the_original`, `::test_reproduction_of_skips_a_prior_reproduction_of_the_same_original`, `::test_reproduction_of_leaves_an_unrelated_prior_as_a_dedup_target`, `::test_lever_unset_dedups_against_the_original`; `tests/ops/submit/test_runner_reproduction_lever.py::test_layer2_reproduction_does_not_dedup_against_its_original`, `::test_layer2_reproduction_skips_a_prior_reproduction_too`, `::test_layer2_still_dedups_an_unrelated_identical_submit`, `::test_layer2_reproduction_still_dedups_against_an_unrelated_prior`; `tests/ops/submit/test_reproduce_run.py::test_reproduction_of_and_reproduces_stamped`, `::test_resolve_blocked_short_circuits_before_handoff` | the lever skips a match it did not name (an unrelated same-params prior stops deduping), unset behaviour drifts from byte-identical dedup, or only one of the two layers routes through it |
| The run story is a PURE projection — every event is IDENTITY (which run/scope/section) + ORDERING (recorded ts) + COUNTING (sha pointers, row/job counts) over OPAQUE records, never a metric value, a role, or LLM narrative: the event shape is the closed D3 key set, the stream vocabulary is the dossier's stores MINUS the two opaque ones (`aggregated`/`sidecar`) PLUS the ratified `overnight-ledger` Class-C2-finding source (overnight-repair §7.4), no wire model names domain semantics, the render path imports nothing LLM-adjacent and no `_wire` + takes no free-prose parameter, and a metric value crafted into evidence is DROPPED from the rendered line (counts-only) (run-story D3/D4/T5) | `tests/contracts/test_run_story_boundary.py::test_event_dict_construction_is_exactly_the_d3_key_set`, `::test_stream_vocabulary_is_dossier_sources_minus_the_opaque_stores`, `::test_wire_models_expose_no_domain_vocabulary`, `::test_render_path_imports_nothing_llm_adjacent_and_no_wire`, `::test_render_entry_point_takes_no_free_prose_parameter`, `::test_counts_only_metric_value_never_reaches_the_line`; `tests/ops/test_story_render.py::test_counts_only_metric_value_never_renders` | an event dict grows a fifth meaning-bearing key, `STREAMS` admits `aggregated`/`sidecar` (or drifts from the closed set), a wire field is named for a caller role, the render path imports an LLM/prose module or `_wire` or grows a prose parameter, or a metric value reaches the rendered markdown line |
| The run story has ONE ordering definition — `state/run_story.merge_events` (the D2 `(ts, stream_rank, intra_index)` merge) is the single `sorted(...)` over events, and the assembly op windows by SLICING and NEVER re-sorts; a second ordering re-derivation forks the timeline (run-story D2/T5) | `tests/contracts/test_run_story_boundary.py::test_state_module_orders_events_only_in_merge_events`, `::test_assembly_op_never_re_sorts_the_timeline`; `tests/state/test_run_story.py::test_merge_stream_rank_tie_break`, `::test_merge_preserves_intra_stream_order`, `::test_merge_ts_major`; `tests/ops/test_run_story_op.py::test_events_are_exactly_the_merge_order` | `state/run_story.py` grows a second `sorted()` over events, `ops/run_story.py` calls `sorted(...)`/`.sort(...)` (re-deriving the order instead of routing through `merge_events`), or the op's rendered events diverge from `build_story`'s merge order |
| Run-story windowing is NEVER silent — a `since_ts`/`limit` window surfaces `total_events` + `omitted_count` in both the result and the markdown ("showing N of M events (K older events omitted)"), and `story_sha` is taken over the WINDOWED canonical JSON (the counts ride the pre-image), so a window can never masquerade as the whole story nor a sha be passed off as covering events it omits (run-story D6/T5) | `tests/ops/test_story_render.py::test_window_header_honesty`, `::test_story_sha_sensitive_to_content_and_counts`, `::test_no_window_renders_plain_count`; `tests/ops/test_run_story_op.py::test_limit_window_reports_honest_counts`, `::test_since_ts_floor_drops_and_counts_older_events` | a windowed render omits the "omitted" line, `story_payload` drops `total_events`/`omitted_count` from the sha pre-image, or the op returns a windowed event list without the honest full-count/omission-count pair |
| Every attention-queue collector routes through its named source predicate (`find_parked_runs`/`is_latest_committed_greenlight`, `find_stalled_runs`, `scan_dead_detached_workers`, `digest_run`+`ANOMALY_STATUSES`+`recommendation_for`, `latest_decision`, `audit_module`, `read_unacknowledged_alerts`, `open_circuit_lines`) — the queue AGGREGATES + orders, never re-implements a liveness/staleness/greenlight verdict; and the D2-revision fan-out is COUNTED from record structure (`find_runs_by_campaign`+`TERMINAL_STATUSES`, the sidecar `audited_source` echo glob), never scored (attention-queue D5 + D2-revision) | `tests/ops/attention/test_queue.py::test_route_through_source_symbols`, `::test_fanout_walk_routes_through_campaign_membership_predicate`, `::test_collect_items_stamps_greenlight_fanout_one`, `::test_collect_items_stamps_audit_fanout_from_sidecar_echoes`, `::test_collect_items_stamps_campaign_fanout_from_remaining_runs` | a collector re-inlines its predicate instead of calling the source symbol, or the fan-out walk fabricates a leverage number instead of counting an encoded edge (a kind with no encoded edge gaining a non-zero `unblocks`) |
| The JSONL append discipline (flock + fsync + sort_keys, whole-line-atomic, append-only) has ONE definition — `infra/io.append_jsonl_line` — every append-only ledger routes through: the decision journal / decision briefs / scope look ledger (`state/decision_journal._append_jsonl_line` delegates to it) AND the guaranteed-harvest marker (`ops/monitor/harvest_guard._write_marker`, keeping its never-raise `finally`-safe wrapper because the seam CAN raise OSError). Because prior lines stay whole, the harvest-ledger reader (`aggregate_blocks._harvest_ledger_tail`) scans BACKWARD to the newest PARSEABLE marker — a crash-torn final line falls back to the prior good one; only an entirely-unparseable ledger yields None (B2, the "finished run parked" class) | `tests/infra/test_atomic_locked_update.py::test_append_jsonl_line_concurrent_appends_stay_whole_lines`; `tests/ops/monitor/test_harvest_guard.py::test_write_marker_routes_through_canonical_append_seam`, `::test_write_marker_swallows_seam_oserror_and_harvest_continues`; `tests/ops/aggregate/test_blocks.py::test_harvest_ledger_tail_falls_back_over_torn_final_line`, `::test_harvest_ledger_tail_none_only_when_entirely_unparseable` | `_write_marker` re-inlines a bare `open(...).write` bypassing the seam (interleaved bytes / no fsync), the harvest-ledger reader reverts to `json.loads(lines[-1])` (a torn tail strands the whole marker), or the never-raise wrapper around the seam is dropped (a full-disk OSError masks the terminal cause) |
| The attention queue is watermark-neutral and store-free — no `mark_seen_by_human`, no `acknowledge_alerts`, no file write anywhere under the attention package (`ops/attention_queue.py`, `ops/attention_render.py`); it is a standing TODO recomputed on every read, never persisted (attention-queue D6) | `tests/ops/attention/test_queue.py::test_no_write_calls_in_the_package_source`, `::test_collect_queue_is_watermark_neutral` | a mutation call (`mark_seen_by_human`/`acknowledge_alerts`/`append_decision`/…) or a write path (`.write_text`/`.mkdir`/`atomic_write_json`) appears in the attention package, or collecting advances a seen/ack watermark |
| Snapshot v2 and the `attention-queue` verb share ONE ordering definition — `status-snapshot`'s additive `attention` brief field and the verb both route through `ops/attention_queue.collect_queue`/`order_items`; the snapshot never re-sorts or re-collects (attention-queue D2/D4) | `tests/ops/status/test_snapshot_attention.py::test_snapshot_brief_carries_attention_ordered_by_the_one_seat`, `::test_snapshot_calls_the_shared_collect_queue_seat_not_a_local_sort`, `::test_snapshot_attention_is_empty_and_additive_when_nothing_pending` | `status_blocks` re-sorts or re-collects the queue inline instead of calling `collect_queue`, or the snapshot's `attention` order diverges from the verb's |
| The detached-failure terminal is HONEST about disclosure — it never asserts the worker log carries a failure the write path cannot guarantee (run-#13 finding 2: a worker died exit-2 on a VPN-severed scp having flushed nothing, yet the terminal claimed "the log carries the disclosed failure"). The message + the `log_disclosed` flag are DERIVED from a bounded log-tail read (`crash_disclosure.log_has_fatal_marker`): a flushed `[fatal]` marker → "[fatal] block present", none → "WITHOUT disclosure … last log line: <line>". The worker side arms `faulthandler` + flushes a `[fatal]` block on every catchable exit path so the marker is really there when disclosure ran | `tests/cli/test_detached_failure_terminal.py::test_terminal_honest_when_fatal_block_present`, `::test_terminal_honest_when_no_disclosure`, `::test_terminal_no_disclosure_when_log_env_unset`; the worker/read seam `tests/_kernel/lifecycle/test_crash_disclosure.py` (emit fires per exit path, faulthandler dumps on a hard signal, the tail read reports marker + last line) | `_record_detached_failure_terminal` stamps a fixed "the log carries the disclosed failure" message without reading the log tail, or `log_disclosed` disagrees with the `[fatal]` marker's presence |
| Stack-minted local output stores are deploy-excluded LOCKSTEP with their mint-site names — every dir in `ops/aggregate_flow.py::LOCAL_PULL_MIRROR_DIRNAMES` (the no-combiner fallbacks' `_per_task_results` / `_per_task_traces` mirrors) and `ops/export_dossier.py::DOSSIER_DIRNAME` (`_dossier`) appears in `infra/transport.PROTECTED_OUTPUT_DIRS` as `<name>/`, so a code deploy can never re-ship a prior run's analysis outputs / export archives as "code" (run-13 finding 4: run 12's 2,700-file mirror rode a deploy back to the cluster as a 1.18 GB payload) | `tests/infra/test_pull_dest_excludes.py::test_minted_pull_dests_are_all_deploy_excluded`, `::test_lockstep_pin_fires_on_rename` | a mint constant (`_per_task_results` / `_per_task_traces` / `_dossier`) is renamed / added without the matching `PROTECTED_OUTPUT_DIRS` entry |
| The combine's per-task mirror compares each source piece's cmd_sha (`.hpc_cmd_sha`, fallback mtime+size) against its cached copy and refreshes on drift (run-13 finding 13-addendum: an idempotent-by-task-id cache reduced a graft's stale pieces into a wrong citable table); a refreshed task invalidates its wave partial (`combined_waves`→`failed_waves`) | `tests/ops/aggregate/test_flow_stale_mirror.py` (fires: stale cached piece with moved cmd_sha is evicted + clean-re-pulled; passes: unchanged pieces are not re-pulled, first harvest is a single pull) | `_per_task_metrics_reduce` drops `.hpc_cmd_sha` from the pull include, stops evicting a cached task dir whose fingerprint moved, or stops invalidating the touched wave partial |
| Opaque blocking cluster-side stages (combine wave, final reduce) emit start/heartbeat/end `[transport]` progress lines — the >10s-progress rule applied to the aggregate worker's silent stage (run-13 finding 12c: a 25-min combine was indistinguishable from a hang) | `tests/infra/test_combiner_progress.py::test_run_combiner_emits_stage_lines`, `::test_run_final_reduce_emits_stage_lines`, `::test_stage_heartbeat_fires_on_slow_work` | `run_combiner_checked` / `run_final_reduce` run their blocking ssh without the stage-heartbeat wrapper, or the heartbeat stops emitting on slow work |
| A connection-level SSH success must not reset the breaker's incident open-cycle counter (`reopen_cycles`) — only incident-window expiry does; ≥`DEGRADATION_CYCLE_THRESHOLD` cycles classify a degraded preamble, NAME the hanging stage, and suggest `host-retarget <sibling>`; circuit-open remediation prose demands verification with the SAME command class that failed, never a bare connect (run-13 findings 10/12b: the probe-OK/preamble-timeout livelock rode 9+ ticks while the prose licensed an echo "verification") | `tests/infra/test_ssh_circuit.py::TestPreambleDegradation` (esp. `test_connection_success_preserves_the_reopen_cycle_counter`, `test_two_cycles_classify_degradation_naming_the_preamble`); `tests/ops/recover/test_net_triage.py::test_breaker_open_remediation_carries_degradation_and_host_retarget`; `tests/ops/monitor/test_flow_poll_tolerance.py::test_degraded_preamble_breaker_surfaces_host_retarget_on_the_tick` | `record_connection_success` wipes the cycle fields again, the classifier stops naming the stage / suggesting a sibling, or the remediation prose re-licenses a bare-connect check |
| The relay-audit tokenizer is flood-precise and the brief corpus is complete (run-13 finding 8): a bare month-day date ('07-09'), a hyphenated count phrase ('300-task') or 'run-<word>' compound, a du-style unit-suffixed size ('886M'), and a state word quoted from a log line or adjacent to 'canary' are span-consumed / skip-with-accounting, never contradictions; a brief string field's numbers ('300 … = 3600 core-hours') pool PER-TOKEN so a verbatim relay of the brief's own cost line verifies clean | `tests/ops/test_verify_relay.py` (the run-13 finding-8 fires-and-passes block: month-day, hyphenated-count, run-level, log-quote timeout, canary-adjacent verification, unit-suffix, brief cost-line pairs) | `_is_run_id_like` re-widens, `_collect_source_numbers` reverts to the whole-string identifier skip, or the month-day / size-suffix / log-quote / canary guards stop consuming — reintroducing the false-positive flood |
| The relay-audit Stop hook never floods the operator: surfaced corrections are deduped across runs/passes (claim, kind, scope-stripped detail) and capped at `_MAX_SURFACED_CORRECTIONS=3` severity-ordered (state>run_id>number) with an explicit "+K more suppressed" tail; a decision-state verb inside a quoted/fenced line is the model quoting the gate's own render, not a fresh claim (run-13 findings 8/8-addendum: 7-10 noise corrections per boundary taught the operator to ignore the gate) | `tests/_kernel/hooks/test_relay_audit_stop.py::test_rejector_rate_bounds_correction_flood`, `::test_rate_bound_orders_state_before_number`, `::test_cross_pass_dedup_collapses_repeated_claim`, `::test_completer_rate_bounds_flood_with_suppressed_tail`, `::test_state_claim_ignores_quoted_brief_decision_line`, `::test_state_claim_still_fires_on_bare_assertion` | the cap/dedup is removed or bypassed on either output path, the suppressed tail disappears, or quoted-brief lines re-enter decision-state attribution |
| The relay-audit cross-scope corrector checks each notebook-audit claim against ONLY the journal of the audit that provably owns it (run-14 finding 5): a status/sha claim binds to the audit whose id is mentioned NEAREST it (`_nb_claim_ownership`, tri-state), a sibling-owned claim is checked under the sibling's scope, and an EQUIDISTANT tie is owned by NEITHER → NO correction under any scope (a false correction shown to the human is worse than silence), the tied span counted-and-disclosed via the shared `ambiguous_out` set (deduped across scopes) instead of fired as a confident false correction against the wrong journal; the completer discloses the count on its non-blocking `systemMessage` unconditionally, the rejector only piggybacks on a block it is already emitting (never manufacturing a bounce to inform) | `tests/ops/test_verify_relay.py::test_notebook_sibling_audit_sha_not_checked_against_this_journal`, `::test_notebook_ambiguous_tie_yields_no_correction_under_either_scope`, `::test_notebook_ambiguous_tie_counted_once_across_scopes`, `::test_notebook_owned_wrong_claim_flagged_and_not_recorded_ambiguous`, `::test_notebook_genuinely_wrong_claim_about_this_audit_still_flagged`; `tests/_kernel/hooks/test_relay_audit_stop.py::test_rejector_discloses_ambiguous_scope_skip_alongside_a_finding`, `::test_rejector_never_manufactures_a_block_solely_to_disclose_ambiguous`, `::test_completer_discloses_ambiguous_scope_skip_even_when_alone` | `_nb_claim_ownership` re-widens a tie back into scope (the pre-ruling `sibling >= mine` confident false correction returns), an ambiguous skip stops being counted/disclosed (a silent cap), the rejector manufactures a block solely to disclose an ambiguous skip (blocking a clean relay), or a sibling-owned/owned claim stops being checked under its own scope |
| Lazy-root eager smoke covers every deferred symbol (latency unit 1.3: `src/hpc_agent/__init__.py` defers its heavy imports via PEP-562 `__getattr__`) | `tests/contracts/test_eager_import_smoke.py::test_eager_smoke_covers_the_deferred_set` | a `__getattr__`-deferred name is missing from the eager import test — a symbol could silently break under lazy resolution with no fire path |
| The fused Stop-hook prefilter is a sound necessary condition and stdlib-only: it may skip dispatch ONLY when no guard could act (neither `<cwd>/.hpc` nor the journal home exists), and it never imports `hpc_agent` or re-implements a guard predicate (latency unit 1.7 — 3 interpreters/turn fused to 1 with a <0.4s no-op floor) | `tests/_kernel/hooks/test_stop_multiplex.py::test_prefilter_negative_means_every_guard_is_noop` + `::test_noop_turn_does_not_import_guard_modules` | any input a guard would act on fails the prefilter, or the prefilter imports hpc_agent / re-implements a guard predicate |
| Source-of-truth ledger appends are DURABLE-OR-LOUD: `append_jsonl_line` defaults `fsync_required=True` (an fsync OSError surfaces as the verb error — no ack without durability; RULED 2026-07-16: the one-shot CLI fallback is byte-identical MODULO this shared durability fix), restores a torn tail line boundary inside the lock, and honors `dedup_key` replay no-ops | `tests/infra/test_io_durability.py` (fsync-raises -> raise; torn tail -> both records parse; dedup replay) | the seam re-suppresses source-of-truth fsync failures, stops healing torn tails, or replays append duplicates 
| Latency plan Row 19 (unit 4a) — The fused `block-drive --approve` commit routes through the ONE `append_decision` definition: one `--approve` tick journals the greenlight through `block_drive._commit_fused_approval` → `AppendDecisionInput.model_validate` → the SAME `ops.decision.journal.append_decision` a standalone `append-decision` calls, so every trust-seam gate (bare-ack refusal, human-authorship on `REQUIRED_CALLER_FIELDS`, code-derived-field refusal, token-exact prior-nudge, unlock authorship, malformed-payload `SpecInvalid`) fires IDENTICALLY on the fused path — there is no second "lite" commit path to keep in sync — and a refused commit NEVER advances the driver (the block verb must not run); a `dry_run` tick neither journals nor advances. R3 CONSUMPTION LEG (same unit): a plain-`y`/approve ADVANCE runs the successor UNDER the code-composed sha-pinned materialized spec (`.hpc/specs/next/<run_id>.<verb>.json`) — `block_drive._r3_materialized_advance_spec` recomputes `block_chain.successor_spec_sha` and compares it to the park-time `next_spec_sha`; a match advances under the spec the human saw, a drift REFUSES LOUDLY (nonzero exit, `action="skip"`) and keeps the pending marker so the human can re-approve, and no stamp/sidecar is byte-identical to the pre-R3 resolved-derived advance | `tests/_kernel/lifecycle/test_block_drive_fused_approve.py::test_one_approve_call_journals_and_returns_next_brief`, `::test_fused_path_refuses_a_code_derived_field_like_append_does`, `::test_fused_path_refuses_a_malformed_payload`, `::test_dry_run_does_not_journal_the_approval`; `tests/_kernel/lifecycle/test_block_drive_specs.py::test_r3_plain_y_advance_runs_the_sha_verified_materialized_spec`, `::test_r3_sha_drift_refuses_loudly_and_keeps_the_marker`, `::test_r3_falls_back_to_resolved_when_no_sidecar_materialized` | `_commit_fused_approval` re-implements the commit instead of routing through `append_decision` (a second commit path drifts from the gate stack), the fused path accepts something a standalone `append-decision` refuses, the driver advances after a refused/dry-run commit, or `_r3_materialized_advance_spec` advances under a sidecar whose recomputed `successor_spec_sha` ≠ the stamped `next_spec_sha` (the spec that would execute is not the one the human saw) |
| Latency plan Row 20 (unit 4a) — The rendezvous decision Stop guard, DEMOTED to a backstop by the fused-approve path, STILL FIRES on a committed-but-unadvanced `y` (verify-a-guard-can-fire applied to the demotion — the demotion must not have silently deleted the guard): the fused advance clears the CONSUMED boundary's marker and re-parks a fresh un-answered boundary, so `decision_rendezvous_stop_guard.build_hook_output` is SILENT on the fused path; but a `y` journaled WITHOUT the fused advance (the legacy two-call path, or a fused tick that failed after the commit) still trips the guard (`decision:"block"`, naming the run) | `tests/_kernel/lifecycle/test_block_drive_fused_approve.py::test_backstop_fires_on_unfused_committed_y`, `::test_fused_advance_clears_the_old_marker_so_the_guard_is_silent` | the Stop guard stops blocking on a committed-but-unadvanced `y` (the demotion deleted the backstop rather than demoting it), or the fused advance leaves the consumed boundary's marker so the guard false-fires on the fused path |
| Latency plan Row 21 (unit 4b, L2) — The §5 pending-decision marker at a parked boundary has ONE definition — `block_drive.park` (the `_park` alias, promoted public) — with TWO seats (the scope_gate/notebook_gate blessed one-definition-two-seats pattern): seat 1 is the driver tick; seat 2 is the detached worker's own exit, `submit_blocks._worker_exit_park`, which ROUTES THROUGH `park` and never re-inlines `mark_pending_decision`. A detached worker's terminal now leaves BOTH the pending-decision marker (no extra driver tick needed to notice the recorded terminal → the doctor reads "parked", not "stalled") AND the block-terminal record (the durable wake payload `wait-detached` hands back); a direct, non-detached call (`HPC_DETACHED_BLOCK` unset) does NOT park — the driver tick owns the park on that path | `tests/_kernel/lifecycle/test_worker_exit_park.py::test_park_has_exactly_one_definition_in_block_drive`, `::test_worker_exit_park_routes_through_the_one_definition`, `::test_detached_worker_parks_itself_at_its_terminal`, `::test_non_worker_call_does_not_park` | `_worker_exit_park` re-inlines `mark_pending_decision` instead of routing through `park` (a second marker-writing definition forks), a detached worker's terminal leaves no pending-decision marker (the doctor reads "stalled" and an extra driver tick is needed to park), or a direct non-detached call parks (a double-park the driver-tick seat already owns) |
| Latency plan Row 22 (unit 4b, L3; R2-APPROVED) — The code-fired speculative canary at S1's clean resolved-park reuses the #249 canary TTL cache (`state.canary_cache`) as its ONE speculation-state definition — no second cache: `submit_blocks._fire_speculative_canary` launches `submit-speculate` detached over the SAME S1→S2 materialized `submit` sub-spec, and `ops.submit_speculate` consults `canary_cache.is_canary_validated_fresh` before firing (the budget-of-1 dedup + breaker-invalidation ride the TTL cache, no extra machinery). `stop_after_canary` is UNSTRIPPABLE on the speculative path (the main array is unreachable from `submit-speculate`, which composes `submit-and-verify(stop_after_canary=True)`); `cmd_sha` identity flows through the composed spec so a nudge that MOVES `cmd_sha` ORPHANS the speculative canary (cache miss → S2 re-canaries); required ambiguities → SKIP (the same rule the hpc-submit skill applies); opt-in via `HPC_S1_SPECULATE=1`, OFF by default, and the speculative fire leaves the journal UNTOUCHED (a `_detached/` handle only — no decision, no brief) | `tests/ops/test_block_gate_and_speculate.py::test_speculate_noops_when_canary_validated_fresh`, `::test_speculate_runs_canary_when_not_cached`, `::test_s1_resolved_park_fires_speculative_canary_in_code`, `::test_s1_speculation_off_by_default`, `::test_s1_speculation_skips_ambiguous_walk`, `::test_s1_speculative_fire_leaves_the_journal_untouched` | the speculative path grows a second speculation-state cache instead of consulting `canary_cache.is_canary_validated_fresh`, `stop_after_canary` becomes strippable (the main array becomes reachable from `submit-speculate`), a moved `cmd_sha` fails to orphan the speculative canary (a stale canary is reused), `_fire_speculative_canary` fires on an ambiguous walk or when `HPC_S1_SPECULATE` is unset, or the speculative fire writes a decision/brief into the journal |
| The reconcile/settle terminal harvest is JOURNAL-EVIDENCE-backstopped, not transition-only (audit U8 / rank 2, `docs/plans/transport-robustness-2026-07-17`): `ops/monitor/reconcile.py`'s three settle arms (`_settle_from_announcements`, the kill-confirmed arm, the reporter-backed settle arm) route their guaranteed `harvest_on_terminal` through the ONE `_harvest_if_owed` gate — it fires on a verdict TRANSITION (`terminal_cause != pre_reconcile_status`) OR, absent a transition, as a backstop iff the run is terminal with NO harvest receipt (`harvest_guard.harvest_receipt_exists` over the durable `<run_id>.harvest.jsonl` ledger). So a session-death BETWEEN `mark_run(terminal)` and the harvest (reconcile has no `monitor_flow`-style `finally`) re-drives the dropped harvest on the next reconcile EXACTLY once, and a run whose receipt already landed does not re-fire — "harvest owed" is derived from durable evidence, never in-process state, so it is idempotent both ways. The human-directed sibling `ops/settle_run.settle_run` mirrors the SAME receipt-gated decision over the PUBLIC `harvest_guard.harvest_receipt_exists` seam (reconcile's `_harvest_if_owed` is private cross-package AND returns no marker, so it is mirrored, not imported — settle_run needs the marker for `SettleRunResult.harvest`): its harvest fires on a status transition OR a terminal-with-no-receipt backstop, and `SettleRunResult.stage_reached` reports which fired as `settled` / `harvest_backstopped` / `already_terminal` (no silent re-interpretation of a backstop as a transition). A `run_not_terminal` abnormal-exit skip is NOT a receipt; the verdict stays revisable (a `complete`→`failed` downgrade IS a transition and still harvests) | `tests/ops/monitor/test_reconcile_harvest_backstop.py::test_terminal_with_no_receipt_refires_harvest_exactly_once`, `::test_terminal_with_receipt_does_not_fire`, `::test_normal_transition_fires_once_then_idempotent`, `::test_harvest_receipt_exists_predicate`; `tests/ops/monitor/test_reconcile_kill_confirmed.py::test_kill_confirmed_reporter_ok_still_terminal_no_double_harvest`; `tests/ops/monitor/test_reconcile_canary_pairing.py::test_idempotent_reconcile_does_not_reharvest`, `::test_verdict_downgrade_reharvests`; `tests/ops/submit/test_settle_run.py::test_terminal_with_no_receipt_backstops_harvest_exactly_once`, `::test_already_terminal_with_receipt_does_not_reharvest`, `::test_transition_settle_then_resettle_is_idempotent` | a reconcile OR settle_run settle arm reverts to a transition-ONLY gate (a terminal-with-no-receipt run never harvests — the dropped-harvest bug), the backstop re-fires when a receipt already exists (a double pull on every idempotent re-reconcile / re-settle), `settle_run` collapses `harvest_backstopped` back into `already_terminal` (a silently re-interpreted backstop), or `harvest_receipt_exists` counts a `run_not_terminal` skip as a performed harvest |
| Reducibility is ONE predicate (`aggregate_flow.per_task_fallback_reducible`) with now THREE seats — the run-path refuse (`_per_task_metrics_reduce`), the aggregate-CHECK disclosure (`aggregate_blocks._reducibility_issue`), and the S1 shift-left disclosure (`submit_blocks._reducibility_brief`, RULED *disclose* 2026-07-17) — and no seat re-derives the JSON-name test. The S1 seat routes through the aggregate-CHECK issue rendering VERBATIM (reused, not copied), fires at the resolved boundary BEFORE submit-s2 detaches, and DISCLOSES an irreducible plan (never GATES — the bare `y` flow is byte-unchanged; a reducible plan adds no brief key); when the sidecar is unreadable / no run_id was minted it SAYS could-not-run (one honest line, the no-silent-caps rule), never a silent skip | `tests/ops/submit/test_s1_reducibility_brief.py::test_reducibility_brief_irreducible_carries_the_issue_verbatim`, `::test_reducibility_brief_reducible_returns_none`, `::test_reducibility_brief_unreadable_sidecar_says_could_not_run`, `::test_s1_seat_routes_through_the_one_aggregate_predicate`, `::test_s1_resolved_brief_carries_reducibility_disclosure`, `::test_s1_reducible_plan_brief_omits_the_key`; `tests/ops/aggregate/test_blocks.py::test_check_surfaces_non_reducible_csv_artifact_before_greenlight` | the S1 seat re-inlines its own JSON-name test instead of calling `aggregate_blocks._reducibility_issue`, stops firing before submit-s2 detaches, silently skips when the sidecar is unreadable, adds a brief key for a reducible plan (breaking byte-identity), or GATES/refuses an irreducible plan (the ruling is disclose-not-block) |
| The `submitting` journal status is READER-TOLERATED everywhere BEFORE any wheel mints it (submit-once U3 phase-1, `docs/plans/transport-robustness-2026-07-17`, premortem Δ3 reader-tolerance-first): `submitting` is a `JournalStatus` kept OUT of `TERMINAL_STATUSES` (and NOT a `LifecycleState` — no schema value, Δ8), so `prune_terminal_runs` — guarding on `status not in TERMINAL_STATUSES`, never `== "in_flight"` — KEEPS a submitting orphan (the latent GC-orphan bug: an old co-resident wheel's `!= "in_flight"` guard would delete the only durable evidence reconcile-recovery needs); `find_submitting_runs` (NEW, mirrors `find_in_flight_runs`) surfaces submitting records while `find_in_flight_runs` and `is_resubmittable_terminal` stay UNCHANGED (a submitting run has no `job_ids` — not the monitor live set, not resubmittable-by-plain-submit); `find_stalled_runs` ALSO scans submitting records so a submit that died in its dispatch window surfaces as a `doctor` recovery proposal that routes to reconcile-recovery (NOT a blind re-arm); and the status-snapshot/`doctor` renders DISPLAY a submitting record ("submitting — dispatch in flight / awaiting id") without crashing. OWED at later phases (do NOT yet mechanize — no `src/**` mints the state): the `submitting`-before-dispatch MINT ordering, `_resolve_layer1` routing a submitting run to reconcile, and reconcile as the sole transition-out owner | `tests/state/test_submitting_state.py::test_prune_keeps_a_submitting_record`, `::test_old_guard_would_have_pruned_the_submitting_orphan` (the RED-under-old-guard regression pin), `::test_find_submitting_runs_returns_only_submitting`, `::test_find_in_flight_runs_ignores_submitting`, `::test_find_stalled_surfaces_a_lapsed_submitting_run`, `::test_mark_run_accepts_submitting_and_promotes`; `tests/_kernel/contract/test_vocabulary.py::test_submitting_is_a_non_terminal_journal_status`; `tests/_kernel/contract/test_vocabulary_consistency.py::test_journal_status_matches_observable` (the intentional journal-only asymmetry); `tests/ops/status/test_submitting_render.py`; `tests/ops/recover/test_doctor.py::test_doctor_submitting_orphan_drafts_reconcile_not_rearm` | `prune_terminal_runs` reverts to the `== "in_flight"` guard (a submitting orphan is garbage-collected — Δ3 fire), `submitting` is added to `TERMINAL_STATUSES` / a `LifecycleState` / the status schema enum (Δ8 fire), `find_in_flight_runs` starts returning submitting records (the monitor treats a jobless pre-dispatch run as live), `find_stalled_runs` stops scanning submitting records (a stalled submit orphan never surfaces to `doctor`), or a status render crashes / drops the submitting display projection |
| aggregate-stream reduces a custom-`aggregate_cmd` run ONLY through the run's OWN reducer (`cluster_reduce`, scoped to one arm's task ids via the `HPC_STREAM_TASK_IDS` allowlist) over CENSUS-COMPLETE arms — positive census evidence gates eligibility, a pending/incomplete arm is NEVER reduced, and one invocation per newly-complete arm is memoized on the durable `(run_id, arm, cmd_sha)` receipt (a moved `cmd_sha` re-fires); a reducer failure on a complete arm is disclosed VERBATIM (`arms_reduce_failed`) while the other complete arms keep streaming — NEVER a silent built-in `reduce_metrics` fallback (a built-in number cited amid the run's own reducer numbers is the divergence bug reborn); cross-arm/union/pairwise stats stay final-harvest-only (`aggregated_metrics={}` on the custom path) and every emission labels its partiality (`value_scope` + an N-of-M `completeness_label`); the built-in weighted-mean path is byte-unchanged for a non-custom run — the custom-only provenance keys never appear on it (s4-gaps item 5, RULED build-per-arm not refuse 2026-07-17, commit 3c9e5599 — the streaming sibling of the row-107 "never a fabricated scalar" honesty) | `tests/ops/aggregate/test_stream.py::test_custom_reducer_streams_run_own_values_only_for_complete_arms`, `::test_custom_reducer_failure_arm_disclosed_others_continue`, `::test_custom_reducer_memo_skips_re_reduce_and_cmd_sha_change_re_fires`, `::test_builtin_path_byte_unchanged_for_non_custom_run` | `stream.py` reduces a custom-`aggregate_cmd` run's arm through the built-in `reduce_metrics` mean (a built-in number is cited for a custom-reducer run), reduces an incomplete/pending arm, swallows a reduce failure instead of the verbatim `arms_reduce_failed` disclosure (or substitutes a built-in number for the failed arm), emits mid-stream cross-arm stats instead of final-harvest-only, or drops the N-of-M `completeness_label`/`value_scope` partiality label from an emission |

## Drift log

**2026-07-17 — submit-once U3 phase-1: the `submitting` reader-tolerance wave (premortem Δ3, `docs/plans/transport-robustness-2026-07-17`).** The standalone prerequisite the premortem mandates ship+deploy EVERYWHERE before any wheel mints `submitting`-before-dispatch: an old co-resident wheel on the same journal home must tolerate a `submitting` record its own `prune_terminal_runs` would otherwise garbage-collect (the deployment-skew orphan-deletion, premortem C1/Δ3). Landed READERS ONLY — nothing in this wave mints, transitions, or writes `status="submitting"`: `SUBMITTING="submitting"` added to `JournalStatus` (kept OUT of `TERMINAL_STATUSES`, NOT added to `LifecycleState` — the status query never runs on a jobless submitting record, so per Δ8/OPEN-3 no schema value and no schema bump); the `prune_terminal_runs` guard fixed from `== "in_flight"` to `not in TERMINAL_STATUSES` (the latent GC-orphan bug — RED-then-GREEN evidence: under the old guard `test_prune_keeps_a_submitting_record` evicts the orphan, `removed==2`); `find_submitting_runs` added (mirrors `find_in_flight_runs`); `find_in_flight_runs` and `is_resubmittable_terminal` UNCHANGED (submitting correctly excluded from both); `find_stalled_runs` extended to scan submitting records too so a lapsed submit orphan surfaces to `doctor`; the `doctor` stalled proposal branches on `submitting` → a reconcile-recovery draft (re-derive from the cluster), never a re-arm; and the status-snapshot render (`relay_render._row_line`) displays "submitting — dispatch in flight / awaiting id". The `test_journal_status_matches_observable` vocabulary-consistency test was updated to encode the intentional journal-only asymmetry (submitting has no lifecycle-wire projection — the same shape the `timeout` terminal asymmetry already carries). The enforcement row above mechanizes the phase-1 clauses ("prune treats submitting as terminal" = the fire) and NAMES as owed the later-phase clauses (mint ordering, `_resolve_layer1` routing, reconcile transition-out) — the design's §7 row is not yet fully mechanizable because no `src/**` mints the state.

**2026-07-17 — S1 reducibility shift-left, RULED *disclose* (s4-gaps item 1).** Finding 28's reducibility predicate (`per_task_fallback_reducible`) was already a TWO-seat contract — the run-path refuse and the aggregate-CHECK disclosure — but S1 never ran it, so an irreducible plan (NO `aggregate_cmd` + a non-JSON summary artifact) was only caught AFTER submit-s2 detached and spent a full run + a 40+ min results/ pull. The user RULED *disclose, not hard-refuse* (the system amplifies human effort; gates don't tighten — a human may register the reducer between S1 and aggregate). Built: `submit_blocks._reducibility_brief` runs the SAME predicate the aggregate-CHECK seat keys on against the sidecar `resolve_submit_inputs` just wrote, and attaches a `reducibility` block to S1's resolved brief beside the `deploy_payload` disclosure — reusing `aggregate_blocks._reducibility_issue`'s rendering VERBATIM (a THIRD seat of the one predicate, no copy). Disclosure-only: no gate, no new decision point, the bare `y` flow unchanged; a reducible plan leaves the brief byte-identical. Per the no-silent-caps rule the seat is honest about a missing input — an unreadable sidecar / an unminted run_id yields a `checked: False` could-not-run line, never a silent skip. Layering: `_reducibility_issue` is a private symbol in `hpc_agent.ops.aggregate_blocks`, a role-root sibling of `submit_blocks` in the SAME `hpc_agent.ops` package, so the reuse trips NEITHER `lint_private_cross_package_imports` (same package) NOR `lint_subject_imports` (role-root files aren't subjects) — the `_assert_no_post_greenlight_drift` sibling-import precedent, no shared-home extraction needed. The row above extends the reducibility contract to its THIRD seat.

**2026-07-16 — rows 19-22 backfill: the block-drive-tail doc-close debt (b1ea0d5d).** The 4a/4b integration (`b1ea0d5d`) landed fused `block-drive --approve` + the R3 sha-pinned consumption leg, worker-exit park (the `_park`/`park` second seat), and the R2 code-fired speculative canary with their targeted batteries GREEN, but its own commit message recorded the enforcement map as unwritten ("Enforcement rows 19-22 owed at doc close"). The four rows above close that debt, each naming the fire-path test that landed with the same commit: Row 19 folds the R3 consumption drift-refusal (`_r3_materialized_advance_spec` recompute vs the park-time `next_spec_sha`) into the fused-approve one-definition row, since both are the same advancing tick's mechanism; Row 20 is the verify-a-guard-can-fire pin on the demoted rendezvous backstop; Rows 21-22 are the `park` one-definition-two-seats and the one-TTL-cache speculation-state pins. The `Row N` labels track the latency-elimination-2026-07-16 plan's enforcement-map numbering (`docs/plans/latency-elimination-2026-07-16/unit-specs.json`), the source the tests' `Row 19/20/21/22` docstrings cite. Note: the predecessor plan rows 14-16 (unit 3.1 materialized-successor-spec composer / exclusive dereference / park-time sha bind) and 17-18 (unit 3.3 daemon, gated on the unbuilt R4 daemon) are NOT yet mapped here — out of scope for this debt-close, tracked by the plan.

**2026-07-17 — `_PACKAGE_ROOT` Path→str; the root import and the Stop-hook prefilter are pathlib-free (hook-floor unit).** The lazy-root rows above (the eager-smoke row and the fused Stop-hook prefilter row) gained a sibling obligation: a bare `import hpc_agent` — which every Stop-hook fire runs 3×/turn via `python -m …stop_multiplex` — must import no `pathlib`. Maintainer-approved: `hpc_agent._PACKAGE_ROOT` was converted from a `pathlib.Path` to an eager `os.path` STRING (`os.path.dirname(os.path.abspath(__file__))`), and `pathlib`/`warnings`/`importlib.util` moved from the root `__init__` module scope into the functions that use them, dropping ~10-15 ms/fire of `pathlib` import off the hook floor. G4's meaning is unchanged — `_PACKAGE_ROOT` stays a REAL EAGER attribute (never behind `__getattr__`), honest `AttributeError` semantics intact — only its TYPE changed; consumers wrap `Path(_PACKAGE_ROOT)` at their own site. The compat hazard of the type change is waived: the maintainer is the only consumer of materialized scaffolds. The `stop_multiplex.prefilter_should_run` / `_journal_home` path was rewritten to `os.path` equivalents with byte-identical decisions and outputs. New fire paths: `tests/contracts/test_eager_import_smoke.py::test_bare_import_does_not_load_pathlib` and `tests/_kernel/hooks/test_stop_multiplex.py::test_noop_turn_does_not_import_pathlib`; the G4 attr test `::test_package_root_resolves_without_getattr_import_work` was rewritten to string form.

**2026-07-17 — relay-audit cross-scope corrector: an equidistant tie is owned by NEITHER (run-14 finding 5 residual + doc-debt close).** `5bf7a17a` closed the systematic run-14 cross-fire — two audits sharing slug names (`causal_tune_linear`/`causal_tune_tree`, both `data-selection`) had each sha checked against the OTHER's journal — by binding a claim to the nearest-mentioned audit id, but left an EQUIDISTANT tie in scope for BOTH audits ("a genuinely-wrong claim is still caught") and shipped NO enforcement row. The run-14 docket verdict was the opposite priority: a claim provably owned by neither must yield NO correction, because a false correction shown to the human is worse than silence. The nearest-id check became a tri-state (`_nb_claim_ownership`: `own` / `sibling` / **`ambiguous`**); a tie is now `ambiguous` → skipped under every scope, its `(start, end)` span recorded ONCE (deduped across the per-audit calls) into the shared `ambiguous_out` set the Stop hook threads through `_gather_violations`, and the count is DISCLOSED — the completer on its non-blocking `systemMessage` unconditionally, the rejector only when already blocking for another finding (it never manufactures a bounce to inform, which would block a clean relay). Demonstrated red-then-green: a tree sha equidistant between the two audit ids drew a confident false correction against LINEAR's journal before the ruling, none after. The row above closes the finding-5 residual AND the enforcement-row debt `5bf7a17a` left implicit.

**2026-07-17 — reconcile settle-arm harvest is journal-evidence-backstopped (audit U8 / rank 2, transport-robustness-2026-07-17).** A latent correctness bug, not a prediction: reconcile's three settle arms did `mark_run(terminal)` → transition-gated `harvest_on_terminal`, and a process-death BETWEEN the mark and the harvest left the run terminal-with-no-harvest — every later reconcile saw the journal already terminal (no TRANSITION) and never re-fired the guaranteed harvest, so the artifacts sat unharvested on the cluster forever. `monitor_flow` has a `finally` backstop; reconcile (invoked directly by drivers / the skill) did not. Fix: the three arms now route through ONE `_harvest_if_owed` gate that fires on a transition OR when the run is terminal with NO harvest RECEIPT, where the receipt is the durable `<run_id>.harvest.jsonl` ledger read by the new `harvest_guard.harvest_receipt_exists` (the real `harvest_on_terminal` always appends a marker as its LAST step, even on failure/`scope_locked`; a `run_not_terminal` abnormal-exit skip is excluded). "Harvest owed" is thus derived from durable journal evidence, never in-process state — idempotent both ways: a gap re-fires exactly once, a receipted run never re-pulls. The backstop discloses (a `logging.warning` naming the recovered gap) and the harvest's own marker append keeps the repair non-silent. The three sibling reconcile test doubles (`test_reconcile_{canary_pairing,kill_confirmed,announce}.py`'s `_count_harvests`) were made faithful — they now write the receipt the real guard writes, so their idempotency assertions hold for the right reason. A KNOWN sibling of the same pattern, `ops/settle_run.settle_run` (human-directed, human present at the boundary), was left transition-only: closing it cleanly touches its `SettleRunResult` `harvested`/`stage_reached` contract, out of this fix's minimal shape.

**2026-07-17 — aggregate-stream arm-complete custom reduce: the OWED enforcement row landed here, not in `determinism-boundary` (s4-gaps item 5, commit `3c9e5599`).** The streaming custom-reduce build (`3c9e5599`, R2 ruled *build-per-arm not refuse*) drift-logged an OWED enforcement row and its author's note guessed the `determinism-boundary` section. On close inspection the invariant is NOT a judgment-vs-verb / affordance-removal rule (that section's whole family): it is a reduce-value HONESTY rule — a streamed number is the run's OWN reducer's output over a census-complete arm, never a silent built-in fallback, with partiality labeled — which is the direct sibling of this section's row-107 ("a cluster-reduce whose reducer emits a non-dict JSON persists `aggregated_metrics: {}` — the honest empty, never a fabricated scalar") and row-131 (reducibility). The reduce/aggregate/harvest family lives HERE (rows 102, 107, 118, 119, 130, 131); `determinism-boundary` carries zero reduce rows. So the row was written into THIS section. All four clauses were already fire-shaped-pinned by the tests the build shipped (`test_stream.py`'s custom-reducer battery): own-reducer-over-complete-arms-only + pending-never-reduced (`test_custom_reducer_streams_run_own_values_only_for_complete_arms`, which also pins `reduce_path=="custom"`, `aggregated_metrics=={}`, and the N-of-M `completeness_label`/`value_scope` partiality label), verbatim-failure-no-built-in-fallback (`test_custom_reducer_failure_arm_disclosed_others_continue` — the failed arm is ABSENT from `per_arm_metrics`, not built-in-meaned), one-invocation-per-arm memo + `cmd_sha` re-fire (`test_custom_reducer_memo_skips_re_reduce_and_cmd_sha_change_re_fires`), and built-in-path byte-parity for a non-custom run (`test_builtin_path_byte_unchanged_for_non_custom_run`) — so no new test was owed; the row cites the existing pins.

**2026-07-17 — the `settle_run` sibling closed: human-directed settle is journal-evidence-backstopped too.** The known sibling recorded just above is closed. `ops/settle_run.settle_run` did `mark_run(terminal)` → transition-gated `harvest_on_terminal`, so a directed re-settle of a run that died in the `mark_run`→harvest window (already terminal, no receipt) never re-fired the guaranteed harvest — the identical dropped-harvest gap the reconcile arms had. Fix: the harvest decision now routes through the SAME receipt-gated pattern — fire on a transition OR, absent one, as a backstop when `harvest_guard.harvest_receipt_exists` reads no receipt on the durable `<run_id>.harvest.jsonl` ledger — never solely on in-process transition state. Reconcile's `_harvest_if_owed` was NOT reused: it is a private cross-package name (a `lint_private_cross_package_imports` violation to import) and returns `None`, but settle_run needs the marker for `SettleRunResult.harvest`; so the pattern is MIRRORED over the public `harvest_receipt_exists` seam (both boundary lints pass). The `SettleRunResult` contract was updated HONESTLY rather than silently re-interpreted: `stage_reached` gained a third value `harvest_backstopped` (already terminal, no receipt → harvest re-fired via backstop) distinct from `settled` (a real transition) and `already_terminal` (receipt present, a true no-op), and `harvested` now says "transition OR backstop", not "only on a transition". Schemas + `operations.json` regenerated (`build_schemas`/`bake_operations_json`); the only `SettleRunResult` consumer is the primitive itself (the model is serialized straight into the CLI/MCP envelope — no render branches on the enum), so the additive third value is backward-safe. Tests: `tests/ops/submit/test_settle_run.py::test_terminal_with_no_receipt_backstops_harvest_exactly_once` (the gap: RED under the old transition-only gate — `harvested` was `False` — GREEN after), `::test_already_terminal_with_receipt_does_not_reharvest` (the old `test_already_terminal_resettle_does_not_reharvest` made faithful — it now writes the real receipt first, mirroring the reconcile fix's doubles, so it asserts the no-op for the right reason), and `::test_transition_settle_then_resettle_is_idempotent`. The reconcile-backstop enforcement row above was extended to name the settle_run arm + its three fire-path tests.

**2026-07-17 — the canary reducer check: rung 2 of the reducibility ladder (amortized-reduction-check-2026-07-17).** The S1 static reducibility disclosure answers "is a reducer DECLARED?" from the sidecar — a predicate, never an execution — so a custom `aggregate_cmd` passes it unconditionally while saying nothing about whether the reducer actually runs (a py3.13-syntax reducer against a login py3.8, a missing `import pandas`, a wrong output path, non-JSON output all pass S1 and die at final harvest, after the whole array computed). The new middle rung EXECUTES the declared reducer against the verified canary's ONE real task-0 row — a genuine task artifact of the exact shape the main array writes — before the main array launches. Mechanism: `submit_and_verify._check_reducer_on_canary` calls the SAME `cluster_reduce` the final harvest calls (one-definition) with the canary's run_id + the main run's declared `aggregate_cmd`, so the reducer's literal `python3` binds the run's env interpreter (the only way to catch the py3.8-vs-3.13 class before the array runs). It asserts ONLY the contract SHAPE (exit 0, parseable JSON, top-level keys) — never a value. Failure semantics are doctrine-fixed: a reducer that RAN and failed (`RemoteCommandFailed`: non-zero exit / missing output / non-JSON) is a `disclosed` VERBATIM disclosure on the S2/S3 brief that the bare `y` still crosses (it MIGHT be a benign "needs ≥2 rows" false alarm a single row cannot satisfy — the machinery surfaces the stderr and stops, never interpreting "broken code" vs "needs more rows"); a check that could not COMPLETE (`SshUnreachable`/`SshCircuitOpen`/`SshSlotWaitTimeout`/`ClusterTimeout`, or any unexpected raise) is `unverified` (UNKNOWN), never `passed` — the same positive-evidence-only posture as `reporter_unreachable` and the combiner truncation rule. Best-effort by contract like `_mint_double_canary_sample`: it NEVER fails a submit whose canary verified ok. On-by-default gated on a declared `aggregate_defaults.aggregate_cmd`, opt-out `HPC_NO_CANARY_REDUCER_CHECK` mirroring `HPC_NO_DOUBLE_CANARY`; a run with no custom reducer SKIPS byte-identically. The compute amortizes into the S2→S3 human-review dead window (the array does not launch until the S3 greenlight regardless), so the check adds zero critical-path wall-clock — the exact analogue of the canary WALLTIME calibration (`canary_calibration.py`), which already uses the canary's measured wall-clock to size the array before it launches. Placement: past BOTH canary verdicts (once per proceeding submit), the sanctioned "folded into the double-canary block" position (memo §4.2) rather than the literal `:1046`, which avoids a wasted reduce exec on a nondeterminism-blocked run. `ReducerCheckResult` is a nested-only wire helper (added to `build_schemas._HELPER_NAMES` alongside `MpiSpec` so its `Result` suffix does not strand a top-level schema the reachability lint rejects). The wave-0 partial-harvest rung (window d, memo RC3) and the S1 static disclosure (concurrent unit) are DEFERRED/out of scope for this unit. New fire paths: `tests/ops/test_canary_reducer_check.py` (the six named in the enforcement row above).
