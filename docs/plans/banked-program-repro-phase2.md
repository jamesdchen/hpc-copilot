# Banked ruling: program-repro phase 2

Scope note: **no phase-2 design doc exists** — `grep -ri 'program-repro' docs/` → zero matches. The banked item is a one-line sketch (memory banner 2026-07-19: "BANKED needs-user-ruling: program-reproduction PHASE 2 (only a one-line sketch — needs a user-facing design sentence)"). The sketch (memory, run-15 session file, "DESIGNED-NOT-BUILT QUEUE"): *"program-reproduction PHASE 2 pre-publication ceremony (fresh fan-out + cross-run reducer + sealed step-by-step dossier; phase-1 program-verify built)."* Open items below are reconstructed from the seams phase 1 left; each is flagged doc-grounded vs inferred.

## Landed (what exists today)

- **`verify-reproduction`** (`src/hpc_agent/ops/verify_reproduction.py`; wire `_wire/queries/verify_reproduction.py`; schemas `verify_reproduction.input.{json}`). Phase-1 pair comparator: tiered verdict `auto_cleared`/`needs_verdict`/`mismatch`/`incomparable`; durable append-only receipt `_aggregated/<repro>/reproduction_receipts.jsonl` (v1 byte-preserved / v2 tiered); **accreting fingerprint sample** appended back per comparison (`_aggregated/_fingerprints/<cmd_sha[:16]>.jsonl`); `ReproTolerance` = default abs/rel + per-key overrides, caller-owned, never invented; partial reproductions compare per-task with no-silent-caps disclosure; data/env/hw drift dimensions disclosed, never gated; data-trace stage-interlock localizes the first diverging stage.
- **External-baseline (claim-check) mode** on the same spec (`ExternalBaseline`: `claimed_values` + own `tolerance` + optional `claimed_data_sha`), per `docs/design/onboard-by-reproduction.md` rulings 6a/6b/6c (built 2026-07-08): receipt kind `claim-check` in a **separate** ledger, the anti-laundering guard `_assert_receipt_kind_matches_baseline` refuses any reproduction-kind receipt with an external baseline, code-emitted `CLAIM_CONSISTENT_SENTENCE`, and **no fingerprint sample minted** (observed-runs-only lock).
- **Determinism fingerprint** (`docs/design/determinism-fingerprint.md`, shipped 2026-07-08/09): envelope = order statistics only (range + labeled n, never a fitted distribution); judge-before-append; one admission rule (sample enters envelope iff passing verdict — code `auto_cleared` or human `append-decision` block `reproduction-verdict`, authorship-gated at `ops/decision/journal/reproduction.py`); `evidence_meets` demand predicate consumed by registration; dossier seals the raw ledger bytes.
- **`program-verify` — program-level phase 1** (`src/hpc_agent/ops/program_verify.py`, commit `8d189aab`; wire `_wire/queries/program_verify.py`; `docs/primitives/program-verify.md`): "RECEIPTS-FIRST" pure projection over recorded judgments — never re-compares a metric. Five constituent classifications (`reproduced_within_tolerance` / `mismatch_on_record` / `evidence_incomparable` / `evidence_stale_identity` / `no_reproduction_on_record`), severity fold (k/N reproduced), write-once signed **identity-only** program manifest `.hpc/provenance/program-<sig[:12]>.json`. Module docstring: *"fresh re-runs are a later phase's ceremony."*
- **Adjacent machinery**: `reproduce-run` (non-blocking mint + `next_block=submit-s2`; code-drift refusal via `state/code_drift.py::detect_code_drift`; `task_sample` partials), `extract-recipe` (program seed walk), `cite-check`, `export-bundle` (`1a50a270`), `provenance-manifest`, `export-attestations`.

## Open (what remains undecided)

1. **Fresh fan-out ceremony** (sketch-named; `program_verify.py` docstring defers it): no verb orchestrates reproducing a whole run-set. `no_reproduction_on_record`/`evidence_stale_identity`/`evidence_incomparable` constituents stay that way until someone hand-drives `reproduce-run` + submit + `verify-reproduction` per constituent.
2. **Cross-run reducer** (sketch-named): phase 1 folds severity over constituents; there is no program-level aggregation of metric *evidence* — fingerprint ledgers key per identity (`cmd_sha`), one ledger per experiment identity (D-store), and nothing pools or re-discloses them at program altitude.
3. **Sealed step-by-step dossier** (sketch-named): `export-bundle` seals recipe+dossier+manifest+cite-check+attestations, and the program manifest is deliberately identity-only ("verdicts stay OUT so re-runs are idempotent", `_program_manifest_body`). No per-constituent reproduce→verify replay transcript is sealed anywhere; `docs/design/publication-bundle.md` is still `status: plan`.
4. **Drifted-constituent recovery** (inferred): `reproduce-run` REFUSES drifted code (`docs/design/reproduction-receipt.md`, drift-refusal decision), so `evidence_stale_identity` constituents are permanently unreproducible by machinery — phase 2 must fix the disclosure posture (finding vs exclusion from the denominator).
5. **Claim-check → recorded-original upgrade** (inferred, lightly grounded): 6b's naming lock forbids any upgrade of the receipt itself; the fresh observed runs minted during a claim-check *could* seed a later recorded-original reproduction, but no verb stitches that, and 6a deliberately forces no memory record.
6. **Cross-fleet reproduction** (inferred, not doc-demanded): `evidence-brief`/`attention-queue` carry `fleet=true`; `verify-reproduction`/`program-verify` are single-experiment. No doc asks for multi-experiment reproduction.
7. **NOT open** (settled — do not re-litigate): accreting-sample policy — append-only, no eviction, no trust weighting beyond admission + evidence labels; newest sample has no special authority (D-consume). Envelope math is range+n forever.

## Ruling questions

1. **Does phase 2 get a design doc before any build?** (a) Yes — write `docs/design/program-reproduction-phase2.md` first (my rec — the banked item itself says "needs a user-facing design sentence"); (b) no, build the minimal fan-out and doc after. *Why: every other landed phase here shipped doc-first; the sketch is one line.*
2. **Fan-out shape?** (a) Full `program-repro` block-drive chain: mint N `reproduce-run`s, drive S2/S3/verify per constituent, re-project `program-verify`; (b) thin `program-fan-out` minting verb only — human drives each submit chain (my rec — mirrors `reproduce-run`'s non-blocking doctrine and costs one verb); (c) defer fan-out, ship only a "what-to-reproduce" planning projection. *Why: an orchestrator that blocks on N arrays re-opens the exact MCP-wedge `reproduce-run` was built to avoid.*
3. **Cross-run reducer scope?** (a) Keep receipt-counts rollup, no new store (my rec — minimal, doctrine-clean); (b) program-level pooled envelope (new store — risks a second admission definition, forbidden by the fingerprint enforcement row); (c) reuse `evidence_meets` demands at program altitude. *Why: (b) collides with "the admission rule has ONE definition".*
4. **Drifted constituents in the roll-up?** (a) Finding-only disclosure, full denominator (my rec — honest k/N, no silent caps); (b) exclude from denominator with disclosed "attemptable k/N"; (c) machinery-assisted re-extraction. *Why: no-silent-caps is the standing posture.*
5. **Sealed artifact?** (a) Extend `export-bundle` with a `program-verify` member (my rec, small); (b) new program-scoped step-by-step replay artifact; (c) none — manifest + receipts are already durable. *Why: bundles grow by adding a `source`, per `reproduction-receipt.md`'s extensibility note.*
6. **Claim-check upgrade path?** (a) None — 6b lock holds, observed runs are independently available as originals (my rec); (b) a convenience verb linking a claim-check's fresh run as a reproduction original. *Why: any mechanical upgrade pressures the anti-laundering line.*

## Build scope if ruled (minimal path: Q1a + Q2b + Q3a + Q4a + Q5a + Q6a)

| Unit | Files | Size |
|---|---|---|
| Design doc | `docs/design/program-reproduction-phase2.md` | S |
| Fan-out minting verb | `ops/program_repro.py` + `_wire/queries/program_repro.py` + schema regen + `operations.json` bake | M |
| Export-bundle member | `ops/publication_bundle.py` + `_EXPECTED_SOURCES`-style pin | S |
| Tests | `tests/ops/test_program_repro.py` + contract pin (mint refuses drifted constituents with the drift evidence) | M |

Total **M**, severable into two S slices (doc+bundle first). Test plan: fan-out mints exactly the non-`reproduced_within_tolerance` constituents; drift refusal fires; manifest signature unchanged by verdicts; bundle member seals.
