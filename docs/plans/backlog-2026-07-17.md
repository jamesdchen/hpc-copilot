# Build-ready backlog — mining sweep 2026-07-17

Baseline `main @ a01d3356` (working tree DIRTY — four in-flight sessions, §0).
**Docs-only unit** — the sole file this writes is this memo. No `src/**`, no
commit.

Purpose: the project starves max-throughput swarms on the question *"what is
dispatch-ready RIGHT NOW."* This is the authoritative answer, produced by mining
every `docs/plans/**` doc + every `docs/internals/principles/*` drift log for
ratified-but-unbuilt work, then **verifying each candidate against the current
code + `git log`**. Stale plan items are the dominant failure mode: this sweep
found whole plan docs (fable-sweep-2026-07-13, deployed-bugsweep, migrate,
half the latency program) already LANDED while their headers still say
"banked / no fixes applied." Every LANDED verdict below cites the commit; every
OPEN verdict cites the grep that proves absence.

---

## §0 — In-flight scopes (EXCLUDED from the ladder; do not dispatch onto these)

`git status` at sweep time confirms these are being edited by concurrent
sessions — treat every file they own as claimed:

| In-flight unit | Files owned (claimed) | Verified in-tree |
|---|---|---|
| **U3 phase-1** (reader-tolerance wave) | `state/index.py`, `_kernel/contract/vocabulary.py`, status renders, `ops/recover/doctor.py` | `SUBMITTING` enum + `find_submitting_runs` present in `state/index.py:184`; `tests/state/test_submitting_state.py` untracked |
| **S1 reducibility disclosure** | `ops/submit_blocks.py`, `docs/internals/principles/lifecycle-verdicts.md` | `tests/ops/submit/test_s1_reducibility_brief.py` untracked; s4-gaps drift log records it BUILT |
| **U4 protect-filter fix** | `infra/transport/__init__.py` | dirty in `git status`; U4 (a′) primary already landed `1edf806f` |
| **R2 arm-complete streaming** | `ops/aggregate/stream.py` | s4-gaps item 5 (custom-reducer streaming), `ops/relay_render.py` dirty |

---

## §1 — Counts

- **Candidates mined:** ~110 across 33 plan docs + 11 principles drift logs.
- **LANDED (stale plan items):** ~70 — see §2. The big stale clusters:
  fable-sweep-2026-07-13 (F01–F58), deployed-bugsweep (DB1–DB8), migrate
  (M-CENSUS…M-BRIEF), devx B1–B5 + A1–A7/WS1–WS4, latency O-wave ranks
  1/2/4/5/6/7/8/9/11/13/14/17/18/19/20/21/25, D-FSYNC, s4-gaps items 1/2/3/4,
  b3′ ack-lint, AR-1/AR-E1.
- **PARTIAL:** 3 (DB1 structural partials; reducer-contract-doc streaming
  caveat; latency-row doc-mapping debt).
- **OPEN / build-ready:** 21 (§3) — of which **~9 are cleanly dispatch-ready
  now** (§5 wave 1).
- **OPEN but ruling-gated or blocked:** 8 (§4), incl. the whole daemon DW1–DW3
  program (parked on one maintainer ruling).

---

## §2 — LANDED (stop re-queueing these)

Verified against code + git log. The coordinator should treat these as DONE.

| Cluster | Verdict | Evidence |
|---|---|---|
| **fable-sweep-2026-07-13** F01–F58 (WP-A…J) | **LANDED** (doc header stale) | F47 `_dedup_existing`+pre-stamp `submit_flow.py:2307`; F05 foreign-partial run_id filter `combiner.py:296,469`; matches MEMORY "fable-sweep train PRs #14/#15 landed" |
| **deployed-bugsweep DB1–DB8** | **LANDED** | `b03c11bc` ("8 confirmed cluster-code bugs, 4 HIGH") |
| **migrate-remainder** M-CENSUS/M-KILL/M-DERIVE/M-HARVEST/M-BRIEF | **LANDED** | `28a8c8ad`; full `ops/migrate/` package; live MCP `migrate-remainder` tool |
| **devx B1–B5** | **LANDED** | `d53749bd` (B1/B3), `a0dd47dd` (B4), `16c44241` (B2), `6a3c8dba` (run_lint_gauntlet) |
| **devx A1/WS1 regen-all** | **LANDED** | `scripts/regen_all.py --write` referenced live in CLAUDE.md |
| **latency-audit O-wave ranks** 1,2,4,5,6,7,8,9,11,13,14,17,18,19,20,21,25 | **LANDED** | O-wave merges `3a76982e`…`dfd76985`; audit-3 `445ce69a`, audit-10 `267e87ef` |
| **latency-elimination waves 0–5** (bulk) | **LANDED** | `1f9bce31` (w0-1), `88dae04d` (w2a), `0592ed99` (w2), `b1ea0d5d` (4a/4b/5.4), `a14a82f2` (rows 19-22), `f088a06f` (3.1 leg 1) |
| **D-FSYNC** | **LANDED** | `58e8e51a`; `fsync_required` split `infra/io.py:309` |
| **s4-gaps item 1** (S1 reducibility shift-left) | **LANDED / in-flight** | drift log 2026-07-17 "BUILT, RULED disclose"; = §0 in-flight |
| **s4-gaps item 2** (write-run-sidecar) | **LANDED** | primitive + all writers route through it; settle-run analogue `settle_run.py` |
| **s4-gaps item 3** (reducer-contract doc) | **LANDED** | `fd6bc3af`; doc now carries env_python/ship/stream sections |
| **s4-gaps item 4** (S4 ships own reducer) | **LANDED** | `d2b18bfd` derive+ship+stage-refuse+env-python |
| **settle_run receipt-gated harvest** (the just-closed sibling) | **LANDED** | `45d94feb` |
| **b3′ ack-less-stdout lint** (principles drift log candidate) | **LANDED** | `scripts/lint_remote_read_ack.py` exists (drift log said "not yet a row" — stale) |
| **AR-1 transport module→package split** | **LANDED** | `infra/transport/` is a package; `transport.py` gone |
| **AR-E1 pack-receipt-prereq** | **LANDED** | `_check_pack_receipt`→`pack_receipts.slot_status` `prereqs.py:543` |
| **upstream-fixes-2026-07** (+ U-#25, U-#62 ledger corrections) | **LANDED** | doc header "COMPLETE"; F49/F22 marked landed WP-C/WP-F |
| **U4 stage-swap atomicity** (a′ primary) | **LANDED** | `1edf806f`; = §0 in-flight residue |
| **U8 reconcile-settle backstop** | **LANDED** | `a738b0fb` (journal-evidence harvest backstop) + `45d94feb` |

---

## §3 — OPEN / build-ready units (the real queue)

Each verified OPEN. Fields: source · one-line spec · files · size · build-ready?
· file-conflict group.

### Publication-time extraction (clean-reproduction-extraction-2026-07-17)

**BR-1 · extract-recipe Task 1 (provenance foundation).**
Source §4 #1 Task 1 + G4a/G4c. Extend `_persist_local_aggregate` provenance
(`{incomplete_waves,source,reduced_at}`) with `contributing_run_ids`,
`piece_cmd_shas`, `hpc_agent_version`; mirror in `combiner._final_reduce` footer;
add `hpc_agent_version` to `provenance_manifest._RUN_PROVENANCE_FIELDS`
(bump `manifest_schema_version`). Files: `ops/aggregate_flow.py`,
`execution/mapreduce/combiner.py`, `ops/provenance_manifest.py`. **S.**
Build-ready (needs R3 schema-bump ruling — recommend bump). Verified OPEN:
`grep contributing_run_ids aggregate_flow.py` = 0; `_RUN_PROVENANCE_FIELDS`
lacks `hpc_agent_version`. **Conflict group: AGG-PROVENANCE** (with BR-9).

**BR-2 · extract-recipe verb.** Source §4 #1. New read-only `query`
`ops/extract_recipe.py` (artifact→minimal-run-set→runnable recipe, gap-disclosing)
+ `ops/recipe_render.py` + `_wire/queries/extract_recipe.py` + regen. Composes
`trace`, `provenance_manifest`, `sibling_run_ids`, `lineage_chain`,
`harvest_receipt_exists`. **M.** Build-ready but **blocked-on BR-1**; needs R1
(MCP-curate: recommend not) + R2 (artifact input boundary). Verified OPEN: file
absent. **Conflict group: NEW-FILES** (disjoint).

**BR-3 · settle-aggregate.** Source §4 #2 (closes G4d, run-13 finding-14
operator-bypass). New `ops/settle_aggregate.py` (directed-evidence aggregate
sign-off + synthesize missing `metrics_aggregate.json` provenance + harvest-style
receipt), composing `settle_run`'s shape. **M.** Build-ready, **blocked-on BR-1**.
Verified OPEN: file absent. **Conflict group: NEW-FILES.**

**BR-4 · recipe portability in sealed dossier.** Source §4 #3. Ship recipe
inside the dossier (`recipe` store noun in `DOSSIER_SOURCES`). **S-M.** DEFER
until BR-2 lands. **Conflict group: DOSSIER.**

**BR-5 · evidence-brief recipe citation kind.** Source §4 #4. Add `recipe` to
`CITATION_KINDS` (`state/evidence.py`). **S.** DEFER until BR-2's signature
exists. **Conflict group: EVIDENCE.**

### Amortized reduction check (amortized-reduction-check-2026-07-17)

**BR-6 · canary reducer-check (= "reducer-check build", queued).** Source §4.2.
Best-effort `_check_reducer_on_canary` riding the S2 detached canary worker: run
the run's declared `aggregate_cmd` through `cluster_reduce` over the canary's
task-0 output (bounded 300s), assert contract SHAPE only, DISCLOSE errors verbatim
(never refuse, severed-read → `unverified`). Files: `ops/submit_and_verify.py`
(helper + call site post-`:1046`), `ops/submit_blocks.py` (S2-brief `reducer_check`
field — coordinate with in-flight S1 editor). `cluster_reduce.py` reused unchanged.
**S-M.** Build-ready; needs RC1 (on-by-default: recommend yes), RC2 (disclose-not-block:
recommend disclose), RC3 (wave-0 rung: recommend defer). Verified OPEN: grep
`_check_reducer_on_canary` = 0. **Conflict group: SUBMIT-VERIFY** (submit_and_verify
disjoint; the submit_blocks brief add sequences after S1 in-flight lands).

### Transport robustness (transport-robustness-2026-07-17)

**BR-7 · delta-push consolidation — FALLBACK variant (Options 1+4).** Source
DELTA-PUSH §5. Fold prune-plan read into leg-A manifest read; collapse prune `rm`
+ retained-union reseal into one trailing `_ssh_bounded` script. **Zero
`transport/__init__.py` edits** (avoids in-flight U4). Files: `_delta.py`,
`_prune.py`. **M.** Build-ready NOW. Verified OPEN: no read-once/ack-append/seal-once
symbols in transport. **Conflict group: DELTA (_delta/_prune).**

**BR-7′ · delta-push consolidation — PRIMARY variant (read-once/ack-append/seal-once).**
Source DELTA-PUSH §4. Adds the ack-gated checkpoint ride inside each tar leg —
touches `transport/__init__.py` delta path + `_tar_ssh_push` (delete=False branch).
**M/L. Blocked-on U4** (in-flight) landing/rebase. Choose PRIMARY *or* BR-7, not
both. **Conflict group: TRANSPORT-INIT** (collides with U4).

**BR-8 · U3 submit-once phases 2+ (queued "U3 phases 2+").** Source
SUBMIT-ONCE-PREMORTEM §3 build-units U3-b/c/d/e + wire-render. The submit atom
(Δ1 atomic compare-and-mint, Δ4 rc-gate, Δ7 both command shapes; new
`ops/submit/jobmap.py` marker write/read + `__HPC_JOBMAP_ACK__`), correlation key
(Δ2 `--comment`/`-ac`/PBS resource), reconcile recovery (Δ5/Δ6 4-outcome table),
fault drills (same-PR). Files: `ops/submit/runner.py`, `infra/backends/_remote_base.py`,
`infra/backends/_engine.py`, `ops/monitor/reconcile.py`, new `ops/submit/jobmap.py`,
`tests/faultinject/`. **L.** Build-ready (all 8 premortem deltas ruled; OPEN-1…5
resolved) but **blocked-on U3 phase-1** (in-flight) deployed to all envs first
(Δ3 two-phase deploy). **Conflict group: SUBMIT-ATOM.**

**BR-9 · DB1 structural run-scoped wave partials (deployed-bugsweep follow-up).**
Source `b03c11bc` message ("structural run-scoped partials docketed as a larger
follow-up"). Write `_combiner/<run_id>/wave_<N>.json` (run-scoped) so a shared
`remote_path` graft never clobbers a foreign run's wave — the structural fix behind
the run-13 disaster class (only the disclosure boundary was closed). Files:
`ops/aggregate_flow.py`, `execution/mapreduce/combiner.py`, `transport/_combiner.py`.
**M.** Build-ready. **Conflict group: AGG-PROVENANCE** (same files as BR-1 —
sequence after BR-1, or fuse).

**BR-10 · U1 sync-files primitive.** Source AUDIT §9 U1. Extract canonical
`sync-files` primitive over `rsync_push/rsync_pull/_rsync_deploy/_deploy_transfer/
push_run_sidecar/_write_*_manifest`. **M.** Build-ready (mechanical). **Conflict
group: TRANSPORT-INIT** (coordinate with U4 + BR-7′).

**BR-11 · U2 run-script-returning-JSON primitive.** Source AUDIT §9 U2. Extract
primitive + sentinel-ack spine over `ssh_status_report/ssh_batch_scheduler_states/
_remote_{push,pull}_manifest/run_combiner*`. **M.** Build-ready. **Conflict group:
TRANSPORT-INIT-ADJ.**

**BR-12 · U5 breaker-slot uniformity + named-pipe-wrap contract test.** Source
AUDIT §9 U5 + §6. Route every ssh-touching site through `guarded_call` +
named-pipe-retry or a named exemption; contract test enforcing it. **S-M.**
Build-ready. **Conflict group: SSH-SITES** (broad — sequence vs U3/transport work).

**BR-13 · U9 scheduler-detection consolidation.** Source AUDIT §9 U9. Fold
`probe_cluster`'s 6–12 serial dials into one JSON-returning batch. Files:
`scheduler_resolve.py::probe_cluster`. ~~Verified OPEN~~ **LANDED —
`dbe5df73`** (probe + scheduler-detect consolidated in one commit:
`_build_probe_script` single round-trip, `__HPC_SCHEDPROBE_ACK__` sentinel,
severed→UNKNOWN, byte-equivalence oracle vs the serial reference, 4→1 and
10→1 dial pins; the ":211 still serial" grep had matched the NEW
implementation's docstring — dispatch agent verified 2026-07-17, 34 tests
green).

### Principles drift-log residuals

**BR-14 · attestation-migrate greenlight/unlock.** Source `determinism-boundary.md`
enforcement-map final row. ~~Build-ready~~ ~~STOP — RULING-GATED~~ **RULED A +
CLOSED (2026-07-17 same session): row satisfied by T6+T8; greenlight/unlock
formally deferred as sha-less precedence records, row parenthetical amended in
`determinism-boundary.md` with the rationale + drift entry. No build.** T8 auto-clear is ALREADY migrated (route-through asserted in
`test_auto_clear_writer_routes_through_the_kernel_bind` — this entry was stale);
the only remaining member, greenlight/unlock, does NOT fit the sha-keyed kernel:
greenlight records carry no content sha for `bind` to lock (the provenance gate
enforces by name-membership, not hash), and `reduce`'s newest-valid-wins would
regress the gate's timestamp-anchored + nudge-supersession selection
(`test_gate_greenlight_survives_later_unrelated_touchpoints`) — a consent hole,
not a refactor. RULING: (A, recommended) declare the row satisfied by T6+T8,
formally defer greenlight/unlock as not-fitting (update the row's parenthetical);
or (B) extend the kernel with a distinct non-sha precedence reducer — a kernel
API design change, not the "S" migration assumed here.
**Conflict group: ATTESTATION** (disjoint).

**BR-15 · pack-aliased-import-pin.** Source `domain-packs.md` row A7 ("known
predicted-risk, not yet pinned"). Extend the pack-boundary import-name scan to catch
aliased from-imports (`from importlib import import_module as load`). Files:
`tests/contracts/test_pack_boundary.py`, `ops/pack/*`. **S.** Build-ready. Verified:
scan exists at `test_no_import_or_execute_of_pack_content:395`, aliasing uncaught.
**Conflict group: PACK-BOUNDARY** (disjoint).

---

## §4 — OPEN but ruling-gated or blocked (NOT clean dispatch)

| id | source | why not dispatch-ready |
|---|---|---|
| **s4-gaps item 5** — aggregate-stream custom reducer | s4-gaps §item5 | **= §0 in-flight (R2)**; refuse-vs-per-arm-invoke |
| **s4-gaps item 2 residual** — sidecar-write lint | s4-gaps §item2 | build-ready but **DEFER** (behavior gap closed; belt-and-suspenders) |
| **Daemon DW1–DW3** (13 units: D-CORE/CLIENT/CONTEXT/VERBS/LANES/POOL/SUPERVISOR/SOAK/HOOKS/CLI/INSTALL/SHIM/DRIVE) | daemon-engineering ARCHITECT-MEMO §2a | **PARKED on R4 sec-2a step-3** (maintainer's call: does ~28× residual gap justify 13 units); D-FSYNC already extracted+landed |
| **WS-AGENT build** | cluster-agent-design.md | **blocked-on latency unit 2.6 (WS-PUSH) cluster-scale telemetry cycle** (mandatory gate) |
| **U6 deadline-argv unification** | AUDIT §9 U6 / §5 | **needs-a-ruling**: one shared deadline-derivation seam vs two |
| **unlock-synonym-pin** | determinism-boundary.md B8 | needs-ruling (accepted-posture caveat; synonym `reopen`/`release` evades substring pin) |
| **stage-swap drill re-point** | STAGE-SWAP drift log | needs-ruling: re-point rank-3 xfail at rsync primary vs keep on cp-a fallback |
| **latency rows 14-16 doc-mapping** (unit 3.1) | lifecycle-verdicts.md drift log | doc-mapping debt (mechanism may exist; row mapping deferred "tracked by plan") |
| **latency 3.2 P2 idle-horizon / 5.2 breaker cooldown** | latency ARCHITECT-MEMO | conditional/telemetry-gated (3.2) / R6-defer (5.2) |

---

## §5 — DISPATCH LADDER

Wave-able groups of **mutually file-disjoint, build-ready** units. Excludes all
§0 in-flight scopes. The coordinator can dispatch each wave verbatim.

### Wave 1 — dispatch NOW (6 units, fully file-disjoint, no in-flight collision)

| unit | files (exclusive) | ruling to pre-resolve |
|---|---|---|
| **BR-1** extract-recipe Task 1 | `ops/aggregate_flow.py`, `execution/mapreduce/combiner.py`, `ops/provenance_manifest.py` | R3 schema-bump (recommend bump) |
| **BR-6** canary reducer-check | `ops/submit_and_verify.py` (+ deferred submit_blocks brief) | RC1 yes / RC2 disclose |
| **BR-13** U9 scheduler-detection | `infra/.../scheduler_resolve.py` | none |
| **BR-14** attestation-migrate greenlight/unlock | `state/attestation.py`, attestation writers, `tests/state/test_attestation.py` | none |
| **BR-15** pack-aliased-import-pin | `tests/contracts/test_pack_boundary.py` | none |
| **BR-7** delta-push FALLBACK (Options 1+4) | `infra/transport/_delta.py`, `infra/transport/_prune.py` | choose fallback-vs-primary |

Disjointness proof: {aggregate_flow+combiner+provenance_manifest} ∩ {submit_and_verify}
∩ {scheduler_resolve} ∩ {attestation} ∩ {test_pack_boundary} ∩ {_delta+_prune} = ∅.
None touches the in-flight set {state/index, submit_blocks(S1), transport/__init__(U4),
stream(R2)}. BR-6's submit_blocks brief-field is the one soft overlap — land it as a
separate small edit after S1 lands.

### Wave 2 — after Wave 1 + in-flight clear (5 units)

| unit | files (exclusive) | unblocks-on |
|---|---|---|
| **BR-2** extract-recipe verb | NEW `ops/extract_recipe.py`, `ops/recipe_render.py`, `_wire/queries/extract_recipe.py` | BR-1 |
| **BR-3** settle-aggregate | NEW `ops/settle_aggregate.py` | BR-1 |
| **BR-9** DB1 structural run-scoped partials | `ops/aggregate_flow.py`, `combiner.py`, `transport/_combiner.py` | BR-1 (SAME files — sequence/fuse) |
| **BR-8** U3 submit-once phases 2+ | `ops/submit/runner.py`, `infra/backends/_remote_base.py`, `_engine.py`, `ops/monitor/reconcile.py`, NEW `ops/submit/jobmap.py`, `tests/faultinject/` | U3 phase-1 deployed to all envs (Δ3) |
| **BR-12** U5 breaker-slot uniformity | ssh-touching sites + contract test | sequence after BR-8/transport waves (broad SSH-SITES) |

BR-2 and BR-3 are new-file-disjoint and can run parallel in Wave 2. BR-9 must
sequence after BR-1 (AGG-PROVENANCE conflict). BR-8 is the largest single unit.

### Wave 3 — transport consolidation (after U4 lands)

BR-7′ (delta-push PRIMARY, if chosen over BR-7) · BR-10 (U1 sync-files) · BR-11
(U2 run-script-JSON) — all in/adjacent to `transport/__init__.py`; sequence after
in-flight U4 and coordinate ownership (TRANSPORT-INIT conflict group). Then BR-4
(dossier recipe) + BR-5 (evidence-brief citation) once BR-2 exists.

---

## §6 — File-conflict groups (units within a group MUST sequence)

- **AGG-PROVENANCE** — `aggregate_flow.py` + `combiner.py`: BR-1, BR-9.
- **TRANSPORT-INIT** — `transport/__init__.py`: in-flight U4, BR-7′, BR-10.
- **DELTA** — `_delta.py` + `_prune.py`: BR-7.
- **SUBMIT-ATOM** — `runner.py`/`reconcile.py`/`_remote_base.py`/`_engine.py`: BR-8.
- **SUBMIT-VERIFY** — `submit_and_verify.py`: BR-6.
- **SUBMIT-BLOCKS** (in-flight S1) — BR-6 brief-field sequences behind it.
- **NEW-FILES** (disjoint) — BR-2, BR-3 (parallel-safe).
- **SCHEDULER-RESOLVE** — BR-13. **ATTESTATION** — BR-14. **PACK-BOUNDARY** — BR-15.
- **SSH-SITES** (broad) — BR-12, BR-11: sequence against SUBMIT-ATOM/TRANSPORT.

---

## §7 — Clusters needing re-verification before dispatch (low-confidence)

Not fully verified this sweep (large, mostly-landed, header-stale — spot-checks
came back LANDED, so treat any residual as suspect until re-verified):

- **architecture-review-2026-07-13-delta "What is GENUINELY LEFT"** (AR-2 promote-public,
  AR-4a/b/c refactors, AR-5 legacy frontmatter, AR-7/AR-8 dead-code, AR-E2/E3/E6):
  AR-1 + AR-E1 verified LANDED, so the delta is partly stale. Re-verify per-item.
- **devx pack-seams P1a-P1d / WS5-WS7** (program-init, compose-rewrite, echo-collisions):
  devx B1-B5 + A1/WS1 LANDED; program-init adopt sequence noted pending in MEMORY.
  Re-verify `program-init` verb + `PackManifest.derived_from`.
- **fable-sweep "Follow-up sweep worklist"** (8 unswept-ground drills): these are
  sweeps-to-run, not built units — not dispatchable as code work.
- **handoff-packages m-elicit / l-snap / l-fastpath**: m-server/m-poll verbs
  (read-decisions, verify-relay, attention-queue, poll-detached) are in the live
  MCP tool list = LANDED; l-snap/l-fastpath unverified.

---

## Drift log

**2026-07-17 — created (mining sweep, docs-only).** Baseline `main @ a01d3356`.
Mined all 33 `docs/plans/**` docs + 11 `docs/internals/principles/*` drift logs
via a 4-agent parallel extraction fan-out (transport-robustness / latency+daemon /
devx+older / principles-logs) + direct read of the four 2026-07-17 memos, then
verified every candidate against `git log --oneline -120` + targeted grep. Central
finding: the stale-plan failure mode is pervasive — fable-sweep-2026-07-13 (F01-F58),
deployed-bugsweep (DB1-DB8), migrate-remainder, devx B1-B5, half the latency program,
and s4-gaps items 1-4 all LANDED while their plan headers still read banked/unbuilt;
each is cited to its commit in §2 so the coordinator stops re-queueing them. ~21
units verified genuinely OPEN + build-ready (§3); the daemon DW1-DW3 program (13
units) is the largest OPEN block but parks on one maintainer ruling (R4 sec-2a
step-3). Dispatch ladder §5: wave 1 = 6 fully-disjoint units dispatchable now
(BR-1/6/13/14/15/7), wave 2 = 5 (BR-2/3/9/8/12), wave 3 = transport consolidation
after in-flight U4. Excluded the four §0 in-flight scopes throughout. This doc is a
point-in-time backlog; re-mine after the next integration wave (the §7 clusters were
not exhaustively verified — spot-checks landed, so their residuals are suspect).
No `src/**` touched; no commit.
