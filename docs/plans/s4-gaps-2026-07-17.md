# S4 gaps — ground-truth + remaining specs

2026-07-17 · baseline `main @ c893d2fa` (clean tree). **Scoping-only unit** — this
doc establishes ground truth for the queued "S4 gaps" bundle against the CURRENT
code and produces build-ready specs for what remains. No src/** change lands here.

A lot moved through this area since the items were first remembered: the
**streaming-aggregate** train (`d2b18bfd`, 2026-07-16) shipped `aggregate-stream`
plus the two reducer-contract *fixes*; the **cluster-final reduce default**
(`83dd5988`), the **finding-28 reducibility surface** (`9f2760b5`), the
**settle-run** verb (finding-25), and the **write-run-sidecar** primitive (#200)
all landed. The net effect is that **two of the four items are DONE**, one is
**PARTIAL** (the doc, not the code), and one is genuinely **OPEN** — plus one
adjacent gap the streaming v1 left behind.

Verdict summary:

| # | Item | Verdict |
|---|---|---|
| 1 | S1 reducibility shift-left | **OPEN** (predicate exists + shared; never called at S1) |
| 2 | write-run-sidecar not hand-edit | **DONE** (primitive + all writers route through it; journal analogue also closed) — one optional guard-rail residual |
| 3 | reducer-contract doc | **PARTIAL** (doc exists but predates the streaming-era contract additions) |
| 4 | S4 pushes its own reducer | **DONE** (`d2b18bfd`: derive + ship + stage-refuse + env-python) |
| 5 | *(sweep)* aggregate-stream ignores the run's custom reducer | **BUILT** (2026-07-17: arm-complete-gated per-arm custom reduction; R2 ruled build-not-refuse) |

---

## Item 1 — S1 reducibility shift-left

**Lead (as remembered):** reducibility problems surface only at S4/aggregate time;
shift the check left to S1 so an irreducible plan is refused before compute is spent.

**Ground truth.**
- The reducibility predicate is **`per_task_fallback_reducible(summary_name)`**
  (`src/hpc_agent/ops/aggregate_flow.py:305`, exported `:98`) — TRUE iff the declared
  summary artifact is a JSON name the built-in no-combiner per-task fallback
  (`reduce_metrics`, a JSON weighted-mean) can actually reduce.
- It is **already lifted into a check-time gate**: `_reducibility_issue`
  (`src/hpc_agent/ops/aggregate_blocks.py:210`) reads the sidecar, and when a run
  has **no `aggregate_defaults.aggregate_cmd`** AND a **non-JSON summary artifact**,
  emits a never-auto-masked `non_reducible_summary_artifact` readiness issue. The
  docstring is explicit that "the condition is knowable HERE from the sidecar the
  submit wrote" (`:217-220`).
- **But that gate fires only at aggregate CHECK time** — `aggregate_blocks.py:436`
  appends it to the aggregate readiness issues, i.e. **after the whole run has
  computed and after a 40-min `results/` pull** (`:216-217` names exactly this
  cost). This is finding-28's landing (`9f2760b5`, "surface … at CHECK time, before
  greenlight") — a shift-left *within* aggregate, not to S1.
- **S1 does NOT run the check.** `submit_blocks.py`'s S1 leg discloses a
  `deploy_payload` brief (`:736`, finding-4 payload disclosure) and mints the
  sidecar via `resolve_submit_inputs` (`:719`), but there is **no reducibility
  call** anywhere in `submit_blocks.py` or `walk_submit_ambiguities.py` (grep:
  zero hits for `reducib` / `per_task_fallback` in either). The sidecar the check
  needs (`aggregate_defaults.aggregate_cmd` + `resolved_summary_artifact`) **exists
  by S1's resolved boundary** — `rr.sidecar_path` / `rr.submit_spec` at
  `submit_blocks.py:719-728`.

**Verdict: OPEN.** The predicate is shared and cheap; only the call site at S1 is
missing. This is the smallest of the open items — it reuses `_reducibility_issue`
verbatim against the just-written sidecar.

**Remaining spec (build-ready).**
- **Files touched:**
  - `src/hpc_agent/ops/aggregate_blocks.py` — export `_reducibility_issue` (or lift
    it + `per_task_fallback_reducible` into a shared `ops/aggregate/_reducibility.py`
    so submit does not import an aggregate-block internal; layering-lint will demand
    the shared home — check `scripts/lint_subject_imports.py`).
  - `src/hpc_agent/ops/submit_blocks.py` — in the S1 resolved leg, right beside the
    `deploy_payload` disclosure (`:736`), call the shared predicate against
    `rr.sidecar_path` and attach a `reducibility` block to `brief` when it fires.
  - `tests/ops/test_submit_blocks.py` (or the S1 block test home).
- **The change:** at the S1 resolve boundary, run the same predicate the aggregate
  check runs; surface a `non_reducible_summary_artifact` **disclosure** on S1's
  brief (the human boundary before submit-s2 detaches and spends compute). Because
  the predicate is code-computed from the sidecar and fail-open, it mirrors the
  `deploy_payload` finding-4 pattern exactly.
- **USER RULING owed (one, small):** *disclose vs hard-refuse* at S1. Finding-4's
  payload is disclosed-not-blocking; a non-reducible plan is arguably worse (it
  **cannot ever** produce a number, so any compute is wasted). Recommend: surface as
  a **loud readiness disclosure that does not auto-mask** (same class as the check
  gate) and let the greenlight cross it deliberately — NOT a hard block, since a
  human may intend to register the reducer between S1 and aggregate. The ruling is
  only "disclose vs block"; everything else is pure engineering.
- **Pins/tests owed:** fires-and-passes pair — (a) an S1 spec with no `aggregate_cmd`
  + a `.csv` summary artifact → S1 brief carries the reducibility disclosure; (b) a
  spec with an `aggregate_cmd` (custom reducer) OR a `.json` artifact → clean S1,
  no disclosure. One test asserting the S1 predicate and the CHECK predicate are the
  **same call** (they can never disagree — the finding-28 "lifted so the two can
  never disagree" invariant, extended to a third seat).
- **Enforcement row owed** (`docs/internals/principles/lifecycle-verdicts.md`,
  format `| Rule | Enforced by | Fires when |`):
  `| Reducibility is one predicate, now THREE seats (run-path refuse, aggregate-CHECK disclosure, S1 disclosure) — all call per_task_fallback_reducible; no seat re-derives it | tests/ops/test_submit_blocks.py::test_s1_surfaces_non_reducible, tests/ops/test_aggregate_blocks.py::test_check_surfaces_non_reducible | an S1/check seat inlines its own JSON-name test instead of the shared predicate, or the S1 seat stops firing before submit-s2 detaches |`
- **Size:** SMALL (~1 shared-home extraction + 1 call site + 2 tests + 1 row).

---

## Item 2 — write-run-sidecar not hand-edit

**Lead (as remembered):** S4/recovery/retarget flows have users/agents HAND-EDITING
run sidecars; the `write-run-sidecar` primitive exists — the gap is flows that don't
route through it.

**Ground truth.**
- The primitive exists: `docs/primitives/write-run-sidecar.md` (verb `mutate`,
  `hpc_agent.ops.write_run_sidecar.write_run_sidecar`), created for #200 to *replace*
  the inline `python -c "import hpc_agent.state.runs; write_run_sidecar(...)"` agents
  used to make (primitive doc `:19`).
- **Every code writer routes through the primitive / the state-layer functions.**
  `write_run_sidecar` / `update_run_sidecar` importers (grep):
  `ops/resolve_submit_inputs.py`, `ops/submit_flow.py`, `ops/submit/runner.py`,
  `ops/revise_resolved.py`, `ops/campaign_refill.py`,
  `ops/decision/journal/verify_relay.py`, `ops/recover/features_glue.py`,
  `ops/recover/runner_failures.py`, `ops/aggregate/cluster_reduce.py`.
- **Retarget routes through revise-resolved, not a hand-edit.** `ops/retarget_run.py`
  re-derives the target sidecar via `revise-resolved`'s reconstruction
  (`_reconstruct_resolve_spec`, `retarget_run.py:11-13,73-78`); the write itself is
  `revise_resolved`'s `write_run_sidecar` call. The side-effect declaration is
  `writes-sidecar → <experiment>/.hpc/runs/<new_run_id>.json` (`:174-175`).
- **No residual agent-facing "hand-edit the sidecar" guidance** survives in
  `skills/`, the recovery ops, or error messages (grep for `python -c` / `edit the
  run` / `by hand` against those trees returns only the write_run_sidecar doc itself
  and an unrelated recover/resolve codegen note).
- **The JOURNAL-record analogue is also closed.** run-12 finding-25 ("no sanctioned
  human-directed settle — closing run 12 required journal surgery",
  `docs/design/history/run12-findings.md:536-559`) proposed a `settle-run` verb; it
  **landed** (`src/hpc_agent/ops/settle_run.py`, `operations.json:2062-2072`,
  schemas present) and runs the same `mark_run + harvest_on_terminal` transition
  the probe path runs — "no journal surgery."

**Verdict: DONE.** Both the sidecar store and its journal-record analogue are
mechanized; all flows route through the primitives.

**Optional residual (low-priority engineering, NOT the queued item).** The
discipline is *followed* but not *mechanized*: there is no lint that FAILS a future
call site which writes `.hpc/runs/<id>.json` directly (`json.dump` to the runs path
outside `state/runs.py`). Today the honor system holds because the affordance is
convenient, but per the CLAUDE.md "remove the affordance" doctrine
(`docs/internals/principles/determinism-boundary.md`) a guard-rail would make it
stick. If desired as a separate micro-unit:
- **Files:** `scripts/lint_*.py` (a new "sidecar writes go through state/runs" rule)
  + its allowlist for `state/runs.py` and the primitive.
- **Enforcement row:** `| A run sidecar is written ONLY through state.runs.write_run_sidecar/update_run_sidecar | scripts/lint_sidecar_writes.py | any module outside state/runs.py opens .hpc/runs/<id>.json for write |`
- **Size:** SMALL. **No user ruling.** Recommend DEFER unless a regression appears —
  the behavior gap is closed; this is belt-and-suspenders.

---

## Item 3 — reducer-contract doc

**Lead (as remembered):** the contract between task outputs and the reducer lacks a
canonical doc; check whether the streaming Step-C reducer-contract fixes landed — the
DOC may still be owed even if the fixes landed.

**Ground truth.**
- The canonical doc **EXISTS**: `docs/reference/reducer-contract.md` — the four-point
  contract (reads `$HPC_RUN_ID`, writes `$HPC_AGGREGATED_OUTPUT`, valid JSON, exit
  code), cluster-vs-local (`$HPC_RESULTS_DIR`) semantics, a minimal example, and the
  scalar-mean→combiner / anything-else→cluster-reduce split rule. It is referenced
  from `ops/aggregate/cluster_reduce.py:19,35`.
- **But it predates the streaming-era contract additions.** Last touched `d81ed2d0`
  (pure-API reduction honors mode/aggregate_cmd) — BEFORE the streaming train
  `d2b18bfd`. It does **not** mention (grep for `stream` / `arm` / `activation` /
  `env_python` / `deploy` → zero hits) the three things that landed since and are now
  part of the effective contract a reducer author must know:
  1. **Interpreter binding (S-REDUCE §3.C.1, `cluster_reduce.py:287-299`).** The
     reducer's literal `python3`/`python` now binds the **run's env interpreter** via
     `remote_activation_for_sidecar`, not bare login python (retires the run-14
     py3.8-vs-3.13 crash). A reducer author must know their command runs under the
     run's cluster env, and that a local-env miss is disclosed as the pinned
     interpreter, not a cluster bug.
  2. **Ship-the-reducer + submit-time refusal (S-REDUCE §3.C.2, item 4 below).** A
     **file-path** reducer (`python3 specs/reduce_x.py`) is auto-derived
     (`_deploy_items.py:69 reducer_relpath_from_aggregate_cmd`), shipped as a
     content-hashed deploy item, and **must be on disk at submit or the run REFUSES
     at stage** (`submit_flow.py:907-912`). A **module** reducer (`python -m pkg.x`)
     is assumed installed and is NOT shipped — a materially different author contract
     the doc never states.
  3. **Streaming interaction (item 5).** `aggregate-stream` currently reduces via the
     **built-in** path only, so a custom-reducer run's streamed partial does not use
     the author's reducer — the doc should warn that streaming ≠ final for a custom
     reducer until item 5 lands.

**Verdict: PARTIAL — the DOC is owed for the streaming-era additions** (the code
fixes landed; the canonical doc did not absorb them).

**Remaining spec (build-ready, pure docs).**
- **Files touched:** `docs/reference/reducer-contract.md` (extend, don't rewrite).
- **The change:** add two sections —
  - *"Where the interpreter comes from"*: the reducer runs under the run's
    `env_python` activation; literal `python3` resolves to the run env; disclose the
    file-path-vs-`python -m` distinction and its shipping consequence.
  - *"Deployment & the on-disk requirement"*: a file-path reducer must exist under the
    experiment repo at submit; it ships automatically (content-hashed); a missing
    reducer refuses at submit, not mid-harvest. Cross-link
    `docs/primitives/aggregate-stream.md` with the "streaming uses the built-in path
    today" caveat (retire the caveat when item 5 lands).
- **Pins/tests owed:** none in the doc itself, BUT add a **doc-freshness pin** so the
  doc can't silently rot again: a contract test that greps `reducer-contract.md` for
  the `env_python`/`ship`/`stream` keywords, OR a row in the aggregate principles
  section naming the doc as the contract's source of truth. Recommend the latter.
- **Enforcement row owed** (`docs/internals/principles/determinism-boundary.md` or a
  new aggregate section):
  `| The reducer contract (interpreter binding, on-disk-ship refusal, JSON output) has ONE source-of-truth doc: docs/reference/reducer-contract.md; cluster_reduce + _deploy_items + submit_flow's stage-gate behavior match it | tests/reference/test_reducer_contract_doc.py (keyword pins) | the doc omits a shipped contract behavior, or a code seam diverges from the documented contract |`
- **Size:** SMALL (docs + one keyword-pin test + one row). **No user ruling.**

---

## Item 4 — S4 pushes its own reducer

**Lead (as remembered):** S4/aggregate should deploy the reducer code it needs
(version-matched) rather than relying on whatever is on the cluster; check whether the
recent cluster-reducer work already made this true.

**Ground truth — this LANDED in `d2b18bfd` (S-REDUCE §3.C.2).**
- **Derive:** `reducer_relpath_from_aggregate_cmd(aggregate_cmd)`
  (`src/hpc_agent/infra/transport/_deploy_items.py:69`) tokenizes the sidecar's
  `aggregate_defaults.aggregate_cmd` and extracts the `specs/reduce_*.py` file token
  (a `python -m` module reducer returns `None` — it's assumed installed, `:81-86`).
- **Ship:** the derived reducer is added to the deploy set as a **content-hashed**
  item (`_deploy_items.py:221-227`) and forwarded through
  `submit_flow._resolve_reducer_deploy_item` (`submit_flow.py:858`) → `deploy_runtime`
  as `extra_files` (`:853`), staged like `.hpc/_hpc_combiner.py`.
- **Stage-refuse:** a declared-but-absent reducer **refuses at submit**
  (`submit_flow.py:907-912`): "declares a custom reducer … NOT on disk … the cluster
  reduce would die mid-harvest with 'no such file' (the run-14 … manual-scp class)."
  The gate fires per-fresh-spec across a batch (`:884-891`).
- **Version-matched interpreter:** the shipped reducer runs under the run's env via
  the S-REDUCE §3.C.1 activation threading (`cluster_reduce.py:287-299`).

**Verdict: DONE.** S4 no longer relies on whatever reducer is on the cluster — it
derives, ships (content-hashed), refuses-at-submit-if-absent, and runs it under the
run's own interpreter. Landing = `d2b18bfd`.

**Residual (belongs to item 5, not this item):** the *streaming* path
(`aggregate-stream`) does not yet **invoke** the shipped custom reducer — it reduces
built-in. The deploy/refuse machinery is done; the streaming consumer is not. See
item 5.

---

## Item 5 (completeness sweep) — aggregate-stream ignores the run's custom reducer

**Where it came from.** Sweeping the streaming spec's own §3.B / §6-S4 against what
`stream.py` actually shipped, plus the run-14 motivating artifacts
(`docs/plans/run14-rigor-runsheet.md:21-32` — the live lgbm/xgb runs use
`aggregate_cmd = python3 specs/reduce_causal_tune_tree.py` emitting `qlike`/`dm_better`).

**Ground truth.**
- `aggregate-stream` (`src/hpc_agent/ops/aggregate/stream.py`) reduces complete arms
  through the **built-in** `reduce_metrics` weighted-mean (`:235,242`) or the
  ownership `multi_parent_reduce` (`:438`). It **never reads `aggregate_cmd`** and
  **never calls `cluster_reduce`** (grep: zero `aggregate_cmd` / `cluster_reduce`
  hits in `stream.py`).
- Consequence: for a run that declares a custom reducer — **the exact live case the
  whole streaming train was built for** — the streamed partial table carries built-in
  weighted-mean numbers that **do not match** the run's own reducer output
  (`qlike`/`dm_better` are not a plain mean of per-task scalars). And it does so
  **silently**: there is no refuse, no disclosure that "these numbers are not your
  reducer's."
- The streaming spec named this exactly (§3.B, §5, §6 row S4): the intended contract
  was either an **`HPC_STREAM_ARMS` allowlist** the custom reducer honors, or a
  **per-complete-arm `cluster_reduce` + row-union** (reducer-agnostic). Grep confirms
  **neither shipped** — no `HPC_STREAM_ARMS` in the tree. v1 shipped built-in-only.

**Verdict: BUILT (2026-07-17).** R2 was ruled **build, not refuse**: a custom-reducer
run now streams **arm-complete-gated per-arm custom reduction** — each complete arm is
reduced through the run's OWN reducer (`cluster_reduce`, restricted to the arm's task
ids via an `HPC_STREAM_TASK_IDS` allowlist), memoized with a durable receipt keyed
`(run_id, arm, cmd_sha)`, one invocation per newly-complete arm. Cross-arm/union/
pairwise stays final-harvest-only (`aggregated_metrics={}` on the custom path, disclosed).
A reducer failure on a complete arm is disclosed verbatim (`arms_reduce_failed`) and the
other arms still stream — never a silent built-in fallback. The built-in path is
byte-unchanged for non-custom runs (regression-pinned). See the drift log below.

**Remaining spec (build-ready).**
- **Files touched:**
  - `src/hpc_agent/ops/aggregate/stream.py` — add a reduce-path selector: if the run
    declares an `aggregate_cmd`, reduce each complete arm through `cluster_reduce`
    over that arm's dirs and union the rows (the spec's reducer-agnostic fallback);
    OR, minimally, **refuse-to-stream** with "run declares a custom reducer; streaming
    the built-in mean would not match — final harvest only" (mirrors the existing
    non-wave-aligned refuse, `stream.py:386`).
  - `tests/ops/aggregate/test_stream.py`.
- **The change:** honor the custom reducer for streaming, or refuse honestly. Do NOT
  silently emit built-in numbers for a custom-reducer run.
- **USER RULING owed (one, real):** *invoke-per-arm vs refuse-to-stream* for the v1.
  Per-arm `cluster_reduce` is N SSH reduce invocations per census tick (cost) but
  gives a true progressive custom-reducer table; refuse-to-stream is cheap and honest
  but drops streaming for exactly the custom-reducer runs that motivated it. Recommend
  **refuse-to-stream in the immediate fix** (honest, ships now, zero wrong numbers)
  with **per-arm invoke as the follow-on** once the census-tick cost is acceptable.
  The ruling is behavioral (what the live lgbm/xgb operator gets), so it needs the
  user.
- **Pins/tests owed:** (a) a custom-`aggregate_cmd` run → stream refuses (or invokes
  the custom reducer, per the ruling), never emits a built-in mean; (b) a
  built-in-reduce run → unchanged. A doctrine test asserting stream never emits a
  number the run's declared reducer didn't compute.
- **Enforcement row owed** (`docs/internals/principles/determinism-boundary.md`):
  `| aggregate-stream emits only numbers the run's OWN reducer would compute — a custom-aggregate_cmd run is reduced through cluster_reduce or refused, never through the built-in mean | tests/ops/aggregate/test_stream.py::test_custom_reducer_run_not_built_in_meaned | stream.py reduces a run with a declared aggregate_cmd through reduce_metrics |`
- **Size:** SMALL if refuse-to-stream; MEDIUM if per-arm invoke (touches the
  census→dir→reduce plumbing + multi-invocation error handling).

---

## Recommended build order + dependencies

All units are file-disjoint except where noted; classify per CLAUDE.md.

1. **Item 3 (reducer-contract doc)** — `[∥]` no code dependency; pure docs +
   keyword-pin test + one row. Do first / in parallel: it documents the behavior
   items 4 (done) and 5 (pending) hinge on, and its "streaming uses built-in today"
   caveat is written now and retired when item 5 lands. SMALL.

2. **Item 1 (S1 reducibility shift-left)** — `[∥]` with item 3. Needs the one small
   USER RULING (disclose vs block) resolved first, then it is a shared-home extraction
   + one S1 call site + tests + row. Independent of items 3/5. SMALL.

3. **Item 5 (aggregate-stream custom reducer)** — `[seq]` needs the USER RULING
   (refuse-vs-per-arm-invoke). Land AFTER item 3's doc caveat is in (so the doc and
   the code move together), and after item 4's deploy machinery (already done) is the
   substrate the per-arm-invoke path would use. If the ruling is refuse-to-stream:
   SMALL, can go in parallel with items 1/3. If per-arm-invoke: MEDIUM, sequence last.

4. **Item 2 residual (sidecar-write lint)** — `[∥]` DEFER. Behavior gap is closed;
   ship only if a future direct-write regression appears. SMALL.

**Dependency graph:** item 3 (doc) is a soft prerequisite for items 1 and 5 (both
reference it); items 1 and 5 are otherwise independent and file-disjoint (submit_blocks
vs stream.py). Two USER RULINGS gate the open items — resolve both up front:
- **R1 (item 1):** at S1, disclose-not-block vs hard-refuse a non-reducible plan.
  *Recommend disclose (never auto-mask).*
- **R2 (item 5):** custom-reducer streaming = refuse-to-stream (ship now) vs
  per-arm cluster_reduce+union (follow-on). *Recommend refuse now, invoke later.*

Everything else is pure engineering.

---

## Drift log

**2026-07-17 — created (scoping-only).** Established ground truth for the queued
"S4 gaps" bundle against `main @ c893d2fa`. No src/** touched; no commit. Findings:
items 2 and 4 are DONE (write-run-sidecar #200 + all-writers-route-through-it and its
settle-run journal analogue; `d2b18bfd` derive+ship+stage-refuse+env-python for the
reducer), item 3 is PARTIAL (doc exists but predates the streaming-era contract
additions — the doc is owed, not the code), item 1 is OPEN (predicate shared, S1 call
site missing), and the completeness sweep surfaced item 5 (aggregate-stream reduces a
custom-reducer run through the built-in mean, silently — the streaming v1's residual,
same doctrine class). Two USER RULINGS (R1 disclose-vs-block, R2 refuse-vs-invoke)
identified as the only non-engineering gates. This doc is the canonical spec home the
bundle previously lacked; the streaming design lives in
`docs/plans/streaming-aggregate-2026-07-16.md` (the S-REDUCE/S-STREAM units) and is
not re-litigated here. Enforcement rows named above are OWED at build time in the
principles sections cited (formats copied from `lifecycle-verdicts.md`); regen the
principles index (`python scripts/regen_all.py --write`) when they land.

**2026-07-17 — item 5 (aggregate-stream custom reducer) BUILT, RULED *build (per-arm), not refuse*.** R2 was resolved **arm-complete-gated per-arm custom reduction**, not refuse-to-stream. A run that declares a custom `aggregate_cmd` now streams the RUN'S OWN reducer, per arm: `ops/aggregate/stream.py` gained a third reduce path (`_custom_reduce`, selected when ANY parent declares `aggregate_cmd` and no migrate-ownership pair applies; the built-in and ownership paths are untouched). For each COMPLETE arm (the census already whole-arm-gates — no partial-arm reduction ever), `cluster_reduce` is invoked ONCE, restricted to that arm's task ids via `extra_env={HPC_STREAM_ARM, HPC_STREAM_TASK_IDS}` — the same guarded/breaker ssh reduce leg the final harvest rides (cost honesty). Each reduced arm is memoized with a durable JSONL receipt keyed `(run_id, arm, cmd_sha)` under `_aggregated/_stream_reduce_memo/<run_id>.jsonl` — one invocation per newly-complete arm, not per tick; a changed `cmd_sha` re-fires (the combine-cache B1 cross-run-clobber class, `7aaff88f`, is avoided by the per-run file + cmd_sha-in-key). Cross-arm/union/pairwise stays final-harvest-only: `aggregated_metrics={}` on the custom path, disclosed via `value_scope` + `value_labels` (`per-arm-final` / cross-arm final-harvest-only) + a `completeness_label` (`"N-of-M arms complete"`) on every emission. A reducer FAILURE on a complete arm (non-zero exit, non-dict output, or severed leg) is disclosed VERBATIM in `arms_reduce_failed` and the other arms continue — never a silent built-in fallback (a built-in number for one arm amid custom numbers is the divergence bug reborn). Zero new gates/decision points (bare-y projection surface). Built-in path byte-unchanged for non-custom runs (regression-pinned: the custom-only provenance keys never appear on the built-in snapshot). Wire model `_wire/queries/aggregate_stream.py` gained `StreamArmReduceFailed` + result fields `arms_reduce_failed`/`completeness_label`/`value_scope` (additive) -> regenned `schemas/aggregate_stream.output.json` ONLY. `docs/reference/reducer-contract.md` "Streaming does not run your reducer yet" section REWRITTEN to the new truth (per-arm-final values, the `HPC_STREAM_TASK_IDS` allowlist convention, cross-arm residual, verbatim-failure disclosure, the `(run_id,arm,cmd_sha)` memo). Tests: `tests/ops/aggregate/test_stream.py` +4 (red-then-green): run-own values for complete arms only / incomplete never reduced / memo skip + cmd_sha re-fire / failure disclosed-others-continue / built-in byte-unchanged / N-of-M label present. Enforcement row in `determinism-boundary.md` still OWED (out of this unit's touch scope — noted for a follow-on). The item-2 residual (sidecar-write lint) remains DEFER.

**2026-07-17 — item 1 (S1 reducibility shift-left) BUILT, RULED *disclose*.** R1 was resolved *disclose, not hard-refuse* (the system amplifies human effort; gates don't tighten — a human may register the reducer between S1 and aggregate). Built `submit_blocks._reducibility_brief`, wired into `_submit_s1_impl`'s resolved leg beside the `deploy_payload` disclosure: it runs the SAME predicate the aggregate-CHECK seat keys on (`aggregate_blocks._reducibility_issue` -> `per_task_fallback_reducible`) against the just-written sidecar and attaches a `reducibility` block to S1's brief on a hit — reusing the aggregate-block issue rendering VERBATIM (a THIRD seat of the one predicate). Disclosure-only: no gate, no refusal, no new decision point, the bare `y` flow byte-unchanged; a reducible plan adds no brief key. No-silent-caps honored — an unreadable sidecar / unminted run_id yields a `checked: False` could-not-run line. The predicate did NOT move: `_reducibility_issue` stayed in `hpc_agent.ops.aggregate_blocks`, a role-root SIBLING of `submit_blocks` in the SAME `hpc_agent.ops` package, so the reuse trips neither `lint_private_cross_package_imports` (same package) nor `lint_subject_imports` (role-root files are not subjects) — no shared-home extraction needed, matching the `_assert_no_post_greenlight_drift` sibling-import precedent. Tests: `tests/ops/submit/test_s1_reducibility_brief.py` (7, red-then-green). Enforcement row + drift-log landed in `docs/internals/principles/lifecycle-verdicts.md`. Surfaces (CLI/schema/wire) unchanged -> no regen. Items 3/5 and the item-2 residual remain per the verdict table above.

**2026-07-17 — item 5 OWED enforcement row LANDED.** The `3c9e5599` build entry above named the determinism-boundary enforcement row as OWED ("still OWED ... noted for a follow-on"). It is now written — into `docs/internals/principles/lifecycle-verdicts.md`, NOT `determinism-boundary.md`. The R2 author's `determinism-boundary` guess was verified against the section files and corrected: the invariant is reduce-value honesty (own-reducer-over-complete-arms / never-a-silent-built-in-fallback / partiality-labeled), the direct sibling of lifecycle-verdicts row-107 ("never a fabricated scalar") and row-131 (reducibility) — and the entire reduce/aggregate/harvest family lives in lifecycle-verdicts (rows 102, 107, 118, 119, 130, 131); determinism-boundary carries zero reduce rows. The row cites the four existing custom-reducer fire-path tests in `tests/ops/aggregate/test_stream.py` (own-values-complete-arms-only, failure-disclosed-others-continue, memo-skip-and-cmd_sha-re-fire, built-in-byte-unchanged) — every clause was already fire-shaped-pinned by the build, so no new test was owed. Principles index regenerated (`build_principles_index.py`). Item 5 is now fully closed (build + doc + enforcement row); item 3 (reducer-contract doc), item 1 residual, and the item-2 residual stand per the verdict table.
