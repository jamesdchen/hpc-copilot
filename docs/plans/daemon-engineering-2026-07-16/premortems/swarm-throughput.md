Note: claude-sonnet-5[1m] (the safety classifier) was unavailable when reviewing this subagent's work. Please carefully verify the subagent's actions and output before acting on them.

All verification reads complete. Findings below.

---

# SWARM-THROUGHPUT PREMORTEM — WS-DAEMON v2 build decomposition

**Baseline correction (verify first, the memo's own Wave-0 rule):** HEAD is no longer 57b31f9c — the SGE agent's `a2b73bfb` has LANDED (tree clean). Its surface: `infra/backends/__init__.py`, `infra/failure_signatures.py`, `ops/verify_canary.py`, `ops/recover_flow.py`, `execution/mapreduce/dispatch.py`, `_wire/workflows/submit_flow.py`, `state/runs.py`, 9 regenerated schemas, `hpc_preamble.sh`, `scripts/lint_subject_imports.py`. Treat `infra/backends/**` + `infra/failure_signatures.py` as STILL claimed (classifier work continuing). Re-run `git status` at every dispatch.

## 1. Citation audit — the design's file/line map vs the real tree

Verified accurate at current HEAD: `mcp_server.py` (`_shield_real_stdin` 513-533, `_in_process_cli_runner` 536-621, state-leak audit 556-585, SystemExit 616-617, `_refuse_blocking_over_mcp` 701, `_build_invocation` 896, `_tool_result` 942, `_invoke_cli` 1220 (design says 1231-1238 — body, fine), `_elicitation_applies` 1283/1310, `_elicit_then_retry` 1312, `handle` 1567, `_request_from_client` 1672, win32 no-op deadline 486-489); `env_flags.py` (44-52, 63-78, 93); `io.py` (`_fsync_dir` 92, `atomic_write_json` 110, `append_jsonl_line` 257-287, suppress-fsync 286-287, 120s lock 283, `advisory_flock` 378); `_fast_path_cache.installed_dist_signature` 60; `dispatch.py` (:21, `_record_detached_failure_terminal` cwd :290, reconfigure loop 383-387, fast path 399); `detached.py` (pid_alive re-export 53, `SUPPORTED_DETACHED_BLOCK_VERBS` 104, `DetachedLeaseHeld` 122); `ssh_engine.py` (IDLE_CLOSE_SEC 142, loop thread 311-332); `cli/mcp.py` header; `infra/backends/__init__.py:551,680` (`cwd = cwd or Path.cwd()` — confirmed post-SGE-commit). The cwd grep reproduces the design's classification (27 hits; design missed only `mcp_server.py:1352` — shim-side, stays put — and `experiment_kit/checkpoint.py:114`, worker-runtime, exempt).

**Five assumptions in the design that the tree contradicts or complicates:**

1. **The Windows CI leg is `continue-on-error: true` and runs DEFAULT tier only** (`ci.yml:420-452`, ref #163, ~86 untriaged win32 skips). §8's "Windows leg mandatory and gating" does not exist to inherit — and the cited precedent `test_serve_survives_sequential_tool_calls_over_real_pipes` is `@slow`, and the `-m slow --timeout=0` step runs **only on the Linux 3.12 leg** (ci.yml:259-263). That test has NEVER run on Windows CI. The daemon program must BUILD its gate: a new required `test-windows-daemon` job (checkout + pip install + `pytest tests/daemon/ -q` + the named slow soaks with `--timeout=0`), separate from the non-blocking full-suite job. Cost: ~3-5 min setup (pip install -e .[dev], cached) + battery. Budget the battery: real daemon spawns are the cost driver — pre-2.1 (no bake) a cold `python -m hpc_agent daemon-serve` on a windows-latest runner under AV is 3-10s; cap total spawns at ~12-15 (one session-scoped daemon shared by all read-only/parity/context tests; fresh spawns only for lifecycle/skew/kill/single-instance tests), soak 200 calls ≈ 30-90s, N=8 concurrency ≈ 1 min → job total ≈ **7-10 min, acceptable as required**. Two pins: (a) test fixtures use their own boot deadline (≥60s) decoupled from the product's 15s activation budget, or windows-runner AV flake becomes the gate's flake; (b) `HPC_DAEMON_DIR=<tmp>` in EVERY fixture (already in the design — make it a conftest autouse guard so a forgotten test can't dial a dev's real daemon).

2. **`daemon_client`'s "stdlib-only, import weight IS the product" claim has an unstated hard dependency on unit 1.3.** `hpc_agent/__init__.py` is EAGER today (module-scope imports of `contract.layout`, `registry.primitive`, `infra.clusters` — the 0.9-1.3s floor), and any `hpc_agent.cli.daemon_client` import pays it. `cli/__init__.py` is docstring-only (clean). So rung 1's win math only holds post-1.3 (lazy PEP-562 root). Pin it: **1.3 is a declared prerequisite of D-CLIENT**, and the unit ships a contract test `python -X importtime -c "from hpc_agent.cli.daemon_client import try_daemon"` asserting no pydantic / no `_kernel.registry` / no `infra.clusters` in the import graph (same shape as 1.3's eager smoke, inverted).

3. **The HPC_\*-reader census is 70 read sites across 39 files** outside the design's four allowlisted modules — not a residue. Roughly half are structurally exempt (cluster-side deploy files: `execution/mapreduce/*`, scaffold templates — Python-3.8-floor files that can never run in the daemon; worker-only: `experiment_kit/*`, `crash_disclosure.py`, `heartbeat.py`; hook-process-only). But the `state/*` cache modules (describe/discover/canary/preflight/draft_context caches, runtime_prior, runs, run_record), `ops/aggregate_flow.py`, `ops/preflight/check.py`, `_submit_preflight_cache.py`, `capabilities.py`, and `cli/dispatch.py` itself are Lane-A-reachable. Residual-risk 3's "allowlist starts empty" is unachievable — replace with: allowlist starts with the **enumerated exempt buckets** (each entry tagged `deploy|worker|hook|lane-b-only` with a one-line justification), and the lint fails on any UNTAGGED reader. That is the honest version of the same guard.

4. **`append_jsonl_line` has 16 consumer files, not 4.** The §5 `fsync_required` split must classify all of: `state/decision_journal.py`, `decision_briefs.py`, `scopes.py` (look ledger), `data_trace.py`, `data_manifest.py`, `fingerprint_store.py`, `conformance_store.py` (source-of-truth candidates) vs `ops/monitor/harvest_guard.py`, `transport/_prune.py`, `aggregate_flow.py`, `overnight.py`, `pack/init_op.py`, `pack/refresh_op.py`, `recover/heal_taxonomy.py`, `verify_reproduction.py` (case-by-case). The unit's deliverable is the classification TABLE + the flag, and its battery must cover every consumer's test dir — this is exactly the block-drive-lesson shape: the seam change is one file, the breakage surface is sixteen.

5. **Lane A dispatches through `cli.dispatch.main`, which arms process-global machinery per call**: `detached_heartbeat()` + `install_crash_faulthandler()` + `_record_detached_failure_terminal` (env-triggered by `HPC_DETACHED_RUN_ID/_BLOCK`, cwd-resolved at :290). Today's mcp-serve no-ops these because its env lacks the markers — but §4 threads the CLIENT's env per-call, so a detached worker (or any process with stale `HPC_DETACHED_*` exported) calling through the daemon would activate worker-terminal recording against the DAEMON's cwd. Engineered fix, not prose: `daemon_client` never forwards `HPC_DETACHED_*`, AND the daemon's Lane-A admission refuses `hpc_env` containing them (`daemon_env_mismatch` family) — plus a fire test. Also note `faulthandler.enable()` is process-global: the daemon should arm it ONCE at boot to its own log, and Lane A should enter at `_dispatch_main`/`_invoke_parsed` altitude (below `main()`'s worker wrapper), mirroring how the design already picked the in-proc runner seam over `main` for 2.3 (technical D1). The in-proc runner calls `main` today — acceptable for mcp-serve; for the daemon, entering below `main` removes the whole class. Decide loudly in the D-CORE unit.

One more latent trap for §4: argparse `--experiment-dir` defaults are `default=Path.cwd()` **evaluated at parser BUILD time** (`cli/_helpers.py:255` et al.). 2.1 memoizes the parser; a memoized parser in a long-lived daemon freezes the boot cwd into the default forever. The client-side always-materialize contract dodges it, but the guard must be able to fire: the daemon refuses (or telemetry-flags) any `x-hpc/cli` argv for an `--experiment-dir`-accepting verb that lacks the flag. Add to the D-CONTEXT battery.

## 2. Generated-artifact regen points

`daemon-serve` should be Tier 3 (long-lived, like `mcp-serve`, registered in `parser._register_tier3_modules`) → no bake regen, but it must be exempted/handled by unit 1.2's new parser-truth lint. `daemon-status` + `daemon-stop` as `@primitive` verbs trigger the full chain at wave integration: `bake_operations_json.py --write` (operations.json), `build_verb_module_map.py` (fast-path map — daemon verbs must be verdict-correct there: `daemon-status` when the daemon is DOWN must still work fast inline), `build_schemas.py` (if `_wire` spec models — recommend yes for daemon-status), `build_primitive_frontmatter.py`/`build_primitive_index.py`, `check_no_pending_primitive_docs.py` (author the doc templates IN the unit or CI reds), and the frozen-count seam (`test_doc_frozen_counts` reads regen_all's exported counts — regen handles it, but `tests/cli/test_cli_completeness.py` and any curated-catalog pins move by hand). Making `daemon-status` a curated extra edits `_CURATED_EXTRA_VERBS` (mcp_server.py:177) and must keep `test_curated_catalog_is_derived_blocks_union_extras` green. Per memo §0.16: units never commit regenerated artifacts — one `regen_all.py --write` per wave at integration, and the daemon wave has exactly one regen-forcing unit (D-VERBS below).

## 3. In-flight interaction

| In-flight item | Collision with daemon design | Resolution |
|---|---|---|
| SGE agent (backends/** + failure_signatures.py, ongoing) | §4 audit fix list names `infra/backends/__init__.py:551,680` | **Carve out of D-CONTEXT.** Both sites are on submit paths that are Lane-B/detached by construction (submit verbs are `_DETACH_REQUIRED`); classify `lane-b-only` in the census, no code change now; revisit only if a backend-touching verb ever seeks Lane-A eligibility. Zero shared files remain. |
| Latency 1.2 (parser.py, setup.py) — not landed | D-VERBS' one-line Tier-3 registration in parser.py; D-CONTEXT's setup.py cwd fix | Sequence after 1.2 lands; trivial rebase. |
| Latency 1.3 (root `__init__.py`) — not landed | daemon_client import weight (finding 2 above) | Hard prerequisite of D-CLIENT's acceptance numbers (the code can merge earlier; the <200ms hook claim can't). |
| Latency 1.7 (stop_multiplex — does NOT exist yet) — not landed | Rung 1 target set: the design already says "post-1.7". Hook modules are 1.7's files | **Hard gate: D-HOOKS after 1.7.** One daemon head in `stop_multiplex` + heads in `utterance_capture`/PostToolUse pair/write fence — never heads in the three legacy stop guards (they're being deleted from settings by 1.7's migration). `agent_assets.py` is also 1.7's file → D-INSTALL after 1.7. |
| Latency 2.1 (dispatch.py, mcp_server.py, parser.py, setup.py, `_fast_path_cache.py`) — not landed | D-CLI (`_try_daemon_dispatch`), D-SHIM (mcp_server per-connection state), §2's per-RPC `installed_dist_signature` reuse | **Hard gate: D-CLI and D-SHIM after 2.1** (2.1 merges first in Wave 2 anyway). Also §1's spawn-wait ≤15s budget cites WS-BAKE as a dependency — that IS 2.1. Daemon core (D-CORE etc.) does not touch these files and can proceed in parallel with Wave 2. |
| Latency 2.3 (block_drive, drive.py, preflights) — not landed | Rung 4; Lane A's eligibility list "extends unit 2.3's list" | D-DRIVE after 2.3 AND after 3.1/4a (block_drive.py is the program's most contended file: 2.3→3.1→4a→5.1/5.3 already queue on it; rung 4 joins the END of that chain). Lane A's eligibility list must be THE SAME enumerated artifact as 2.3's in-process list (one definition, two consumers) — if D-CORE ships before 2.3, D-CORE creates it and 2.3 consumes it; agree with the integrator which unit owns the file. |
| Latency 3.1/4a/5.x | block_drive + SKILL.md chain | D-DRIVE last; no other overlap. |

**Net sequencing:** daemon core waves can run concurrently with latency Waves 1-2 **only on the new-file units**; every surface-migration unit gates on a latency unit. Do not dispatch D-HOOKS/D-CLI/D-SHIM until 1.7/2.1 are integrated — the alternative is four units rebasing onto three moving seams.

## 4. Corrected unit decomposition

Key structural correction: the design's "new files: `daemon_server.py`" makes ~60% of the build same-file-sequential. **Ship the daemon as a package** — `src/hpc_agent/_kernel/extension/daemon/` with `__init__.py` (assembly), `transport.py` (Listener/discovery/locks/staleness), `identity.py` (fingerprint, dist-signature, editable-stat), `lanes.py` (queue, admission, watchdog, Lane-B semaphore), `supervisor.py` (watch table, `_detached/` scan), `lifecycle.py` (activation/drain/idle-exit) — so waves DW2's units are file-disjoint. Run `lint_subject_imports.py`/`lint_private_cross_package_imports.py` on the package from day one (allowlist files exist at `scripts/*_allowlist.txt`).

**DW0 (sequential, before any dispatch)**
- **D-SPEC**: Finalize agent writes the corrected plan into `docs/plans/daemon-engineering-2026-07-16/`. `git status` re-check; confirm SGE stand-down state.
- **D-FSYNC** (= §5): `infra/io.py` `fsync_required` split + first-append parent-dir fsync + the 16-consumer classification table + fault-injection seam. Files: `io.py`, the source-of-truth writer call sites (`state/decision_journal.py`, `decision_briefs.py`, `scopes.py`, `data_trace.py`, `data_manifest.py`, `fingerprint_store.py`, `conformance_store.py`), fire tests. Sequential-first because D-CONTEXT and daemon fire tests build on it, and it's valuable standalone (one-shot CLI durability improves too). Merge risk: LOW (additive kwarg, default preserves today's behavior). NOTE: `state/journal.py` is NOT edited here (it's D-CONTEXT's) — journal's writer is `decision_journal.py`; verify at build.

**DW1 (parallel, new-file-dominant, concurrent with latency Wave 2)**
- **D-CORE** [∥]: `daemon/transport.py`, `daemon/identity.py`, `daemon/lifecycle.py`, `daemon/__init__.py` with serial Lane A ONLY (memo 3.3's v1 shape), wrapping `McpServer.handle()` read-only (imports it, doesn't edit mcp_server.py). `tests/daemon/` (new, owns conftest with `HPC_DAEMON_DIR` autouse). Merge risk: LOW (new files). Includes: never-unpickle framing, hello, per-RPC identity, drain, idle-exit, spawn/instance locks, discovery-file ACL, the Lane-A-entry-altitude decision (finding 5).
- **D-CLIENT** [∥]: `cli/daemon_client.py` (stdlib-only: discover→pid-check→connect→hello→call, deadline, inline-fallback contract, HPC_DETACHED_* strip) + import-weight contract test. Merge risk: LOW. Acceptance numbers gated on 1.3.
- **D-CONTEXT** [∥, after D-FSYNC]: `env_flags.call_env()` ContextVar + refactor the four readers in env_flags + `state/journal.py`/`state/index.py` assert-on-daemon guards + `cli/setup.py` (after 1.2) + the bucketed reader-census contract test (extends `test_multi_human_boundary.py::test_env_actor_is_the_sole_reader_of_hpc_actor` pattern). Merge risk: MEDIUM — env_flags is imported everywhere; the refactor must be behavior-identical when the contextvar is unset (default `os.environ`), pinned by running the full existing battery.
- **D-VERBS** [∥]: `cli/daemon.py` (Tier-3 `daemon-serve`; `@primitive` `daemon-status`/`daemon-stop`), parser.py one-line Tier-3 hook (after 1.2), `_wire` spec models, primitive doc templates. **The wave's only regen-forcing unit.** Merge risk: MEDIUM (regen fan-out; frozen counts; curated extras — defer curating daemon-status to D-SHIM if contention bites).

**DW2 (parallel where file-disjoint, after DW1 integrates)**
- **D-LANES** [∥]: `daemon/lanes.py` — Lane B via `run_capture_bounded`, watchdog/WEDGED, `daemon_busy` overflow, eligibility-list artifact + lint (planted-violation test per the `test_src_subprocess_timeout_discipline` pattern; coordinate ownership with 2.3 per §3 above). Files: lanes.py + scripts/lint + tests. 
- **D-POOL** [∥]: asyncssh pool adoption + transport-env snapshot/`daemon_env_mismatch` refusal. Files: `daemon/__init__.py` seam ONLY if pre-split cleanly, else sequence after D-LANES (same-file check at dispatch).
- **D-SUPERVISOR** [∥]: `daemon/supervisor.py` + daemon-status projection wiring + rolling log. Consumes `detached.py` read-only.
- **D-SOAK** [∥]: `tests/daemon/` batteries 1-3 of §8 (concurrency, 200-call soak, per-invariant fire paths) + the new ci.yml windows job. **Sole ci.yml owner for the daemon program** (memo §0.13 discipline).

**DW3 (surface rungs — sequential gates, each ships only after the prior rung's telemetry line lands)**
- **D-HOOKS** (rung 1): after 1.7. Files: `stop_multiplex.py`, `utterance_capture.py`, the PostToolUse pair, `scheduler_write_fence.py` heads. Merge risk: MEDIUM (1.7 just rewrote these; rebase-first).
- **D-CLI** (rung 2): after 2.1. File: `cli/dispatch.py` (insert before `_try_fast_dispatch` at the then-current seam). Merge risk: MEDIUM.
- **D-INSTALL**: doctor daemon check (locate module in `ops/recover/` at build), `agent_assets.py` stop step (after 1.7), release skill SKILL.md + slash twin (`lint_skill_command_sync.py`). Merge risk: LOW.
- **D-SHIM** (rung 3): after rungs 1-2 telemetry. Files: `mcp_server.py` (per-connection capability state, shim forwarding), `cli/mcp.py`. Merge risk: **HIGH** — the elicitation pump, dark-channel state, and refusal seams live here; the full elicitation test quartet gates.
- **D-DRIVE** (rung 4): after 2.3 + 3.1 + 4a, joining the block_drive queue LAST. Merge risk: HIGH (most-contended file in two programs).
- Rung 5: no build (recorded as declined, per design).

Integration checklist per wave = the memo's standing protocol (ordered merge → one regen → trio → batteries → push → CI matrix green → enforcement rows 17/18 + the new rows [fingerprint-in-address, transport-env split, client-deadline-falls-inline, detached-env refusal] appended to `engineering-principles.md` → telemetry.md deltas).

## 5. Per-unit pre-push battery

Universal every unit: `.venv/Scripts/python.exe -m pytest` targeted + backgrounded (never `uv run`; full suite = GitHub CI) + `ruff check --fix` / `ruff format` / `mypy src/hpc_agent` + `regen_all.py --check`.

| Unit | Targeted tests (default tier) | Named `-m slow --timeout=0` | Lints/checks beyond universal |
|---|---|---|---|
| D-FSYNC | tests/state/ (ALL — 7 source-of-truth writers), tests/ops/decision/ (journal+briefs consumers), tests/ops/monitor/test_harvest_guard.py, tests/ops/aggregate/ (flow marker writes), tests/_kernel/hooks/ (utterance write path), new fault-injection fire tests | — | `lint_atomic_durable_writes.py` |
| D-CORE | tests/daemon/ (new: hello/skew/stale-file/single-instance/drain/idle), tests/test_mcp_server.py (handle() consumed read-only — parity must hold) | new pipe-soak twin of `test_serve_survives_sequential_tool_calls_over_real_pipes` | `lint_subject_imports.py`, `lint_private_cross_package_imports.py`, `lint_atomic_durable_writes.py` (discovery file) |
| D-CLIENT | tests/daemon/test_client_fallback.py (dead/absent/mid-boot/deadline→inline byte-identical vs `_subprocess_cli_runner` oracle), import-weight contract test | — | `lint_pure_files.py` if applicable |
| D-CONTEXT | tests/contracts/test_multi_human_boundary.py (sole-reader pin), the reader-census test (buckets, untagged=red), §4 fire test (two cwds/envs → two journals, actor from call env), tests/state/test_journal*/index tests, tests/cli/test_setup*.py, doctor/net-triage/status-snapshot env-echo tests (active_env_overrides consumers: tests/ops/recover/test_net_triage.py, tests/ops/status/), **full tests/cli/ trio** (env_flag behavior-identity) | — | reader-census lint fire test (planted `os.environ["HPC_..."]` read) |
| D-VERBS | tests/cli/test_cli_completeness.py, tests/contracts/test_doc_frozen_counts.py, tests/scripts/test_regen_all.py, tests/contracts/test_generated_merge_driver.py, tests/cli/test_fast_dispatch.py (map verdicts for new verbs), tests/_wire/ | — | `bake_operations_json.py --check`, `build_schemas.py --check`, `check_no_pending_primitive_docs.py`, parser-truth lint (1.2's) |
| D-LANES | tests/daemon/ lane tests (busy-overflow→inline, watchdog WEDGED, planted-slow-verb HOL, planted `os.chdir` verb → lint red, `_refuse_blocking_over_mcp` over the pipe), tests/contracts/test_src_subprocess_timeout_discipline.py | — | eligibility lint fire test; `lint_mirror_ledger.py` (Lane-B runner vs `_subprocess_cli_runner` twin risk) |
| D-POOL | tests/infra/test_ssh_engine.py, tests/daemon/test_env_mismatch.py (transport-var disagree → refusal+inline, disclosed) | tests/infra/test_ssh_engine.py -m slow | — |
| D-SUPERVISOR | tests/daemon/test_supervisor.py (daemon kill+respawn: detached worker survives, poll-detached reads it; watch-table reconstruction from `_detached/` scan), tests/ops/monitor/test_wait_detached.py, tests/_kernel/lifecycle/test_detached_* (Linux CI gates the win32 severance skips) | — | — |
| D-SOAK | batteries 1-3 of §8; lock-discipline test (post-RPC every `.lock` acquirable); durability kill-pair (pre-fsync/post-fsync) | 200-call soak; N=8 concurrency (both slow) | ci.yml: new required windows-daemon job — verify it RUNS red-able (seeded fail once) |
| D-HOOKS | ALL tests/_kernel/hooks/, tests/cli/test_agent_assets_settings_hook.py, hook-head fire tests (daemon up=pipe / down=inline byte-identical / fire-and-forget spawn) | — | conformance lane (claude_code adapter) |
| D-CLI | tests/cli/ fast-path trio (fast path must not regress), tests/cli/test_cli_dispatcher_inline_parity.py, tests/cli/test_envelope.py, tests/integration/test_dispatch_smoke.py (+ daemon leg) | — | — |
| D-INSTALL | doctor tests, tests/cli/test_agent_assets_* | — | `lint_skill_command_sync.py` + slash twin, `lint_skills.py` |
| D-SHIM | tests/test_mcp_server.py (ALL), test_mcp_server_envelope_parity.py, test_mcp_curated.py, **all four** test_mcp_elicitation_*.py (E4 re-pointed: refusal→popup→retry-once through the shim; pipe surface never elicits), tests/daemon/ elicitation-locality fire test | tests/test_mcp_server.py -m slow (both pipe soaks) | conformance lane BOTH adapters |
| D-DRIVE | ALL tests/_kernel/lifecycle/, cross-consumer block-drive battery (the block-drive lesson, verbatim from 2.3's spec): tests/ops/attention/, tests/ops/status/test_snapshot_attention.py, tests/ops/monitor/test_blocks.py, tests/ops/aggregate/test_blocks.py, tests/meta/campaign/test_blocks.py, tests/ops/test_block_gate_and_speculate.py, preflight tests | — | `lint_mirror_ledger.py` (drive.py twin, post-2.3 state) |

## 6. Merge-risk ranking + order

`D-FSYNC → [D-CORE ∥ D-CLIENT ∥ D-CONTEXT ∥ D-VERBS] → regen+integrate → [D-LANES ∥ D-POOL* ∥ D-SUPERVISOR ∥ D-SOAK] → integrate+windows-CI-green → D-HOOKS (post-1.7) → D-CLI (post-2.1) → D-INSTALL → telemetry gate → D-SHIM → D-DRIVE (post-2.3/3.1/4a)`. (*D-POOL sequences after D-LANES if the package split leaves them sharing `daemon/__init__.py`.)

Risk: LOW = D-CORE, D-CLIENT, D-SUPERVISOR, D-INSTALL, D-SOAK (new files/tests). MEDIUM = D-FSYNC (16 consumers), D-CONTEXT (env_flags blast radius), D-VERBS (regen fan-out + frozen counts), D-HOOKS/D-CLI (rebase onto just-landed 1.7/2.1), D-LANES (eligibility-list ownership vs 2.3). HIGH = D-SHIM (elicitation machinery), D-DRIVE (block_drive queue position 5 of 5 across two programs — schedule it dead last, alone).

Biggest schedule risk overall: the three surface rungs are each hostage to a latency-plan unit that has not landed. If latency Waves 1-2 slip, the daemon core (DW0-DW2, opt-in flag, zero default-path deltas) still ships and soaks standalone — recommend exactly that cut line to the maintainer.