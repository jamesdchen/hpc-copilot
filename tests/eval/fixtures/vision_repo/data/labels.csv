# Minimal stand-in dataset for the vision (GPU) eval fixture repo.
# Not consumed by the eval — present so the fixture is a self-contained "repo"
# (executors + clusters.yaml + data). The eval grades the GPU-job decision.
image,label
img_0.png,0
img_1.png,1
img_2.png,0
